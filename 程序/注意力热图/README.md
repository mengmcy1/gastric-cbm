# 注意力热图

## 正式代码

- `gradcam_batch.py`：为全部有效样本批量生成原图、Grad-CAM 热图、叠加图和清单。
  脚本独立读取`dataset_manifest.csv`，不再依赖已归档的旧训练脚本；恒定CAM会安全输出
  全零热图，避免归一化除零。

早期单图demo和说明已移动到`程序/归档/注意力热图/`，旧生成结果位于
`结果/归档/注意力热图/`。
