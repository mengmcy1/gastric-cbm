# 第二批数据去偏与重训练 TODO

> 最后更新：2026-07-22
> 当前阶段：P1 v1.1 专项 debug 已完成，等待最终肉眼验收
> 状态说明：`[ ]` 未开始，`[-]` 进行中，`[x]` 已完成，`[!]` 阻塞/待人工确认

## 总目标

在不补充新图像的前提下，对 `数据/第二批整理后` 进行可追溯的裁剪、质控和来源匹配，降低图像尺寸、医院、设备、年份及界面样式与标签之间的关联；随后使用固定清单重新训练并完成内部交叉验证、来源分层评估和现有多中心外部验证。

## 不可变原则

- 原始数据只读，禁止覆盖或物理删除。
- 癌/非癌使用同一套自动裁剪规则；设备模板不得按标签制定。
- 数据取舍不参考模型预测、概率、Grad-CAM 或测试集表现。
- 所有筛选结果写入 manifest，保留排除原因和原始路径。
- 患者不得跨 train/val/test；同一组实验的不同模型必须使用同一清单和划分。
- 测试集不参与裁剪阈值、筛选规则、超参数或分类阈值的选择。
- `full`、`matched` 和 `excluded` 仅通过清单区分，不重复或删除原始文件。

## 双版本推进策略

- **弱标注版（本轮主交付，不等待 P3）**：P2 自动筛图完成后，使用中心、自动设备/画面风格、相近年份三层信息构建 `matched_weak_v1`；所有样本统一保留 `review_status=pending`。本轮直接使用该版本完成裁剪、匹配、训练、内部验证和现有外部验证，并保存独立权重与结果。
- **精标注版（后续可选增强）**：时间允许时再由医生确认/替换入选图并补齐 anatomy、view_type、lesion_visible，生成 `matched_reviewed_v1`；不纳入本轮验证关键路径。
- P3 不阻塞也不纳入本轮 P4-P8；本轮报告必须明确逐图病灶可见性未经临床确认，不能把弱标注结果表述为病灶级金标准验证。
- 本轮关键路径：P0 参数固化 -> P1 自动裁剪 -> P2 自动质控/限图 -> P4 弱标注匹配 -> P5 数据验收 -> P6 训练适配 -> P7 训练 -> P8 外部验证 -> P9 归档。

## P0：基线审计与方案固化

- [x] 确认正式数据清单：5229 张，2321 位患者，癌/高级别 1600 张，非癌 3629 张。
- [x] 确认无完全重复 SHA-256、无重复路径、无跨标签患者。
- [x] 确认当前划分无患者或相同 SHA-256 跨 train/val/test。
- [x] 确认显著尺寸偏差：1265/1600 张癌图为 1920x1080，非癌图中该尺寸为 0 张。
- [x] 完成尺寸元数据基线：仅宽高、比例和面积即可取得 Test AUC 0.968、Accuracy 0.951。
- [x] 确认来源偏差与外部泛化下降，并记录于 `AGENTS.md`。
- [x] 明确预处理边界：处理黑边、外框及视野外固定界面；保留视野内器械、气泡、黏液和反光。
- [x] 明确数据组织：同一份裁剪结果生成 `full`、`matched`、`usable_unmatched` 和 `excluded` 清单。
- [x] 明确双版本推进：弱标注版先跑通全链路，精标注版后续替换并冻结正式结果。
- [x] 确定时间受限条件下以完全弱标注版完成本轮全链路验证，P3 延后。
- [x] 固化裁剪、图像质量、重复判定和患者限图参数，见 `数据预处理参数_v1.md`。

## P1：裁剪与质控脚本

- [x] 新建独立预处理脚本，不修改原始图片。
- [x] 实现中心最大连通区域检测，避免边缘文字干扰黑边定位。
- [x] 支持圆形、八角形和矩形胃镜视野，取中心 ROI 后按边长内缩 1%～2%，记录裁剪方法与坐标。
- [x] P1 不使用设备模板裁剪状态栏；仅当后续边缘对照证明残留界面仍可预测标签时，再新增针对性方案。
- [x] 裁剪后保存高质量母版，训练时再 resize 到模型输入尺寸。
- [x] 保留原目录层级，输出到独立的裁剪数据目录。
- [x] 生成 `processed_manifest.csv`，至少包含：
  - patient_id、label、center、年份/来源信息
  - original_path、processed_path、原始宽高
  - crop_bbox、crop_method、processed_sha256
  - sharpness、crop_status、failure_reason
- [x] 增加调试参数，只处理指定数量的患者/图片。
- [x] 对主要中心、标签和高频分辨率抽样生成裁剪前后对照图。
- [x] 人工检查调试样本；217张 debug 自动验收通过，用户确认除1张设备文字残留外其余正常，该例按统一规则保留。
- [x] 全量处理 5229 张图片并汇总：2912张 cropped、2317张 full_frame、0张 decode_error/fallback。
- [x] 完成全量自动验收：5229/5229输出有效、0硬失败、0跨标签重复，验收结论 PASS。
- [x] 完成 v1 全量分层复核；用户确认第002～009页存在左右薄边、单侧UI和底部播放进度条残留，v1 最终肉眼验收未通过。
- [x] 新增独立 v1.1：原轮廓框后执行左右独立扫描，并专项检测底部低方差进度条；顶部不执行通用收紧。
- [x] 使用195张异常与正常混合样本完成 v1.1 专项 debug：72张左右收紧、33张移除进度条，自动验收 PASS、0硬失败。
- [!] 人工检查 v1.1 的13页专项联系图，确认侧栏/进度条改善且未切掉有效视野；等待用户最终验收。
- [ ] v1.1 验收通过后重新全量处理5229张，并重新生成正式自动验收与分层联系图。

## P2：自动筛图与人工标记准备

- [ ] 在裁剪后的有效 ROI 上计算清晰度，避免文字和黑边抬高分数。
- [ ] 在患者内进行相似图检测，记录 duplicate_group，不直接删除文件。
- [ ] 制定固定的自动选图规则：质量过滤 -> 去重 -> 多样性选择。
- [ ] 每位患者自动推荐 1～3 张；不足 3 张时不强行补足。
- [ ] 时间戳可解析时用于连续帧间隔；不可解析时使用固定回退规则。
- [ ] （后续可选）生成医学生标记表和患者级联系图/预览材料。
- [ ] （后续可选）标记表至少包含：
  - quality、crop_status、duplicate
  - lesion_visible、anatomy、view_type
  - selected、selection_rank、review_required、review_status、exclusion_reason
- [ ] 基于中心、原始尺寸/比例、裁剪几何和可用来源信息生成自动设备/画面风格字段，供弱标注匹配使用。
- [ ] 输出弱标注自动选图清单，所有未审核样本标记 `review_status=pending`。
- [ ] 本轮 P2 验收只要求自动质控、去重、限图和弱标注清单生成完成，不等待任何人工字段。
- [ ] （后续可选）完成一小批患者试标，检查字段是否清楚、工作量是否可接受。
- [ ] （后续可选）固化医学生操作说明和医生复核规则。

## P3：人工标记与临床复核（后续增强，本轮不执行）

- [ ] 医学生完成图像可用性、裁剪质量、重复组、部位和视角标记。
- [ ] 医学生对癌图标记病灶明确可见/不可见/不确定。
- [ ] 汇总所有需医生复核的癌图、可疑非癌图和裁剪失败图。
- [ ] 医生复核所有癌患者最终入选图和疑难样本。
- [ ] 医生随机抽查 10%～20% 普通非癌入选图。
- [ ] 记录修改前后差异、审核人和审核日期。
- [ ] 生成并冻结精标注版清单，不再因模型结果反向修改选择。

## P4：构建固定数据清单

- [ ] 生成 `full_manifest.csv`：全部通过质控的裁剪图。
- [ ] 生成 `patient_limited_manifest.csv`：每患者最多 1～3 张。
- [ ] 按中心精确统计可用癌/非癌患者及图片数量。
- [ ] 定义弱标注匹配层级：中心 -> 自动设备/画面风格 -> 相近年份。
- [ ] 在患者级执行固定种子的 1:1 主匹配，并保留 1:2 作为敏感性分析方案。
- [ ] 生成本轮主清单 `matched_weak_v1_seed42.csv`，不依赖 anatomy、view_type 或医生确认，全部记录 `review_status=pending`。
- [ ] P3 完成后增加解剖部位/视角和病灶可见性约束，生成 `matched_reviewed_v1_seed42.csv`。
- [ ] 生成 `usable_unmatched_manifest.csv`：作为审计子集归档；其样本仍包含在 full 实验中，但不单独参与本轮训练，也不进入 matched 实验。
- [ ] 生成 `excluded_manifest.csv`，包含明确排除原因。
- [ ] 冻结患者级 5 折交叉验证清单，保证来源和标签尽量平衡。
- [ ] 生成逐中心留出评估清单；小中心结果需附置信区间并说明样本量。
- [ ] 再次验证患者、图片哈希和重复组均不跨折/集合。

## P5：去偏效果验收

- [ ] 比较 full 与 matched 的中心、年份、原始尺寸、画面风格和标签分布。
- [ ] 重跑尺寸/来源元数据基线，确认其预测能力显著下降。
- [ ] 检查裁剪后黑边比例、ROI 面积、长宽比和失败率是否在标签间保持接近。
- [ ] 使用无需训练的像素/颜色/压缩统计检查标签与设备风格残留关联。
- [ ] 随机抽检各中心、标签和风格的入选/排除样本。
- [ ] 形成训练前数据审计报告，明确仍无法消除的偏差和适用范围。

## P6：训练代码适配

- [ ] 训练脚本改为显式接收 manifest 和 fold/split，不在运行时临时随机匹配。
- [ ] ResNet50 与 EfficientNet-B0 共用相同训练清单、数据增强和评估口径。
- [ ] 训练 resize 与裁剪母版解耦，默认输入 224x224。
- [ ] 实现分辨率破坏性训练增强：随机降采样后升采样至 224x224、随机 JPEG 压缩、轻微高斯模糊；癌与非癌使用相同参数范围，验证/测试不使用随机破坏。
- [ ] 在调试样本上确认增强不会系统性抹除细微病灶，冻结参数后再正式训练。
- [ ] 按训练集动态计算类别权重，不读取验证/测试标签统计调整训练。
- [ ] 保存每阶段最佳权重，并在验证集上比较 S1/S2 后再确定测试模型。
- [ ] 输出图片级和患者级 AUC、Sensitivity、Specificity、Precision、F1、CM。
- [ ] 增加按中心、年份/风格和原始尺寸分层的评估输出。
- [ ] 增加配置快照，记录数据清单哈希、fold、随机种子和代码版本。

## P7：正式实验

- [ ] 实验 A：full cropped 数据，两个模型各跑一次固定 70/15/15 患者级划分，作为原结果对照。
- [ ] 不单独训练原实验 B；每患者限图已作为 matched 构建的固定步骤。
- [ ] 实验 C：matched 1:1 数据，两个模型完成相同的 5 折患者级交叉验证，作为论文核心结果。
- [ ] 实验 D：matched 1:2 数据，两个模型各跑一次固定 70/15/15 患者级划分，作为比例敏感性分析。
- [ ] 汇总各折均值、标准差和 95% 置信区间。
- [ ] 在验证折上选择临床阈值，测试折仅执行锁定评估。
- [ ] 比较裁剪前后、full/matched、ResNet/EfficientNet 的变化。
- [ ] 分析内部指标下降是否伴随来源间差距和外部敏感度改善。
- [ ] 使用核心模型和固定划分训练边缘区域对照，检查残留界面和黑边是否仍可预测标签。
- [ ] 使用核心模型和固定划分训练中心 ROI 对照，评估胃黏膜内容本身的判别能力。
- [ ] 使用核心模型和固定划分运行灰度对照，评估设备色调依赖；仅在发现异常信号时扩展到另一模型或 5 折。

## P8：外部验证与解释性复核

- [ ] 使用现有多中心癌数据评估裁剪重训模型的图片级和患者级敏感度。
- [ ] 与旧模型 ResNet50 60.3%、EfficientNet-B0 69.4% 患者级敏感度对比。
- [ ] 外部非癌缺失时明确不报告 AUC、Specificity 和 Accuracy。
- [ ] 对内部正确/错误及外部漏诊病例生成 Grad-CAM。
- [ ] 检查模型是否仍关注文字、黑边、边缘、器械或反光。
- [ ] 更新 MOCE/SAE 输入模型前，先确认重训模型通过去偏验收。

## P9：结果归档

- [ ] 将数据处理规则、统计结果和最终实验结果更新到 `AGENTS.md`。
- [ ] 保存裁剪日志、人工审核记录、匹配清单和划分清单。
- [ ] 保存训练命令、环境、权重、阈值和完整评估 CSV。
- [ ] 弱标注结果单独标记版本，并在报告中注明患者级标签继承、逐图病灶可见性未确认等局限。
- [ ] 明确区分内部测试、逐中心留出验证和独立多中心外部验证。
- [ ] 汇总论文可用表格、图像和局限性说明。

## 进度记录

| 日期 | 阶段 | 更新内容 | 结果/下一步 |
| --- | --- | --- | --- |
| 2026-07-21 | P0 | 完成第二批数据尺寸、来源、划分及外部泛化问题复核 | 下一步固化裁剪参数并实现预处理脚本 |
| 2026-07-21 | P0 | 建立本 TODO，明确 full/matched 双路线和人工审核边界 | 后续每完成一项同步更新状态与结果 |
| 2026-07-21 | P0 | 改为弱标注/精标注双线推进；移除 P3 对工程链路的阻塞 | 弱标注版先跑 P4-P8，正式结论等待精标注版 |
| 2026-07-21 | P0 | 简化 P1 裁剪、重排 P5/P7、补充分辨率破坏增强并收敛实验矩阵 | 核心训练约 14 次，三个对照实验按信号逐步扩展 |
| 2026-07-21 | P0 | 本轮改为完全弱标注主线，不等待人工标记或医生复核 | 先完整跑通裁剪、匹配、训练、内部及外部验证；P3 延后 |
| 2026-07-21 | P0 | 冻结数据预处理参数 v1 | P0 完成，进入 P1 裁剪与质控脚本实现 |
| 2026-07-22 | P1 | 完成217张分层 debug 裁剪与自动验收 | 自动 PASS；0 硬失败，59张进入肉眼复核清单 |
| 2026-07-22 | P1 | 完成5229张全量裁剪 | 2912张裁剪、2317张全幅；无解码失败或回退 |
| 2026-07-22 | P1 | 修正验收语义和ROI面积硬门槛 | fallback改为警告；16张低ROI经复核均裁剪正确，硬门槛由50%修正为45% |
| 2026-07-22 | P1 | 全量自动验收及195张分层内部视觉检查 | 自动PASS；少数设备界面残留待用户最终肉眼确认，旧数据暂不归档 |
| 2026-07-22 | P1 | 用户完成v1全量分层肉眼检查 | 第002～009页存在薄边、单侧UI和视频进度条残留，v1未通过最终验收 |
| 2026-07-22 | P1 | 完成v1.1二次收紧与195张专项debug | 72张左右收紧、33张移除进度条；自动PASS，等待用户检查13页联系图 |
