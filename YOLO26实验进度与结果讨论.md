# gastric-cbm YOLO26实验进度与结果讨论

## 新对话恢复项目的固定提示词

> 该提示词只规定恢复上下文与协作方法；即时状态以本文后续章节和正式输出为准。

```text
请继续协助我开展 gastric-cbm 的YOLO26胃早癌病灶定位研究。

先确认项目根目录为 /home/mcy/gastric-cbm，并按顺序阅读：

1. AGENTS.md：长期项目背景、数据/test边界、目录、文件安全、Git和代码注释规则；
2. YOLO26实验进度与结果讨论.md：当前阶段、冻结协议、最新结果、阻塞项和下一步；
3. 实验进度与结果讨论.md 的“阶段决策”：M0-M5历史结论和启动YOLO路线的原因；
4. 本轮相关的正式代码、CSV/JSON/config和冻结manifest。数字以正式产物为准，不只依赖Markdown。

开始操作前先只读检查git status、目标输出目录和现有运行状态，不要重复运行完整产物。
GPU任务必须先运行nvidia-smi，同时检查显存、利用率和进程，再选择设备；不得默认GPU编号。

每轮先用通俗语言说明：解决什么问题、输入和输出是什么、是否接触internal test/external、
通过后进入哪一步。正式训练前必须确认超参数、checkpoint选择规则、阈值和停止条件已经冻结。

实验原则：
- patient-level split优先，患者不得跨split；
- Y0-Y3只使用train/val，internal test在Y4一次性揭盲，external只作Y5诊断；
- 一轮只改变预注册变量，不按test/external结果返调模型、阈值或框后处理；
- 没有预测框的癌图仍进入几何分母，IoU/coverage/center-hit记0；
- 自动坐标检查不能代替临床确认多病灶标注完整性；
- 先smoke再全量，不覆盖冻结清单、权重、历史结果或人工标注；
- 不批量删除，不上传患者图像、预测明细或权重到公共仓库；
- 长任务提供运行命令时，同时提供实时日志监看命令。

每完成一个正式阶段，按“做了什么、结果如何、确认与未确认结论、风险、下一步、文件与验证”
详细但易懂地汇报，并实时更新YOLO26实验进度与结果讨论.md。只有长期规则、稳定事实、正式入口
或重要阶段结论变化时才更新AGENTS.md。旧实验进度与结果讨论.md已经结束维护，不再追加新日志。
```

## 文档边界

本文只维护2026-08-13开始的YOLO26专用检测器阶段。此前第二批数据、去偏分类器、
M0-M5、MOCE和SAE的完整历史保留在`实验进度与结果讨论.md`，该文件已结束维护。

## 当前状态

更新时间：2026-08-14

| 阶段 | 状态 | 当前结论或阻塞项 |
| --- | --- | --- |
| Y0数据转换与审计 | 已完成并放行 | 文件、SHA、尺寸、坐标和精确重复通过；47组跨患者pHash候选逐对确认均非重复；当前数据暂无已知多处分离目标，单框结构可用于本阶段 |
| Y1 YOLO26n smoke | 已完成并放行 | 环境、数据、空标签、end-to-end检测头、GPU训练/验证和标准产物均通过；5轮指标不作性能结论 |
| Y2-B平衡集YOLO26s 640/960 | 已完成并冻结 | 640按预注册层级入选；两组癌图召回均为79/87，640的IoU50和mean IoU更高 |
| Y3-B平衡主实验三种子 | 已完成并稳定通过 | 三种子均保持癌图召回0.9080，同时IoU50和mean IoU均高于同seed M1；正式结论为`stable_success_3_of_3` |
| Y0-F/Y3-F完整集诊断 | 已完成并稳定通过 | 三种子均保持癌图召回0.9000，mean IoU平均0.6343、IoU50平均0.7681；正式结论为`stable_success_3_of_3` |
| Y4 internal test | 已完成；主检验未通过 | Balanced的IoU50提升3/3且平均差CI下界>0，但seed42/202未通过Sensitivity安全门槛；Full诊断线3/3通过，不能补救主检验 |
| Y5 external诊断 | 已完成；Full探索路线 | 癌患者触发0.893-0.942，但非癌患者误触发0.487-0.560；无bbox，不能评价外部定位IoU |
| Y6 ROI分类 | 当前下一步；Full探索路线 | 需先冻结ROI产生、无框回退和分类评价协议；不得用已揭盲internal test或Y5 external选阈值 |

## 前置结论

- 冻结M0 EfficientNet-B0仍是当前正式癌症分类器。
- 冻结M1能粗略定位，内部test mean IoU约`0.446`、center-hit约`0.907`。
- M3/M3b抑制、M3c-B硬门控和M4-M5c预测ROI分类干预均未在锁定internal test形成稳定产品收益。
- 因此新路线只检验：专用检测器能否在保护癌图检出的同时，提高病灶框几何质量。

## 冻结路线

```text
Y0-B  来源内平衡数据转换、bbox完整性、患者频次、精确/近重复审计
Y1    YOLO26n-640 smoke test，只验证代码和格式
Y2-B  平衡集YOLO26s-640 vs YOLO26s-960，固定seed42受控预筛
Y3-B  Y2-B唯一入选配置在平衡集运行seed 42/202/503（正式主实验）
Y0-F  完整集单独转换与审计，不读取其test作调参
Y3-F  完整集复用Y2-B唯一配置运行seed 42/202/503（预注册诊断分支）
Y4    一次性锁定同一198张internal test；M1、Balanced YOLO、Full YOLO作患者配对比较
Y5  无bbox external诊断投影，不用外部结果选模型或阈值
Y6  仅Y4成功后进行预测ROI分类
```

Balanced与Full的角色从本阶段起固定。Balanced来源内1:1.3清单为主实验，负责选择输入
分辨率并形成主要科学结论；Full完整清单只在配置冻结后增加训练监督，不能反向选择分辨率、
optimizer、epoch、阈值或后处理。完整清单共`3348张/1732人`，癌/非癌图片为
`1670/1678`，但癌/非癌患者为`446/1286`，患者及来源/风格明显不平衡；其`1670张`癌图
bbox覆盖100%，因此能检验更多框监督是否改善定位，同时必须报告可能的来源捷径。

Full训练只使用自身train/val（train `2350张`、val `497张`），其原有test `501张`不参与
模型选择，也不作为另一套正式测试。Y4中M1、Balanced YOLO与Full YOLO统一评价Y0-B已经
锁定的同一`198张/153人`internal test，保证所有配对差值来自相同患者和图像。Balanced为
主要结果，Full为预注册次要诊断；不能用Full结果补救Balanced失败。

## Y0协议与结果

输入为M1来源内1:1.3 Keep主清单：`1373张/1020人`。类别固定为
`early_cancer_or_HGD`。train/val转换为YOLO单类别视图；internal test只生成带SHA的
冻结队列，不进入`data.yaml`，未生成预测或指标。

正式输出：

```text
数据整理记录/图像裁剪/胃早癌概念提取训练集0804_预处理_v1/
10_Y0_YOLO26检测数据_20260813/
```

| split | 非癌图片/患者 | 癌图片/患者 | 是否导出到YOLO |
| --- | ---: | ---: | :---: |
| train | 551/402 | 424/312 | 是 |
| val | 113/86 | 87/67 | 是 |
| test | 112/86 | 86/67 | 否，仅冻结队列 |

自动验收：1373张全部可解码，文件SHA和记录尺寸一致；癌图框均存在、合法且界内；非癌图
定位监督关闭；患者不跨split；精确重复为0。每患者1-3张，各split/类别的中位数均为1、
最大值均为3，未见单患者图片数量失控。

近重复审计使用64-bit DCT pHash、Hamming distance<=6，共提出跨患者47组，其中跨split
19组。2026-08-13已逐对查看全部47组：均为构图、亮度或低频纹理相近，但黏膜纹理、皱襞、
反光、开口或病灶形态不同；未发现同图、同帧或可明确判定的相邻帧泄漏，不修改split。

框面积占全图比例：train中位数`0.235`、val `0.199`、锁定test `0.299`。该差异先作为
数据描述记录，不使用test分布调YOLO参数。

多病灶边界确认（2026-08-13）：根据当前数据认知，暂无已知同图存在多个分离的早癌/HGD
目标而只标一处的情况，因此单框CSV结构满足本阶段训练要求，Y0正式放行。该结论不扩展为
“任何胃镜图都不可能多病灶”；后续一旦发现多处分离目标，必须补为一图多框YOLO标签，
并重新冻结受影响的清单。非癌空标签仍只表示无早癌/HGD，不代表没有其他异常。

## Y1-Y4预注册

Y1安装并锁定Ultralytics、torch和CUDA版本，保存完整`args.yaml`。Y2/Y3使用相同预训练
来源、optimizer、学习率、weight decay、batch、epoch、patience和增强；只允许改变
`imgsz`、模型规模和seed。正式checkpoint统一采用锁定Ultralytics版本按内置validation
fitness保存的`best.pt`。当前锁定版本的Detection fitness须在Y1中由源码核验并记录；若为
当前官方定义，则`best.pt`实质按val mAP50-95选择。不得根据Top-1 IoU50、癌召回、非癌FP、
internal test或单seed重新挑epoch。Y1只做smoke，不产生性能结论；Y2开始前把实际官方
默认值完整展开并冻结，不能只记录“default”。

Top-1几何规则：正式主分析使用YOLO26默认`end2end=True`路径和训练一致的letterbox；
`imgsz`等于对应实验输入；geometry confidence固定`0.001`、`max_det=100`；不额外施加
NMS，NMS IoU不适用于该正式路径；Top-1为end-to-end最终输出中置信度最高框。Y1必须由
实际模型和源码核验并记录`end2end`状态。如果锁定版本实际走`end2end=False`传统路径，
则停止正式运行并单独记录`NMS=True, iou=0.70`，不得静默混入口径。无候选时IoU、coverage、
center-hit均记0，所有癌图都进入分母。

deployment threshold只在val癌图上冻结为满足图像级Sensitivity>=0.90的最高置信阈值。
主要终点为癌图Top-1 IoU>=0.50比例；次要终点为mean/median IoU、coverage、center-hit、
预测框面积、非癌positive-trigger rate、每张非癌框数、mAP50、mAP50-95及病灶大小分层。

安全性界值是`Sensitivity_YOLO - Sensitivity_M1 >= -0.02`的点估计，并报告患者聚类
bootstrap 95%CI。只有CI下界高于`-0.02`时才称统计学非劣效，否则只称通过工程安全门槛。

Y2先过安全门槛，再以Top-1 IoU50选择640或960；平手依次比较mean IoU、非癌触发率和
计算成本。Y4描述性地按`42/202/503`对应展示YOLO和M1结果，但配对来自两个模型评价完全
相同的冻结患者/图像，并非“随机种子数字相同”。正式成功要求：三seed IoU50均高于对应
M1，三seed平均配对提升的患者bootstrap 95%CI下界高于0，且每seed均通过Sensitivity
安全门槛。bootstrap单位为患者：每次有放回抽取test患者并带入其全部图像，分别计算每seed
的YOLO-M1差值，再对三seed差值求平均；重复5000次，使用percentile 95%CI。不得把三个
seed当成`n=3`进行t检验。失败则停止，不进入Y6。

## Y1工程冒烟结果

2026-08-13在启动前检查GPU显存、利用率和进程后，选用当时资源相对充足的GPU 1运行
`YOLO26n-640`、seed 42、batch 16、5 epoch。Y1只验证整条工程链能否正确执行，不参与
Y2模型、分辨率、阈值或checkpoint选择。

环境与关键源码行为已经锁定：

| 项目 | Y1实际值 |
| --- | --- |
| Python | `3.12.13` |
| Ultralytics | `8.4.118` |
| PyTorch / torchvision | `2.11.0+cu128` / `0.26.0+cu128` |
| PyTorch CUDA runtime | `12.8` |
| 检测头 | `Detect`，head与model均为`end2end=True` |
| 推理后处理 | `nms=False`，正式YOLO26路径不额外NMS |
| checkpoint fitness | `[0, 0, 0, 1]`，即val `mAP50-95` |

数据入口再次核验为train/val共`1175张`：癌图`511张`均有单类框标签，非癌`664张`均为空
标签；internal test `198张`未导出到YOLO视图，`data.yaml`不含test，external未读取。
训练完成5/5 epoch，AMP、GPU前向/反向、验证和标准结果保存均正常。`best.pt`、`last.pt`、
`args.yaml`、`results.csv`、训练批次图、val标签图和预测图均存在；人工抽查看到框坐标与图像
对齐，空标签背景样本可正常参与训练。Y1使用完整train/val而非少量伪数据，因此也验证了
正式数据读取链。

第5轮val `mAP50=0.06579`、`mAP50-95=0.02693`。这只是5轮冒烟值：预测仍稀疏，模型尚未
完成正式收敛，不能据此判断YOLO26路线成功或失败，也不能与M1作性能比较。本次`best.pt`
与`last.pt` SHA相同，表示5轮范围内内置fitness最优点恰为最后一轮，不改变正式checkpoint
规则。

正式输出：

```text
结果/YOLO26定位_0804/Y1_smoke/y1_yolo26n_640_seed42/
  args.yaml
  results.csv
  y1_smoke_config.json
  weights/best.pt
  weights/last.pt
```

备注：首次外层`tee`的日志父目录未预先创建，因此shell最终返回非零，但Python训练与产物
验收已完整成功，模型输出不受影响。后续长任务命令必须先创建明确的日志目录再启动`tee`。
Y1的`args.yaml`中`end2end: null`表示调用端没有覆盖模型设置；实际模型头和加载后的
`best.pt`均已核验为`end2end=True`。其中Ultralytics常规val的`max_det=300`用于框检测mAP，
正式项目Top-1几何分析仍按预注册单独使用`confidence=0.001, max_det=100, nms=False`；两种
口径用途不同，Y2实现时必须分别记录。

## Y2-B冻结参数与实现

Y2-B只比较`YOLO26s-640`和`YOLO26s-960`，固定seed 42。预训练权重为官方
`yolo26s.pt`，SHA-256为
`646f8bc3fe0a656803d95c294f7852321748cb29d13466a1af8862e2db384a1b`；模型约
`9.95M`参数，检测头与加载后的checkpoint必须保持`end2end=True`。两候选除`imgsz`外
使用完全相同的参数：

| 类别 | 冻结值 |
| --- | --- |
| 训练长度 | `epochs=100, patience=20` |
| batch/worker | 训练`batch=16, workers=4` |
| optimizer | 显式`AdamW, lr0=0.002, momentum=0.9, weight_decay=0.0005` |
| 调度 | `lrf=0.01, cos_lr=False, warmup_epochs=3, warmup_momentum=0.8, warmup_bias_lr=0` |
| loss权重 | `box=7.5, cls=0.5, dfl=1.5` |
| 色彩增强 | `hsv_h/s/v=0.015/0.7/0.4` |
| 几何增强 | `translate=0.1, scale=0.5, fliplr=0.5`；旋转、剪切、透视、上下翻转均为0 |
| 组合增强 | `mosaic=1.0, close_mosaic=10`；mixup/cutmix/copy-paste均为0 |
| 其他 | `amp=True, deterministic=True, rect=False, multi_scale=0, cache=False` |
| checkpoint | Ultralytics内置val fitness，即val `mAP50-95`最高的`best.pt` |

不使用`optimizer=auto`：在当前锁定源码中，平衡集100轮约6100 iterations会自动选AdamW，
完整集约14700 iterations则可能自动切成MuSGD；显式AdamW可避免Balanced与Full因数据量
不同而静默改变优化器。`lr0=0.002, momentum=0.9, warmup_bias_lr=0`与Y1中单类别数据触发
的auto实际选择一致。

标准检测验证继续使用Ultralytics val口径（`max_det=300`）计算mAP；项目Top-1几何评估
单独使用`confidence=0.001, max_det=100, nms=False`。几何推理batch固定为4，并把val目录
交给`LoadImagesAndVideos`分批读取；不得传`list[str]`，因为锁定版本会把路径列表一次性
转成内存图像并将全部200张作为一个batch，造成960推理显存溢出。deployment threshold、
M1配对、5000次患者整簇bootstrap、病灶大小分层和无候选记0均由统一评估入口完成。

Y2-B选择层级固定为：先通过`Sensitivity_YOLO-Sensitivity_M1>=-0.02`点估计安全门槛；
再按IoU>=0.50比例、mean IoU、非癌触发率、计算成本依次选择。mAP只作标准辅助指标，
不越过项目主终点直接选择分辨率。

2026-08-13已完成`YOLO26s-960`最坏显存路径debug：完整train `975张`跑1 epoch，确实包含
`424张`癌图和`551张`非癌图；训练峰值约`11GB`，AdamW冻结参数正确落盘；200张val按
batch 4完成Top-1推理、路径回配、阈值、M1配对、bootstrap与分层输出。该一轮模型的
IoU50=`0.0345`、mean IoU=`0.1230`仅证明评估能识别尚未收敛的模型，不作性能结论。
最初`fraction=0.1`恰按文件顺序抽到98张全非癌图，已归档诊断现场并把debug改为完整train
一轮；正式路径从未使用fraction抽样。

## Y2-B正式结果与分辨率冻结

2026-08-13已在来源内平衡train/val上完成seed 42的640/960受控对照，未读取internal test或external。
两组都在各自val冻结工作点检出 **79/87=0.9080** 张癌图，与同队列M1的 **79/87=0.9080** 持平。
按预注册主终点，640的Top-1框几何明显更好，因此冻结为Y3-B/Y3-F唯一分辨率。

| 指标 | YOLO26s-640 | YOLO26s-960 | 解读 |
| --- | ---: | ---: | --- |
| 标准val mAP50 | 0.3037 | 0.2414 | 辅助指标，640更高 |
| 标准val mAP50-95 | 0.1307 | 0.0900 | 辅助指标，640更高 |
| 癌图召回 | 0.9080（79/87） | 0.9080（79/87） | 两者均与M1持平 |
| IoU达到0.50 | **0.7356** | 0.6322 | 预注册分辨率主选择指标 |
| mean IoU | **0.5776** | 0.5349 | 640定位更准 |
| median IoU | **0.6360** | 0.5670 | 640的典型框重叠更好 |
| center-hit | 0.8736 | 0.8621 | 640略高 |
| 非癌图触发率 | 0.6814 | 0.6814 | 两者相同 |
| 非癌图平均候选框数 | **1.4956** | 1.7345 | 640较少 |

640在第73轮取得最佳checkpoint，第93轮早停。因close_mosaic=10，它只经历了约3轮无Mosaic训练，
但这些轮次未超过第73轮。960跑满100轮，关闭Mosaic后继续改善，最佳checkpoint为第100轮，
但最终几何指标仍低于640。因此不为追求对称而重跑Y2。

首次自动评估中，pandas默认浮点解析改变了M1固定边界分数的最后一位，导致M1被误计为78/87。
评估入口已改为round-trip精度读取，旧几何文件已归档，两分辨率已在相同修正源码下重算。
该修正只恢复M1历史冻结口径，不改变YOLO权重、预测框或分辨率选择。

### Y2-S无Mosaic追加微调敏感性实验

由960在第100轮仍刷新最佳指标，增加一条不改写Y2-B的独立收敛诊断。该实验不读取
internal test或external，640与960都从各自Y2-B best.pt出发，避免只给960增加训练量。

| 项目 | 冻结值 |
| --- | --- |
| 输入与随机种子 | 640与960，seed 42 |
| 初始权重 | 各自Y2-B的best.pt，并记录SHA-256 |
| 训练长度 | 固定20轮，patience=20，不允许早停造成训练量不对称 |
| 学习率 | AdamW，lr0=1e-4，lrf=0.1，线性降至1e-5；1轮warmup |
| 增强 | mosaic=0且close_mosaic=0；其余色彩、平移、尺度与翻转保持Y2-B口径 |
| checkpoint | 20轮中val mAP50-95最高的best.pt |
| 主评估 | 复用Y2-B Top-1几何口径和M1配对安全门槛 |

每个分辨率同时报告微调前后的IoU50、mean IoU、癌图召回和非癌触发率变化。
若微调后960按Y2-B原排序层级超过640，只记为训练预算影响证据，并触发新的三种子正式复核；
否则继续使用已冻结的640进入Y3-B。无论结果如何，本实验都不直接覆盖Y2-B结论。

2026-08-13两组已完整跑满20轮并完成冻结Top-1几何评估。两组癌图召回均保持
`79/87=0.9080`，但追加训练没有使960追上640：

| 指标 | 640原Y2 | 640微调 | 960原Y2 | 960微调 |
| --- | ---: | ---: | ---: | ---: |
| val mAP50-95 | **0.1307** | 0.1164 | 0.0900 | 0.1047 |
| 癌图召回 | 0.9080 | 0.9080 | 0.9080 | 0.9080 |
| IoU达到0.50 | **0.7356** | 0.7126 | 0.6322 | 0.6322 |
| mean IoU | 0.5776 | **0.5866** | 0.5349 | 0.5489 |
| median IoU | 0.6360 | **0.6663** | 0.5670 | 0.5895 |
| center-hit | 0.8736 | **0.8966** | 0.8621 | 0.8851 |
| 非癌触发率 | 0.6814 | 0.6814 | 0.6814 | **0.6372** |
| 非癌平均候选框数 | **1.4956** | 1.7168 | 1.7345 | 1.6637 |

960的mean IoU提升`0.0140`，非癌触发率下降`0.0442`，说明更长的无Mosaic收敛确实对
960有有限帮助；但主指标IoU50仍为`0.6322`，未追上微调后640的`0.7126`，更未追上
原640的`0.7356`。640微调后mean IoU略升，但IoU50下降2张且非癌候选框增多，也没有净收益。
因此“960只是训练轮数不足”不能解释Y2-B差距；Y3-B继续使用原Y2-B 640 best.pt及原冻结配置，
不使用任何Y2-S微调权重。

## Y0-F审计与Y3实现状态

Y0-F已从冻结的M1完整清单构建。源清单为
`09_M1全量诊断清单_20260810/m1_full_keep_split_seed42.csv`，SHA-256为
`a02b0d0f45250d418454d36a267b73b0269710c281b4d6441ac05dd97300d7c2`。总计
`3348张/1732人`；其中train为`2350张/1212人`，val为`497张/260人`，原full-test为
`501张/260人`。Y0-F只向YOLO导出train/val，原full-test只写入排除清单，未导出、未评估，
也不作为Y4队列；Y4仍只使用平衡路线已冻结的`198张/153人`internal test。

全部3348张均通过解码、SHA、尺寸、bbox范围、定位监督和患者不跨split检查，精确重复为0。
宽松pHash筛出340对跨患者候选，其中142对跨split；真正涉及Y3-F训练泄漏的train-val候选为
53对。二级审计将两图确定性缩放到224灰度后计算SSIM：均值`0.4970`、中位数`0.5121`、
最大值`0.6696`，没有一对达到预设复核线`0.85`；最低pHash距离对照也确认是低纹理胃黏膜
造成的哈希碰撞，不是同帧或相邻帧。因此不修改数据，Y0-F放行。旧full-test相关相似候选不
参与Y3训练决策。

Y3冻结协议如下：

- 唯一模型为`YOLO26s-640`，每个新增seed均从同一官方`yolo26s.pt`独立训练；
- optimizer、学习率、增强、100轮上限、patience 20及checkpoint规则全部继承Y2-B；
- Balanced seed42直接复用已冻结Y2-B 640结果，避免重复训练；Balanced新增seed202/503；
- Full独立运行seed42/202/503，不从Balanced续训；
- 每个YOLO seed与同角色、同seed的M1 warmup-only产品配对；
- val癌图上锁定满足Sensitivity>=0.90的最高YOLO阈值，内部test、external及原full-test均不读；
- 单seed成功要求：相对M1癌召回差不低于`-0.02`，且IoU>=0.50比例和mean IoU均严格提高；
- 阶段成功要求3/3 seed均成功；同时报告按患者有放回抽样5000次的配对差值95%CI。

平衡与完整角色均已完成1 epoch隔离冒烟：训练、Ultralytics参数落盘、best/last权重、
Top-1推理、同seed M1配对、阈值、几何指标和患者bootstrap均通过。完整集首次冒烟发现公共
推理函数默认固定读取平衡val目录，造成只返回`200/497`张；现已增加向后兼容的可选
`source_dir`，Y2默认行为不变，Y3显式传角色对应val目录。修复后完整集覆盖`497/497`张。
debug模型只训练1轮，其低定位指标不作性能结论。

2026-08-13首次Y3-B正式运行未产生性能结论：seed202在`results.csv`完整保存55行、
`last.pt`写入后，于下一轮训练期间被主机内存OOM终止；seed503尚未启动。服务器未重启，
checkpoint、优化器状态和参数清单均完整。训练入口现支持显式`--resume`，矩阵启动器只在
“存在`last.pt`和历史、尚无正式训练配置”时自动续训；续训前再次核对role、seed、数据目录
及全部冻结参数，完成后仍执行原产物校验、同seed M1几何评估和三种子汇总。

2026-08-14使用上述受控断点恢复完成seed202，并从相同官方预训练权重独立完成seed503；
Balanced seed42按预注册复用冻结Y2-B 640结果。seed202从第56轮继续，恢复了`708/708`
项权重及优化器状态，最佳epoch为78，因patience在第98轮结束；seed503完整训练100轮，
最佳epoch为88。两次训练完成后均由`best.pt`执行固定Top-1几何评估，internal test、external
和full原test均未读取。

Y3-B正式三种子结果：

| seed | mAP50-95 | 癌图Sensitivity | IoU>=0.50 | M1 IoU>=0.50 | IoU50差值 | mean IoU | M1 mean IoU | mean IoU差值 | center-hit | 非癌触发率 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 42 | 0.1307 | 0.9080 | 0.7356 | 0.4368 | +0.2989 | 0.5776 | 0.4437 | +0.1339 | 0.8736 | 0.6814 |
| 202 | 0.1204 | 0.9080 | 0.6667 | 0.3678 | +0.2989 | 0.5531 | 0.4466 | +0.1065 | 0.9080 | 0.7168 |
| 503 | 0.1494 | 0.9080 | 0.6667 | 0.4023 | +0.2644 | 0.5591 | 0.4346 | +0.1245 | 0.8966 | 0.7345 |
| 平均 | 0.1335 | 0.9080 | 0.6897 | 0.4023 | +0.2874 | 0.5633 | 0.4417 | +0.1216 | 0.8927 | 0.7109 |

三种子全部通过单seed工程安全门槛，阶段结论为`stable_success_3_of_3`。在癌召回固定为
79/87时，专用YOLO26检测器把平均mean IoU从M1约`0.442`提高到`0.563`，IoU>=0.50比例
从约`0.402`提高到`0.690`；三个seed方向一致。seed202和503的患者聚类bootstrap中，
IoU50与mean IoU配对提升的95%CI下界均高于0；seed42统一汇总时也按相同协议重新配对。
因此可确认YOLO26明显改善框几何质量。非癌触发率仍为`0.681-0.735`，说明本阶段没有解决
“非癌图不应出框”的展示门控问题；该指标作为后续部署风险保留，不能用它否定已确认的
癌图定位几何提升，也不能把当前检测器表述为已具备足够特异性。

正式汇总位于：

```text
结果/YOLO26定位_0804/Y3固定640三种子/自动汇总_balanced/
```

Y3-F于2026-08-14按冻结640配置启动。seed42完整训练100轮并完成正式几何评估，最佳epoch
为97：mAP50=`0.5629`、mAP50-95=`0.2467`；在val癌图Sensitivity固定为
`216/240=0.9000`时，mean IoU由同seed M1-F的`0.4777`提高到`0.6370`，IoU>=0.50
由`0.4500`提高到`0.7625`。两项配对提升分别为`+0.1592`和`+0.3125`，患者聚类
bootstrap 95%CI分别为`[0.1176,0.1980]`和`[0.2296,0.3930]`；单seed正式通过。
非癌触发率为`0.5019`。病灶大小分层中small/medium/large癌召回为
`0.8105/0.9481/0.9706`，提示剩余漏检主要集中于小病灶。

同一矩阵的seed202在完成35轮并进入第36轮后，于`2026-08-14 11:32:25`由DataLoader
报告训练图片不存在；随后的seed503前置检查确认整个
`数据整理记录/图像裁剪/胃早癌概念提取训练集0804_预处理_v1`目录已经不存在。该目录的
父目录mtime与报错时刻一致，项目其他位置及当前用户回收站均未找到移动后的副本。因此本次
中断不是模型、CUDA或主机OOM失败，而是训练输入在运行期间被外部文件操作移除。seed202
保留35轮`results.csv`、带优化器状态的`last.pt`及中间`best.pt`，尚未生成正式
`y3_train_config.json`，满足代码定义的受控续训现场；seed503未开始训练。恢复前不得汇总
Y3-F，也不得用seed42单seed结果反向修改冻结配置。

数据恢复后，M1-F清单SHA、Y4冻结队列SHA、Y0-F清单/映射行数、2847个train/val图片
链接与标签、全部链接目标均重新通过；缺失的train-val近重复二级审计由原正式脚本确定性
重建，53对的均值、中位数、最大SSIM及阈值以上计数与seed42冻结记录逐项完全一致。
`load_data_audit('full')`返回内容与seed42产品内嵌的`data_audit`逐字段相等，seed202的
持久化Ultralytics参数也通过恢复校验。因此seed202从第36轮继续，不重跑已完成部分；随后
seed503从相同官方预训练权重独立训练，矩阵正常汇总。

Y3-F正式三种子结果：

| seed | best epoch | mAP50-95 | 癌图Sensitivity | IoU>=0.50 | M1 IoU>=0.50 | IoU50差值 | mean IoU | M1 mean IoU | mean IoU差值 | center-hit | 非癌触发率 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 42 | 97 | 0.2467 | 0.9000 | 0.7625 | 0.4500 | +0.3125 | 0.6370 | 0.4777 | +0.1592 | 0.9208 | 0.5019 |
| 202 | 86 | 0.2358 | 0.9000 | 0.7583 | 0.4542 | +0.3042 | 0.6270 | 0.4541 | +0.1729 | 0.9250 | 0.5058 |
| 503 | 87 | 0.2505 | 0.9000 | 0.7833 | 0.4375 | +0.3458 | 0.6391 | 0.4550 | +0.1841 | 0.9417 | 0.4786 |
| 平均 | - | 0.2443 | 0.9000 | 0.7681 | 0.4472 | +0.3208 | 0.6343 | 0.4623 | +0.1721 | 0.9292 | 0.4955 |

三种子均通过单seed成功判据，正式阶段结论为`stable_success_3_of_3`。每个seed相对同seed
M1-F的IoU50和mean IoU提升均为正，且各自患者聚类bootstrap 95%CI下界均高于0；Full的
更多框监督与更大开发集在自身val上形成了稳定几何收益。该结果是预注册诊断证据，不能与
Balanced不同的val队列作严格配对比较，也不能反向替换Y3-B主实验结论。

训练量判断：三个seed的最佳epoch为`97/86/87`。seed42虽在97轮刷新最佳，但93--100轮
mAP50-95围绕平台波动而非单调上升；seed202和503均在100轮前较早达到最佳，且最后学习率
已降至约`3.98e-5`。因此当前没有“100轮普遍不足”的证据，不追加正式训练轮数。若以后
研究更长训练，只能另立敏感性实验，不能覆盖当前100轮冻结结果。

正式汇总位于：

```text
结果/YOLO26定位_0804/Y3固定640三种子/自动汇总_full/
```

实现入口：

```text
程序/模型训练/正式代码/train_y2_yolo26.py
程序/模型训练/正式代码/evaluate_y2_yolo26.py
程序/模型训练/正式代码/summarize_y2_yolo26.py
程序/模型训练/正式代码/run_y2_yolo26_matrix.sh
程序/模型训练/正式代码/train_y2_resolution_sensitivity.py
程序/模型训练/正式代码/evaluate_y2_resolution_sensitivity.py
程序/模型训练/正式代码/summarize_y2_resolution_sensitivity.py
程序/模型训练/正式代码/run_y2_resolution_sensitivity_matrix.sh
程序/模型训练/正式代码/build_y0_full_yolo_dataset.py
程序/模型训练/正式代码/audit_y0f_near_duplicates.py
程序/模型训练/正式代码/train_y3_yolo26.py
程序/模型训练/正式代码/evaluate_y3_yolo26.py
程序/模型训练/正式代码/summarize_y3_yolo26.py
程序/模型训练/正式代码/run_y3_yolo26_matrix.sh
程序/模型训练/正式代码/evaluate_y4_yolo26_locked_internal_test.py
程序/模型训练/正式代码/run_y4_yolo26_locked_internal_test.sh
程序/模型训练/正式代码/evaluate_y5_yolo26_full_external.py
程序/模型训练/正式代码/run_y5_yolo26_full_external.sh
```

## Y4锁定internal test结果

2026-08-14在所有checkpoint、SHA、验证集部署阈值和`198张/153人`队列完成预检后，使用
GPU 0一次性揭盲Y4。队列包括非癌`112张/86人`、癌`86张/67人`；六组模型使用完全相同
的图片和患者。YOLO和M1的检出阈值均直接读取Y2/Y3验证集冻结配置，未在test上重新扫描。
缺失候选框按预注册计入分母且几何为0；bootstrap按癌患者有放回抽样并带入其全部图片，
重复5000次。

Balanced正式主线结果：

| seed | M1 Sens | YOLO Sens | Sens差值 | M1 IoU50 | YOLO IoU50 | IoU50差值 | M1 mean IoU | YOLO mean IoU | 非癌触发率 M1→YOLO | Sens安全门槛 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 42 | 0.9302 | 0.9070 | -0.0233 | 0.4302 | 0.6163 | +0.1860 | 0.4454 | 0.5518 | 0.8214→0.6696 | 未通过 |
| 202 | 0.9535 | 0.8605 | -0.0930 | 0.3953 | 0.6628 | +0.2674 | 0.4453 | 0.5575 | 0.8482→0.7411 | 未通过 |
| 503 | 0.9535 | 0.9535 | 0.0000 | 0.4651 | 0.7093 | +0.2442 | 0.4486 | 0.5764 | 0.7857→0.7589 | 通过 |

三seed平均IoU50差值为`+0.2326`，患者聚类bootstrap 95%CI为
`[+0.1481,+0.3217]`；mean IoU平均差值为`+0.1154`，95%CI为
`[+0.0789,+0.1526]`。因此“专用检测器提高病灶框几何质量”在独立内部test上得到
明确支持。与此同时，Sensitivity平均差值为`-0.0388`，95%CI为
`[-0.0894,+0.0116]`；seed42比同seed M1少检出2张癌图，seed202少检出8张，均低于
预注册工程安全线`-0.02`。正式成功要求三seed同时满足IoU和Sensitivity规则，故
**Balanced Y4主检验判定未通过**。不能因几何结果很好而事后放宽安全门槛。

冻结训练集面积三分位的描述性分层显示，seed202的主要问题集中在small组：21张小病灶
中Sensitivity由M1的`0.8571`降到YOLO的`0.6190`；medium和large分别为
`0.9231/0.9487`。seed42的小病灶Sensitivity也由`0.9048`降至`0.8095`。与此同时，
YOLO在三个大小组的IoU50均高于M1，说明问题不是框一旦检出就不准，而是Balanced冻结
阈值在部分小病灶上未能稳定产生足够高的置信度。

Full预注册诊断线结果：

| seed | M1 Sens | YOLO Sens | Sens差值 | M1 IoU50 | YOLO IoU50 | IoU50差值 | M1 mean IoU | YOLO mean IoU | 非癌触发率 M1→YOLO |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 42 | 0.8837 | 0.9651 | +0.0814 | 0.5698 | 0.8023 | +0.2326 | 0.4851 | 0.6627 | 0.8214→0.5268 |
| 202 | 0.9302 | 0.9186 | -0.0116 | 0.5465 | 0.7791 | +0.2326 | 0.4897 | 0.6626 | 0.8125→0.6071 |
| 503 | 0.9186 | 0.9419 | +0.0233 | 0.5233 | 0.8140 | +0.2907 | 0.4631 | 0.6693 | 0.7500→0.4911 |

Full三seed均通过点估计安全门槛且IoU50均提升；IoU50平均差值`+0.2519`，患者bootstrap
95%CI为`[+0.1573,+0.3492]`，Sensitivity平均差值`+0.0310`。这项结果说明YOLO26
路线本身有效，并提示更多框监督可能提高跨seed检出稳定性；但Full使用不同且更大的开发
数据，患者/来源不平衡，按预注册只能作为诊断证据，不能替代Balanced主结果或把Y4改判
成功。

本次Y4已消耗内部test，今后该队列只能作固定复核，不能用于修改训练数据、选择Balanced
或Full、重定阈值、改后处理或筛选新模型。正式产物位于：

```text
结果/YOLO26定位_0804/Y4锁定内部测试_20260814/
```

### Y4后Full继续决策

2026-08-14，用户在确认Balanced主检验因Sensitivity安全门槛未通过、Full仅属预注册诊断
证据后，明确决定后续直接使用Full三seed继续。该决定不追溯修改Y4判定，而是启动独立标记
的“Y4后探索性Full路线”：

- 固定使用Y3-F的seed 42/202/503三个`best.pt`，不根据Y4或外部表现删选seed；
- 每个seed继续使用自身Y3-F val冻结的部署阈值，不在external重新扫描阈值；
- Y5使用既有完整外部Keep清单`1941张/1329人`，无bbox，因此不报告IoU或中心命中；
- Y5只报告癌/非癌图及患者的框触发率、候选框数量、Top-1置信度、预测框面积，以及来源和
  尺寸分层；任何置信度差异都只能解释为“检测器响应”，不能等同病灶定位准确率；
- external结果只用于描述部署域行为，不用于返调Full权重、阈值、epoch或后处理；
- 后续Y6若启动，必须继续标注为探索性Full路线，不能写成原Balanced预注册主线成功后的
  确证实验。

## Y5 Full外部诊断结果

2026-08-14使用Y3-F seed 42/202/503三个冻结`best.pt`和各自val部署阈值，投影到既有完整
外部Keep集`1941张/1329人`。其中癌`542张/121人`，非癌`1399张/1208人`。外部没有bbox，
因此本阶段只报告检测器响应；未读取Y4内部test，未在external重新选择seed、阈值或后处理。

| seed | 阈值 | 癌图触发 | 非癌图触发 | 癌患者任一图触发 | 非癌患者任一图触发 | 患者癌触发95%CI | 患者非癌触发95%CI |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 42 | 0.0298 | 0.8911 | 0.5354 | 0.9421 | 0.5588 | [0.9008,0.9835] | [0.5298,0.5861] |
| 202 | 0.0517 | 0.8727 | 0.5347 | 0.9256 | 0.5604 | [0.8760,0.9669] | [0.5331,0.5877] |
| 503 | 0.0551 | 0.8100 | 0.4639 | 0.8926 | 0.4868 | [0.8347,0.9421] | [0.4578,0.5149] |

三seed图像级多数触发的癌/非癌率为`0.8672/0.5089`；患者级多数触发为
`0.9256/0.5373`，即Sensitivity约92.6%、Specificity约46.3%。三seed在图片和患者层面
完全一致的比例分别为`0.7532/0.7404`，说明总体模式一致，但仍有约四分之一病例存在seed
差异。不能只挑误触发最低的seed503作为新产品，因为它的癌触发也最低，且预先冻结的是
三seed诊断而非external择优。

分层显示：外部癌图主要位于large组（406/542），其触发率低于medium组；2023年相较
2022年普遍呈现癌触发略低、非癌触发略高，提示仍存在年份/设备域偏移。small癌图只有
14张/2人，conservative输入只有19张癌图和16张非癌图，均只作描述，不作稳定结论。
外部目录根本身按“早癌/非癌”组织，来源字段与标签完全混杂，因此没有伪造独立的中心
分层结果。

Y5确认Full检测器在外部癌患者上多数能够产生候选框，但非癌误触发仍约一半；由于没有
外部bbox，不能确认这些癌框是否真正覆盖病灶，也不能把约92.6%的患者触发率写成外部定位
Sensitivity。正式产物位于：

```text
结果/YOLO26定位_0804/Y5外部诊断_Full_20260814/
```

## Y5-Y6边界

Y5无bbox，只报告框触发率、框数、置信度、面积和来源/尺寸分层，不把置信度AUC写成定位
IoU。Y6开始前重新冻结局部分类器数据；第一版优先用GT bbox加位置、宽高和尺度jitter
模拟预测误差，OOF预测ROI仅作高成本候选。存在有效ROI时才融合全局与局部概率；无ROI时
必须回退`p_global`，不得把`p_local=0`后继续平均。

## Y6探索性Full局部分类协议（冻结于2026-08-14）

Y6回答的问题是：**Y3-F给出的高分辨率候选区域，经过一个独立的局部分类器后，能否为冻结
M0-F全局分类器提供稳定的互补信息？** 本阶段不是重新证明Y3-F定位成功，也不修改Y4已失败
的Balanced主检验结论；全部结果继续标记为“Y4后探索性Full路线”。

### 数据与隔离

- 开发清单固定为`11_Y0F_YOLO26完整诊断数据_20260813/y0f_mapping.csv`的train/val。
  该表3348行中的原38个字段已与
  `09_M1全量诊断清单_20260810/m1_full_keep_split_seed42.csv`逐值一致，只新增YOLO路径；
- 训练只使用train的`2350张/1212人`，选择只使用val的`497张/260人`；Y0F原test、Y4
  internal test和Y5 external均不得进入训练、checkpoint选择、ROI margin或融合权重选择；
- 三个seed分别绑定同seed的M0-F EfficientNet-B0和Y3-F YOLO26，不允许按Y4/Y5表现挑seed；
- 全局分类器全程冻结；YOLO全程冻结，并沿用同seed Y3-F val已锁定的部署阈值。

### ROI生成

训练局部分类器时不使用训练集上的YOLO预测，以避免让同一训练集拟合出的检测器再次制造
乐观的ROI：

- 癌图以医生GT bbox为中心，先按`margin=0.20`扩边并转为不补黑边的正方形ROI；训练时
  对ROI中心在横纵方向各加入不超过边长10%的随机平移，尺度乘以`Uniform(0.90,1.10)`；
- 非癌图没有GT框。每次读取时从train癌图扩边后ROI的宽高分布随机抽一个尺寸，再在该非癌
  图内随机放置黏膜ROI。尺寸分布与癌图相同，因此“裁剪块大小/是否有框”本身不能泄露标签；
- train的局部标签直接等于图片标签。患者/类别平衡采样继续保留，避免多图患者或多数患者
  主导局部分类器；
- val固定使用同seed Y3-F的Top-1预测框。Top-1置信度达到冻结阈值时，按相同
  `margin=0.20`扩边生成预测ROI；未达到阈值时标记为无ROI，不伪造局部结果。

### 模型与训练

- 局部Encoder为同seed M0-F EfficientNet-B0 Encoder的副本，局部分类头也从M0-F分类头
  初始化；它与冻结全局分支参数独立；
- 阶段A共5轮：冻结局部Encoder，只训练局部分类头，学习率`1e-3`；
- 阶段B最多15轮：仅解冻局部`features[7:]`和局部分类头，学习率`1e-4`，patience为5；
- 损失为局部二分类交叉熵，label smoothing固定`0.1`。每轮在val执行完整部署数据流并按
  患者级AUC选择checkpoint，平手依次比较图像级AUC和较低val交叉熵；
- 全局分支参数和BN running统计在训练前后做SHA校验，任何变化均拒绝产物。

### 冻结融合与判定

对每张val图：

```text
无YOLO框: p_final = p_global
有YOLO框: p_final = (p_global + p_local) / 2
```

第一版不搜索融合权重，不把YOLO置信度、框面积或几何字段输入分类器。患者概率严格沿用
M0-F正式口径，即该患者全部图片概率的均值。产品保护允许在候选融合不高于全局时回退
同seed全局模型，但“Y6有收益”必须
来自真实局部模型而非回退：

- 单seed候选融合患者AUC必须严格高于同seed冻结全局患者AUC；
- 阶段成功要求3/3 seed均提高，且三seed平均提升至少`0.01`；
- 同时报告图像级AUC、患者级AUC、ROI触发率、癌/非癌局部头AUC、按病灶大小和来源分层，
  以及患者聚类bootstrap的融合减全局AUC差95%CI；
- 若未达到上述标准，结论为“Y3-F框几何改善未稳定转化为分类收益”，不得再用Y4 test或
  Y5 external返调。后续若要做OOF预测ROI训练，必须另立新协议。

实现冒烟后首次正式启动在seed42训练中发现患者聚合误写为`top2_mean`：图像AUC与M0-F
完全一致，但患者AUC为`0.9353`，无法复现M0-F记录的`0.9133`。核对历史训练函数确认M0-F
使用患者全部图片概率均值后，任务立即停止，未产生正式config；中断现场保留为
`y6_full_efficientnet_b0_seed42_invalid_top2_interrupted_20260814_1507`，不作结果。
代码已改为全图均值，并增加“每个正式epoch的冻结全局患者AUC必须在`1e-7`内复现对应
M0-F配置”的硬校验，防止口径再次漂移；修正后GPU冒烟重新通过。

修正后seed42完成，但seed202/503在同一硬校验处分别出现`0.0000773`的AUC差异。进一步
逐图比较确认：当前环境重推理与M0-F历史落盘概率的最大绝对差分别约`0.0011/0.0017`，
平均约`0.00015`，导致各有一对正负患者排序翻转；这是checkpoint重推理的浮点漂移，不是
数据或权重错配。为避免把运行环境差异混入Y6增益，正式实现改为在val直接绑定M0-F当时
已冻结保存的逐图概率，局部模型只新增`p_local`并执行预注册固定均值；绑定后必须逐路径、
患者和标签一一对应，并以`1e-12`复现M0-F患者AUC。此前seed42完整结果及seed202/503
失败现场均保留为无效诊断，三seed从头按同一冻结概率口径重跑。

## 下一步

1. 保持Y4判定冻结，不用内部test或external继续选择Full seed、阈值、epoch或后处理；
2. 按上述冻结协议实现并运行Y6三seed：train使用GT癌框加jitter及尺寸匹配的非癌随机ROI，
   val使用冻结Y3-F预测框，无框回退全局，固定1:1概率均值；
3. Y6不得用Y4 internal test或Y5 external选择seed、检测阈值、ROI margin或融合权重；在
   新的独立带框确认数据到位前，仍不能宣称Full已获得外部定位确认。
