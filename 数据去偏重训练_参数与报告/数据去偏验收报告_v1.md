# 第二批数据去偏自动验收报告 v1

> 日期：2026-07-22  
> 范围：完全弱标注版 P1-P5  
> 结论：自动预处理与清单结构通过；统计去偏未完全通过

## 1. 已完成工作

- 5229 张图使用统一规则完成黑边、侧栏和视频进度条处理，自动验收与分层肉眼检查通过。
- P2 在患者内使用 dHash 候选加 SSIM>=0.95 确认近重复，4099 张图入选，每患者最多 3 张。
- 生成全病例松弛 1:1、松弛 1:2 和严格共同支持 1:1 三套清单。
- 所有模型外审计均使用患者级 5 折 OOF；matched 清单中同一病例及其配对对照不跨折。

## 2. 清单结构结论

| 清单 | 患者 | 图像 | 主要用途 |
| --- | ---: | ---: | --- |
| patient-limited | 2321 | 4099 | 匹配前对照 |
| 松弛 1:1 | 830 | 1623 | 全部 415 名癌患者覆盖分析 |
| 松弛 1:2 | 1245 | 2197 | 病例对照比例敏感性分析 |
| 严格共同支持 1:1 | 180 | 238 | 粗设备/风格控制敏感性分析 |

松弛匹配虽然完全平衡中心患者数，但 415 对中 331 对必须放宽到不同风格，无法解决共同支持缺失。严格集将中心、粗 style_group 和每对图像数配平，但只覆盖 21.69% 癌患者。

## 3. 非图像元数据基线

随机森林患者级 OOF AUC：

| 清单 | 尺寸 | 无年份中心/粗风格 | 仅预处理 | 综合元数据 |
| --- | ---: | ---: | ---: | ---: |
| patient-limited | 0.940 | 0.935 | 0.952 | 0.960 |
| 松弛 1:1 | 0.970 | 0.954 | 0.970 | 0.982 |
| 松弛 1:2 | 0.969 | 0.954 | 0.967 | 0.978 |
| 严格共同支持 | 0.751 | 0.500 | 0.751 | 0.815 |

严格集的无年份中心/粗风格 AUC 为 0.500，说明预定义粗匹配有效。剩余预测力主要来自粗风格组内的精确尺寸、ROI/轮廓几何和年份代理信息。

## 4. 低层像素统计基线

不使用空间病灶语义，仅使用颜色、边界构图、纹理和 JPEG 统计。随机森林患者级 OOF AUC：

| 清单 | 颜色/亮度 | 边界构图 | 纹理/压缩 | 全部低层统计 |
| --- | ---: | ---: | ---: | ---: |
| patient-limited | 0.822 | 0.837 | 0.714 | 0.880 |
| 松弛 1:1 | 0.816 | 0.838 | 0.803 | 0.897 |
| 松弛 1:2 | 0.839 | 0.848 | 0.796 | 0.905 |
| 严格共同支持 | 0.706 | 0.703 | 0.762 | 0.770 |

严格集较松弛集明显下降，但仍存在中等强度的低层预测信号。严格集绝对 SMD 最高的残余项为：ROI 面积比例 -0.921、轮廓面积比例 -0.808、边缘密度 0.675、边界与中心纹理差 -0.642、年份 0.636。

低层颜色和纹理既可能反映设备，也可能包含真实黏膜差异，因此该结果用于提示捷径风险，不能单独证明所有信号均为偏倚。

## 5. 验收结论

- **工程验收：PASS。** 图像、清单、患者标签、配对关系、OOF 完整性和结果重算均通过独立检查。
- **松弛清单去偏验收：FAIL。** 元数据和低层统计仍高度预测标签，只能用于全病例覆盖分析。
- **严格清单去偏验收：PARTIAL PASS。** 已消除中心和粗风格差异，但精确构图、纹理和年份仍不平衡，且样本覆盖有限。
- 现有数据无法支持“模型已经完全去偏”的结论。后续结果必须并列报告 full/松弛/严格清单，并依赖多中心外部验证判断泛化。

## 6. 进入训练前的要求

1. P6 对训练集加入随机降采样再升采样、随机 JPEG 压缩和轻微高斯模糊，验证/测试不使用随机破坏。
2. 增加适度的随机裁剪/缩放，降低固定 ROI 面积和边界构图信号；参数需保守，避免裁掉病灶。
3. 训练脚本显式读取冻结 manifest 与患者级 fold，不在运行时重新匹配或随机拆患者。
4. 正式报告中将松弛集定义为覆盖分析、严格集定义为风格控制敏感性分析，不指定单一“无偏核心集”。
5. 训练后继续执行边缘遮挡、中心 ROI、灰度对照和 Grad-CAM，检查模型是否依赖残余边界与设备纹理。
