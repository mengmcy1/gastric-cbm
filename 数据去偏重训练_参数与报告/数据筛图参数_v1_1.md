# 第二批数据自动筛图参数 v1.1

> 冻结日期：2026-07-22  
> 输入：`数据/第二批裁剪后_v1_1/processed_manifest.csv`  
> 正式输出：`数据整理记录/第二批/筛图_v1_1/full`  
> 适用路线：完全弱标注 `matched_weak_v1`

## 1. 质量处理边界

- 所有质量指标均在 v1.1 裁剪后的 512x512 图像上计算。
- 清晰度使用统一缩放到 224x224 后的灰度 Laplacian variance，仅在患者内部排序。
- 不设置全局模糊硬阈值，避免不同设备、中心或标签因清晰度分布不同而被不对称删除。
- 损坏或无法解码图像才判为不可用；本批 5229 张均可用。
- 不读取模型预测、概率、Grad-CAM、测试结果或病灶可见性进行筛选。

## 2. 患者内近重复判定

| 步骤 | 冻结参数 | 用途 |
| --- | --- | --- |
| 候选召回 | 64-bit dHash，Hamming distance <= 4 | 快速找到可能相似的图像对 |
| 二次确认 | 224x224 灰度 SSIM >= 0.95 | 仅合并结构高度一致的连续帧 |
| 分组方式 | 确认重复边的连通分量 | 生成患者内 `duplicate_group` |
| 代表图 | 组内 Laplacian variance 最高 | 清晰度相同时按路径稳定排序 |

dHash-only v1 在全量审计中得到 130 个候选图像对，但只有 19 对满足 SSIM >= 0.95；最低候选 SSIM 为 0.344。视觉检查发现 dHash 会把带器械或不同病灶视角误合并，因此 v1 结果不得用于后续匹配或训练。v1.1 将 dHash 降为候选召回，必须经 SSIM 二次确认。

## 3. 每患者限图

1. 每位有可用图像的患者至少保留 1 张，最多保留 3 张。
2. 第一张选择所有重复组代表图中清晰度最高者。
3. 后续图片最大化其与已选图片的最小 dHash 距离，清晰度作为并列决胜条件。
4. 文件名时间戳可解析时，优先保证与已选图至少间隔 2 秒；候选不足时允许放宽并记录 `time_gap_relaxed=True`。
5. 所有未入选图仍保留在 `selection_manifest.csv`，原因记录为 `near_duplicate` 或 `patient_image_limit`，不删除任何图像文件。
6. 所有图像与患者统一标记 `review_status=pending`，表示尚未经过临床逐图病灶可见性确认。

## 4. 自动画面风格字段

匹配前生成不依赖标签的三个字段：

- `size_group`：原图长边分为 `<=512`、`513-1024`、`1025-1600`、`>1600`。
- `aspect_group`：竖图、近方形、近 4:3、近 16:9、其他横图。
- `frame_profile`：全幅、轮廓框、侧栏 UI、底部进度条 UI 或回退全幅。

`style_group = size_group | aspect_group | frame_profile`。原始精确分辨率继续保留用于审计，但不直接作为匹配组，避免 463x410 与 464x410 等同设备画面被过度拆分。

## 5. 正式结果

- 输入：5229 张，2321 位患者。
- 最终入选：4099 张；每患者 1 张 1203 人、2 张 458 人、3 张 660 人。
- SSIM 确认并剔除的近重复图：19 张，确认重复对 SSIM 为 0.9759-1.0000。
- 时间戳可解析：3244 张；174 张入选图因候选不足记录时间间隔放宽。
- 自动验收：PASS；0 硬失败，最大每患者入选数为 3。
- 风险复核材料：159 位患者，覆盖全部确认重复患者、全部时间放宽患者、图片最多患者和中心×标签样本。
- 全部确认重复组经视觉检查均为高度一致连续帧，未发现不同病灶或不同视角被错误删除。

## 6. 已知限制

- 自动筛图只保证质量、去重和患者权重上限，不保证癌患者每张入选图都能看到病灶。
- 画面风格与标签仍高度相关。例如 `>1600 + 16:9 + contour_frame` 的 1073 张图全部为癌图，必须在 P4 通过患者级来源/风格匹配处理。
- 边界相似帧采取宁可保留的策略；同一患者不会跨数据划分，因此保守保留不会造成患者泄漏。
- dHash 仍用于多样性排序，后续临床精标注版可由医生替换自动入选图。
