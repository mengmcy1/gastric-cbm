# 双电脑实验同步与产物管理说明

## 1. 目的与适用范围

本仓库同时维护两条互不混淆、但共享统一评价协议的实验线：

1. 自研 Framework：继续验证显露补全与补充高斯能否支撑水平总视角 30°（中心左右各 ±15°）。
2. InfiniSplat 对照复现：先复现官方 RGB 单图流程，再把冻结 PLY 接入相同的 `true_arc` 5°/15°/30° 协议。

两台电脑共用同一个 Git 仓库。Git 用来同步代码、配置、文档和小型证据，不用来镜像全部 `outputs/`。电脑上是否存在某个大型输出，只表示该电脑能否立即复核，不改变已经记录并有哈希支撑的实验事实。

本项目固定使用以下机器标识，后续记录不得只写含义会随位置变化的“本机”或“另一台电脑”：

| 机器 ID | 系统与硬件 | 主要用途 | 命令环境 |
|---|---|---|---|
| `win5060` | Windows，RTX 5060 Laptop GPU 8 GB | 本地开发、人工播放、SHARP/自研与 InfiniSplat 小规模实验 | PowerShell 7，命令显式通过 `pwsh.exe -NoLogo -NoProfile -Command` 执行 |
| `linux5080` | Linux 服务器，RTX 5080 | 较重推理、正式矩阵和服务器侧复核 | Linux shell；不得照搬 Windows 的 PowerShell、盘符或 Conda 绝对路径 |

## 2. 权威入口

| 内容 | 权威位置 |
|---|---|
| 长期协作与实验规则 | `AGENTS.md` |
| 自研 Framework 实时进度 | `03_实验记录/自研Framework实验/01_Stage0_1_角度与显露补全/实验进度与结果讨论.md` |
| InfiniSplat 实时进度 | `03_实验记录/InfiniSplat复现实验/实验进度与结果讨论.md` |
| 自研可同步证据 | `03_实验记录/自研Framework实验/01_Stage0_1_角度与显露补全/records/` |
| InfiniSplat 可同步证据 | `03_实验记录/InfiniSplat复现实验/01_官方RGB单图冒烟/records/` |

两条实验线各自更新自己的进度文档。自研进度文档第 16～17 节保留为双分支启动时的历史记录，后续 InfiniSplat 新结果不再追加到自研文档。

## 3. 产物分为三层

### 3.1 必须通过 Git 同步

- 正式脚本、配置、输入清单和补丁；
- 实时进度与方案文档；
- `records/` 中的小型 JSON、CSV、环境锁定信息、完整性结果和人工评分；
- 能唯一指向大型产物的清单：运行 ID、相对路径、字节数和完整 SHA256。

### 3.2 按里程碑选择性传输

- 已冻结、准备接入下一阶段的 PLY；
- 等待另一台电脑或用户人工播放的最终 MP4；
- 无法重新下载且确实需要复核的关键模型或中间结果。

这类文件不提交 GitHub。使用局域网、移动硬盘或其他大文件传输方式复制；复制后必须用清单中的字节数和 SHA256 校验。

### 3.3 仅保留在产出机器

- 全部逐帧 RGB、Depth、Alpha、掩码和 NPY；
- dry-run、失败运行、诊断缓存、联系表和临时日志；
- 可从脚本重新下载的 checkpoint；
- 未冻结的候选 PLY 和视频。

这些内容继续放在各实验的 `outputs/`、`models/`、`logs/` 或 `99_过程文件_可删除或归档/` 中，并由 `.gitignore` 排除。

### 3.4 默认采用“重跑优先”而不是“结果搬运优先”

另一台机器已经具备正式脚本、配置、输入和环境时，默认在目标机器重新运行，不传输原机器的 PLY、MP4、checkpoint、逐帧图像或整个 `outputs/`。重新运行必须使用新的运行 ID并登记自己的 `producer_machine_id`、环境、字节数和 SHA256；由于随机性、CUDA算子和硬件差异，新运行是“同配置复现”，不自动等同于原运行的逐字节副本。

只有以下情况才传输大型产物：

- 必须继续使用同一 SHA256 的冻结 PLY；
- 需要用户在另一台机器完整播放同一份视频；
- 重跑成本明显高于传输成本，且结果确实要复用；
- checkpoint 或输入无法稳定重新下载；
- PPT、失败诊断或审计必须引用原运行素材。

### 3.5 程序跑完后的最小 Git 同步集合

每轮运行后默认只提交：正式程序、配置、输入清单、自动汇总 JSON/CSV、人工评分、环境记录、产物 manifest 和实时进度 Markdown。大型结果目录只需遵守统一架构，并由 `run_receipt.json` 或 `artifact_manifest.json` 记录；目标机器需要复现时运行对应脚本。

InfiniSplat 在 Windows 和 Linux 共用跨平台入口：

```text
03_实验记录/InfiniSplat复现实验/01_官方RGB单图冒烟/scripts/run_official_rgb_smoke.py
```

脚本要求先激活独立 `infinisplat` 环境，自动验证输入、checkpoint、冻结上游版本、输出防覆盖，并在本地输出目录生成 `run_receipt.json`。Windows 专用 `.ps1` 保留用于已验证环境，Linux 不使用 Windows gsplat 补丁或 `.pyd` 缓存。

## 4. 每次换电脑前后的固定流程

### 4.1 开始实验前

`win5060` 使用 PowerShell 7：

```powershell
pwsh.exe -NoLogo -NoProfile -Command 'git status -sb'
```

`linux5080` 使用服务器的 Linux shell：

```bash
git status -sb
```

若 `git status -sb` 显示未提交修改，必须停止在 `git pull --ff-only` 之前，先判断修改应提交、保留还是由用户处理。不得用强制覆盖、自动 rebase 或随意 stash 掩盖冲突。两台机器不要同时修改同一份进度文档或同一个 `records/`：交接方先提交并推送，接手方确认工作区干净后再拉取。若 `pull --ff-only` 失败，应保留现场并核对两端提交，不得强推。

仅在工作区干净时执行拉取。`win5060`：

```powershell
pwsh.exe -NoLogo -NoProfile -Command 'git pull --ff-only'
```

`linux5080`：

```bash
git pull --ff-only
```

然后按顺序检查：

1. 阅读本说明、`AGENTS.md` 和当前实验线的进度文档；
2. 查看对应 `records/README.md` 是否仍有“待同步”项目；
3. 核对本机所需模型、冻结输入和大型产物是否存在，并校验哈希；
4. 本机缺少大型产物时，先判断能否重跑或重新下载；只有无法接受重算成本且确需复用时才传输大文件。

**`git status -sb` 出现未提交修改时的快速判断表：**

| 修改类型 | 默认动作 |
|---|---|
| 进度文档、`records/`、正式脚本、配置、输入清单、补丁 | 交接方先提交并推送；接手方确认工作区干净后再拉取 |
| 本地临时文件、日志、缓存、逐帧输出、模型权重 | 不应出现在工作区（应被 `.gitignore` 排除）；出现即停下核对 ignore 与是否误追踪 |
| 大型产物（PLY/MP4/checkpoint）误入工作区 | 立即停止，核对是否误删 ignore 项，不执行强制覆盖 |
| 不确定来源的修改 | 保留现场，向用户报告，不自动提交、不 stash |

不得因为本机 `outputs/` 为空，就把已完成实验改写成“未完成”；应记录为“实验已完成，本机产物未同步”。

### 4.2 完成一次正式运行后

1. 使用新的唯一运行目录，不覆盖同名旧输出；
2. 记录源码 Git commit、外部源码 commit、运行 ID、配置、输入哈希、环境和硬件；
3. 记录候选冻结产物的相对路径、字节数、完整 SHA256、帧数或高斯数量；
4. 将小型汇总、自动指标、人工评分和完整性检查复制或整理到对应 `records/`；
5. 更新对应实验线的实时进度文档；
6. 执行 `git status`，确认没有误提交模型、视频、PLY 或逐帧输出；
7. 提交并推送，另一台电脑开始工作前执行 `git pull --ff-only`。

## 5. 命名、冲突与冻结规则

- 运行目录名必须包含样本、阶段、用途、版本和日期，例如 `P01_stage01_true_arc_formal_v4_fallback_20260807`；当两台机器可能生成同名运行时，必须追加 `win5060` 或 `linux5080`。
- 同名运行的哈希不同，说明它们不是同一个产物。保留两份并分别登记，不得覆盖或选一份冒充另一份。
- 正式比较前必须冻结某一份 PLY/视频，以完整 SHA256 为身份，不以“最新文件”或本机绝对路径为身份。
- 文档只写仓库相对路径；盘符、用户名、Conda 安装位置和显卡型号属于机器环境字段。
- `records/` 只保存可读、可审计的小型证据，不保存 PLY、MP4、checkpoint 或逐帧图像。
- 每份产物清单至少记录 `schema_version`、`run_id`、`producer_machine_id`、生成日期、配置与输入哈希、仓库相对路径、字节数、`SHA256` 和是否为冻结候选；运行时项目 commit 未记录时必须写 `null` 和原因，不得用后来的提交号代替。

### 5.1 SHA256 与产出机器必须同时记录

同一逻辑运行在两台机器上因随机性、CUDA 算子或硬件差异，PLY/视频的字节数与 SHA256 可能不同（SHARP Benchmark v1 已实测 5060 与 5080 不完全逐字节一致）。因此：

- 每条产物记录必须同时保存 `producer_machine_id` 和完整 `SHA256`。机器可读 JSON 使用两个独立字段；Markdown 可简写为 `win5060:<完整SHA256>`，例如 `win5060:DEAB7295F86174EF25D0B0FC7A394C108F46D9F59E4552F82B2664E56D4B999B`；
- 两台机器对“同一逻辑运行”各自计算出的哈希不同时，两份都保留、分别登记，不得相互覆盖或选一份冒充另一份；
- SHA256 标识文件的字节内容：两台机器得到相同完整 SHA256 时可视为逐字节相同；`producer_machine_id` 另外标识产出来源和运行环境。两者组合用于追踪“哪台机器生成了哪份具体内容”。

### 5.2 冻结动作只由用户确认

脚本、进度文档或任一机器不得自行把候选标记为“已冻结”。用户确认冻结时，须同时指定：

- 冻结身份机器 ID（哪台机器的产物作为后续复用/校验基准）；
- 该机器上的完整 SHA256；
- 其他机器若已生成同逻辑产物，登记其各自哈希并注明“可重跑，不要求逐字节一致”。

### 5.3 完整流程保证

每条实验线至少在一台机器上完整跑通“生成 → 自动指标 → 用户完整播放评分 → 结论”全过程；该机器上完成的人工评分记录跨机可信，其他机器复核时如需同一份视频，按需选择性传输。

## 6. 当前两台电脑的对账状态（2026-08-11）

### 6.1 自研 Framework

- `win5060` 已把旧输出无损整理为四类目录；整理后的当前统计为 1,391 个文件、3,935,498,041 bytes，其中包括后续新增的导航与人工评分内容。v4 正式入口是 `outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v4_fallback_20260807/`。
- `linux5080` 是否持有同一批大型 outputs 以其本地核验结果为准；不要求为了目录外观一致而全量镜像、移动或重新生成。
- 用户已完成 v4 六个视频的完整播放；5°通过，15°和30°因涂抹严重度为 2 未通过，Stage 1 未冻结。
- `win5060` 已把 v4 正式小型汇总、关键产物清单和人工评分提升到 Git 可同步的 `records/`；大型视频和 PLY 仍只按需要选择性传输。

两台机器的新运行统一使用以下 outputs 结构；空目录不通过 Git 同步，由脚本运行时创建：

```text
outputs/
├─ 00_Stage0_角度协议/
│  ├─ 01_dryrun/
│  ├─ 02_校准冒烟/
│  └─ 03_正式A基线/
├─ 01_Stage1_legacy开发基线/
│  ├─ 01_组件冒烟/
│  └─ 02_完整链路冒烟/
├─ 02_Stage1_true_arc/
│  ├─ 01_dryrun/
│  ├─ 02_功能冒烟/
│  └─ 03_正式矩阵/
└─ 90_失败与诊断归档/
   ├─ 01_早期脚本失败/
   └─ 02_深度质量门预期失败/
```

`linux5080` 如果已有旧布局，只增加分类层级，保留运行目录原名和内部内容；整理前后核对文件数和总字节数，不删除、不覆盖，也不要求与 `win5060` 全量镜像。

### 6.2 InfiniSplat

- 官方 P01 RGB 的 PLY-only 和 60 帧视频工具链已在 `win5060` 跑通；`win5060` 当前持有 RGB checkpoint、两份 PLY 和官方 MP4。
- `linux5080` 不要求复制约 3.14 GB checkpoint、Windows PLY 或官方 MP4；服务器先按相同输入和配置重新运行，并把结果登记为新的 `linux5080` 运行。
- 约 4.7 MB 的 Windows 官方视频只在需要播放同一结果时传输；约 82.7 MB 的 Windows PLY只在必须继续使用同一 SHA256 时传输。一般 true_arc 适配可冻结服务器新生成的 PLY。
- `win5060` 已补齐自动运行、环境、视频完整性和完整哈希记录；用户尚未完成官方视频人工播放，因此 `manual_review.json` 暂不创建。

InfiniSplat 两台机器统一采用：

```text
01_官方RGB单图冒烟/
├─ configs/
├─ inputs/
├─ scripts/
├─ records/
├─ outputs/
│  └─ <唯一运行ID>/
└─ logs/
```

源码固定在 `源码/InfiniSplat/`，checkpoint 固定在 `源码/InfiniSplat/checkpoints/`；checkpoint 和 outputs 不进入 Git。服务器只需 Git 中的目录规则、源码、配置、输入图片与 manifest，再在独立 Linux 环境重跑。

### 6.3 `win5060` 当前其他 Git 忽略大目录

| 目录 | 当前用途 | `linux5080` 是否需要镜像 |
|---|---|---|
| `03_实验记录/SHARP浅3D实验/outputs/` | 冻结 SHARP Benchmark 的校准、静止、运动、裁剪、剪枝、性能、角度和独立验证结果 | 否；SHARP 已冻结，只有复核特定原结果时才按 manifest 传输 |
| `03_实验记录/SHARP浅3D实验/01_先导样本/**/scene_full.ply` | P01～P05 冻结基础高斯 | 默认不镜像；自研或对照必须使用同一基础 PLY时才传输并校验 |
| `03_实验记录/自研Framework实验/**/models/`、`logs/` | LaMa 等模型和本地运行日志 | 模型优先按哈希重新获取；日志只在诊断特定运行时传输 |
| `源码/InfiniSplat/checkpoints/` | InfiniSplat checkpoint | 服务器重新下载并校验，不复制 Windows 环境 |
| `SHARP真实图片测试/输出结果/` | 早期真实图片结果和 PPT 素材 | 不用于服务器正式实验；制作汇报时按需传输 |
| `文献/` | 本地论文库 | 不要求服务器镜像 |
| `99_过程文件_可删除或归档/` | 工具、缓存、联系表、临时文件和制作过程 | 不作为正式入口，不要求服务器镜像 |
| `迁移包_*/` | 离线整机迁移快照 | 不复制到服务器实验目录，不作为后续增量同步机制 |
| `03_实验记录/自研Framework实验/**/models/lama/` | LaMa 权重（205,669,692 bytes，SHA256 `344C77BBCB158F17DD143070D1E789F38A66C04202311AE3A258EF66667A9EA9`） | 已由 `win5060` 持有；服务器按哈希重新获取或传输后校验 |
| `03_实验记录/SHARP浅3D实验/01_先导样本/P01_building/scene_full.ply` | 自研 Stage 1 A/B 必须固定同一份基础高斯 | 属于“必须复用同一 SHA256”情形；服务器需要时传输并校验（`win5060:DEAB7295F86174EF25D0B0FC7A394C108F46D9F59E4552F82B2664E56D4B999B`） |

SHARP outputs 当前顶层为 `00_工具校准/`、`02_静止基线/`、`03_运动实验/`、`04_裁剪实验/`、`06_剪枝实验_crop03正式/`、`07_性能测试/`、`08_角度协议/`、`08_角度协议脚本收尾冒烟/` 和 `09_独立验证/`。这是冻结历史结构，不要求为美观重新编号或在服务器重建。

**两个 08 目录的关系**：`08_角度协议/` 是正式角度协议结果（P01 5°/15°/30° 基线）；`08_角度协议脚本收尾冒烟/` 是角度渲染脚本修复后的收尾验证，属过程性冒烟。前者是正式产物，统计与汇报只使用前者；后者仅工具链验证，不作为正式结论。

**LaMa 与 P01 基础 PLY 已完成**：`win5060` 已持有 LaMa 权重（205,669,692 bytes，SHA256 `344C77BBCB158F17DD143070D1E789F38A66C04202311AE3A258EF66667A9EA9`）和 P01 基础 PLY（66,061,086 bytes，SHA256 `win5060:DEAB7295F86174EF25D0B0FC7A394C108F46D9F59E4552F82B2664E56D4B999B`）。`linux5080` 需要跑自研 Stage 1 时，LaMa 权重按哈希重新获取或传输，P01 基础 PLY 因 A/B 必须固定同一份高斯，属于“必须复用同一 SHA256”情形，按需传输并校验。

## 7. `linux5080` 拉取本次提交后的待办

1. 不移动、不删除服务器现有 `outputs/`；先确认当前分支与 Git 状态干净，再使用 `git pull --ff-only`。
2. 阅读两个 `records/README.md` 和机器可读记录，确认 `producer_machine_id=win5060`，不要把这些记录误写成服务器本地产物。
3. 如果下一步只做代码或方案工作，不传输任何大型输出。
4. 在服务器独立创建或核对 `infinisplat` 环境，重新下载 checkpoint 并按 records 的 3,142,241,921 bytes 和完整 SHA256 校验；不从 Windows 复制 Conda 环境或 gsplat 编译缓存。
5. 使用跨平台脚本和相同 P01 输入在服务器重跑官方 RGB 冒烟，运行 ID不得覆盖 Windows 结果；服务器输出的 `run_receipt.json` 随后提升到 records。
6. 服务器登记自己生成的明确哈希 PLY并报告给用户；只有用户明确确认 `linux5080` 与完整SHA256后才冻结并开始 true_arc 适配。只有必须复用 Windows 原 PLY时才执行大文件传输。
7. `linux5080` 产生新运行时登记 `producer_machine_id=linux5080`，生成独立 records 后提交并推送；`win5060` 再在干净工作区拉取。

暂不引入 DVC 或新仓库。只有当冻结大产物数量持续增加、手工清单和选择性传输已经成为稳定瓶颈时，再评估对象存储或 DVC。
