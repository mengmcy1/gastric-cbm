# Stage 0+1 角度协议与显露补全：实验进度与结果讨论

> 状态更新时间：2026-08-14
>
> 当前状态：**Stage 1.8-2首个目标条件联合候选已正式不通过：val两类support分离通过，但遮挡后/视锥外RGB MAE为43.46/59.43、深度AbsRel为59.3%/107.4%，未读取held-out test。下一步做深度尺度对齐上限与冻结预训练生成先验审计；不继续只加训练步数、不接P01/Gaussian Spawn。**
>
> 当前主方案：`02_方案设计/总体Framework讨论总结_自适应3DGS.md` 第 14 章。
> 结论边界：v4 已通过30°左右端 Alpha 空洞覆盖自动门，且用户判断 B 整体优于 A；但15°和30°均存在严重度2的明显涂抹，因此不能冻结 Stage 1，也不能宣称30°视觉可用。

## 1. 本阶段要解决的问题

固定同一份 SHARP 完整高斯作为基础表示，只增加“显露区域检测 → RGB 补全 → 深度估计与对齐 → 补充高斯”，比较：

- A：基础高斯，不补全；
- B：基础高斯 + 显露补全高斯。

目标是先验证大视角移动时新露出的区域能否被补上，并检查补全是否引入模糊、漂浮点、深度冲突、中心视角污染和跨端点冲突。本阶段不加入 Gate、剪枝、材质感知或移动端优化。

## 2. 输入、环境与目录

### 2.1 冻结输入

- 样本：P01 校园建筑；
- 基础表示：`03_实验记录/SHARP浅3D实验/01_先导样本/P01_building/scene_full.ply`；
- PLY SHA256：`DEAB7295F86174EF25D0B0FC7A394C108F46D9F59E4552F82B2664E56D4B999B`；
- SHARP 权重 SHA256：`94211A75198C47F61FCA7D739BA08A215418D8D398D48FDDF023BACCC24F073D`；
- LaMa TorchScript SHA256：`344C77BBCB158F17DD143070D1E789F38A66C04202311AE3A258EF66667A9EA9`；
- 渲染焦距：1597.53662109375 px；
- 实际环境：既有 `sharp` Conda 环境，未安装、替换或重装 Python、PyTorch、CUDA、gsplat 等核心依赖。

输入清单位于 `inputs/p01_input_manifest.json`。模型权重只在本地 `models/` 保存，不进入 Git。

### 2.2 正式脚本

`scripts/` 当前包含：

- `stage01_common.py`：路径、哈希、JSON 和安全输出目录公共函数；
- `stage01_angle_render.py`：`legacy_lateral` 与 `true_arc` 两种显式命名的角度渲染；
- `stage1_detect_disocclusion.py`：由端点 Alpha、深度冲突和连通区域种子生成显露掩码；
- `stage1_lama_inpaint.py`：LaMa 静态 RGB 补全；
- `stage1_estimate_align_depth.py`：冻结 SHARP 单目深度重估、环带仿射对齐及质量门；
- `stage1_build_supplement_gaussians.py`：由 RGB、深度、掩码生成补充高斯，并与基础 PLY 合并；
- `stage1_summarize_ab.py`：左右端点及中心帧 A/B Alpha 覆盖统计；
- `stage01_validate_outputs.py`：帧数、编码、分辨率和必要文件完整性检查；
- `run_stage01_smoke.py`：单端点串联冒烟；
- `run_stage01_matrix.py`：左右端点、三档角度的可复现矩阵调度。

所有脚本均通过 `py_compile` 和 `--help` 检查。脚本默认拒绝覆盖已存在的输出目录。

## 3. 已完成的检查与冒烟

### 3.1 既有角度基线检查

- `outputs/08_角度协议/P01/{ang5,ang15,ang30}/` 的 P01 三档产物完整，每档 61 帧，并包含 RGB、Alpha、深度、配置和端点诊断产物；
- 既有 `sharp_angle_protocol_render.py` 的运动实际是 `z=0` 横向位移 `x=focus_depth×tan(angle)`，不是严格圆弧；`radius_ratio` 只记录、未参与相机位置计算；
- 已修复该脚本在渲染完成后的终端汇总仍引用旧 `radius` 变量的问题，并以 P01、5°、3 帧在新目录复测通过；配置明确记录 `trajectory=legacy_lateral`、2048×1536；
- 因此既有结果保留为可复现基线，但其协议应标记为 `legacy_lateral`，不得作为 `true_arc` 结论。

### 3.2 真圆弧渲染校准

- P01、30°、3 帧 `true_arc` 冒烟已通过；
- 彩色视频为 H.264、yuv420p、2048×1536；
- 左端相机约 `(-1.3469, 0, 0.1773)`，P95 位移 443.84 px，占画宽 21.67%；
- 右端相机约 `(1.3469, 0, 0.1773)`，P95 位移 450.40 px，占画宽 21.99%；
- 本实现为保持中心帧相机在原点并注视参考点，圆弧半径取参考深度。它不能同时满足原草案中的 `R=2×参考深度`。

### 3.3 LaMa 与完整双端点链路

- LaMa 输入缩放为 768×1024，单次推理约 1.08～1.35 s，模型加载约 0.70～0.83 s；
- 峰值 CUDA allocated 约 822 MB；该数值仅为模型推理阶段诊断，不代表整条管线峰值显存；
- 精化后左端显露掩码占画面 8.9977%，右端占 12.4741%；
- 左端补充 17,564 个高斯，右端补充 24,701 个，合并后共 1,221,913 个，较基础高斯增加 42,265 个；
- P01 双端点 3 帧矩阵 13/13 步完成，A/B 彩色视频均为 H.264、yuv420p、2048×1536，完整性检查通过。

掩码区域 Alpha 空洞率自动统计：

| 位置 | A 基线 | B 补全后 | 相对变化 |
|---|---:|---:|---:|
| 左端 | 0.86385 | 0.00518 | -99.40% |
| 右端 | 0.88738 | 0.000247 | -99.97% |
| 中心全画面 | 0.00600 | 0.00400 | -33.3% |

这些数值只说明 Alpha 覆盖明显增加，不说明补入内容的颜色、几何或跨视角一致性正确。

## 4. 问题、质量门与结论

### 4.1 已完成

- 独立阶段目录、配置、输入清单和九个正式功能脚本已建立；
- LaMa、冻结深度、补充高斯、双端点融合、A/B 汇总和完整性验证均已跑通；
- 未修改 SHARP 官方源码，未覆盖任何历史输出；
- 失败运行均保留在独立诊断目录，没有批量删除。

### 4.2 待解决问题

- LaMa 补全区域存在明显模糊和树状涂抹；
- 简单追加补充高斯会产生漂浮斑块，并污染中心视角；
- 当前缺少“只在原来被遮挡、当前被显露时才显示”的可见性或置信度控制；
- 左右端点补充内容尚未做冲突消解；
- P01 右端全局深度对齐异常：`scale=0.010927`、RMSE=13.4358 m、参考深度中位数=27.9791 m、归一化 RMSE=0.4802。

深度脚本现已加入正式质量门：要求仿射尺度在 `[0.1, 10]` 且归一化 RMSE 不高于 0.25。右端异常用例返回非零状态并保留 `depth_run.json` 诊断；只有标记为 `smoke_only` 的配置才能显式使用 `allow_poor_alignment=true` 放行。正式候选配置默认关闭放行。

### 4.3 当前结论边界

- **有充分证据**：Stage 0+1 所需工具链可以在 P01 上串联运行；LaMa 可以为显露掩码填入二维颜色；质量门可以阻止明显异常的全局深度对齐继续进入正式高斯生成。
- **候选判断**：LaMa 适合保留为第一版二维补全基线，但最终质量可能需要 MAT、ZITS、扩散补全或结构引导模型替换。
- **当前不能下结论**：不能宣称 Stage 1 质量通过、30° 已可用、补充高斯几何正确，不能开始完整样本矩阵或冻结补全模型。

## 5. 相机协议待冻结项

Framework 中存在两组不能同时成立的约束：

1. 三档均为 61 帧，与固定每步 0.5° 不兼容。两种候选为：
   - 方案 A：5°/15°/30° 均 61 帧，步长分别为 0.0833°/0.25°/0.5°；
   - 方案 B：固定 0.5° 步长，帧数分别为 11/31/61。当前推荐方案 B，以保持角度采样变量一致。
2. 中心相机在原点、固定注视参考深度点、圆弧半径为两倍参考深度，三者不能同时满足。当前 `true_arc` 实现保留前两项，因此半径等于参考深度；如需两倍半径，必须重新定义圆心、中心相机位置或注视点。

在用户确认前，两项均保持“候选、未冻结”状态。

## 6. 下一步

1. 确认并冻结唯一的帧采样与真圆弧几何协议；
2. 将全局单一仿射深度对齐改为端点/连通区域级对齐，并为无可靠邻域的区域提供显式失败状态；
3. 为补充高斯增加可见性、置信度或端点权重，避免中心帧及对侧视角污染；
4. 只在 P01 做 5°/15°/30° 小规模正式 A/B，完整人工播放，检查空洞、漂浮、深度冲突、闪烁和中心视角污染；
5. P01 通过后再扩展到 P02、P05；Stage 1 通过后才进入双端点一致性和更换补全网络。

## 7. 文件与验证情况

- 正式代码、配置和输入清单：本目录下 `scripts/`、`configs/`、`inputs/`；
- 本地模型、日志和大型输出：`models/`、`logs/`、`outputs/`，均由 `.gitignore` 排除；
- 成功双端点冒烟：`outputs/01_Stage1_legacy开发基线/02_完整链路冒烟/P01_matrix_smoke_legacy_ang30_both_v1/`；
- 真圆弧校准：`outputs/00_Stage0_角度协议/02_校准冒烟/P01_true_arc_ang30_render_smoke_v1/`；
- 深度质量门预期失败诊断：`outputs/90_失败与诊断归档/02_深度质量门预期失败/P01_right_depth_gate_expected_fail_v1/`；
- 未覆盖原有 PNG、MP4、NPY、PLY、指标或历史实验结果；
- 仍需的人工检查：正式协议冻结后，对新生成的 5°/15°/30° A/B 视频进行完整播放，冒烟视频的目视问题只作为开发诊断。

## 8. 2026-08-07 换机交接

### 8.1 本轮实际完成的核验

- `outputs/`、`models/`、`logs/` 已同步到当前实验目录，并完成存在性检查；
- 本地 LaMa 权重 `models/lama/big-lama.pt` 的 SHA256 为 `344C77BBCB158F17DD143070D1E789F38A66C04202311AE3A258EF66667A9EA9`，与配置冻结值一致；
- 已复核 `outputs/01_Stage1_legacy开发基线/02_完整链路冒烟/P01_matrix_smoke_legacy_ang30_both_v1/`：13 个步骤全部返回 0，完整性检查通过，A/B 视频均为 H.264、yuv420p、2048×1536、3 帧；
- 已复核自动结果：左端掩码区域 Alpha 空洞率由 0.86385 降至 0.00518，右端由 0.88738 降至 0.000247；这些数值只证明覆盖增加，不证明颜色、深度或时序正确；
- 已复核补充高斯数量：左端 17,564，右端 24,701，合并后总数 1,221,913；当前融合方式仍为直接追加，没有去重或冲突消解；
- 已目视检查左右端点和中心帧：B 能填掉大面积黑洞，但出现树状模糊、漂浮斑块及明显中心视角污染；当前 Stage 1 视觉质量不通过；
- 已确认右端旧冒烟深度对齐异常：`scale=0.010927`、RMSE=13.4358 m、归一化 RMSE=0.4802。当前新版深度脚本已加入质量门，正式配置会拒绝该结果；
- 已确认完整双端点冒烟使用的是 `legacy_lateral + 30° + 3 帧`，不是真圆弧正式实验；真圆弧只有 30° 三帧渲染校准结果。

### 8.2 停止原因与当前文件状态

- 当前电脑没有可调用的 Conda/Python/CUDA 环境，`conda`、`python`、`nvidia-smi` 均不可用，无法在本机继续 GPU 渲染和正式 A/B；
- 因此本轮在实现修改前主动停止，没有修改 Stage 0+1 的 Python 脚本或 JSON 配置，也没有生成新的实验输出；
- 已同步的历史输出清单和日志中包含原电脑绝对路径（如 `C:\\Users\\MCY`、`D:\\develop\\Miniconda\\envs\\sharp`），它们只作为历史运行证据；新电脑运行时应使用当前仓库相对路径和新环境中的 `sys.executable`，不要照抄历史绝对路径；
- 工作区还存在与本阶段无关的 PPT、Word、PDF 删除或新增状态，后续不得回退或删除这些用户文件。

### 8.3 下一台 CUDA 电脑的执行顺序

1. 先阅读本文件、`AGENTS.md` 和 `02_方案设计/总体Framework讨论总结_自适应3DGS.md` 第 14 章；
2. 检查 `sharp` 环境、GPU、SHARP 权重、LaMa 权重、P01 基础 PLY 及其 SHA256，不重装或污染既有环境；
3. 冻结 Stage 0 正式协议，当前建议采用方案 B：`true_arc`，5°/15°/30° 分别 11/31/61 帧，固定 0.5° 步长，圆弧半径等于参考深度，中心相机在原点并注视参考深度点，不使用裁剪；
4. 先只运行 P01 的真圆弧 A 基线，检查中心帧、左右对称性、相机位置、P95 位移、Alpha 空洞率和视频帧数；
5. 再修改 Stage 1：显露掩码过滤内部小碎片；深度改为逐连通区域对齐并输出接受/拒绝掩码；补充高斯保留左右来源身份，并按相机方向渐入渐出，保证中心和对侧视角不可见；
6. 完成静态检查和 3 帧真圆弧冒烟后，运行 P01 的 5°/15°/30° 正式 A/B；完整播放视频并评价 DISOC、COVER、DEPTH、拉伸、闪烁和中心污染；
7. P01 达到候选门槛后再扩展 P02、P05；不要直接启动全部样本矩阵；
8. 每轮结束后更新本文件，区分“脚本具备”“冒烟跑通”“视觉通过”和“正式冻结”。

### 8.4 新对话恢复提示词

```text
请先阅读 AGENTS.md、02_方案设计/总体Framework讨论总结_自适应3DGS.md 第14章，以及 03_实验记录/自研Framework实验/01_Stage0_1_角度与显露补全/实验进度与结果讨论.md，重点看第8节换机交接。继续完成 Stage 0+1：先检查 CUDA/Conda/权重和冻结输入，再冻结 true_arc 的 11/31/61 帧协议；随后实现连通区域级深度对齐、显露掩码过滤和左右补充高斯的视角可见性控制，完成 P01 三档正式 A/B。P01 视觉通过后再扩展 P02、P05。不要修改 SHARP 官方源码，不要覆盖历史输出，每轮都更新实验进度 Markdown。
```

## 9. 2026-08-07 环境与冻结输入复核（已完成）

### 9.1 本轮做了什么

- 只读检查 `sharp` Conda 环境、CUDA、GPU、PyTorch、gsplat、三项冻结输入以及 `outputs/`、`models/`、`logs/`；
- 未安装、重装或替换依赖，未修改 SHARP 官方源码，未覆盖任何历史输出；
- 实际环境：Python 3.13.14、PyTorch 2.8.0+cu128、CUDA 可用、gsplat 1.5.3、RTX 5060 Laptop GPU（8,151 MiB）。

### 9.2 输入与哈希结果

- SHARP 权重：2,809,738,232 bytes，SHA256=`94211A75198C47F61FCA7D739BA08A215418D8D398D48FDDF023BACCC24F073D`，通过；
- LaMa 权重：205,669,692 bytes，SHA256=`344C77BBCB158F17DD143070D1E789F38A66C04202311AE3A258EF66667A9EA9`，通过；
- P01 基础 PLY：66,061,086 bytes，SHA256=`DEAB7295F86174EF25D0B0FC7A394C108F46D9F59E4552F82B2664E56D4B999B`，通过；
- `outputs/`、`models/`、`logs/` 均存在。本轮环境与输入核验通过，可以进入 Stage 0 协议冻结与 A 基线测量。

## 10. 2026-08-07 Stage 0 方案 B 冻结与 P01 A 基线（已完成）

### 10.1 实验目的、输入与固定协议

- 目的：把“30°”落实为可复现的真圆弧相机运动，先确认尺子本身正确，再研究显露补全；
- 输入：同一份冻结 P01 SHARP 基础 PLY；输出为三档 A 基线 RGB/Alpha/Depth 帧、视频、逐帧指标、相机参数及汇总；
- 正式冻结：`trajectory_mode=true_arc`，固定步长 0.5°；5°/15°/30° 总视角分别为 11/31/61 帧；圆弧半径=参考深度 5.203963 m；中心相机位于原点；注视点为 `[0, 0, 5.203963]`；固定内参和 2048×1536 分辨率；裁剪为 0；
- 冻结配置：`configs/p01_stage0_protocol_b_frozen_v1.json`；正式输出：`outputs/00_Stage0_角度协议/03_正式A基线/stage0_p01_protocol_b_a_frozen_20260807_v1/`。

### 10.2 自动结果与完整性

| 总视角 | 帧数 | 左/右端相机 x（m） | 左/右 P95（px） | 左/右 P95（画宽） | 左/右 Alpha<0.95 空洞率 |
|---|---:|---:|---:|---:|---:|
| 5° | 11 | -0.226994 / +0.226994 | 76.33 / 76.32 | 3.727% / 3.726% | 0.676% / 0.618% |
| 15° | 31 | -0.679253 / +0.679253 | 223.59 / 225.59 | 10.917% / 11.015% | 2.400% / 2.720% |
| 30° | 61 | -1.346885 / +1.346885 | 443.84 / 450.40 | 21.672% / 21.992% | 8.357% / 11.450% |

- 三档中心帧相机外参均与原相机一致，中心眼位均为 `[0,0,0]`，P95=0；
- 5°、15°、30° 的中心 RGB 与 Alpha 逐像素完全一致；
- 左右端相机位置和注视方向的几何对称检查全部通过；固定内参检查通过；
- 三个视频均为 H.264/yuv420p、2048×1536，帧数分别为 11/31/61，完整性检查通过；
- 30° 左右空洞率不完全相同源于场景内容与遮挡不对称，不是相机轨迹几何不对称。

### 10.3 结论边界与下一步

- **参数正式冻结**：Stage 0 方案 B 的相机协议；
- **自动指标通过**：中心一致性、端点几何对称、固定内参、视频编码/分辨率/帧数；
- **尚未评价**：本轮只有 A 基线，不证明 30° 视觉可用，也不证明显露补全有效；
- 下一轮保持相机协议与基础 PLY 不变，只修改 Stage 1 的掩码过滤、逐连通区域深度对齐和左右补充高斯可见性，然后进行真圆弧小冒烟。

## 11. 2026-08-07 Stage 1 v2 静态检查与真圆弧冒烟（冒烟已跑通，视觉未通过）

### 11.1 本轮做了什么

- 显露掩码新增边界带过滤：保留触及 32 px 边界带、含硬空洞/无效深度种子且不小于 64 px 的连通区域；同时输出 raw、accepted、rejected、internal、small 和逐区域统计；
- 深度对齐改为逐连通区域鲁棒仿射拟合，逐区记录 scale、offset、RMSE、归一化 RMSE、样本数与质量门；失败区域写入 rejected mask，不进入补充高斯；
- 左右补充高斯保留来源身份，采用 `endpoint_linear`：来源端点权重 1，向中心线性降至 0，中心与对侧权重均为 0；每帧权重写入 CSV；
- 追加式融合保存 base/left/right 的 `[start,end)` 索引范围，并明确未做跨端点冲突消解；
- 所有正式脚本通过 `py_compile` 和 `--help`；P01 正式三档调度 dry-run 通过，共 39 个命令；
- 随后用 P01、`true_arc`、30°、3 帧完成双端点 A/B 小冒烟。输出：`outputs/02_Stage1_true_arc/02_功能冒烟/P01_stage01_true_arc_smoke_v2_20260807/`。

### 11.2 自动结果与异常

- 13 个步骤全部返回 0，A/B 视频完整性通过；中心帧 A/B RGB 的平均差、最大差和非零比例均为 0，证明中心补充高斯污染已由可见性函数消除；
- 左端：掩码保留 7.170% 画面、拒绝 1.964%；2 个深度区域全部通过，scale=0.1931/0.3273，归一化 RMSE=0.0277/0.1706；生成 13,992 个补充高斯；掩码内 Alpha<0.95 从 90.02% 降至 1.16%；
- 右端：掩码保留 10.897% 画面、拒绝 1.328%；4 个深度区域仅 2 个通过，另 2 个因 scale 或归一化 RMSE 失败；接受 1,675 px、拒绝 341,120 px，只生成 102 个补充高斯；接受区域内 Alpha<0.95 从 52.36% 降至 1.37%；
- 右端旧全局异常没有被“平均掉”：新的区域质量门明确拒绝了 scale=0.01016 的区域，也拒绝了归一化 RMSE=0.2534 的区域；
- 索引范围已记录：base `[0,1179648)`、left `[1179648,1193640)`、right `[1193640,1193742)`。

### 11.3 观察、结论边界与下一步

- **脚本已经具备、冒烟已经跑通、自动完整性通过**；中心视角和对侧视角的可见性隔离按数值检查通过；
- **抽帧观察未通过**：左端大空洞被覆盖，但补全区仍是明显的模糊/涂抹色块；右端大部分区域被深度门正确拒绝，因此主体空洞仍保留；
- 三帧冒烟不能判断闪烁，也不能作为 30° 可用结论；Stage 1 参数尚未冻结；
- 下一轮运行 P01 的 5°/15°/30° 正式 A/B，获得完整逐帧退化曲线。若正式视频仍有明显涂抹或右端覆盖不足，则判定当前 LaMa+规则高斯版本不通过，停止扩展 P02/P05，继续在 P01 修复或更换补全/深度策略。

## 12. 2026-08-07 P01 正式 A/B v2（自动门未通过，待用户完整播放）

### 12.1 本轮做了什么

- 固定同一份 P01 SHARP 基础 PLY 和已冻结 Stage 0 方案 B，只比较 A（基础高斯）与 B（基础高斯+显露补全高斯）；
- 正式运行 5°/15°/30°，分别 11/31/61 帧；没有裁剪，补充高斯使用逐区域深度门和来源端点可见性；
- 第一次正式调度保留在 `outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v2_20260807/`，它在 5° 左端因过滤后无合格显露区域而停止。该情况随后被修正为显式 no-op：有效补充高斯数为 0，B 保持等于 A；由于 SHARP 官方 PLY 写入器不能保存零顶点，空补充 PLY 只保存一个 opacity=1e-8 的格式哨兵，并在统计中独立标注，不计入有效补充高斯；
- 修复后使用新目录完整重跑：`outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v2b_20260807/`；三档 39 个步骤全部完成；正式汇总位于其 `formal_summary/`。

### 12.2 自动指标

| 总视角 | 端点 | A空洞率 | B空洞率 | 相对下降 | 深度门 | 接受/拒绝像素 | 有效补充高斯 |
|---|---|---:|---:|---:|---|---:|---:|
| 5° | 左 | 0.676% | 0.676% | 0% | 无掩码/no-op | 0 / 0 | 0 |
| 5° | 右 | 0.618% | 0.618% | 0% | 无掩码/no-op | 0 / 0 | 0 |
| 15° | 左 | 2.400% | 1.703% | 29.01% | 部分通过 | 28,755 / 3,221 | 1,773 |
| 15° | 右 | 2.720% | 2.519% | 7.41% | 部分通过 | 8,487 / 57,521 | 548 |
| 30° | 左 | 8.357% | 1.984% | 76.25% | 通过 | 225,546 / 0 | 13,992 |
| 30° | 右 | 11.450% | 11.422% | 0.24% | 部分通过 | 1,675 / 341,120 | 102 |

- 三档中心帧 A/B RGB 平均差、最大差和非零比例均为 0；左右补充高斯中心权重为 0、对侧权重为 0、来源端点权重为 1；
- P95 仍由同一基础高斯统计：5° 左/右 76.33/76.32 px（3.727%/3.726% 画宽），15° 223.59/225.59 px（10.917%/11.015%），30° 443.84/450.40 px（21.672%/21.992%）；
- 三档 A/B 视频均通过 H.264、yuv420p、2048×1536 和 11/31/61 帧检查；
- 30° 要求左右端点空洞率均相对 A 下降至少 30%。左端通过、右端未通过，因此自动候选门整体失败；改善样本数为 0/1，改善端点数为 1/2，不能用左端平均值掩盖右端失败。

### 12.3 视觉检查状态

- 已将每个 A/B 视频的所有解码帧生成顺序联系表并逐帧查看，缓存位于 `99_过程文件_可删除或归档/渲染检查缓存/Stage01_P01_formal_v2b_20260807/`；这属于**助手全帧联系表复核**，不是用户完整视频人工播放；
- 5°：B 与 A 一致，没有引入中心或对侧污染；
- 15°：补充量较小，端点黑区略有覆盖，但边缘仍有模糊色块，右端改善有限；
- 30°：左端黑洞大幅减少，但被明显的深绿色/灰色模糊涂抹区替代；右端大面积黑洞基本不变。联系表未显示补充内容越过中心到达对侧，但对闪烁、跳变和首次明显伪影帧仍须以用户实际播放为准；
- 用户人工评分模板：`outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v2b_20260807/formal_summary/manual_review_template.json`，当前保持空白，未把助手判断伪装成人工结论。

### 12.4 能得出的结论与后续影响

- **脚本已经具备**：边界连通过滤、逐区域深度门、接受/拒绝掩码、左右来源身份、逐帧可见性、索引范围、矩阵汇总和空掩码 no-op；
- **正式自动实验完整**：P01 三档 A/B 全部生成并通过产物完整性；
- **自动指标未通过、助手抽查未通过、用户人工视觉仍待复核**：当前 LaMa+规则补充高斯版本不能冻结，不能宣称 30° 可用；
- 失败机制分为两类：左端“覆盖增加但内容模糊”，说明二维补全/颜色质量不足；右端“质量门拒绝大区域而无法覆盖”，说明端点深度对齐或隐藏几何估计不足；
- 按既定顺序停止扩展 P02/P05，不进入 Stage 2，也不以放宽深度门或降低全局 opacity 掩盖失败；下一轮继续只在 P01 诊断右端深度对齐替代模型，以及改进显露内容生成质量。

## 13. 2026-08-07 深度模型诊断与 v4 级联回退（自动门通过，视觉仍未通过）

### 13.1 深度对齐诊断

- 固定 v2 的 P01、30°、掩码、LaMa 结果、深度网络输出和质量门，只比较 `depth_affine`、仅尺度、log-depth affine 与 `inverse_depth_affine`；诊断脚本为 `scripts/stage1_diagnose_depth_models.py`；
- 30°右端最大失败区域为 339,368 px：普通深度仿射在统一诊断口径下归一化 RMSE 约 0.258，逆深度仿射降至约 0.175，说明该大区域并非完全没有对齐信息，而是普通深度域的线性模型不合适；
- 右端另一个 1,752 px 区域在四种模型下均不可靠，最佳统一诊断 RMSE 仍约 0.461，因此应继续拒绝；
- 直接把所有区域统一切换为逆深度仿射的 v3 候选保存在 `outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v3_inverse_20260807/`。它在 15° 左端使所有区域失败，矩阵按质量门停止，证明逆深度不能作为全局替代。

### 13.2 v4 单变量修复与正式结果

- v4 只改变深度对齐选择规则：先使用原普通深度仿射；只有该区域失败时才尝试逆深度仿射；两者均不通过相同 scale 与归一化 RMSE 门时继续拒绝；其余掩码、LaMa、基础 PLY、补充高斯、可见性和相机协议与 v2 相同；
- 配置：`configs/p01_stage01_true_arc_v4_depth_fallback.json`；输出：`outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v4_fallback_20260807/`；三档 39 个步骤全部完成并通过完整性；
- 5°和15°结果与 v2 保持一致；30°左端空洞率仍由 8.357% 降至 1.984%（下降 76.25%）；
- 30°右端最大区域改用逆深度仿射并通过，scale=0.8354、归一化 RMSE=0.1754；右端接受/拒绝像素变为 341,043/1,752，生成 21,469 个有效补充高斯；全帧空洞率由 11.450% 降至 1.315%，相对下降 88.51%；
- 30°左右端均超过“空洞率下降至少30%”的自动门，改善样本数为 1/1、改善端点数为 2/2。三档中心帧 A/B 仍为逐像素零差异，来源端点/中心/对侧权重仍为 1/0/0。

### 13.3 视觉结论与当前状态

- 已检查 v4 六个视频的全部解码帧联系表，缓存位于 `99_过程文件_可删除或归档/渲染检查缓存/Stage01_P01_formal_v4_fallback_20260807/`；
- v4 解决的是“能否给右端大区域生成通过深度门的几何”，没有解决 LaMa 颜色内容质量。30°左右端黑洞均被大面积深绿/灰色模糊涂抹替代，边缘与原树木、地面结构不连续；
- 因候选验收还要求拉伸/涂抹等不得恶化到严重度>=2，故**自动 Alpha 门通过不等于 Stage 1 通过**。当前只能标记为“自动覆盖通过、助手全帧联系表视觉未通过、用户完整播放待复核”；
- Stage 1 仍未冻结，不能进入 P02/P05 或 Stage 2。下一技术瓶颈已从深度对齐转为二维显露内容生成；继续推进需要在 P01 上替换或增强 LaMa（如结构引导/MAT 类补全），并保持 v4 深度回退、掩码和可见性不变进行单变量 A/B。

## 14. 2026-08-08 outputs 目录整理（已完成）

### 14.1 整理目的与规则

- 原 `outputs/` 顶层混放 Stage 0、legacy 冒烟、true_arc 正式矩阵、dry-run 和失败诊断，共 25 个运行目录，不利于恢复实验和选择 PPT 素材；
- 本轮只增加分类层级，保留所有运行目录原名和目录内部内容；没有删除、覆盖或改写任何实验结果；
- 分类后顶层固定为 `00_Stage0_角度协议/`、`01_Stage1_legacy开发基线/`、`02_Stage1_true_arc/` 和 `90_失败与诊断归档/`；
- 正式结果、冒烟、dry-run 和失败现场分开保存。后续运行仍必须使用新的独立输出目录并拒绝覆盖。

### 14.2 当前正式入口

- Stage 0 冻结 A 基线：`outputs/00_Stage0_角度协议/03_正式A基线/stage0_p01_protocol_b_a_frozen_20260807_v1/`；
- Stage 1 v2b 完整对照：`outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v2b_20260807/`；
- Stage 1 当前自动覆盖最优结果：`outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v4_fallback_20260807/`；
- v4 状态仍为“自动 Alpha 覆盖门通过、助手全帧联系表视觉未通过、用户完整播放待复核、参数未冻结”，目录整理不改变任何实验结论。

### 14.3 文件与验证状态

- 整理前共有 1,390 个文件、3,935,494,795 bytes；
- `outputs/README.md` 作为结果导航入口，记录各分类用途、主要状态和后续存放规则；
- 旧路径已在本进度文档中同步更新；配置和正式脚本未硬编码现有运行目录名，无需修改；
- 整理后核验通过：排除新增索引文件后仍为 1,390 个实验文件、3,935,494,795 bytes，25 个运行目录全部存在；Stage 0、v2b、v4 三份关键汇总文件的 SHA256 与整理前一致；进度文档中旧输出路径残留为 0。

## 15. 2026-08-08 P01 v4 用户完整播放人工复核（已完成，Stage 1 未通过）

### 15.1 人工播放范围与评分位置

- 用户完整播放 P01 v4 的 5°、15°、30° A/B 共 6 个视频；A 为同一冻结基础高斯，B 为基础高斯加显露补全高斯；
- 正式人工评分写入：`outputs/02_Stage1_true_arc/03_正式矩阵/P01_stage01_true_arc_formal_v4_fallback_20260807/formal_summary/manual_review_template.json`；
- 严重度口径为 0=无、1=轻微、2=明显、3=严重；“B整体优于A”与“达到阶段验收门槛”分开判断。

### 15.2 用户完整播放结果

| 总视角 | DISOC | COVER | DEPTH | 涂抹 | 漂浮 | 闪烁 | 中心/对侧污染 | B相对A | 整体通过 |
|---|---:|---:|---:|---:|---:|---:|---:|---|---|
| 5° | 0 | 0 | 0 | 0 | 0 | 0 | 0 / 0 | 几乎相同 | 是 |
| 15° | 1 | 0 | 0 | 2 | 0 | 0 | 0 / 0 | 更好 | 否 |
| 30° | 0 | 0 | 1 | 2 | 1 | 0 | 0 / 0 | 更好 | 否 |

- 5° A/B 几乎没有区别，未见明显空洞；
- 15° A 第一帧右侧有少量空洞、最后一帧左侧空洞较明显；B 明显补全右侧，但左侧仍有少量残余；
- 30° A 两端空洞都更大，且右侧明显大于左侧；B 补全后未见空洞；
- 15°和30°的B均被用户判断为整体优于A，且未见闪烁或跳变；但两档均存在严重度2的明显模糊/涂抹，30°另有轻微深度/漂浮问题；首次明显伪影帧未记录，保留为 `null` 并在备注中说明。

### 15.3 正式结论与下一步影响

- **人工证据支持**：v4 能有效降低显露空洞；自动 Alpha 下降与用户完整播放结论一致，补全方向有效；
- **人工视觉未通过**：既定门槛要求 DEPTH、拉伸、涂抹和闪烁等不得恶化到严重度不低于2。15°和30°涂抹严重度均为2，因此 Stage 1 不能冻结，不能宣称30°可用；
- 当前瓶颈已明确为二维显露内容生成质量，而不是空洞检测、v4深度回退或来源视角可见性；
- 后续保持相机协议、P01基础高斯、显露掩码、v4深度级联回退、补充高斯方式和可见性不变，唯一改变补全网络；先做固定30°左右端点的 LaMa/MAT 静态补全对比，静态结果通过后再接回高斯和视频；
- 在P01视觉通过前不扩展P02/P05，不进入Stage 2。

## 16. 2026-08-08 双分支推进与 InfiniSplat 官方源码核验（进行中）

### 16.1 两个分支的研究问题

1. **分支 A：继续自研 Stage 1**。研究问题是：在保持 v4 深度级联回退、显露掩码、补充高斯方式、来源视角可见性和 Stage 0 相机协议不变时，替换二维补全网络能否把15°和30°的涂抹严重度从2降到不高于1。第一轮只比较 P01 30°固定左右端点的 LaMa 与一个通过环境兼容性门的候选模型，不同时改变其他变量；MAT为优先候选。
2. **分支 B：InfiniSplat 官方复现对照**。研究问题是：作者报告的 surface-aligned 前馈 3DGS 在同一张自有图片上，能否比 SHARP 和当前自研补全结果保持更稳定的较大视角结构。第一轮只验证官方 RGB 模式工具链，不把官方演示轨迹当作统一 Benchmark 结论。

### 16.2 InfiniSplat 论文与源码事实核验

- 本地论文：`文献/09_前馈生成与显露区域补全/35_InfiniSplat_Implicit_Gaussian_Decoding_for_Large-Baseline_Monocular_View_Synthesis.pdf`；arXiv:2608.02437v1，2026-08-03；
- 论文输入为单张 RGB，输出为包含位置、尺度、旋转、颜色和不透明度的显式 3D 高斯；核心是几何引导支持点采样和基于查询的隐式高斯解码，使高斯从固定像素对齐转向更接近表面对齐；
- 论文与官方项目页声称其在多组跨数据集单图前馈 NVS 评测中达到 SOTA。该表述当前仅属于作者公开结果，尚未经过本项目本地复现和统一协议验证；
- 官方项目页：`https://zju3dv.github.io/InfiniSplat/`；官方仓库：`https://github.com/zju3dv/InfiniSplat`；
- 官方源码已克隆到 `源码/InfiniSplat/`，固定 commit 为 `4146c8683b9bfb7756ddf45bf293093a31848074`，共462个文件、约39.82 MiB，工作树干净；许可证为 Apache-2.0；
- 官方代码支持 RGB-only 和 RGB+深度两种模式；当前对照分支只使用 RGB-only，支持单张 JPG/PNG 等常见图片，默认导出 PLY；安装 gsplat 后可额外生成60帧、10 FPS视频；
- 官方 RGB 权重 `infinisplat_rgb.ckpt` 为 3,142,241,921 bytes，公开且不需要申请；其远端 ETag/SHA256 标识为 `2a1b61fc2851b1bafaf3b80908c9b3971b31da7630da5ebb469fa7077477463c`。当前尚未下载权重。

### 16.3 环境边界与当前状态

- 当前机器为 RTX 5060 Laptop GPU、8,151 MiB 显存；C盘可用空间约81.01 GiB；Conda当前只有 `base` 和 `sharp`；
- 官方推荐独立 `infinisplat` 环境：Python 3.10、PyTorch 2.9.0+cu128、torchvision 0.24.0、xformers 0.0.33.post1，并安装 `requirements.txt`；视频渲染另需 gsplat；
- 该环境与现有 Python 3.13/PyTorch 2.8 的 `sharp` 环境不同，禁止直接安装进 `sharp`。截至本记录，源码下载和只读审计已完成，独立环境尚未创建、依赖尚未安装、权重尚未下载、尚未生成任何 InfiniSplat PLY 或视频；
- 下一步在用户确认环境安装影响后创建独立环境并下载 RGB 权重；第一张输入固定为 P01 原图，先运行官方 CLI 单图冒烟，记录输入/权重/源码 SHA256、PLY、高斯数量、推理时间、峰值显存、视频编码和失败情况；
- 官方冒烟通过后，再研究如何把 InfiniSplat 输出接入已冻结的 true_arc 5°/15°/30°协议。同一输入下必须统一轨迹、分辨率、裁剪和人工评分口径后，才能与 SHARP 或自研 Framework 做正式比较。

### 16.4 MAT 候选兼容性预审

- 当前项目中没有已下载的 MAT 官方源码或权重；官方论文代码仓库为 `https://github.com/fenglinglwb/MAT`；
- 官方运行说明要求 Python 3.7、PyTorch 1.7.1、CUDA 11.0，模型以512×512为主，输入尺寸需为512的整数倍；代码和模型仅供研究用途；
- 当前 RTX 5060 与已有 `sharp` 环境使用现代 CUDA/PyTorch，不能直接照搬 MAT 的旧环境，也不得为此降级或污染 `sharp`；
- 因此 MAT 当前状态为“方法候选、环境未通过”。下一步需先决定使用经过验证的现代 PyTorch 移植方案，或改选原生支持现代环境的结构引导/生成式补全模型；只有候选模型能在独立环境稳定运行后，才进入与 LaMa 的固定掩码静态对比。

## 17. 2026-08-09 InfiniSplat P01 官方 RGB 冒烟闭环（已完成工具链，待用户完整播放）

### 17.1 环境、权重与校验口径

- 独立 Conda 环境 `infinisplat` 已建立：Python 3.10.20、PyTorch 2.9.0+cu128、torchvision 0.24.0+cu128、xformers 0.0.33.post1；`pip check` 无损坏依赖；
- 官方 RGB 权重位于 `源码/InfiniSplat/checkpoints/infinisplat_rgb.ckpt`，大小 3,142,241,921 bytes，文件 SHA256 为 `D68A8C99109F06A264567766BD52D8D9BA81E51D044D0A966C3336160EA7007D`；
- 对第16.2节的哈希口径作追加更正：`2A1B61FC2851...` 是 Hugging Face CDN/Xet 存储对象哈希，官方响应中的 `X-Linked-ETag` 和本地文件 SHA256 均为 `D68A8C...`；字节数与文件 SHA256 双重校验通过；
- P01 输入仍使用冻结原图，SHA256 为 `68EE4C66CDC910673B536AF507F51C757E5325B02139C84585AFE610B0C80DA4`；源码固定 commit 仍为 `4146c8683b9bfb7756ddf45bf293093a31848074`。

### 17.2 Windows gsplat 可用性与环境边界

- 官方 GitHub 当前 gsplat commit `2b902ff1891fc7f73f0f9b8c8bfc932cef2b198c` 的“安装时全量编译”在本 Windows 环境中未通过，未将其冒充为已安装；随后使用同项目 PyPI 稳定版 `gsplat==1.5.3`（与已验证 SHARP 环境的版本一致）；
- PyPI 1.5.3 原始 Windows 后端错把 Linux 参数 `-Wno-attributes` 传给 MSVC，导致 `D8021`；已使用与现有 SHARP 环境一致的 Windows 兼容修复，正式补丁保存于 `03_实验记录/InfiniSplat复现实验/01_官方RGB单图冒烟/scripts/gsplat_1.5.3_windows_backend.patch`；
- 修复后 `_backend.py` SHA256 为 `3767D861...75D2`；新编译的 Python 3.10/CUDA 12.8 `gsplat_cuda.pyd` 为 18,475,520 bytes，SHA256 为 `DB7CD03B...473C`，InfiniSplat 内部 `is_gsplat_available()` 返回 `True`；
- `infinisplat` 只共用 `sharp` 环境中已安装的 CUDA Toolkit 12.8 编译工具链，没有共用 `sharp` 的 Python 解释器、PyTorch、gsplat Python 包或 `py313` 编译缓存；本轮没有重装 CUDA Toolkit，没有修改 SHARP 官方源码；
- xformers 提示 Windows 缺少匹配 Triton，只表示部分优化不可用；本轮 PLY 和视频均成功，不是阻断错误。

### 17.3 P01 PLY-only 冒烟（已完成）

- 配置：`03_实验记录/InfiniSplat复现实验/01_官方RGB单图冒烟/configs/p01_official_rgb_ply_smoke_v1.json`；输出：`outputs/P01_official_rgb_ply_smoke_v1/input/input.ply`；
- 官方 RGB 推理实际成功，产生 1,475,982 个高斯；PLY 为 82,655,790 bytes，SHA256 为 `565D0B17E1F50D8EF682E1AF388ABBBD61DB50A69E458B3F83A0C9F482509AA6`；
- 首次包装脚本在结果生成后因 Conda 将含中文路径的输出按 GBK 重编码而报 `UnicodeEncodeError`，这是返回阶段错误，不是模型失败；正式脚本已加入 `--no-capture-output` 和 UTF-8 环境修复；
- 实测运行约12分40秒，抽样显存高点约7.8/8.15 GB；该时间包含模型加载、推理、漂浮过滤和 PLY 导出，不与论文的其他计时口径直接比较。

### 17.4 P01 官方视频完整冒烟（自动完整性通过）

- 配置：`configs/p01_official_rgb_video_smoke_v1.json`；正式脚本：`scripts/run_p01_official_rgb_video_smoke.ps1`；输出使用新目录 `outputs/P01_official_rgb_video_smoke_v1/`，没有覆盖 PLY-only 运行；
- 完整官方 CLI 结果为 `status=success`；新 PLY 含 1,476,016 个高斯，82,657,694 bytes，SHA256 `2AF5F60B...E9E2`；两次官方运行的漂浮过滤后数量相差34个，表明重复运行存在轻微非确定性，后续统一对比必须冻结某一份 PLY 及哈希；
- MP4 为 H.264 High / yuv420p，3324×2494，10 FPS，6秒，60/60帧可完整解码；大小 4,715,768 bytes，SHA256 `185E3CB5...8320`；
- 全流程约15分钟：日志创建后约11分42秒完成 PLY，约3分14秒后完成 MP4；显存抽样高点约7.83/8.15 GB，未发生 OOM；
- 辅助抽帧联系表位于 `99_过程文件_可删除或归档/渲染检查缓存/InfiniSplat_P01_official_rgb_video_smoke_v1/contact_12frames.jpg`。助手抽帧可见顶部边界大面积黑色显露区域，右上树冠附近有模糊/拉伸风险；这仅是抽帧辅助观察，不代替用户完整60帧播放。

### 17.5 结论边界与下一步

- **已充分验证**：官方 RGB 单图输入可在本地 8 GB GPU 上生成约147.6万高斯的 PLY 和官方60帧视频；环境、权重、gsplat CUDA 后端和产物完整性通过；
- **待人工复核**：用户需完整播放 `input.mp4`，评价显露黑边、拉伸/涂抹、漂浮、深度错误、闪烁和运动幅度；
- **当前不能得出**：官方演示轨迹不是冻结 true_arc 5°/15°/30°，分辨率和计时边界也与 SHARP 不同，因此不能宣称 InfiniSplat 在本地已优于 SHARP 或自研 Stage 1；
- 下一步先完成官方视频人工复核；然后以本次 video smoke 的 PLY 及 SHA256 为冻结输入，单独设计 InfiniSplat PLY 到 true_arc 5°/15°/30°的适配器和 A/B 评价；自研分支仍保持“只替换二维补全网络”这一单变量，不与 InfiniSplat 官方轨迹混合结论。

## 18. 2026-08-11 双电脑同步与进度分流

- 本文继续作为**自研 Framework 分支**的实时进度权威入口；第 16～17 节保留为双分支启动和 InfiniSplat 首轮冒烟的历史记录，不删除、不改写；
- 后续 InfiniSplat 进度独立写入 `03_实验记录/InfiniSplat复现实验/实验进度与结果讨论.md`，不再混入本文；
- 两台电脑的统一同步规则写入根目录 `双电脑实验同步与产物管理说明.md`；Git 只同步代码、配置、文档与小型 `records/`，不要求两台电脑镜像全部 `outputs/`；
- 固定机器 ID 为 Windows RTX 5060 Laptop 的 `win5060` 和 Linux RTX 5080 服务器的 `linux5080`，后续不再用含义会变化的“本机/另一台电脑”指代产物位置；
- canonical v4 大型输出和人工评分当前位于 `win5060`，目录已完成四类重构；当前统计为 1,391 个文件、3,935,498,041 bytes，其中包含整理后新增的导航与人工评分内容；
- `win5060` 已将 v4 的归一化汇总、六端点指标、用户人工评分和关键大产物完整 SHA256 提升到本实验 `records/`。`linux5080` 拉取后无需镜像全部 outputs；只有需要播放或复核时才按 manifest 选择性传输；
- `linux5080` 的 outputs 按与 `win5060` 相同的四类结构创建；默认通过相同脚本、配置和输入重跑，不搬运整套 Windows outputs。服务器新结果使用独立运行 ID和 `producer_machine_id=linux5080`，不要求与 Windows 产物逐字节相同；
- 下一轮仍保持单变量原则：只替换二维补全网络，P01 静态端点对比通过前不扩展 P02/P05、不进入 Stage 2。

## 19. 2026-08-12 暂停 InfiniSplat 并恢复自研主线

### 19.1 外部对照对自研路线的含义

- InfiniSplat 官方复现没有发现包装层 Bug，但官方岩洞示例与 P01 均出现模糊和显露空洞；P01 在恢复原中心相机、1536×1152、总范围 5° 后仍被用户判定不可接受；
- 这进一步支持当前项目判断：只改善已可见表面的高斯布局，不能自动解决单图不可见区域缺少内容的核心矛盾；
- 因此项目不再继续调整原版 InfiniSplat 的焦距、背景、轨迹或高斯数，而是回到“显式显露检测 → 二维内容补全 → 深度对齐 → 补充高斯 → 跨视角可见性”自研主链；
- InfiniSplat 只保留为后续表示层候选：可在显式补全路线成立后，研究其表面对齐采样和隐式高斯解码是否比当前补充高斯生成方式更稳定。

### 19.2 当前唯一实验问题

> 在固定 P01、30°左右端点、同一显露掩码和其余 v4 管线全部不变时，新二维补全模型能否比 LaMa 更清晰地恢复建筑、树木和地面边界，并将涂抹严重度从 2 降至不高于 1？

第一轮只做静态二维补全对比，不重新生成补充高斯，不重跑 5°/15°/30° 视频，不改显露掩码、深度回退或可见性函数。

### 19.3 执行顺序与通过门

1. 从 v4 正式运行中锁定 P01 30°左右端点的原始渲染图、同一接受显露掩码和 LaMa 输出，记录字节数与 SHA256；
2. 对候选补全模型做现代 Python/PyTorch/CUDA 兼容性门，使用独立环境，不污染 `sharp` 或 `infinisplat`；
3. 只把同一 RGB+掩码输入交给 LaMa 和候选模型，统一输出分辨率与回填区域，生成左/右端静态对照和人工评价表；
4. 候选模型必须同时满足：无明显涂抹块、边界与已知树木/建筑/地面连续、不改写掩码外已知像素、两端涂抹严重度均不高于 1；
5. 静态门通过后，才把候选输出接回 v4 深度与补充高斯管线，再重跑 P01 5°/15°/30° A/B。

当前立即任务：核对服务器 v4 端点静态输入是否齐全，然后完成候选补全模型的现代环境审计。

### 19.4 固定静态输入核验（已完成）

- 固定输入清单已写入 `inputs/p01_ang30_static_inpaint_ab_v1.json`；
- 已核对 LaMa 的历史运行记录与执行脚本：实际输入为 `render_a/frame_left/right.png + left/right_mask/mask_accepted.png`，并非后续深度筛选输出；
- 左、右端点源分辨率均为 2048×1536，掩码分别覆盖 225,546（7.17%）和 342,795（10.90%）个像素；
- 清单已记录两张源图、两张掩码和两张 LaMa 基线输出的 SHA256。后续候选必须先通过这些哈希校验，并保证掩码外像素逐值不变。

### 19.5 MAT Places-512 现代兼容性与静态端点 A/B（已完成，静态门未通过）

**环境与来源**

- 官方 MAT 源码固定 commit：`d273d891ecdad2e1df106516423a75bc45b2d800`；官方说明原始环境为 Python 3.7、PyTorch 1.7.1、CUDA 11.0；
- 独立环境 `inpaint-mat` 使用 Python 3.10.20、PyTorch 2.9.0+cu128，不修改 `sharp` 或 `infinisplat`；`pip check` 通过；
- 现代 PyTorch 的 `cpp_extension.load()` 直接返回已加载模块，旧 MAT 代码丢弃返回值后再次 `import`，导致误判 CUDA 扩展失败。已保存最小加载器兼容补丁 `scripts/mat_pytorch29_custom_ops_loader.patch`；补丁不改网络、权重和算子计算；
- 补丁后 `bias_act_plugin` 与 `upfirdn2d_plugin` 均成功加载。随机 512×512 前向输出有限，首次已缓存运行约 2.01 秒、峰值 CUDA 分配约 1.33 GB；
- 官方 README 的 OneDrive 权重链接当前返回 HTTP 404。采用第三方镜像的 `Places_512_FullData.pkl`，大小 661,420,475 bytes，SHA256 `D960C4E6B3266B6B9FA74EE4458A9482160D54C06D7738696BC9A9E2B34C66DC`；两个独立镜像公开的文件大小和 SHA256 一致。记录中只称其为“官方发布权重的第三方镜像”，不冒充官方直链；
- pickle 在隔离环境中仅配合固定 MAT 源码加载；恢复出的 `G_ema` 为 61,562,518 参数、512 训练分辨率。

**正式静态输入与结果**

- 正式脚本：`scripts/stage1_mat_inpaint.py`；人工复核图脚本：`scripts/stage1_make_static_inpaint_review.py`；
- 左右端均使用清单锁定的 2048×1536 RGB 与 `mask_accepted.png`，不缩放、不填充；该分辨率宽高均为 512 的整数倍，符合 MAT 官方输入约束；
- MAT 掩码语义已显式转换为 `1=已知、0=待补`；随机种子固定为 240，`truncation_psi=1.0`、`noise_mode=random`；
- 左端推理 1.90 秒，右端 1.32 秒；两次峰值 CUDA 分配均约 10.71 GB。原生分辨率可在 `linux5080` 运行，但超过 8 GB `win5060` 的直接复现预算；若视觉门通过，再单独设计低显存等比例缩放/分块方案；
- 左右输出均通过“掩码外像素逐值不变”自动门；候选输出 SHA256 分别为 `A7D8E103...DB756` 与 `B824CB6C...97E3C`；
- 本机结果目录：`outputs/02_Stage1_true_arc/04_二维补全候选/P01_ang30_mat_places512_native_v1/`；统一复核图：`review/P01_ang30_LaMa_vs_MAT_static_review.png`，排布为两行左右端点、三列固定红色掩码/Big-LaMa/MAT；
- 助手初步观察仅作定位、不代替用户结论：MAT 生成了比 LaMa 更多的局部建筑/树木结构，但左右边缘仍存在大块暗色涂抹和不可信结构，尚不能据此判定静态门通过。

**当前停止点**

MAT 已停止在静态门，不重建补充高斯、不重跑视频。

### 19.6 MAT 用户静态复核结论（2026-08-12）

- 用户判断左端 MAT 相对 LaMa **稍好，但改善不明显**；
- 右端 MAT 在显露区域凭空生成了灰色楼体、窗户、围栏和墙面等结构，与已有树木、地面和边界的连接不连续；
- 该现象归类为**无观测依据的语义/结构幻觉**，不是普通模糊。二维结果即使局部“像照片”，接入单目深度与补充高斯后也会被实体化为虚假几何，并可能引入错误深度、遮挡冲突或漂浮物；
- 因项目目标是稳定的跨视角 3D，而不是单帧视觉合理性，右端虚假结构属于硬失败。MAT Places-512 本轮静态门判定为 **不通过**；
- 用户本轮没有给出左右端 0～3 数值，因此 `static_review.json` 中两个严重度字段保持 `null`，不由助手代填；定性比较和 `static_gate_pass=false` 已写入；
- 这说明单纯把 LaMa 换成更强的无条件二维生成器并不足以解决 30°问题：它可以减轻涂抹，却可能用不可验证的内容换取局部清晰度。下一候选必须增加来源约束、结构约束或跨视角一致性约束，不能只比较单帧锐度。

## 20. 2026-08-12 明日执行计划：Stage 1.1 几何与结构约束的显露区域静态补全（执行中）

### 20.1 当前判断与研究问题

- LaMa 已证明显露补全能够降低空洞，但 P01 的 15°和30°视频存在严重度2的明显模糊/涂抹；
- MAT Places-512 虽在左端稍有改善，但右端生成无观测依据的楼体、窗户和围栏，静态质量门不通过；
- 因此下一轮不再把“换一个更强的无约束二维补全器”作为主方案，而是验证类似 3D Photo Inpainting 的分阶段思路：先延续遮挡边界和场景结构，再补深度，最后补颜色；
- 当前唯一研究问题是：在 P01 30°固定左右端点、固定显露掩码和其余 v4 条件全部不变时，结构与几何约束能否同时减少 LaMa 的涂抹和 MAT 的虚假结构，并为后续补充高斯提供可信的深度层级。

### 20.2 冻结输入与单变量边界

- 继续使用 `inputs/p01_ang30_static_inpaint_ab_v1.json` 锁定的左右端点 RGB、`mask_accepted.png` 和 LaMa 基线输出，不重新渲染端点，不改变掩码；
- 静态对照统一为：A=未补全端点，B=Big-LaMa，C=MAT Places-512，D=下一种结构/几何约束方法；
- 固定输出分辨率、掩码外像素保持不变、左右端点独立来源身份和人工评价口径；
- 本轮只改变补全方法，不修改 SHARP 基础 PLY、true_arc 相机协议、v4 深度级联回退、补充高斯参数或可见性函数；
- P01 静态门通过前不生成新的补充高斯、不重跑 5°/15°/30°视频、不扩展 P02/P05、不进入 Stage 2。

### 20.3 明日执行顺序

1. **方法与源码审计**：核对本地 3D Photo Inpainting 相关论文、源码和现有脚本，明确“边缘/结构补全 → 深度补全 → 颜色补全”的输入输出、预训练权重、许可证、环境版本和现代 PyTorch/CUDA 兼容性；不得污染 `sharp`、`infinisplat` 或 `inpaint-mat` 环境；
2. **最小静态管线**：优先复用已有 P01 30°端点和显露掩码，先生成结构/边缘延续结果，再生成补全深度与补全 RGB；保存中间结构图、深度图、合成图、accepted/rejected 区域和运行配置；
3. **自动完整性门**：核对输入清单 SHA256、输出分辨率、掩码外像素逐值不变、有限深度比例、显露区与外围深度连续性，以及左右结果均完整生成；
4. **统一静态复核图**：生成左右端点的 A/B/C/D 并排对比，明确区分原始空洞、LaMa 涂抹、MAT 幻觉和新方法结果；
5. **用户人工静态门**：重点判断建筑/树木/地面边界连续性、虚假结构、明显涂抹、深度层级、掩码外污染和左右端内容矛盾；数值严重度继续由用户填写；
6. **分支决策**：静态门通过后才接回 v4 深度和补充高斯管线，重跑 P01 5°/15°/30° A/B；若不通过，保留失败案例并定位失败发生在结构、深度还是颜色阶段，不用裁剪、全局低 opacity 或扩大掩码掩盖问题。

### 20.4 候选静态通过门

- 左右端均不得出现 MAT 右端那类无观测依据的建筑、窗户、围栏或其他硬结构；
- 显露区主要结构应与已知区域的墙体、树木、地面或天空边界连续，不能只追求单帧锐度；
- 明显模糊/涂抹严重度目标为不高于1，并且不能以新增深度错误、漂浮风险或覆盖错误换取清晰度；
- 掩码外已知像素必须逐值不变；左右输出不得互相冒充同一来源，也不得提前声称完成跨端点冲突消解；
- 静态门只决定是否值得接回高斯，不等同于 Stage 1 视频人工视觉通过，更不能据此宣称30°可用。

### 20.5 双机快照与执行位置

- 2026-08-12 的 `win5060` / `linux5080` 快照正式对账已完成：共同路径 2,866，类型不一致 0；两端独有内容主要是各机独立运行、SHARP 冻结历史结果、MAT服务器结果和自研旧输出重组，不需要强制复制或整理为一致；
- 65个同路径大小差异中，64个是 Git 跟踪文本在 Linux LF 与 Windows CRLF 下的正常字节差异；另一个是本地 `manual_review_template.json` 副本，正式人工评分已由 `records/` 管理，不要求同步该大型输出目录内副本；
- MAT 原生 2048×1536 推理峰值约10.71 GB，不适合 `win5060` 8 GB直接复现；明日若候选方法需要较大显存，优先在 `linux5080` 运行，Git 只同步脚本、配置、清单、机器可读结果和进度记录，大型输出继续本地保留；
- 快照对账只说明两台机器各自拥有哪些路径，不替代 `artifact_manifest.json` / `run_receipt.json` 中的正式产物 SHA256。

**当前状态**：计划已确认；Stage 1.1 方法与源码审计已于2026-08-13开始并形成首版结论，详见第21节。尚未启动 GPU 推理或生成新的静态候选图。

## 21. 2026-08-13 Stage 1.1 方法与源码审计（已完成首轮）

### 21.1 官方 3D Photo Inpainting 的实际输入协议

- 已核对论文、项目页和官方仓库 `vt-vl-lab/3d-photo-inpainting`，本地审计源码固定 commit 为 `de0446740a3726f3de76c32e78b43bd985d987f9`；
- 官方方法先由单图 RGB-D 构建带显式像素连接关系的 LDI，再围绕深度不连续边界逐个连通域处理遮挡后方区域；网络顺序是深度边缘延续、深度补全、颜色补全；
- 边缘网络输入为 RGB、归一化视差、已知深度边缘、上下文和洞区；深度网络输入为深度、完成后的边缘、上下文和洞区；颜色网络输入为 RGB、完成后的边缘、上下文和洞区；
- 关键限制：官方网络不是任意整图 `RGB + mask` 补全接口。洞区、上下文、前后景边界和局部裁块都由 LDI 图结构生成。若只把现有 `mask_accepted.png` 直接送入颜色网络，会偏离训练/推理分布，不能称为论文的忠实复现；
- 该方法仍会合成原图不可见区域的颜色和深度，因此“结构约束”不等于“不会幻觉”。它的价值是显式维护遮挡边界与深度层级，而不是提供可观测真值。

### 21.2 候选与环境决定

- 首个可运行候选确定为官方 3D Photo Inpainting 三网络；SLIDE 的方法方向也匹配，但本轮未找到官方公开源码和 checkpoint，不作为第一实现；
- 官方环境为 Ubuntu 18.04、Python 3.7、PyTorch 1.4.0、torchvision 0.5.0、CUDA 10.1，不能直接照搬到当前 CUDA/PyTorch，也不得修改 `sharp`、`infinisplat` 或 `inpaint-mat`；
- 在现有 `inpaint-mat` 环境仅做了不写入的 CPU 构造冒烟：Python 3.10.20、PyTorch 2.9.0+cu128 下三个网络类均可成功实例化，参数量分别约11.43M、51.55M和51.57M；这只证明网络定义可加载，不代表 checkpoint、LDI 图处理或 GPU 完整推理已兼容；
- 源码已发现19处 `np.int` / `np.bool` 等现代 NumPy 已删除别名、未指定 Loader 的 `yaml.load`、旧 NetworkX/依赖固定版本等兼容点；后续只保存最小兼容补丁，不改网络结构或权重计算；
- 官方 checkpoint 位于 Virginia Tech Filebox；2026-08-13 本机探测在超时内未获得响应，环境建立后需继续尝试官方地址并记录文件 SHA256，不以来源不明镜像悄悄替换；
- 仓库主体和 MiDaS 标为 MIT，但 EdgeConnect 组件标为 CC BY-NC 4.0。本候选当前按研究/非商业实验管理，不能仅依据仓库顶层 MIT 推断整套组件可商用。

### 21.3 与冻结 P01 实验的适配边界

- 继续使用 `inputs/p01_ang30_static_inpaint_ab_v1.json` 锁定的左右 RGB、accepted mask 和 A/B/C 对照，不重新渲染、不扩大或缩小 accepted mask；
- 允许 D 方法读取同一 v4 端点已有的深度作为掩码外已知几何信号；这属于“补全方法输入变化”，必须在结果中显式记录，不能继续把 D 描述成纯 RGB 补全器；
- 适配器需从固定洞区边界和已知深度提取背景侧上下文，依次生成结构边缘、洞区深度和 RGB；最终合成仍须保证掩码外像素逐值不变；
- 该输出命名为“3D Photo 网络固定端点适配实验”，不命名为“官方 3D Photo 完整复现”。官方完整 LDI 管线可另做方法行为验证，但不得用其自生成轨迹替代冻结 true_arc A/B；
- 正式审计记录已写入 `records/stage1_1_method_audit.json`。下一步是建立独立 `inpaint-3dphoto` 环境、取得并校验官方三网络权重、跑左端点最小接口冒烟，然后再实现左右端静态 D 候选。

### 21.4 隔离环境、权重与现代 GPU 接口冒烟（已完成）

- 已从 `inpaint-mat` 克隆出独立 Conda 环境 `inpaint-3dphoto`，保留 Python 3.10.20、PyTorch 2.9.0+cu128、torchvision 0.24.0+cu128；原 `sharp`、`infinisplat`、`inpaint-mat` 均未修改；
- 官方 Virginia Tech Filebox 在 `linux5080` 上解析正常，但经代理和绕过代理直连均出现 TLS/连接超时；官方 GitHub release 为空，仓库 issue #219 也记录了2025年以来相同的官方权重地址不可达问题；
- 找到两个独立第三方 Hugging Face 存档 `ai-minamo/3d-photo-inpainting` 与 `camenduru/3d-photo-inpainting`。两边三网络文件的文件名、字节数和 LFS SHA256 逐项相同，因此从前者下载、以后者交叉校验；不把镜像来源误写为官方托管；
- 三网络权重已写入 Git 忽略的 `models/3d-photo-inpainting/`：edge 45,974,122字节、SHA256 `B1D768...17C9`；depth 206,272,258字节、SHA256 `2D0E63...76F8`；color 206,331,633字节、SHA256 `383C9B...2C20`；
- `torch.load(..., weights_only=True)` 后三个 state_dict 均以 `strict=True` 完整匹配官方网络定义；
- 按 GPU 安全规则检查后避开有两个未知计算进程的 GPU 0，选择仅有约313 MiB既有占用的 GPU 2。P01 左端512×384三网络前向成功：总推理0.558秒、峰值约1.01 GB，边缘/深度/颜色输出形状正确且全部有限；
- 首次冒烟在网络计算前暴露一个适配调用点：官方颜色网络重写 `train()` 却不返回 `self`，因此不能链式写成 `.cuda().eval()`；已在自研适配脚本中改为分行调用，不修改官方网络或权重。

### 21.5 固定端点 3D Photo 三网络适配 v1（已完成，静态门不通过）

- 新增 `scripts/stage1_3dphoto_adapter.py`：严格校验冻结清单和三权重 SHA256；按 accepted mask 外接裁块；从已有端点深度中筛选局部较远背景侧上下文；按边缘→对数深度→RGB顺序调用官方三网络；最后只在冻结 accepted mask 内合成；
- 该结果明确标记为“3D Photo 网络固定端点适配实验”，不是官方 LDI 完整复现。D 相比 A/B/C 额外读取同一端点已有深度，因此是结构/几何输入方法，不再描述成纯 RGB 补全单变量；
- 左端工作分辨率1024×384，推理0.797秒、峰值约1.71 GB；右端工作分辨率768×1024，推理0.838秒、峰值约2.69 GB；左右掩码外像素均逐值不变，洞区补全深度有限正数比例均为1.0；
- 输出保存在本机 `outputs/02_Stage1_true_arc/05_结构几何约束候选/P01_ang30_3dphoto_adapter_v1/`，包括背景侧上下文、已知/完成结构图、补全深度、原始/合成RGB和逐端运行记录；大型输出继续不进 Git；
- 新增 `scripts/stage1_make_abcd_review.py`，统一复核图为 `review/P01_ang30_ABCD_3dphoto_adapter_v1.png`，两行对应左右端，四列为A未补全、B Big-LaMa、C MAT、D 3D Photo adapter v1；
- 助手视觉预判：D 的左右外侧都出现大面积暗绿/灰色涂抹，未显示出足以优于 Big-LaMa 的改善；虽然没有复现 MAT 那种明确的虚假楼体/窗户，但仍不满足“明显涂抹严重度不高于1”的门；
- 这说明工程链路与权重本身可以在现代 RTX 5080 上正确执行，但“把 LDI 局部三网络适配到大块 alpha 显露洞区”的 v1 假设不成立。当前停止在静态门，不生成补充高斯、不重跑视频、不扩展 P02/P05，也不通过反复调参掩盖输入协议失配；
- 正式机器记录写入 `records/p01_ang30_3dphoto_adapter_v1_review.json`。下一步只需用户查看统一 A/B/C/D 图并确认左右涂抹严重度与静态门；若确认不通过，研究方向应从“继续换/套二维补全网络”转为利用源图重投影、多层表示或多视图生成，而不是继续优化本适配器。

### 21.6 3D Photo adapter v1 用户静态复核结论（2026-08-13）

- 用户对统一 A/B/C/D 图完成复核：左端涂抹严重度3，右端涂抹严重度3；
- 虚假硬结构记为1，用户指出主要问题仍是**虚假建筑**；
- D 与 B/C 的整体效果判断为“接近”，没有形成足以继续接回三维管线的质量增益；
- 静态门正式判定为**不通过**。3D Photo adapter v1 到此停止，不生成补充高斯、不重跑视频、不扩展 P02/P05，也不继续做局部参数搜索；
- 至此，Big-LaMa、MAT 和 3D Photo 固定端点适配分别暴露了涂抹、语义/结构幻觉，以及在协议失配下同时涂抹并生成虚假建筑的问题。继续横向更换单帧补全器的边际价值已经很低；
- 下一研究问题改为：在进入生成式补全前，能否先利用中心原图、基础深度和相机几何把**真实可重投影的观测内容**搬到30°显露区，只对重投影后仍无来源的残余区域使用受限补全，并显式保存来源/置信度。该方向应先做静态可观测覆盖率分析，再决定是否实现多层表示或残余补全。

## 22. 2026-08-13 Stage 1.2 中心真实观测重投影覆盖率（已完成）

### 22.1 冻结问题与实现口径

- 输入清单固定为 `inputs/p01_ang30_observed_reprojection_v1.json`：P01 原始4096×3072 RGB、基础 PLY、中心/左右端深度与 Alpha、左右 accepted mask、`true_arc` ±15°相机；所有输入均登记 SHA256；
- 原图用 Lanczos 缩至冻结2048×1536，只提供颜色；几何使用中心 GSplat 的归一化 Z-depth 与 Alpha，不用 LaMa/MAT/3D Photo 生成内容；
- 新增 `scripts/stage1_observed_reprojection.py`：中心 RGB-D 反投影到世界坐标，再投影至端点，以2×2双线性前向 splat 和最近深度 z-buffer 决定来源；可靠目标深度只用于拒绝位于已有表面后方的样本和计算一致性，不用于凭空制造覆盖；
- 每个目标像素保存重投影 RGB、深度、中心源坐标、置信度、raw/observable/high-confidence 覆盖和残余掩码；红色表示 accepted 中无来源，黄色/绿色表示低/高置信真实来源；
- 本轮只回答覆盖率，不做学习式补全、不建补充高斯、不改相机/掩码/基础 PLY。

### 22.2 几何验证

- 中心→世界→中心恒等重投影最大误差为 `1.61e-13 px`，深度最大误差为0，左右内参差为0；坐标变换实现通过；
- 左端已知可靠区域 observable 覆盖84.84%，右端87.29%；该值未达到100%的部分包括前向采样裂缝、端点出画和遮挡变化，不能转而当成生成目标；
- 在可与端点可靠深度比较的位置，左端深度一致率89.51%、右端90.18%；中位相对误差分别约0.330%和0.331%，支持当前 Z-depth 与遮挡判断；
- 初次左端正式运行在所有图/数组写完后因结果 JSON 中 Python 布尔拼写错误中断，未产生正式 JSON。该部分产物保留为 `left_failed_result_json_typo/` 且不参与结论；修正后在规范 `left/` 目录完整重跑。

### 22.3 accepted 显露区覆盖率结果

| 端点 | accepted 像素 | 有中心来源 | observable 覆盖 | 高置信覆盖 | 真正无观测残余 |
|---|---:|---:|---:|---:|---:|
| 左端 -15° | 225,546 | 229 | 0.1015% | 0.0031% | 99.8985% |
| 右端 +15° | 342,795 | 398 | 0.1161% | 0.0055% | 99.8839% |
| 合计 | 568,341 | 627 | 0.1103% | — | 99.8897% |

- 左端最大残余连通域222,556像素，右端339,368像素；这不是少量边缘裂缝，而是几乎覆盖整个外侧显露区域的大块不可见内容；
- 新增 `scripts/stage1_summarize_observed_reprojection.py`，统一复核图为 `summary/P01_ang30_observed_reprojection_review.png`；大型逐像素输出仍留本机，正式小型证据写入 `records/p01_ang30_observed_reprojection_v1.json`；
- 结论非常明确：单中心 RGB-D 重投影对 accepted 大洞几乎没有可用增量。仅把627个有来源像素建成补充高斯没有实验价值，也不能解决此前的涂抹或虚假建筑。

### 22.4 分支决定

- **停止**单层中心 RGB-D 重投影作为30°补全方案；保留其作为来源/置信度审计工具；
- **停止**继续横向替换无约束单帧二维补全器；LaMa、MAT、3D Photo adapter 已提供足够失败证据；
- 下一阶段应先解决“隐藏内容从哪里来”：候选是显式隐藏层预测，或引入外部/多视图先验。无论使用何种模型，都必须输出隐藏层深度、颜色、来源类型和置信度，再由端点重投影验证，不能直接把端点二维幻觉实体化；
- 下一技术交付物应是 Stage 1.3 隐藏层表示协议：定义前景层、背景隐藏层、层间遮挡、置信度与端点监督/评价接口。协议冻结后再选择第一个可运行模型，不立即训练或生成视频。

## 23. 2026-08-13 Stage 1.3 隐藏层表示协议（v1 已冻结）

### 23.1 路线结论

- 已创建 `Stage1_3_隐藏层表示与验证协议.md`，正式定义 Canonical Layer Bundle（CLB）；
- CLB 不仅包含中心可观测表面和遮挡后方表面，还显式包含“中心视锥外、但会在 ±15° 进入画面”的扩展表面。仅使用中心视角 LDI 不足以完整表达 P01 外侧大缺口；
- 每个表面块必须输出 RGB、Z-depth、Alpha、有效掩码、逐像素来源、几何置信度和外观置信度。置信度与渲染 Alpha/Opacity 分离；
- 新增加的任何预测内容都要先通过层间深度顺序、中心泄漏、端点重投影和跨视角冲突检查，才允许进入 Gaussian Spawn；
- 旧 `stage1_build_supplement_gaussians.py` 没有逐像素来源和独立置信度接口，且左右融合只做 append。它保留为 legacy 证据，未适配 CLB 前不再用于新主线。

### 23.2 文献与候选审计

- 3D Photo Inpainting 支持可变层数 LDI 和遮挡后方局部 RGB-D 合成；SLIDE 使用前景软可见性与背景 RGB-D 双层；TMPI 在局部 tile 中用少量可变深度 RGBA 平面；
- 这些方法证明“先表达隐藏层，再统一渲染”的方向合理，但不直接解决 P01 的中心视锥外扩展内容；
- 2026-08-13 核对官方项目入口：3D Photo 有官方代码，但其 P01 端点适配已经失败；SLIDE 官方项目页未提供代码/checkpoint；本轮未找到 TMPI 作者公开的官方代码/checkpoint 入口。因此不把任何一个直接定为首个可运行模型。

### 23.3 已落盘接口与下一步

- 机器可读冻结配置：`configs/stage1_3_hidden_layer_protocol_v1.json`；
- 结构校验器：`scripts/stage1_validate_clb.py`，校验必需数组、dtype/形状/范围、来源枚举、观测层来源与部署禁用 synthetic oracle 等硬条件；
- 第一个可运行候选冻结为 `CLB-SYN-01`：受控合成遮挡层 + 左右视锥外扩展层，先在 0°/±15° 验证表示和渲染能力；
- 该候选不训练模型、不使用 P01 调参、不生成 P01 高斯或视频。只有合成硬门通过后，才进入 `CLB → supplemental Gaussians`，之后再选预测模型。

### 23.4 `CLB-SYN-01` 执行结果（已通过）

- 新增 `scripts/stage1_clb_synthetic_smoke.py`，已生成 5 个受控表面块：观测背景、观测前景、遮挡后方背景、左视锥外扩展层、右视锥外扩展层；
- CLB 清单及 5 个 NPZ 表面块已通过 `stage1_validate_clb.py`的结构和语义校验；
- 中心隐藏层泄漏为0像素；左右端点均显露2,470像素遮挡后方层与11,392像素对应视锥外层；
- 端点深度相对误差 P95 为0；错误遮挡顺序率0.05208%，低于0.1%门；中心↔端点往返重投影中位误差 `4.02e-14 px`；
- 全部 6 项硬门通过，正式记录为 `records/clb_syn_01.json`；canonical 本机输出为 `outputs/02_Stage1_true_arc/07_隐藏层表示/CLB-SYN-01_roundtrip/`；
- 本结果只证明“表示 + 直接表面渲染”能做对，不证明单图能预测出正确隐藏内容，也不证明 P01 30° 可用。

**下一个单变量任务**：仅用 `CLB-SYN-01` 合成真值实现 `CLB → supplemental Gaussians`，检验转换前后的中心泄漏、端点覆盖、深度顺序和跨视角一致性。不接 P01，不接预测模型。

### 23.5 `CLB-GS-SYN-01` 高斯转换（v2 已通过）

- 新增 `scripts/stage1_clb_to_gaussian_smoke.py`，从 CLB 锚点 RGB-D 构建共享世界坐标高斯，并保留层 ID、来源类型和高斯索引范围 sidecar；
- 依 GPU 规则检查资源后，保留 GPU 0/2 的高占用进程和 GPU 1/3 的既有进程，仅用剩余显存充足的物理 GPU 1 运行小型合成渲染；
- v1 `scale_factor=0.42` 因斜视端点高斯覆盖不足失败，保留为 `records/clb_gs_syn_01_v1_failed.json`；
- v2 只将 `scale_factor` 改为0.70，不使用旧的 endpoint angle fade。共121,600个高斯，其中观测层93,312个，隐藏/视锥外层28,288个；
- 中心可靠内部隐藏层泄漏最大差0.000713，低于 `1/255`；全图边界带诊断最大差0.0973，仍在记录中显式保留；
- 左/右隐藏区 RGB MAE 相对 observed-only 下降98.18%/97.98%；三视角可靠内部 Alpha 覆盖均100%；深度相对误差 P95 为0.0306%/1.0178%/1.0178%；
- 5 项高斯转换硬门全部通过，正式记录为 `records/clb_gs_syn_01.json`，canonical 本机输出为 `outputs/02_Stage1_true_arc/07_隐藏层表示/CLB-GS-SYN-01_v2/`；
- 结论边界：这只证明合成 oracle CLB 可被正确转为高斯，不证明单图隐藏层能被正确预测，也不证明 P01 30° 质量已通过。

**下一个任务**：确定 `HiddenLayerProvider` 的第一个可运行候选与小型多视图真值数据，先验证它能否输出 CLB 需要的隐藏几何、外观和独立置信度，不直接在 P01 上以主观效果选模型。

## 24. 2026-08-13 Stage 1.4 HiddenLayerProvider 候选与双真值协议

### 24.1 Provider 拆分与首候选

- Stage 1.3 的统一 `HiddenLayerProvider` 正式拆成 `OcclusionHiddenProvider` 与 `OutsideFOVProvider`；前者处理中心视锥内的遮挡后表面，后者处理中心原图视锥外、会在 ±15°进入画面的扩展表面；
- Flash3D 与 MINE 都只在源相机视锥内建立表示，不能承担 P01 大块视野外内容。首候选确定为 Flash3D `re10k_v2`，且只进入 `OcclusionHiddenProvider`；`OutsideFOVProvider` 当前保持未选择；
- Flash3D 默认每像素两层高斯：第一层来自 UniDepth，第二层为累计的正深度增量，两层分别输出 Gaussian opacity/scale/rotation/appearance。该结构比 MINE 更接近现有 CLB→Gaussian 接口；
- 官方输出没有独立的几何/外观置信度。Opacity 是渲染覆盖量，不得作为置信度；正式 adapter 必须另做误差校准并显式将视锥外标为 unsupported。

### 24.2 官方源码、权重与许可边界

- Flash3D 官方源码固定 commit `a71c9b92b07a76cf944f2ed894f384f0891a1960`；MINE 固定 commit `ef16cd83ae99c70f7970a583c533016c63587373`；Hypersim 官方仓库固定 commit `c85b2879c8da8ded2f4c24d2630c1ab4451999b2`；
- 三个外部源码镜像仅保留本机并由 `.gitignore` 排除，主仓库只保存官方 URL、commit、适配脚本和小型证据，另一台机器按记录重新 clone，不 vendor 整个外部仓库；
- Flash3D 本地源码顶层没有 LICENSE 文件；其 Hugging Face 模型页面的 MIT 标签不能自动替代码授权下结论。MINE 仓库为 MIT；Hypersim 数据集为 CC BY-SA 3.0；
- 官方 Flash3D checkpoint 已下载至忽略目录：690,161,102字节，SHA256 `4d717b5543941069fc120733ab4001e6d11fe73ea094d6c6054c5e8b48922420`；`torch.load(weights_only=True)` 可读取390项模型状态，双 Gaussian decoder 与 depth increment decoder 均存在；
- CPU 静态预检全部通过，记录为 `records/stage1_4_flash3d_provider_preflight_v1.json`。这不是 GPU 推理或质量通过结论。

### 24.3 `HLP-APP-01`：RealEstate10K 真实角度预检

- 已下载并校验 RealEstate10K 官方相机轨迹压缩包：752,332,631字节，SHA256 `af18e4d560ee9f73120128e53a56da215e118f3fb52018c14a66aa0f8ed691db`；只读取 tar 内元数据，没有全量解压；
- 新增 `scripts/stage1_select_re10k_angle_triplets.py`，从 world-to-camera 外参计算目标光轴在源相机坐标下的真实有符号水平角；
- 明确拒绝把 Flash3D 官方 `tgt5/tgt10` 时间偏移标签当成5°/10°。本项目30°总范围固定筛选 `-15° / +15°`，单端容差±2.5°，内参相对变化不超过2%；
- 官方 Flash3D 验证 split 涉及48个独立序列，48个元数据全部找到；其中16个序列有合格三元组，当前每序列最多保存3个，共48组；记录为 `records/stage1_4_re10k_angle_triplets_v1.json/.csv`；
- RealEstate10K 有目标 RGB、内参和位姿，可评价新视角外观，但无稠密深度，不能作为隐藏几何真值，也不能用整图 PSNR 替代深度顺序评价。

### 24.4 `HLP-GEO-01`：Hypersim 几何真值预审

- Hypersim 官方数据包含 RGB、逐像素相机距离、世界坐标、完整相机位姿和场景内参，能力上可构造遮挡后区、视野外区和 Z-depth 真值；
- 官方 `depth_meters` 是像素到相机光心的欧氏距离，不是 CLB 所需的 Z-depth，后续必须依据射线方向转换；相机位置的 asset units 与米还需按场景比例统一；
- 全量约1.9TB，不下载。采用官方 test split，使用仓库自带的 HTTP Range 子文件下载工具：先远程读取选定 scene ZIP 的姿态与索引，筛出约±15°且显露量足够的轨迹，再只下载3个场景各1组三元组所需 RGB、depth/position、姿态与内参；
- 当前只冻结数据能力和最小规模，尚未冻结具体3个三元组，也尚未下载 Hypersim 图像数据。不得把“候选已确认”写成“几何验证集已就绪”。

### 24.5 冻结评价与下一步

- 外观指标为 PSNR/SSIM/LPIPS/DISTS；几何指标为 AbsRel、δ<1.05、深度顺序错误、中心泄漏和跨视角冲突；置信度报告 risk-coverage、selective AURC 与 ECE；
- `occlusion_hidden`、`outside_source_fov` 和 whole-target 分开报告；没有真值的列保持 N/A，不补造；
- 机器可读协议为 `configs/stage1_4_hidden_provider_protocol_v1.json`，正式说明为 `Stage1_4_HiddenLayerProvider候选与双真值协议.md`；
- 下一步建立独立且兼容 RTX 5080 的 Flash3D 环境。官方 PyTorch 2.2.2/CUDA 11.8 组合不能直接假定兼容新架构；不得污染 `sharp`、`infinisplat` 或其他补全环境；
- 环境通过后先做官方 checkpoint 单图最小推理和输出 schema 检查，再下载5个 RealEstate10K序列的少量目标帧并实现 Flash3D→CLB adapter；不直接在 P01 上跑30°视频。

### 24.6 `linux5080` 独立环境与最小推理断点（2026-08-13）

- 已从现有 `infinisplat` 环境克隆建立独立 `flash3d` Conda 环境，未修改 `sharp`、`infinisplat` 或补全环境；当前版本为 Python 3.10.20、PyTorch 2.9.0+cu128、torchvision 0.24.0+cu128、xFormers 0.0.33.post1、Lightning 2.6.5，`pip check` 无破损依赖；
- 环境内已安装 CUDA Toolkit 12.8.93。Flash3D 官方 pose rasterizer 在现代 GCC/CUDA 下仅需补充 `<cstdint>` 头文件即可面向 SM 12.0 编译；最小补丁保存为 `scripts/flash3d_rasterizer_cuda128_cstdint.patch`，未改算法；
- Flash3D、UniDepth 和 pose rasterizer 源码分别固定在 commit `a71c9b9`、`8d8cfe4` 和 `f6ac989`；UniDepth v1 ViT-L 权重已完整下载到 Git 忽略的本地 Hugging Face cache，文件大小1,388,239,045字节，SHA256 `da8f7910dbcf1d14d2d9f9699d791a740fad8c41ba2ab0ae5729dcff20efbb60`；
- 最小层输出脚本 `scripts/stage1_flash3d_layer_smoke.py` 已按官方 `load_model()` 逻辑处理训练 batch=16 与推理 batch=1 的非学习 `backproject_depth` 缓存形状差异，并显式记录运行前选定的物理 GPU；
- 启动前完整检查 `nvidia-smi`：GPU 1 当时约占2.2 GiB、利用率0%，比 GPU 0/2/3 更空，因此本轮使用物理 GPU 1，没有终止或碰触任何既有进程。后续规则为“每次先检查显存、利用率和进程；资源足够时优先 GPU 1”，不是无条件默认 GPU 1；
- 本轮 GPU 推理**未通过**：xFormers 0.0.33.post1 对 RTX 5080（compute capability 12.0）的 FP32 `memory_efficient_attention` 无可用 kernel；同时当前 xFormers 已移除 UniDepth v1 decoder 所依赖的旧 `NystromAttention` 类。这是现代 GPU/依赖兼容问题，不是 checkpoint 形状或显存不足；
- 本轮在下班交接前停止，未输出“通过” JSON，也没有开始 RealEstate10K 帧下载。下一步是从 xFormers 0.0.25.post1 官方历史实现提取等价 Nyström 兼容层，并将 DINO attention 回退到 PyTorch SDPA；只有最小层输出 schema 完整通过后，才进入 Flash3D→CLB adapter 和5个 RealEstate10K 小样本。

### 24.7 RTX 5080 最小层输出推理（已通过，2026-08-14）

- 追溯发现 Flash3D 初始公开 commit 为2024-07-19，其 `requirements.txt` 固定 xFormers 0.0.25.post1，但 `torch.hub` 对 UniDepth 未固定 commit。为减少 moving-main 偏差，本地 UniDepth 改为 Flash3D 首发时最新的官方 commit `bebc4b2fdda5c223693c390739db76897aee42e5`；
- xFormers 0.0.25.post1 的官方 Nyström 实现只明确覆盖3D `[B×H,N,D]`，而 UniDepth v1 从首个公开 commit 就传4D `[B,N,H,D]`。新增 `scripts/flash3d_xformers_compat.py`：复用官方 commit `7fffd3d` 的 Nyström 公式，显式按标准多头语义折叠/还原 head 维；DINO 主干在 SM 12.0 上回退到历史 UniDepth 自带的 PyTorch reference attention；
- CPU 单元自检中，完整注意力分支与显式参考实现的最大绝对误差为0，Nyström 近似分支输出形状正确且全部有限；
- 每次启动前均重新检查 `nvidia-smi`。正式通过运行时 GPU 1 仅约311 MiB、利用率0%，因此使用物理 GPU 1；GPU 0 的多个既有任务未被触碰；
- P01 单图最小前向运行0.895秒，峰值显存 `2,489,876,480` bytes；两层 depth、means、opacity、scale、rotation 与 appearance 均为有限值，第二层深度增量为正的比例1.0，最小增量 `4.5776e-05`；正式记录为 `records/stage1_4_flash3d_layer_smoke_v1.json`；
- Flash3D checkpoint 缺失的698项全部是未打包的外部 UniDepth 参数，已由单独的预训练权重加载；Flash3D 自身没有 unexpected/missing key。UniDepth 官方权重在历史 commit 上仍固定报告 `register_tokens` 和最终 `norm` 三个 missing key，按上游 `strict=False` 记为已知边界；
- 本次通过只证明 checkpoint 可在 RTX 5080 上产生符合 schema 且数值有效的双层高斯，不证明新视角质量、几何准确性或 P01 30°可用，也不宣称与当年未固定环境逐比特一致。

**下一任务**：从已筛选候选中选5个不同 RealEstate10K 序列，只获取每个序列的源/左/右三帧，建立 `HLP-APP-01`；不运行官方全量下载器，不直接进入 P01 视频。

### 24.8 `HLP-APP-01` 小型外观真值集（完整性门已通过，2026-08-14）

- 新增 `scripts/stage1_download_re10k_triplets.py`，按角度误差由小到大、序列不重复的固定规则，仅下载候选序列的360p渐进式视频并提取源/左/右三帧；没有使用官方的全量下载和删除流程；
- 5轮定向获取共尝试12个候选，7个因 YouTube BotDetection 或远程断连失败，5个成功序列为 `1b87f55dec310243`、`168c85ce00de0c6b`、`195fbef2c08715e9`、`8c2c302994f6cea1`、`c53178af617eaa65`；
- 新增 `scripts/stage1_finalize_re10k_triplets.py`，合并所有回执后重算文件 SHA256，检查图像可读性与组内尺寸，并验证每个帧时间戳确实存在于官方元数据；
- 正式记录 `records/stage1_4_hlp_app_01_final_v1.json` 为 `pass`：5个不同序列、15帧 RGB，全部哈希、时间戳和尺寸门通过；原始与补充回执继续保留失败证据；
- 新发现的正式评价前置条件：RealEstate10K 平移只有任意尺度，Flash3D 官方用 `pcl.test.tar` 中的稀疏 COLMAP 点云和 UniDepth 通过 RANSAC 估计深度尺度。该官方 tar 约10,312,816,640字节，支持 HTTP Range；不直接下载全量，下一步实现按 tar 成员定向取得上述5序列稀疏点云的可验证工具。

**当时的结论边界**：`HLP-APP-01` 已是可追溯的 RGB/位姿外观真值小集，但它没有稠密深度，且在本小节所记阶段平移尺度尚未恢复；后续已在第24.9节完成尺度恢复。本小节单独不宣称 Flash3D 新视角质量或 `OcclusionHiddenProvider` 已通过。

### 24.9 稀疏 COLMAP 尺度恢复与 Flash3D→CLB（schema 已通过，质量未判定，2026-08-14）

- 新增 `scripts/stage1_fetch_remote_tar_members.py`，对官方未压缩 tar 做带 checksum 的512字节 header 验证和按字典序的远程定位；拒绝覆盖、不删除文件。本轮38次 Range 请求共接收289,510,232字节，取得5/5目标成员，未下载10,312,816,640字节全包；
- 5个成员均通过 gzip/pickle 解析，具备 `xys`、`p3D_ids`、`xyz`；序列 PCL 帧数与官方元数据分别为279/279/279/264/257，逐一对齐，源帧可见稀疏点725–2442个且相机 Z 全为正；
- 新增 `scripts/stage1_flash3d_re10k_scale.py`，使用 Flash3D 第一层 UniDepth Z-depth、稀疏 COLMAP Z 和官方同口径的1000次 RANSAC，恢复尺度0.5291、1.0130、0.8901、1.2595、1.4961；最低内点率52.83%，正有限值与对应点硬门全部通过；
- 新增 `scripts/stage1_flash3d_to_clb.py`，显式分离第二层 depth/opacity/SH-DC，以 `RGB=clamp(0.5+C0×DC)` 转为sRGB数值，并将目标相机姿态转到源相机坐标系后仅缩放平移；5/5 CLB manifest 通过结构和语义校验；
- Flash3D opacity 仅作为 CLB alpha，没有当作置信度。模型无独立几何/外观置信度，所以两者全部设为0，记录状态为 `schema_pass_uncalibrated`；这会阻止原始块被误当作已接受融合。
- 诊断边界：5组第二层均有100%有效正深度增量，但 opacity 最小值可接近0；`1b87...` 的 SH→RGB 预裁剪越界分量达12.66%，显示原始外观不能仅凭 schema 通过就视为高质量补全。

**下一个单变量实验**：固定每个序列的源帧、网络输出和已恢复位姿，在左/右目标视角对比“只渲染第一层”与“渲染两层”；用真实目标 RGB 计算整图与第二层实际贡献区改善，再决定如何校准置信度。

### 24.10 Flash3D 第一层 vs 双层目标视角 A/B（部分协议指标已完成，2026-08-14）

- 新增 `scripts/stage1_flash3d_layer_ab.py`，固定同一源帧、同一 Flash3D 前向、同一 RANSAC 尺度和左右约±15°位姿；A 组只把第二层 opacity 设为0，B 组使用完整双层，其余渲染条件不变；
- 延用 Flash3D 官方的5%边界裁剪，计算真实目标 RGB 上的 PSNR/SSIM；另将双层与第一层最大 RGB 差超过 `1/255` 的像素记为“第二层实际作用区”并报告 MAE；该区只是渲染贡献诊断，不是独立的遮挡后真值掩码；
- 10个目标视角中，双层在8/10个提升 PSNR、9/10个提升 SSIM、9/10个降低作用区 MAE；平均 PSNR 变化 `+0.9378 dB`，中位数 `+1.0493 dB`，平均 SSIM 变化 `+0.05696`；
- 主要反例是 `1b87f55dec310243` 左端：PSNR `-2.4852 dB`、SSIM `-0.02430`、作用区 MAE 反而 `+0.01482`；`195fbef2c08715e9` 左端 PSNR 基本持平（`-0.0044 dB`），但 SSIM 与作用区 MAE 改善；
- 人工诊断预览与数值一致：双层常能填充第一层的条纹/空洞，但±15°仍存在明显的表面扭曲；`1b87...` 左端的第二层在右侧生成大块错误黑白结构。中心视锥之外的区域仍为背景色，这不是 Flash3D 第二层能解决的问题。

**当前结论**：“增加遮挡后第二层通常比单层更好”已获得受控 A/B 支持，但不稳定且在个别端点会显著恶化。记录状态为 `complete_metrics_partial_protocol`；在补齐 LPIPS、DISTS、独立遮挡后几何真值与置信度校准前，不宣称 `OcclusionHiddenProvider` 质量通过，也不应接入 P01。

**下一步**：先完成 `HLP-GEO-01` 的3个 Hypersim 稠密真值三元组，得到独立 `occlusion_hidden` 掩码和深度误差；再用这些误差校准几何/外观 confidence。LPIPS/DISTS 可与该轮真值评价一次补齐，不用 P01 调参。

### 24.11 `HLP-GEO-01` 稠密真值小集（v2 已通过，2026-08-14）

- 官方 test split 有46场景、78条相机轨迹。按字典序做姿态预检后，冻结 `ai_001_010` 27/61/65、`ai_005_001` 21/2/25、`ai_008_005` 43/17/72 三组 `cam_00` 源/左/右帧；六个端点角均在±15°附近，且预览确认具有明确共视与遮挡边界；
- `ai_003_007` 47/55/79 虽然角度误差很小，但中心楼梯与端点天花板共视不足，作为预览拒绝证据保留，没有为凑数加入正式集；
- 冻结配置为 `configs/stage1_4_hlp_geo_01_triplets_v1.json`。新增 `scripts/stage1_fetch_hypersim_triplets.py`，从三个官方远程 ZIP 只取姿态、场景尺度与9帧 RGB/depth/position；39文件共44,433,462字节，全部 HDF5 可读，没有下载约12.4 GB完整 ZIP；
- 新增 `scripts/stage1_hypersim_visibility_masks.py`，使用官方 `M_cam_from_uv`、world position、相机姿态和 `depth_meters`将目标像素分为源可对应、遮挡后、源视锥外、几何冲突和未解析；遮挡判定容差冻结为 `max(2 cm, 0.5% source depth)`；
- v1 的“position→depth P99绝对误差≤1 cm”在深度中位数约22.3 m、文件为 float16 的教堂场景失败，记录保留。实测相对 P99 仅0.175%–0.177%，因此 v2 改用相对 P99≤0.2% 门，六个视角全部通过；
- v2 共得到524,138个 `occlusion_hidden` 真值像素和1,122,337个 `outside_source_fov` 真值像素；六视角两类区域都非空。几何冲突最高3.62%，单独保留且排除于遮挡后真值。

**当前结论**：`HLP-GEO-01` 已从“数据集能力预审”进入“3场景稠密真值就绪”。下一个单变量任务是在这六个目标上评价 Flash3D 第二层的遮挡后几何/外观误差，并用独立误差校准 confidence；视锥外区只评为 unsupported，不归因给 Flash3D。

### 24.12 Flash3D 遮挡后稠密几何评价（原样未通过，2026-08-14）

- 新增 `scripts/stage1_flash3d_hypersim_geometry.py`，将 Hypersim 的“右/上/后”相机坐标显式转换为 OpenCV 的“右/下/前”，使用米制平移和由 `M_cam_from_uv` 推导的内参；Hypersim 径向距离在评价前转为 Z-depth；
- Flash3D rasterizer 的 depth 是未归一化的透明度加权和。脚本将第一层 opacity 置零，再以黑底渲染恒白第二层取得累积 alpha，用 `weighted_depth/alpha` 评价第二层；`alpha≥0.05` 只定义支持区，不当作 confidence；
- canonical 原始米制记录为 `records/stage1_4_flash3d_hypersim_geometry_v3.json`：6/6端点完成，遮挡后平均覆盖98.86%、最低94.71%，但支持区平均 AbsRel 23.33%、δ<1.05 12.99%、δ<1.10 24.80%；
- 源视角第一层本身平均 AbsRel 22.86%，三场景的预测/真值中位尺度比为1.284、0.775、0.905，证明 Hypersim 跨域尺度偏移是原始误差来源之一；
- 为分离尺度和结构误差，另保留 `records/stage1_4_flash3d_hypersim_geometry_v5_oracle_scale.json`：它使用源深度真值中位比缩放目标平移并换回米制，只是不可部署的 oracle 诊断。对齐后平均 AbsRel 仍为18.17%、δ<1.10仅39.50%；只有厨房左端达到82.5%，教堂与餐厅多数端点仍仅约21%–43%；
- 人工检查确认目标/预测方向正确，不是左右翻转或相机朝向错误。高 alpha 与几何正确率不稳定单调；例如厨房左端 alpha 0.9–1.0 区间的10%深度正确率仅约20%，因此不将 opacity/alpha 强行校准成 CLB confidence；
- v1 的 NumPy 参数错误与 v4 oracle 的 inference tensor 原地修改失败均已保留独立失败记录和部分输出，没有覆盖或删除。

**阶段结论**：Flash3D 第二层证明了“增加遮挡后表面常能改善外观”，但原样几何精度、稳定性和可校准置信度不足，不通过 `OcclusionHiddenProvider`。其代码、适配器和结果固化为参考基线，不接 P01，不继续针对 Flash3D 调参。

**下一步**：进入 Stage 1.5，先将 `OcclusionHiddenProvider` 接口、HLP-GEO/HLP-APP 评价器和 CLB 输出串成自研可替换骨架，然后实现可训练的遮挡后残差几何 Provider；Flash3D 只作对照。`OutsideFOVProvider` 继续作为独立分支，不与本轮混合。

## 25. 2026-08-14 Stage 1.5 自研 Provider 核心接口启动

### 25.1 第一版代码骨架

- 按仓库长期目录规则新建 `源码/Adaptive3DGS/`，不再把可复用核心逻辑继续堆在单次实验 `scripts/` 中；
- `CanonicalLayerPatch` 直接编码 Stage 1.3 冻结的 CLB：RGB、正向 Z-depth、Alpha、有效掩码、来源、几何/外观 confidence、内参和 world-to-camera 外参均为显式字段；
- `OcclusionHiddenProvider` 与 `OutsideFOVProvider` 是两个独立抽象接口，禁止一个实现用模糊的“hidden”结果混合两类能力；
- `ProviderRegistry` 负责显式注册与构造实现，拒绝同名覆盖；后续 Flash3D adapter、自研训练模型和消融实现可通过相同 ID/接口切换；
- `validate_patch/validate_result` 是融合与 Gaussian Spawn 之前的第一层硬门：检查数组形状和 dtype、有效深度、置信度范围、来源枚举、支持类型、重复 patch ID，并在部署模式拒绝 `synthetic_oracle`；
- 验证器特意独立检查 confidence，即使 Alpha 合法，confidence 为 NaN 仍必须失败，继续落实“Alpha/Opacity 不等于置信度”。

### 25.2 冒烟结果与边界

`PYTHONPATH=源码/Adaptive3DGS/src python -m unittest discover -s 源码/Adaptive3DGS/tests -v` 当前共运行6项测试并全部通过；v2 增加整幅 Alpha 合法性和 Provider 支持类型隔离测试，正式记录为 `records/stage1_5_core_interface_smoke_v2.json`，v1 保留为前一版证据。本轮不使用 GPU。

### 25.3 CLB I/O 与新旧验证器一致性

- 新增 `Camera` 与 `CanonicalLayerBundle`，使三相机表、bundle 级坐标约定和多个 surface patch 不再散落在脚本字典中；
- 新增 `adaptive3dgs.io.load_bundle/save_bundle`：读取兼容现有 `1.0-canonical-layer-bundle`，写出时为每个 NPZ 登记完整 SHA256；拒绝输出目录复用、绝对/越界资源路径、不安全 ID、重复 patch 和 patch/锚点相机矩阵不一致；
- 新增 `scripts/stage1_5_clb_io_roundtrip.py`。canonical 运行从 Stage 1.3 合成 bundle 读取3台相机、5个 patch、121,600个有效像素，经新核心库写出后重新读取；
- 61个相机、metadata、支持类型和七类数组逐元素精确门全部通过；每个写出 NPZ 的记录哈希匹配，旧 `stage1_validate_clb.py` 对新 manifest 同样返回 pass。正式记录为 `records/stage1_5_clb_io_roundtrip_v1.json`；
- 首次手工预跑 v1 目录也通过，但 canonical 可复现脚本使用全新 v2 输出目录；两者都在忽略的 outputs 中，没有覆盖或删除。

### 25.4 统一数组级几何与置信度 evaluator

- 新增 `GeometryTarget`、`GeometryPrediction` 与 `evaluate_occlusion_geometry`，模型只需提供目标 Z-depth、support probability 和独立 geometry confidence；评价器不依赖 Flash3D；
- 输出遮挡后支持覆盖率、AbsRel、MAE、RMSE、δ<1.05、δ<1.10、明显过近/过远率；视锥外真值只计数并显式排除，不混入 `OcclusionHiddenProvider`；
- confidence 以“深度是否在10%内”为 correctness 计算 ECE，并按 confidence 从高到低计算 AbsRel selective AURC；support probability 只决定是否有预测，不冒充 confidence；
- 合成完美预测、全覆盖错误预测与 shape mismatch 三项 evaluator 测试通过。首次使用 NumPy 2.x 的 `trapezoid` API 在当前 NumPy 1.x 环境失败，未形成正式记录；已改为兼容 NumPy≥1.24 的离散累计风险均值；
- 当前核心测试总数增至9项，正式记录为 `records/stage1_5_core_interface_smoke_v3.json`。

### 25.5 HLP-GEO 数据集与相机适配层

- 新增 `adaptive3dgs.datasets.hypersim`，将已冻结的 `HLP-GEO-01` 配置、Hypersim HDF5 图像/距离、相机轨迹、场景尺度和 v2 可见性真值统一加载为 `HypersimSample`、`ProviderContext` 与 `HypersimTargetView`；
- 适配器按 Hypersim 官方相机约定把 right/up/back 转为 OpenCV right/down/forward，从 `M_cam_from_uv` 构造原生1024×768内参，并把径向 metric distance 转为正向 Z-depth；RGB 使用官方 percentile/gamma tone mapping，不在读入阶段缩放或裁剪；
- 防止真值泄漏是默认硬边界：`include_source_depth=False` 时，送入 Provider 的 `ProviderContext.depth_z` 必须为 `None`，元数据同时记录 source truth 未暴露。目标深度和可见性掩码仅保存在 evaluator 侧；
- 真实数据冒烟覆盖3个场景、6个目标，复现524,138个 `occlusion_hidden` 像素和1,122,337个 `outside_source_fov` 像素，与冻结 v2 记录完全一致；相机中心平移量相对配置的最大误差约 `9.77e-15 m`；60项输入、姿态、掩码、统计和泄漏边界门全部通过；
- 冒烟脚本的前两次预跑分别暴露了旧记录键名误用和 `numpy.bool_` JSON 序列化问题，均在正式记录产生前修正，没有伪造失败记录。canonical 证据为 `records/stage1_5_hlp_geo_dataset_adapter_smoke_v1.json`；
- 新增3项相机内参、径向距离转 Z-depth 和 tone mapping 单元测试，核心测试总数增至12项；当前 canonical 核心记录为 `records/stage1_5_core_interface_smoke_v4.json`，v3 保留为前一版证据。

### 25.6 Flash3D CPU 只读参考映射层

- 新增 `adaptive3dgs.adapters.flash3d`，核心包不导入 Flash3D 仓库、PyTorch 或 checkpoint；外部仓库后端只负责原样推理并交付 NumPy 数组，因此对照模型仍保持只读；
- `Flash3DReadOnlyAdapter` 将第一/第二层正向 Z-depth、第二层 opacity 和 SH DC 特征映射为一个 `occlusion_hidden` CLB patch；只有第二层比第一层更深且所有字段有限的像素才有效，视锥外明确列为 unsupported；
- Flash3D opacity 只保留为 Alpha/支持概率，两类 confidence 固定为0并标记 `uncalibrated_zero_not_accepted_for_fusion`；Alpha 越界直接失败，不作静默裁剪或伪校准；
- `flash3d_render_to_geometry_prediction` 按 Stage 1.4 冻结口径将目标渲染的 opacity-weighted Z 除以独立累积 Alpha，并可应用显式模型单位到米制尺度；输出仍保持零 geometry confidence；
- 新增第二层 CLB 映射、目标深度归一化/Alpha语义隔离、Alpha 越界拒绝3项测试。核心测试增至15项，正式记录为 `records/stage1_5_core_interface_smoke_v5.json`；当前只证明 CPU 映射契约，尚未声称通过新适配器复现 GPU 冻结指标。

### 25.7 Flash3D 仓库后端与冻结指标回归

- 在 `scripts/stage1_flash3d_hypersim_geometry.py` 中接入 Stage 1.5 核心接口；官方 Flash3D/UniDepth checkpoint、模型推理和原生 Gaussian renderer 保持不变，仓库后端只把输出交给只读 adapter 与统一 evaluator；
- 启动前检查四卡占用。GPU 1 有未知 `python3` 进程占用约6.2 GB，因此不抢占、不终止；本轮使用有足够余量的物理 GPU 2，峰值显存2,560,214,528字节；
- 3个场景均写出一个可由新 loader 重新读取的 CLB，每个1个 patch、98,304个有效隐藏层像素；两类 confidence 全0，`outside_source_fov_supported=false`，且送入后端的源深度真值为 `None`；
- 统一 evaluator 在6个目标上复现冻结 v3：平均隐藏区覆盖率 `0.9886012228320182`、平均 AbsRel `0.23331674487628928`，与 v3 汇总完全相同；逐视角覆盖率差值全为0，AbsRel 最大绝对差约 `6.43e-10`，远小于运行前冻结的 `5e-5` 容差；
- 正式记录为 `records/stage1_5_flash3d_reference_regression_v1.json`，包含 checkpoint、冻结记录、执行脚本、适配器、evaluator 和三个 CLB manifest 的 SHA256。状态为 `pass_reference_regression`；这只证明统一链路忠实，不改变 Flash3D 几何质量未通过的结论。

### 25.8 Stage 1.6 独立训练数据与隐藏层正证据

- 新增 `Stage1_6_自研可训练OcclusionHiddenProvider协议.md` 与候选配置。官方 split 审计确认365 train、46 val、46 test；HLP-GEO-01三个场景均为 test，禁止参与训练、早停或调参；
- 首批选择8 train、2 val共10种场景/相机用于标签管线冒烟。40个姿态文件定向预取通过后，按真实相机旋转选出10个总范围30°三元组；每个场景有68–93个合格源帧，所选角度误差合计最大约0.193°；
- 定向取得130/130个 RGB/depth/position/pose 文件，共136,035,863字节，没有下载完整场景 ZIP；train/val 与三个 held-out test 场景交集为0；
- 新增 `adaptive3dgs.supervision.build_source_hidden_evidence`：目标 RGB-D 反投影到世界并投回源相机，只保留同时深于源 first hit 1 cm与1%的最近后方表面；无候选像素保持 unknown，不伪造负标签；
- 首轮真实运行因验证场景 `ai_031_010` 的倾斜传感器/非对角 `M_cam_from_uv` 在数组写出前失败。未删除失败目录、未替换场景；核心现用 RQ 将一般投影分解为 K 与相机旋转修正，并支持带 skew 的一般 K；
- canonical v2 生成912,897个正证据像素，单场景最小占比1.6127%、中位12.1352%；10个 NPZ 哈希与数组语义复核通过，50/50真实标签门通过；
- 核心测试增至20项，v6记录为 `records/stage1_6_core_supervision_smoke_v6.json`；真实记录为 `records/stage1_6_hlp_train_01_hidden_evidence_v2.json`。下一步只做一个 train 样本的正深度增量、支持概率、几何置信度三头过拟合，不扩大数据规模。

### 25.9 Stage 1.6 冻结 RGB-only BaseDepthProvider 输入

- 新增 `scripts/stage1_6_freeze_unidepth_base.py`，把 UniDepth V1 ViT-L/14 固定为可替换的 BaseDepthProvider；代码级允许输入键只有 `source_rgb_uint8`，不读取源真值深度，也不传 Hypersim 真值内参；
- 第一次使用离线 `from_pretrained` 时因缓存缺少配置文件在模型构造前失败，0个数据数组被访问、0个文件写出；失败目录与 `records/stage1_6_hlp_train_01_frozen_base_depth_v1_failed.json` 保留；
- canonical v2 直接使用仓库冻结配置与已校验的同一权重。运行前检查 GPU，物理 GPU 1 只有约311 MiB常驻占用且无其他重计算，GPU 2 有约7.3 GiB既有任务，因此使用 GPU 1且没有终止任何进程；
- 8 train / 2 val 共10份768×1024可见 Z-depth 全部有限、严格为正；峰值分配显存2,284,020,736字节，推理段耗时6.14秒；10个NPZ和10张预览共20/20哈希复核通过；
- 正式记录为 `records/stage1_6_hlp_train_01_frozen_base_depth_v2.json`。这一步完成“冻结且无源深度真值泄漏的模型输入”，不构成隐藏层几何质量结论。

### 25.10 Stage 1.6 三头网络与正例/PU 损失契约

- 冻结深度诊断发现，UniDepth 在部分 Hypersim 场景有明显跨域尺度偏移；直接用 `hidden_truth-base_pred` 训练会把大量可靠隐藏正例变成负增量，无法同时满足“部署隐藏层严格在基础层后方”；
- 几何头因此冻结为尺度不变的正相对增量：标签是 `hidden_truth/source_truth-1`，部署结果是 `base_pred×(1+positive_ratio)`。源真值仅在 loss 侧生成比例标签，不进入 RGB/base 模型输入；
- 新增小型 U-Net 三个独立输出头：正增量比例、support probability、geometry uncertainty/confidence。support 使用 non-negative PU risk，unknown 不是逐像素负标签；
- 几何与 uncertainty loss 只读取正证据位置。单元测试把 unknown 真值替换为 NaN/任意负数后损失逐值不变；
- 核心测试由20项增至24项且全部通过，正式记录为 `records/stage1_6_core_trainable_model_smoke_v7.json`。当前只通过实现契约，尚未通过单样本优化门。

### 25.11 Stage 1.6 单样本过拟合门通过

- v1 的确定性 CUDA median 实现门、v2 的 `log(1+r)` 小比例欠权重、v3 的恒定学习率末段回摆均保留为独立失败证据，没有把接近门槛写成通过；
- canonical v4 仍只使用 `ai_001_001` train 样本，750步余弦学习率；相对隐藏比例 AbsRel从3.43988降至0.08091，下降97.65%，低于预设0.10门；
- 正例 support 均值0.84581，unknown均值0.02012；confidence均值0.92120、标准差0.10643。严格后方、有限值、confidence范围等6/6门全部通过；
- GPU 1峰值分配显存约405.2 MB、耗时36.01秒；checkpoint成功重载，checkpoint/数组/曲线/4张预览共7项哈希复核通过；
- 正式记录为 `records/stage1_6_hlp_train_01_single_overfit_v4.json`；直接 `log(r)` 小比例梯度测试加入后核心25/25项通过，记录为 `records/stage1_6_core_trainable_model_smoke_v8.json`。

**当前边界**：只证明可学习与可记忆，不证明跨场景泛化。下一阶段才扩大到8 train/2 val，冻结候选后才运行 HLP-GEO test。

### 25.12 Stage 1.6 首个8/2联合候选未通过

- `configs/stage1_6_hlp_train_01_small_training_v1.json` 在首次查看2个val场景前冻结150 epoch、组合选择分数与6项门；运行未读取HLP-GEO test或P01；
- 最佳epoch 5把val比例AbsRel从2.2998降到1.3452、selection score改善37.13%，但val support正例与unknown均值只差0.0076，未达到0.05门；train比例误差改善47.68%，也略低于冻结50%门；
- 训练后期train support分离可到约0.247，而val仍约0.004，说明8张训练图主要在记忆场景，不能称为跨场景Provider；
- `records/stage1_6_hlp_train_01_small_candidate_v1.json` 正式状态为failed。没有降低门槛，也没有用test结果反向挑checkpoint。

**下一步**：在相同且严格隔离的8个train/2个val场景内扩展多源三元组；先增加样本覆盖，再讨论模型容量或HLP-GEO test。

### 25.13 HLP-TRAIN-02 扩展为80 train / 20 val

- 保持原8个train/2个val场景不变，每场景按真实姿态选10个唯一源帧；100/100组三元组通过±15°端点误差、最小平移和held-out零交集门；
- 获取器按场景合并并对远程ZIP成员去重，100组三元组最终对应769个唯一文件，而非重复写入1300项；
- 定向获取769/769文件、1,093,518,150字节，全部HDF5可读；没有下载10个合计约46.4 GB的完整场景ZIP；
- 正式证据为 `records/stage1_6_hlp_train_02_pose_selection_v1.json` 与 `records/stage1_6_hlp_train_02_fetch_v1.json`。当前只完成姿态和文件完整性，不包含新标签或模型结论。

### 25.14 HLP-TRAIN-02 标签与冻结BaseDepth输入

- 100组标签v1/v2只因8,721,745个正像素中的1个float32边界像素失败；未加容差，而是在生成端按最终落盘精度重做严格1%/1cm门，该像素恢复为unknown；
- canonical v3得到8,721,744个正证据像素，最小/中位样本占比0.11317%/7.27832%，500/500门通过，100个NPZ哈希复核通过；
- GPU 1上生成100份RGB-only UniDepth冻结输入，全部有限、为正；100个NPZ和100张预览共200项哈希通过；
- 正式记录为 `records/stage1_6_hlp_train_02_hidden_evidence_v3.json`、`records/stage1_6_hlp_train_02_frozen_base_depth_v1.json`；量化边界测试加入后核心26/26项通过，记录为 `records/stage1_6_core_quantized_supervision_smoke_v9.json`。

### 25.15 Support 从不可辨识PU转向目标视角free-space证据

- 80/20 candidate v1的nnPU support在val坍缩为正例/unknown约0.096/0.096；深度/RGB边缘对val正证据的只读AUC仅约0.494/0.544，不支持用边缘启发式替代；
- 新增目标视角free-space负证据：预测隐藏点若位于目标first-hit前方的可观测自由空间才是负例；目标first-hit后方继续unknown，不把未观测区伪造成负标签；
- candidate v4虽生成约11.7万val负例，但协议漏掉内参输入且感受野不足；candidate v5补齐相机射线、/16上下文与几何条件头后，batch=1联合训练仍不稳定，两者均正式failed且未运行test；
- train-only单样本free-space过拟合通过：比例AbsRel降至0.0913，support正例/verified-negative达到0.8652/0.1349，分离0.7304，证明标签、投影与模型具备可学习性；
- 下一配置只修正联合优化：4样本梯度平均、1e-3学习率、support权重1.0；这些值来自train-only门，val选择规则与通过门保持不变。

### 25.16 HLP-TRAIN-02 多源但少场景的上限

- candidate v6 用4样本梯度平均后，train support分离可缓慢升到约0.023，说明顺序遗忘有所缓解；但20个val样本仍约0；
- 最佳val比例AbsRel 1.3881，相对初始化2.2446有信号，但val support正例/verified-negative为0.5235/0.5333，方向仍错误；
- 从8张扩到80张时，每个场景从1源帧增至10源帧，独立train场景数仍为8；结果证明帧内扩增不能替代场景多样性；
- `records/stage1_6_hlp_train_02_candidate_v6.json` 保持failed，未运行HLP-GEO test。下一步改建多场景HLP-TRAIN-03。

### 25.17 HLP-TRAIN-03 多场景姿态冻结

- 元数据层先按场景类型冻结60 train / 20 val候选，覆盖22类train场景；80场景唯一、公开帧≥80、test交集0；
- 320/320个姿态/尺度小文件定向获取通过，79/80场景找到合格±15°三元组；唯一失败候选按原样保留；
- 在RGB-D获取前，从59/20合格池再次分层冻结40 train / 10 val，每场景只取1组三元组；train覆盖22类、val覆盖10类；
- 正式配置 `configs/stage1_6_hlp_train_03_triplets_v1.json` 已通过计数、场景唯一、held-out零交集与姿态门。当前还没有下载这50组RGB-D。

### 25.18 HLP-TRAIN-03 训练输入就绪

- 50场景650/650个精确RGB-D/position/pose文件完成，701,705,049字节全部可读且未下载完整ZIP；
- 生成3,388,680个正证据像素，最小/中位样本占比0.40487%/6.78482%，250/250严格门通过；
- 50份RGB-only UniDepth冻结输入全部有限且为正；GPU 1峰值分配显存约2.285 GB；
- 训练配置冻结为40 train场景/10 val场景、batch4、1000次optimizer更新，与HLP-TRAIN-02 v6保持相同更新预算，主变量明确为场景多样性。

### 25.19 HLP-TRAIN-03 候选未通过，Stage 1.6 正式收口

- 物理 GPU 3 按冻结40 train/10 val配置完成1000次更新，峰值分配显存1,018,327,552字节、运行258.69秒；最佳 epoch 为20，checkpoint和history哈希已登记；
- val ratio AbsRel `3.8958→2.3451`，selection score改善37.40%；train ratio AbsRel改善67.22%，两项几何学习门通过；
- 最关键的 val support 正例/verified-negative 为 `0.55777/0.53573`，分离只有`0.02204`，未过冻结0.05门；train分离也只有`0.02456`。这不是单纯增加训练场景即可解决的泛化结果；
- `records/stage1_6_hlp_train_03_candidate_v1.json` 正式状态为 `failed`。遵守协议，未读取 HLP-GEO-01 test、未接 P01、未启动隐藏外观或 Gaussian Spawn；
- 30/30核心实现测试已重跑并冻结为 `records/stage1_6_core_trainable_model_smoke_v10.json`。

**阶段决定**：停止继续在同一 source-only小型三头网络上调优化器。下一步验证会改变信息结构，先审计官方3D Photo的完整“LDI连通关系→局部边缘→深度→颜色→多层渲染”链路；它与已失败的固定端点三网络适配不是同一个实验。若完整链路能以 P01 和统一30°轨迹产生候选，再进入人工评阅；否则转向显式目标视角条件化 Provider。

当前完成的是核心接口、I/O、统一评价、真实数据加载和参考模型回归；尚不包含训练网络、SurfaceFusion 或任何自研 Provider 质量结论。下一实现顺序固定为：

1. ✅ CLB manifest/NPZ I/O 与新旧验证器一致性已完成；
2. ✅ 数组级 evaluator 与 HLP-GEO-01 dataset/相机适配层已完成，且默认阻断源深度真值泄漏；
3. ✅ Flash3D 仓库推理/原生渲染后端已接入，统一 evaluator 已逐视角复现冻结 v3 指标，没有重新调参；
4. ✅ 三头网络、free-space监督与单样本可学习性已验证；❌ 8场景多帧及40 train/10 val场景多样化候选均未通过独立val support门，Stage 1.6已收口；
5. 自研候选在 HLP-GEO/HLP-APP 通过前，不接 P01；`OutsideFOVProvider` 另立数据与实现任务。

### 25.20 Stage 1.7 官方完整 LDI 统一30°候选（等待人工评阅）

- 新增 `scripts/stage1_7_run_official_ldi_true_arc.py`，官方3D Photo源码保持 commit `de044674...` 且工作树不修改；兼容层只在项目包装脚本和独立 `inpaint-3dphoto` 环境；
- 与旧固定端点adapter不同，本轮完整执行LDI连通建图、深度断边、局部边缘/深度/颜色补全、第二轮分层补全、PLY导出和mesh渲染；深度继续用冻结P01中心深度，焦距显式为3195.073 px；
- v1/v2分别保留MoviePy旧依赖缺失和官方整数`gpu_ids`契约失败；v3首次完整生成LDI mesh但圆弧半径/帧数不合协议；canonical v4复用同SHA256 mesh，修正为半径5.203962802886963和精确61帧；
- v4视频为H.264、yuv420p、768×576、30 fps、无裁剪。mesh 99,121,190字节，视频262,409字节，哈希见 `records/stage1_7_official_ldi_true_arc_v4.json`；
- 自动灰背景诊断：左端27.81%、中心0.51%、右端26.81%。三联图可见端点大面积灰色楔形未覆盖以及树木/栏杆拉伸，但最终通过/不通过仍由用户完整播放判定；
- 正式人工门和一次性评分项见 `Stage1_7_完整LDI候选协议.md`。当前到达人工评阅边界，收到评分前停止扩展样本和方法调参。

### 25.21 Stage 1.7 用户人工评分与正式结论

- 用户完整评阅后给出：左端空洞严重度3、右端空洞严重度3、拉伸/虚假结构严重度2、运动连续性尚可、总体不通过；
- 四项冻结门中，仅运动连续性通过；两端空洞门与结构门均失败，因此不因时间连续而放宽总体质量门；
- 该结果与自动灰背景占比27.81%/26.81%相互印证：完整LDI补出了部分连续内容，但中心视锥外覆盖和大角度结构质量仍不够；
- 正式记录为 `records/stage1_7_official_ldi_true_arc_v4_manual_review.json`。停止3D Photo参数搜索、裁剪/改底色掩盖、P02/P05扩展和补充高斯转换；
- 下一阶段改变条件变量：目标视角条件化Provider直接预测指定相机的新显露内容，同时单独设计`OutsideFOVProvider`。完整LDI保留为结构化失败基线。

### 25.22 Stage 1.8-0 目标视角分区与 evaluator 校准通过

- 新增`Stage1_8_目标视角条件化与OutsideFOV协议.md`，把目标画面冻结为`observed_from_source`、`occlusion_hidden`与`outside_source_fov`三个互斥区域；后两类必须分别报告RGB、深度与覆盖率，禁止用平均值掩盖单分支失败；
- Adaptive3DGS新增`TargetViewPrediction`和目标视角RGB-D evaluator，保留support、geometry confidence和appearance confidence的独立语义；核心测试由30项增至32项且全部通过；
- oracle配置v1/v2均因所选val左端没有视锥外真值按原门失败，结果和输出保留；没有事后降低门槛。对10个冻结val场景只读审计后，v3选择第一个左右端均具备视锥外真值的`ai_024_012`，train仍为`ai_024_010`；
- v3可见性记录`records/stage1_8_target_view_visibility_v3.json`通过：2场景、4端点，累计遮挡后191,988像素、视锥外728,090像素，分类与两类非空门全部通过；
- canonical `records/stage1_8_target_view_oracle_smoke_v5.json`通过31/31门：两类新显露区oracle覆盖率1、深度AbsRel 0、RGB MAE 0。runner、evaluator、配置、相机参数和visibility记录SHA256均已登记；
- 本轮无模型训练、无GPU任务，ProviderContext不含源深度真值；目标RGB-D只作为oracle和评价真值。该pass只校准评价工具，不构成目标条件模型质量结论。

**下一步**：新增显式目标相机请求接口与最小target-space RGB-D模型，固定`ai_024_010`单个端点做过拟合门。只有输入泄漏检查、相机条件敏感性、两类新显露区学习和数值门通过后，才扩大到HLP-TRAIN-03 train/val。

### 25.23 Stage 1.8-1 显式目标相机条件单端点过拟合通过

- 新增`TargetViewRequest`、确定性源→目标Z-buffer重投影、目标射线/相机原点条件和`TargetViewUNet`；模型在目标空间同时输出RGB、Z-depth、遮挡后support、视锥外support及独立几何/外观confidence；
- 核心测试从32项增至37项并全部通过，覆盖恒等重投影、目标姿态变化、双独立support头、未分类真值隔离和旧功能回归；
- v1固定`ai_024_010`左端、384×512、1200步。最终绝对质量和10项其他门均通过，但遮挡后/视锥外RGB相对改善89.26%/88.17%，未达到冻结90%，正式记录为failed，没有下调门槛；
- v2唯一改变变量是优化步数1200→2000；同一种子重新初始化训练。遮挡后RGB MAE`29.39→2.15`、视锥外`49.09→3.88`，相对改善约92.69%/92.09%；深度AbsRel为0.00328/0.00238；
- 两分支正support为0.9888/0.9953，负区support为0.0183/0.0108；相反目标相机预测差异门、有限值门和全部12项冻结门通过；
- 物理GPU 1运行约132秒，峰值分配显存约0.51GB。目标RGB-D和掩码只用于loss/evaluation，held-out test未读取；正式记录为`records/stage1_8_target_view_single_overfit_v2.json`。

**阶段结论**：显式目标相机的信息结构具备学习指定新视角RGB-D和区分两类新显露支持的能力。当前仍只是单样本记忆，不能宣称跨场景Provider成立。下一阶段保持模型结构和数据划分，生成HLP-TRAIN-03全部100个端点的三分区真值，再运行独立train/val候选。

### 25.24 Stage 1.8-2 全量资格池与首个联合候选未通过

- 对HLP-TRAIN-03全部50场景、100端点生成三分区真值，共8,222,859个遮挡后像素、25,688,741个视锥外像素；初始记录因13端点未过原0.2% depth-position一致性门而保留为fail；
- 新增逐端点资格审计，不放宽0.2%门；同时要求分类完整、两类新显露均非空并逐文件复核SHA256。最终78端点严格合格，含64 train/14 val，22个拒绝端点及原因全部登记；
- `stage1_8_target_view_candidate_v1`固定TargetViewUNet、384×512、batch4、2000 updates和val-only选择，物理GPU 1运行约762.8秒，峰值分配显存约1.46GB；最佳checkpoint为update 800；
- val两类RGB相对空洞式重投影基线均改善，support正负分离0.147/0.634也通过；说明目标相机条件和区域判断产生了跨场景信号；
- 绝对质量未通过：val遮挡后/视锥外RGB MAE 43.46/59.43（门≤35），深度AbsRel 0.593/1.074（门≤0.30）；train RGB 48.75/45.49、深度0.284/0.628，显示小型从零目标解码器本身也未充分拟合多场景内容；
- 11项冻结门中7项通过、4项失败，记录`records/stage1_8_target_view_candidate_v1.json`状态为failed。未读取HLP-GEO/HLP-APP test、P01或人工结果。

**阶段结论**：显式条件已经解决“目标在哪里”和“两类区域在哪里”的一部分问题，但64个训练目标不足以让小型从零U-Net生成跨场景新内容和可靠米制深度。停止仅增加优化步数；下一步先做逐目标深度尺度oracle诊断和冻结预训练先验审计，再冻结下一结构候选。

### 25.25 深度尺度诊断完成，预训练骨干冻结为 GenWarp multi2

- 对14个val目标逐目标做不可部署正尺度oracle后，遮挡后/视锥外深度AbsRel只从0.593/1.074降至0.441/0.365，仍未达到0.30；尺度范围0.219至7.942，说明局部几何与内容错误不可由单一尺度校准解决；
- 本机没有可直接复用的MAT、LaMa或3D Photo补全权重；现有Flash3D/InfiniSplat均已有独立负结果，不重复作为新骨干；
- 官方公开候选审计后，首选GenWarp `multi2`：单图、单目标、显式相机矩阵接口与当前val端点协议直接匹配，还输出warp/mask/correspondence。ViewCrafter 512版保留为后备，Stable Virtual Camera受门控和非商业限制，MetaView依赖栈过大；
- GenWarp最小下载约8.43GB，代码MIT、权重CC-BY-NC 4.0。因官方没有16GB显存承诺，先做单个合格val目标的隔离环境冒烟；通过前不批跑val、不读取held-out test或P01。

正式审计记录：`records/stage1_8_pretrained_backbone_audit_v1.json`。下一任务是完成隔离安装与单目标显存/输出方向冒烟，随后才决定是否冻结14目标自动评价。

### 25.26 GenWarp 安装与权重完成，按要求暂停

- GenWarp官方源码固定`22c03b6...`，独立`genwarp`环境已建立；PyTorch 2.7.1+cu128与环境内nvcc 12.8.93支持RTX 5080；
- `splatting`官方依赖在新PyTorch上因两处旧`frame.type()`接口编译失败，经最小`scalar_type()`兼容补丁后面向`sm_120`编译通过，关键模块静态导入通过；
- `multi2`、图像编码器与VAE共8个文件、8,429,409,786字节已下载，逐文件SHA256校验完成；源码、环境和权重均保持本机，不进入Git；
- 本阶段没有模型加载和GPU推理，也没有读取held-out test/P01。按用户要求暂停，明日从“先检查GPU，再跑单个合格val目标的显存/方向/有限值冒烟”继续。
- 阶段结束后已更新`linux5080`目录快照：1,295个目录、5,231个文件、1个符号链接，扫描根内普通文件共14,886,197,921字节；与现有`win5060`快照共有1,915条路径，服务器独有4,612、5060独有1,234，类型不一致0、同路径文件大小不一致66。差异只用于换机对照，未自动复制、删除或强制整理任何产物。

正式环境记录：`records/stage1_8_genwarp_environment_v1.json`。
