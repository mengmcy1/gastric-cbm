# SAE 实验归档（2026-07-21）

当前锁定的正式 SAE 不在本归档中，继续保留于上级目录：

`formal_resnet50_sae_corrected_l1_000075_pruning_v5_20260721`

锁定配置为 ResNet50、隐藏维度 512、L1=7.5e-5、best epoch=969，训练和验证完成后保留 511/512 个 feature。测试集尚未用于 SAE 训练或参数选择。

归档分组：

- `早期实现_不可与修正后比较`：损失尺度等实现修正前的探索结果，仅保留过程记录，指标不可与正式扫描直接比较。
- `L1参数扫描`：修正实现后的开发集参数扫描。仅使用训练集训练、验证集选择。
- `流程验证`：小 demo 和冻结 SAE 验证集投影检查，不作为正式结果。

各实验关键指标见 `实验索引.csv`。所有原始 checkpoint、缓存、表格和概览均原样保留，未删除或覆盖。
