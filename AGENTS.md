# 项目协作说明

## 文件安全规则

禁止批量删除文件或目录。

不得使用以下命令：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

需要删除文件时，只能一次删除一个明确路径的文件，例如：

`Remove-Item "C:\path\to\file.txt"`

如果需要批量删除文件，应停止操作，并请求用户手动处理。

## 文件生成与目录整理规则

后续创建、下载或整理本项目相关文件时，默认都应放在当前项目目录下：

`C:\Users\MCY\Desktop\2D转3D`

除非用户明确指定其他位置，不要把交付文件、论文、脚本或缓存散落到桌面、下载目录、临时目录或其他无关文件夹。

建议保持根目录整洁：

1. 最终交付文档、老师提供的 PPT/图片、论文文件夹、项目说明文件可以保留在根目录。
2. 过程脚本、下载报告、渲染缓存、临时检查文件等，应统一放入：

   `99_过程文件_可删除或归档`

3. 如果后续再次生成过程文件，应按用途放入该文件夹下的子目录，例如：

   - `生成脚本`
   - `下载与检查报告`
   - `渲染检查缓存`
   - `临时文件`

4. 如果需要创建新的论文、资料或数据文件夹，应使用清晰编号和中文说明，例如：

   - `papers`
   - `01_文献调研`
   - `02_方案设计`
   - `03_实验记录`

5. 整理目录时，优先采用移动/归档，不要随意删除文件。确需删除时，必须遵守“文件安全规则”。

## 项目背景

**课题名称**：单张 2D 图像转浅 3D 表达 / 单图新视角合成

**核心目标**：输入一张普通 2D 照片，生成一个”浅 3D 表达”——在移动端上用户小幅晃动手机或滑动屏幕，画面产生 3D 立体感和视角变化效果（类似 Apple 3D 照片 / 微信透视窗）。

- **”浅 3D”的含义**：不是完整的 360° 自由旋转重建，而是几度到十几度的视角晃动范围，有深度层次感和运动视差即可。
- **完整管线**：单张图片 → 深度图 → 三维点云 / 分层表示 → 从略有不同的新位置观察。
- **最终目标**：移动端部署，渲染不依赖 GPU 网络推理。
- **输入约束**：只有一张普通照片，没有多视图、没有相机位姿。

**课题要求图片出处**：`课题要求.png`（根目录）

## 用户背景与沟通要求

用户刚开始接触”单张 2D 图像转浅 3D 表达、单图新视角合成”方向，对计算机视觉、深度学习、三维视觉和神经渲染中的部分概念尚不熟悉。

协助用户时应遵循以下要求：

1. 首次出现专业术语时，先给出通俗解释，再说明它在本项目中的作用。
2. 对复杂概念可以使用比喻、示意流程或简单例子辅助解释。
3. 不要默认用户已经了解深度估计、相机模型、MPI、NeRF、3DGS、视角合成等技术。
4. 技术路线应从基础到进阶逐层梳理，并说明每种方法解决什么问题、输入输出、基本原理、优缺点及其与本课题的关系。
5. 使用缩写时，首次出现应同时给出中文名称、英文全称和缩写。
6. 给出论文时，应尽量说明论文的重要程度、阅读难度、建议阅读章节和开源情况。
7. 不应只罗列论文或技术名词，应帮助用户建立知识之间的联系。

## 文档写入审批规则

创建或修改 Markdown、Word、PDF、PPT、Excel 等文档之前，必须先把拟写入的正文或内容提纲提交给用户审阅。

只有在用户明确表示“确认”“可以写入”或作出同等含义的授权后，才能实际创建或修改文件。

如果用户提出修改意见，应先更新草稿并再次提交审阅；未经确认，不得提前写入。

代码文件、配置文件是否需要提前审阅，应根据用户当次要求执行；上述规则默认主要约束说明性文档和交付文档。

## 当前阶段优先级（导师 2025.7.10 指示）

### 当务之急（最高优先级）

**复现 SHARP 论文（Apple，09_单图与前馈3DGS）**：

1. 精读 SHARP 论文，理解其方法
2. 找到并复现其开源代码
3. 在自己环境上跑通，看最终结果
4. 根据结果的问题进行分析
5. 尝试改进

SHARP（Sharp Monocular View Synthesis）是 Apple 发表的单图新视角合成工作，使用前馈式方法从单张图片直接预测 3D 表示。

### 次要重点

关注 3DGS 相关的工作（分类 04/05/06），而非 LDI/MPI 分层表达路线。

### 已调整

- 原计划继续读 02 分类（SLIDE/TMPI/MINE）→ **暂停**，转向 SHARP
- 03 分类（深度估计基础模型）可作为 SHARP 复现阶段的参考阅读

### 0. 总体流程（每篇论文开始前）

在开始逐段带读之前，先给出论文的**整体概览**：

1. **整体脉络梳理**：论文解决什么问题 → 核心方法思路 → 输入输出链路。用流程图或分阶段方式呈现。
2. **核心技术创新总结**：列出 3-5 个关键创新点，每个一句话说清"是什么"和"和什么对比"。
3. **重点阅读章节**：标明优先级（⭐⭐⭐/⭐⭐/⭐），说明每章为什么重要。
4. **与本课题的关系**：该论文的方法与本项目目标的匹配度和差异。

用户确认脉络清晰后再进入逐段带读。

### 1. 逐段带读三步法

1. **原文翻译**：先将当前段落的英文原文尽可能准确地翻译为中文。翻译应忠实于原文，包括专业术语的准确对应，不增不减。
2. **大意梳理**：用通俗语言解释这段话在说什么，它在整篇论文中的作用是什么。
3. **概念拆解**：对段落中出现的专业概念做通俗比喻和解释，帮助建立直觉。

**递进逻辑**：翻译（懂了字面意思）→ 梳理（懂了它在说什么）→ 拆解（真懂了、能迁移）。

当用户表示希望从翻译直接跳到代码或深挖时，可以跳过后续步骤。

### 2. 首次出现的专业术语

在论文带读中首次出现专业术语时，必须先给出通俗解释，再说明在本文中的作用。包含但不限于：LDI、MPI、Mesh、深度估计、去遮挡（disocclusion）、运动视差（motion parallax）等。

### 3. 重点精读与论文总结更新规则

后续精读 03-06 号论文时，不做全文逐段带读，而是优先选择与本课题最相关的重点段落进行精读，包括：摘要、方法总览、核心模块、关键公式、实验结论、失败案例和与移动端浅 3D 表达直接相关的讨论。

阅读过程中，若发现可直接借鉴到课题论文、开题报告、技术路线或汇报中的内容，应及时整理为“可写入论文/报告的表述”。实际写入论文总结文件前，先将拟写入内容提交给用户审阅；用户确认后再写入。

后续阅读 04 SLIDE、05 TMPI、06 MINE 以及其他相关论文时，也遵循同样规则：重点精读、及时提炼、先审阅后写入。

## 论文总结规则

不再单独维护"论文带读笔记"。带读过程只做口头讲解，**读完整篇论文后**再把重点内容一次性汇总到统一的论文总结文档，写得详细一些，便于后续复习。

总结文档路径：`论文总结.docx`，存放在项目根目录。每读完一篇追加一节，不新建文件。

每篇论文的总结按以下**统一结构**组织（对应文档中的 N.1 ~ N.8）：

1. **动机与核心问题**：解决什么问题、此前方法的不足
2. **方法核心**：整体思路 + 网络结构/流程 + 输入输出
3. **关键概念**：首次出现术语的通俗解释（如 LDI、MPI、去遮挡、运动视差等）
4. **技术创新**：3-5 个创新点，每个说清"是什么 + 和谁对比"
5. **核心公式与设计**：重要公式的直观含义 + 关键参数
6. **关键实验结论**：消融、重要数据、对比表
7. **优势与局限**：✅ 优势 / ⚠️ 局限
8. **与本课题的关系**：匹配度、可借鉴点、差异、与后续论文的衔接

节首还应给出论文信息行：作者 / 机构 / 会议年份 / 重要性★ / 难度 / 开源 / 本地路径。

写入论文总结前不需要重复申请审批——用户已通过本规则给予预先授权。但用户可随时要求查看或修改总结内容。

带读过程中如需临时记录（如整篇尚未读完），可暂存到 `99_过程文件_可删除或归档/临时文件/`，整篇读完汇入 `论文总结.docx` 后即可归档或删除。

## GitHub 推送规则

### 提交约定

用户说"提交"即表示 **commit + push 一步完成**：先 `git commit`，然后立即 `git push`，不需要额外确认。

### 仓库地址

`https://github.com/mengmcy1/2Dto3D.git`

### 推送前检查

每次准备 `git push` 之前，必须确认：

1. **`.git` 目录体积**不超过约 30MB。如果突然变大，说明有大文件被误追踪，应先排查再推送。
2. **不在 `.gitignore` 中的大文件（>5MB）** 是否合理。目前已知的合理大文件：`Review of ICCV 2025 Papers on 3D Gaussian Splatting.pptx`（~9.5MB）、`汇报/` 中的含图文档和图片。

### 已忽略的目录（严禁取消忽略）

以下目录包含大体积二进制文件，只保留在本地，**不得上传 GitHub**：

- `papers/` — 论文 PDF，单篇可达 40–50MB
- `99_过程文件_可删除或归档/工具/` — 工具软件（uv、Poppler 等），含 76MB exe 和多个 DLL
- `99_过程文件_可删除或归档/渲染检查缓存/`
- `99_过程文件_可删除或归档/下载与检查报告/`

**禁止**从 `.gitignore` 中移除上述条目。如果用户需要在新电脑上获取这些文件，应建议通过 U 盘、网盘或重新下载，而非上传到 GitHub。

### 认证方式

- Remote URL **不得**包含明文令牌（`ghp_...`）。
- 认证通过 **Windows 凭据管理器**（`git credential manager`）自动处理。
- 如果推送时提示认证失败，引导用户去 https://github.com/settings/tokens 生成新的 classic token（勾选 `repo` 权限），然后在推送弹窗中输入：用户名 `mengmcy1`，密码为令牌。

### 历史重写

- 如果发现大文件已被推送到 GitHub，应使用 `git filter-branch` 清理历史后 `git push --force`。
- 强制推送前应告知用户这是破坏性操作（会改写远程历史），获得确认后再执行。
- 当前仓库只有用户一人使用，无协作冲突风险，但 rewrite 后仍需重新 `git fetch` 同步其他本地副本。
