# 项目背景与初步规划

## 项目背景

本项目围绕“基于概念提取技术的胃早癌诊断模型透明化研究”展开。临床上，白光胃镜是胃癌筛查和诊断的重要基础手段，但早期胃癌病灶常表现为轻微黏膜改变、边界模糊、色调变化、凹陷或隆起等细微特征，容易受到医生经验、图像质量、反光、黏液、气泡和器械干扰等因素影响，存在漏诊风险。

深度学习模型可以辅助胃镜图像中的癌/非癌判别，但常规模型通常只输出分类结果或概率，缺少对“模型为什么这样判断”的解释。对于真实临床场景，仅有癌/非癌结论不足以建立医生信任，也不利于发现模型是否依赖反光、气泡、黏液、边缘阴影等伪相关特征。因此，本项目希望从单纯分类模型逐步推进到可解释诊断模型，使模型输出诊断结果的同时，也能展示其关注区域、关键视觉概念及其对诊断的贡献。

## 当前数据情况

当前正式数据为 `数据/第二批整理后`，目录结构为 `癌或非癌/患者/图片`，并使用 `dataset_manifest.csv` 记录图片相对路径、患者 ID 和标签：

- `1`：癌/高级别
- `0`：非癌
- 图片总数：5229
- 癌/高级别：1600 张，415 位患者
- 非癌：3629 张，1906 位患者
- 所有图片均为 RGB JPG
- 整理后无损坏图片、无相同内容副本、无跨标签患者

第一批数据存在标注和划分问题，仅保留旧实验记录，不再用于正式训练、Grad-CAM 或 MOCE。

数据整理脚本统一放在 `数据整理脚本`，排查报告和清理日志统一放在 `数据整理记录`；不得混入 `程序` 或正式训练数据目录。

## 第一阶段目标

第一阶段目标不是直接完成完整概念瓶颈模型，而是先建立一个可靠的胃镜癌/非癌图像分类基线，并生成注意力热图，证明模型能够关注到可疑病灶区域。

阶段性产出应包括：

- 癌/非癌二分类模型
- 每张图像的癌概率输出
- Grad-CAM 或 Grad-CAM++ 注意力热图
- 原图与热图叠加图
- 模型关注区域的 Top-K 可视化示例
- 对热图合理性的初步人工评估

## 模型选择初步规划

当前正式数据共 5229 张，癌/高级别与非癌图片比例为 1600:3629，属于中小规模医学图像二分类任务。第一阶段建议采用稳健、可解释工具成熟、容易复现的 CNN 模型，而不是一开始就使用复杂 Transformer。

推荐模型路线：

1. 主模型：ResNet50

   - 使用 ImageNet 预训练权重。
   - 替换最后全连接层为二分类输出。
   - 作为第一阶段主模型，用于训练癌/非癌分类器。
   - 优点是结构经典、训练稳定、Grad-CAM 支持成熟，适合后续生成注意力热图和接入 MOCE。
2. 对照模型：EfficientNet-B0

   - 同样使用 ImageNet 预训练权重。
   - 作为轻量对照模型，比较其 AUC、敏感度、特异度和 F1。
   - 如果 EfficientNet-B0 明显优于 ResNet50，可作为性能增强版本。
3. 暂缓模型：Swin Transformer / ConvNeXt

   - 可作为后续增强实验。
   - 不建议第一阶段作为主线，因为样本规模不大，训练和解释成本更高，且注意力热图工作流不如 ResNet50 直接。

## 数据划分建议

训练、验证、测试集必须使用 `dataset_manifest.csv` 中的 `patient_id` 按患者划分，禁止根据图片文件名推测患者，也禁止按图片随机划分。

建议比例：

- 训练集：70%
- 验证集：15%
- 测试集：15%

划分时保持早癌/非癌比例基本一致。

## 训练与评估指标

训练目标为二分类，建议使用交叉熵损失或带类别权重的交叉熵损失。当前非癌图片多于癌/高级别图片，训练时使用根据训练集图片数动态计算的类别权重，并持续监控敏感度与特异度。

主要评估指标：

- AUC
- Accuracy
- Sensitivity / Recall
- Specificity
- Precision
- F1-score
- Confusion matrix

在胃早癌诊断场景中，应重点关注 sensitivity/recall，避免漏诊。

## 可解释性路线

第一阶段先使用 Grad-CAM / Grad-CAM++ 生成注意力热图，回答“模型看哪里”。

后续再基于 MOCE 进一步提取模型导向概念，回答“模型反复关注的视觉概念是什么”。三篇参考文献的优先级如下：

1. MOCE：优先精读并复现

   - 最贴近当前任务。
   - 适合解释已训练好的图像分类模型。
   - 可从模型自身视角提取概念区域。
2. ProtoMIL：第二阶段参考

   - 重点参考 SAE 概念发现、原型图像和人工干预伪相关概念的思路。
   - 原文面向病理 WSI/MIL，与当前白光胃镜单图分类不完全一致。
3. M-CBM：作为最终框架参考

   - 适合指导后续概念瓶颈模型构建。
   - 当前更适合作为理论框架，不建议作为第一阶段复现主线。

## 当前推荐的第一阶段技术路线

1. 使用 `数据/第二批整理后/dataset_manifest.csv` 读取5229张正式图片。
2. 使用标签1=癌/高级别、0=非癌。
3. 按 `patient_id` 分层划分训练/验证/测试集，比例为70%/15%/15%。
4. 使用 ImageNet 预训练 ResNet50 训练癌/非癌分类模型。
5. 使用 EfficientNet-B0 作为对照实验。
6. 在测试集上计算 AUC、sensitivity、specificity、F1 等指标。
7. 对预测正确、预测错误、早癌高置信度、非癌高置信度样本分别生成 Grad-CAM 热图。
8. 请医生初步判断热图是否关注病灶区域，记录伪相关关注案例。
9. 在已有分类模型基础上复现 MOCE，为第二阶段概念提取做准备。

## Phase 1 实验结果（2026-07-08）

### 共同配置

| 配置项     | 值                                                            |
| ---------- | ------------------------------------------------------------- |
| 随机种子   | 42                                                            |
| 数据划分   | 训练 2319 / 验证 497 / 测试 498（分层随机）                   |
| 类别分布   | 早癌 1918 / 非癌 1396（1.37:1）                               |
| Batch Size | 32                                                            |
| 优化器     | AdamW, weight_decay=1e-4                                      |
| 第一阶段   | 冻结 backbone, LR=1e-3, 10 epochs                             |
| 第二阶段   | CosineAnnealing + EarlyStop(patience=8), LR=1e-4, 20 epochs   |
| BN 冻结    | freeze_bn_stats()：冻结层 requires_grad=False 时同步设为 eval |

### ResNet50

| 配置项                         | 值                                       |
| ------------------------------ | ---------------------------------------- |
| 预训练权重                     | ImageNet1K_V2                            |
| 第二阶段解冻范围               | layer4 + fc                              |
| Label Smoothing                | **0.05**（0.1 过强压低了概率分布） |
| Best Val AUC                   | 0.8468                                   |
| Test AUC                       | 0.8427                                   |
| Test Accuracy                  | 0.7751                                   |
| Test Sensitivity               | 0.8160                                   |
| Test Specificity               | 0.7190                                   |
| Test Precision                 | 0.7993                                   |
| Test F1                        | 0.8076                                   |
| **推荐阈值 @Sens≥0.90** | **0.20**（Sens=0.913, Spec=0.522） |

### EfficientNet-B0

| 配置项                         | 值                                            |
| ------------------------------ | --------------------------------------------- |
| 预训练权重                     | ImageNet1K_V1                                 |
| 第二阶段解冻范围               | features[5:] + classifier                     |
| Label Smoothing                | **0.1**（训练动态健康，无过强压制迹象） |
| Best Val AUC                   | 0.8718                                        |
| Test AUC                       | **0.8806**                              |
| Test Accuracy                  | 0.8012                                        |
| Test Sensitivity               | 0.8160                                        |
| Test Specificity               | **0.7810**                              |
| Test Precision                 | 0.8363                                        |
| Test F1                        | 0.8260                                        |
| **推荐阈值 @Sens≥0.90** | **0.22**（Sens=0.903, Spec=0.617）      |

### 测试集最终评估（验证集调阈值 → 测试集锁定评估）

阈值选择策略：验证集上扫描，Sens≥0.90 约束下选 Spec 最高。用该阈值在测试集上做最终评估，测试集全程不参与阈值决策。

#### ResNet50（阈值 0.20）

| 指标        | 验证集           | 测试集           |
| ----------- | ---------------- | ---------------- |
| Sensitivity | 0.913            | 0.892            |
| Specificity | 0.522            | 0.514            |
| Accuracy    | 0.748            | 0.733            |
| Precision   | 0.725            | 0.716            |
| F1          | 0.808            | 0.794            |
| Youden      | 0.435            | 0.407            |
| CM          | (109,100,25,263) | (108,102,31,257) |

#### EfficientNet-B0（阈值 0.22）

| 指标        | 验证集          | 测试集          |
| ----------- | --------------- | --------------- |
| Sensitivity | 0.903           | 0.906           |
| Specificity | 0.617           | 0.595           |
| Accuracy    | 0.783           | 0.775           |
| Precision   | 0.765           | 0.754           |
| F1          | 0.828           | 0.823           |
| Youden      | 0.520           | 0.501           |
| CM          | (129,80,28,260) | (125,85,27,261) |

#### 结论

- 验证集→测试集泛化良好，未出现大幅衰减，阈值选择未在测试集上观察到明显性能衰减
- EfficientNet-B0（5.3M）全面优于 ResNet50（25M）：Test AUC +3.8, Test Spec +6.2 个百分点
- 在 Sens≥0.90 约束下，EfficientNet 测试集 Spec 0.595 vs ResNet 0.514，每 100 例非癌少误报约 8 例
- 后续可解释性路线：**EfficientNet-B0 作为性能主模型，ResNet50 作为可解释性基线**
  - EfficientNet-B0 性能更强（Test AUC +3.8, Spec +8.1 pp @Sens≥0.90），是临床辅助诊断的更好选择
  - ResNet50 残差块梯度流直观、Grad-CAM 在 ResNet 架构上验证最成熟，适合作为热图解释的参考基线
  - 两个模型都生成 Grad-CAM，对照分析能区分"模型看到的" vs "架构特性造成的"，增强结论可信度


## 第二批正式数据重训练结果（2026-07-15）

本轮使用 `数据/第二批整理后`，与 2026-07-08 的第一批数据实验相互独立；旧结果保留用于说明项目迭代过程，不与本轮结果混用。

### 共同配置

| 配置项 | 值 |
| --- | --- |
| 数据规模 | 5229 张；癌/高级别 1600 张，非癌 3629 张 |
| 数据划分 | 按患者分层划分；训练 1624人/3683张，验证 348人/772张，测试 349人/774张 |
| 随机种子 | 42 |
| 类别权重 | 非癌 0.722，癌/高级别 1.627 |
| 第一阶段 | 冻结 backbone，仅训练分类头；10 epochs，LR=1e-3 |
| 第二阶段 | 解冻高层特征与分类头；20 epochs，LR=1e-4，EarlyStop=8 |
| 初始测试阈值 | 0.50（训练结束时的性能表；临床目标阈值另行重标定） |

### ResNet50

| 指标 | 结果 |
| --- | --- |
| 第一阶段 Best Val AUC | 0.9555 |
| 第二阶段 Best Val AUC | **0.9602**（Epoch 1） |
| Test Loss | 0.2544 |
| Test AUC | **0.9900** |
| Test Accuracy | 0.9703 |
| Test Sensitivity | 0.9526 |
| Test Specificity | 0.9779 |
| Test Precision | 0.9485 |
| Test F1 | 0.9505 |
| Test CM | (TN=530, FP=12, FN=11, TP=221) |

第二阶段在 Epoch 1 后连续 8 轮 Val AUC 未超过 0.9602，于 Epoch 9 早停。

#### ResNet50 按数据来源拆分的验证结果

| 子集 | 样本数 | 癌 | AUC | Sens@0.50 | Spec@0.50 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 全量验证集 | 772 | 236 | **0.960** | 0.864 | 0.957 |
| 武大省人民 | 469 | 218 | **0.979** | 0.908 | 0.976 |
| 外院5家 | 303 | 18 | **0.727** | 0.333 | 0.940 |

- 验证集癌图中 218/236（92.4%）来自武大省人民，外院子集中 285/303（94.1%）为非癌，标签与数据来源高度相关。
- 全量 AUC 不能理解为两个来源 AUC 的样本量加权平均；跨来源的癌/非癌样本对会使来源差异参与排序，从而抬高全量 AUC。
- 当前证据支持存在明显的 domain bias 和跨来源泛化下降，但尚不能仅凭该表断言模型具体利用了年份、设备或图像风格。
- 外院只有 18 张癌图，Sensitivity=0.333 对应检出 6/18，估计不稳定；后续需报告患者级指标与置信区间。
- 这些外院数据已有部分患者参与训练，因此该结果属于按来源拆分的内部验证，不是严格独立的外部验证。

#### ResNet50 验证集阈值重标定

阈值选择策略为：在验证集上满足 Sensitivity ≥ 0.90 的候选阈值中，选择 Specificity 最高者。测试集不参与阈值选择。

| 指标 | 验证集结果 |
| --- | ---: |
| 推荐阈值 | **0.30** |
| Sensitivity | 0.903 |
| Specificity | 0.851 |
| Accuracy | 0.867 |
| Precision | 0.727 |
| F1 | 0.805 |
| Youden | 0.753 |
| CM | TN=456, FP=80, FN=23, TP=213 |

- 与自然阈值 0.50 相比，阈值 0.30 将验证集 Sensitivity 从 0.864 提升至 0.903，同时 Specificity 从 0.957 降至 0.851，体现了减少漏诊与增加误报之间的权衡。
- 在该验证集上约每 100 例真癌漏诊 10 例、每 100 例非癌误报 15 例。
- 该阈值受当前验证集来源构成和域偏差影响，暂不能视为跨医院通用阈值。
- 已固定阈值 0.30 完成测试集锁定评估，测试结果见下方汇总；测试结果不再用于调整阈值。

#### ResNet50 测试集锁定评估（阈值 0.30）

验证集选定阈值 0.30 后，在测试集上进行一次锁定评估；测试集全程未参与阈值选择。

| 指标 | 测试集结果 |
| --- | ---: |
| Sensitivity | **0.978** |
| Specificity | **0.867** |
| Accuracy | 0.901 |
| Precision | 0.759 |
| F1 | 0.855 |
| Youden | 0.846 |
| CM | TN=470, FP=72, FN=5, TP=227 |

- 测试集 Sens 和 Spec 均优于验证集（0.903 / 0.851），泛化方向为正，测试集未观察到明显性能衰减
- 每 100 例真癌漏诊 2 例，每 100 例非癌误报 13 例。
- 与自然阈值 0.50 的测试集结果（Sens=0.953, Spec=0.978）相比：阈值降至 0.30 后 Sensitivity 从 0.953 提升至 0.978（少漏诊 6 例），Specificity 从 0.978 降至 0.867（多误报 60 例），体现了筛查场景下减少漏诊优先的权衡。

### EfficientNet-B0

| 指标 | 结果 |
| --- | --- |
| 第一阶段 Best Val AUC | **0.9552**（Epoch 7） |
| 第二阶段 Best Val AUC | 0.9432（Epoch 1） |
| Test Loss | 0.3435 |
| Test AUC | **0.9910** |
| Test Accuracy | 0.9677 |
| Test Sensitivity | 0.9569 |
| Test Specificity | 0.9723 |
| Test Precision | 0.9367 |
| Test F1 | 0.9467 |
| Test CM | (TN=527, FP=15, FN=10, TP=222) |

第二阶段在 Epoch 1 后连续 8 轮 Val AUC 未提升，于 Epoch 9 早停。解冻后第二阶段最佳 Val AUC 比第一阶段下降 0.0120，说明本轮微调没有改善验证集排序性能。

#### EfficientNet-B0 按数据来源拆分的测试结果

| 子集 | AUC | Sens@0.50 | Spec@0.50 |
| --- | ---: | ---: | ---: |
| 全量测试集 | **0.991** | 0.957 | 0.972 |
| 外院5家 | **0.921** | 0.565 | 0.982 |
| 武大省人民 | **0.9999** | 1.000 | 0.961 |

- 全量测试 AUC 明显高于外院子集 AUC，说明混合来源总体指标会高估模型的跨来源泛化表现。
- EfficientNet-B0 在当前外院测试子集上仍有 AUC=0.921，表明模型并非完全依赖来源 shortcut，可能具备一定的跨来源病灶判别能力。
- 该外院子集并非严格独立外部验证集，因为外院其他患者已经参与训练；阳性样本也较少，需补充患者级指标和 95% 置信区间。
- 省人民子集内部 AUC 接近 1.0，提示可能存在与标签相关的年份、设备、采集批次或文件夹风格等域因素，但仅凭该结果不能排除模型同时学习了真实病灶特征。

#### EfficientNet-B0 验证集阈值重标定

阈值选择策略与 ResNet50 相同：在验证集上满足 Sensitivity ≥ 0.90 的候选阈值中，选择 Specificity 最高者。测试集不参与阈值选择。

| 指标 | 验证集结果 |
| --- | ---: |
| 推荐阈值 | **0.22** |
| Sensitivity | 0.903 |
| Specificity | 0.757 |
| Accuracy | 0.802 |
| Precision | 0.621 |
| F1 | 0.736 |
| Youden | 0.660 |
| CM | TN=406, FP=130, FN=23, TP=213 |

- 与自然阈值 0.50 相比，阈值 0.22 将验证集 Sensitivity 从 0.864 提升至 0.903，同时 Specificity 从 0.979 降至 0.757。
- 在该验证集上约每 100 例真癌漏诊 10 例、每 100 例非癌误报 24 例。
- 在相同验证集和 Sensitivity=0.903 下，ResNet50 阈值 0.30 的 Specificity 为 0.851，EfficientNet-B0 阈值 0.22 的 Specificity 为 0.757；两者均漏诊 23 张癌图，但 EfficientNet-B0 多产生 50 张假阳性（130 vs 80）。
- 因此，按当前保存权重和 Sens≥0.90 的临床目标，ResNet50 的验证集阈值表现优于 EfficientNet-B0；该结论仍需通过锁定阈值后的测试集结果确认。
- 已固定阈值 0.22 完成测试集锁定评估，测试结果见下方汇总；测试结果不再用于调整阈值。

#### EfficientNet-B0 测试集锁定评估（阈值 0.22）

验证集选定阈值 0.22 后，在测试集上进行一次锁定评估；测试集全程未参与阈值选择。

| 指标 | 测试集结果 |
| --- | ---: |
| Sensitivity | **0.987** |
| Specificity | **0.799** |
| Accuracy | 0.855 |
| Precision | 0.678 |
| F1 | 0.804 |
| Youden | 0.786 |
| CM | TN=433, FP=109, FN=3, TP=229 |

- 测试集 Sens 和 Spec 均优于验证集（0.903 / 0.757），泛化方向为正。
- 每 100 例真癌漏诊 1 例，每 100 例非癌误报 20 例。
- 与自然阈值 0.50 的测试集结果（Sens=0.957, Spec=0.972）相比：阈值降至 0.22 后 Sensitivity 从 0.957 提升至 0.987（少漏诊 7 例），Specificity 从 0.972 降至 0.799（多误报 94 例）。

### 两模型测试集阈值对比（锁定评估）

两模型均在验证集上选定阈值（Sens≥0.90 约束下 Spec 最高），测试集全程未参与阈值选择。阈值选定后进行一次性锁定评估。

| 指标 | ResNet50 (th=0.30) | EfficientNet-B0 (th=0.22) |
| --- | ---: | ---: |
| Sensitivity | **0.978** | **0.987** |
| Specificity | **0.867** | **0.799** |
| Accuracy | 0.901 | 0.855 |
| Precision | 0.759 | 0.678 |
| F1 | 0.855 | 0.804 |
| Youden | **0.846** | **0.786** |
| CM | TN=470, FP=72, FN=5, TP=227 | TN=433, FP=109, FN=3, TP=229 |

- 两者 Sensitivity 均在 0.97 以上，远超 Sens≥0.90 的临床底线。
- **ResNet50 阈值 0.30 综合表现优于 EfficientNet-B0 阈值 0.22**：Youden 高 0.06，Specificity 高 6.8 个百分点（少 37 例误报），仅 Sensitivity 低 0.9 个百分点（多漏诊 2 例）。
- 在当前全量测试输出中，两种模型在自然阈值 0.50 下表现接近，不能据此认定某一模型显著更优。锁定临床阈值后的对比进一步支持该结论。
- 该结论的局限：测试集与验证集一样存在来源构成偏差，上述指标不能直接推广到新医院。

#### 其他复核事项

- 两个模型的 Test AUC 均约为 0.99，但验证集 AUC 约为 0.95～0.96，且分来源结果显示明显的性能差异。因此测试集高分不能直接作为跨医院泛化能力的证据。
- 目前 ResNet50 的外院指标来自 Val，EfficientNet-B0 的外院指标来自 Test；两者不是同一批患者，不能直接用 0.727 与 0.921 判断模型优劣。需在完全相同的外院 Test 子集上重新评估两个模型。（P0 已完成，见下文。）
- 当前训练脚本每个阶段分别选择最佳权重，第二阶段结束后固定使用第二阶段最佳权重进行测试和保存，不会与第一阶段最佳权重进行全局比较。
- 该逻辑对 ResNet50 没有造成阶段选择冲突；但 EfficientNet-B0 的 S1 Val AUC=0.9552 高于 S2 Val AUC=0.9432，当前 Test AUC=0.9910 实际对应 S2 Epoch 1，并非全流程验证 AUC 最高的 S1 Epoch 7。
- S2 验证 AUC 下降只能说明本轮解冻微调未改善总体排序性能；”解冻后过拟合省人民域特征”目前是待验证假设。需分别评估 S1、S2 权重在相同省人民和外院子集上的表现，如果出现省人民上升而外院下降，才能支持该解释。
- 正式定稿前应保留并比较 EfficientNet-B0 的 S1 最佳权重和 S2 最佳权重，使用验证集选择其中一个后，仅对选定模型进行一次测试集评估。
- 论文中应将混合来源结果称为内部测试，将完全未参与训练的医院数据作为独立外部验证集，并报告患者级指标、样本构成及 95% 置信区间。

### P0：同一测试外院子集两模型对比（2026-07-15）

在完全相同的测试外院子集（103 患者，307 张，癌=23）上评估两个模型，消除因患者不同导致的不可比问题。

| 模型 | 图片级 AUC | 95% CI | Sens@0.50 | Spec@0.50 | 患者级 AUC |
| --- | ---: | ---: | ---: | ---: | ---: |
| ResNet50 | **0.9261** | 0.8458–0.9846 | 0.739 | 0.979 | 0.887 |
| EfficientNet-B0 | **0.9209** | 0.8422–0.9781 | 0.565 | 0.982 | 0.911 |

- 同一批患者上两模型 AUC 差仅 0.005，在 CI 宽度（~0.14）面前可以忽略。之前 EfficientNet 0.921 vs ResNet 0.727 的假象是因为分属不同 split，不是模型差异。
- CI 很宽因为只有 23 张癌图；外院癌图补到 50–80 张后 CI 会明显收窄。
- 该结果直接支撑了"两种模型表现接近、不能据此认定某一模型显著更优"的结论。

## Phase 2 MOCE 进度（2026-07-14）

### 论文精读

- MOCE（Kim & Chae, TPAMI 2024）全文精读完成，逐段带读 16 段
- 产出文献/：`1_MOCE_Kim_TPAMI2024.pdf`、`moce.txt`（pdftotext 提取）、`MOCE_一页纸精读总结.md`（含算法流程图 + 超参速查表 + 项目落地映射 + 小数据集风险预警）
- 精读流程见 memory `paper-reading-workflow.md`，MOCE 局限记录见 `moce-small-dataset-risk.md`
- 未读：[2] M-CBM 和 [3] ProtoMIL — ProtoMIL 即 SAE 路线，下一步精读

### MOCE 代码实现

#### 单图 Demo — `程序/MOCE/demo测试/moce_single_demo.py`

- 完成且 review 通过
- 功能：单张图 → A1-A3（通道重要性 L=ReLU(g·a) → top 50% 通道 → 上采样 → top γ=10% 二值化 → 连通域 → IoU≥0.5 去冗余）→ A4（抠块 GAP 特征 + keep/removed 概率）
- 输出：三联对比图 + candidate_concepts.csv + candidate_features.npz
- 不包含聚类，仅作单图自检和素材采集

#### 正式聚类脚本 — `程序/MOCE/正式代码/moce_cluster.py`

- 完整 MOCE 管线：候选提取 → GAP 编码 → K-Means(k=25) → S^R/S^E/S_h → SSC/SDC
- 已 review，以下问题已确认/修复：
  - **提前 Resize 224×224**：已删，保留原生分辨率，掩码质量和 demo 一致（全量运行关注 CPU 内存）
  - **三层判空**：单图无候选 continue / len(records)<N_CLUSTERS 跳过 / assignment.empty 跳过
  - **掩码存 PNG**：每块候选区域存一张二值掩码 PNG，后续概念级合并可还原
  - **S_h 分母**：使用参与该类别概念评估的图片总数 `total_images`
  - **select_cluster_masks**：同图同簇只取距离中心最近的一个候选区域（符合作者实现），不做掩码并集
  - **SSC/SDC threshold=0.5**：模型原始决策边界，用于概念忠实性评价；临床诊断用模型调优阈值（需新数据重标定）
- α=1.0, β=0.5, k=25, γ=10%, SSC_SDC_TOP_K=5（论文默认）
- 输出目录结构：`结果/MOCE聚类/第二批/{model_name}/class_{0,1}/`
  - `候选区域/` + `候选掩码/`：原始素材
  - `kmeans_model.joblib` + `candidate_features.npz`：聚类模型和特征
  - `cluster_assignments.csv` + `cluster_summary.csv`：簇分配和统计
  - `concept_clusters.png`：每簇 top-5 最近邻可视化
  - `concept_scores_per_image.csv` + `concept_importance.csv`：S^R/S^E/S_h
  - `ssc_sdc_per_image.csv` + `ssc_sdc_summary.csv`：SSC/SDC 曲线数据

### MOCE 聚类与分析结果（2026-07-21）

#### S_R 计算修复

原始 `moce_cluster.py` 中 `evaluate_concept_importance` 使用 `removal_drops / removal_total` 计算 S_R。当 `removal_total < 0` 时，除法翻转所有概念的符号方向，导致概率下降为正的真正重要概念被排在负值之后。

受影响图片比例（旧实现）：
- EfficientNet-B0：class_0 约 38.5%（734/1906），class_1 约 10.4%（43/415）
- ResNet50：class_0 约 38.2%（729/1906），class_1 约 1.4%（6/415）

**修复方案**：clip 负向 drop 为 0 后归一化，使 S_R 只衡量正向贡献；同时 `relative_ranks` 改为 `positive_only=True`，≤0 值不参与排名。

修复后，负向 probability_drop 不再因分母为负而获得错误的正向 S_R；由于 S_h 同时包含 S_R 和 S_E，平均 probability_drop 为负的概念仍可能因 S_E 较高进入重要性前列。癌排序基本稳定（Top 5 集合不变，仅内部顺序微调）。修复已合并到 `moce_cluster.py` 主文件。

#### EfficientNet-B0 MOCE 结果（S_R 修正后）

**癌/高级别 (class 1)** — 415 患者，60,061 候选区域，25 个概念簇

| 排名 | 概念 | S_h | mean_prob_drop | 说明 |
| --- | ---: | ---: | ---: |
| 1 | 概念15 (c14) | 0.642 | +0.030 | |
| 2 | 概念22 (c21) | 0.635 | +0.035 | |
| 3 | 概念11 (c10) | 0.608 | +0.024 | |
| 4 | 概念21 (c20) | 0.597 | +0.021 | |
| 5 | 概念1 (c0) | 0.585 | +0.032 | |

所有 Top 10 概念 mean_probability_drop 均为正。自动分析未对 Top 10 产生质量警告；这不等于医生已确认其医学合理性。

SSC/SDC 曲线：
- Step 0：全黑图癌概率 0.585，SSC=100%（全黑图属于分布外输入，但被模型赋予较高癌概率，提示当前 SSC 基线存在明显偏置；具体原因不能仅归因于类别权重）
- Step 5（Top 5 概念）：SSC=54.0%，SDC=86.5%（Δ=-5.3pp）
- 当前癌类 SSC 受全黑基线偏置影响，不能可靠评价充分性；SDC 下降 5.3 个百分点，说明 Top 5 概念对模型判断存在一定贡献，但效应较弱

发现非医学概念（概念3等含检查文字、设备状态栏、黑色边缘）排名第 9，说明模型编码了部分界面信息，但 Top 5 以黏膜区域为主。

4/25 概念簇为省人民专属（source_count=1），但均排在重要性第 9 及以后；Top 8 概念均跨 6 家医院来源。

**非癌 (class 0)** — 1906 患者，285,911 候选区域，25 个概念簇

| 排名 | 概念 | S_h | mean_prob_drop | 说明 |
| --- | ---: | ---: | ---: |
| 1 | 概念2 (c1) | 0.609 | +0.023 | |
| 2 | 概念11 (c10) | 0.596 | +0.021 | |
| 3 | 概念10 (c9) | 0.588 | -0.004 | 平均概率下降非正 |
| 4 | 概念9 (c8) | 0.587 | -0.016 | 平均概率下降非正 |
| 5 | 概念13 (c12) | 0.582 | +0.030 | |

SSC/SDC 曲线：
- SSC：28.9% → 95.5%（5 个概念可充分重建非癌判断）
- SDC：97.4% → 95.7%（Δ=-1.7pp，效应微弱）

#### ResNet50 MOCE 结果（S_R 修正后）

**癌/高级别 (class 1)** — 415 患者，33,981 候选区域，25 个概念簇

| 排名 | 概念 | S_h | mean_prob_drop | 说明 |
| --- | ---: | ---: | ---: |
| 1 | 概念17 (c16) | **0.842** | **+0.062** | |
| 2 | 概念9 (c8) | **0.808** | **+0.077** | |
| 3 | 概念11 (c10) | **0.792** | +0.049 | |
| 4 | 概念12 (c11) | **0.791** | **+0.081** | |
| 5 | 概念5 (c4) | 0.747 | **+0.076** | |

- 重要性排名前 10 的概念，其 mean_probability_drop 均为正
- 自动分析未对 Top 10 产生质量警告；这不等于医生已确认其医学合理性
- 无纯省人民专属簇出现在重要排名中

SSC/SDC 曲线：
- Step 0：全黑图癌概率 ≈1.0（全黑图属于分布外输入，ResNet50 对其癌偏置比 EfficientNet-B0 更极端；具体原因不能仅归因于类别权重）
- Step 5：SSC=99.3%，SDC=83.4%（Δ=-9.9pp）
- **癌类 SSC 受到全黑基线强烈癌偏置影响（Step 0 已为 100%），暂不能用于证明概念充分性**；SDC 下降约 9.9 个百分点，支持 Top 5 概念对模型判断存在一定贡献

**非癌 (class 0)** — 1906 患者，154,361 候选区域，25 个概念簇

| 排名 | 概念 | S_h | mean_prob_drop | 说明 |
| --- | ---: | ---: | ---: |
| 1 | 概念7 (c6) | **1.106** | **+0.532** | 面积占比 32%（其他多数概念约 7–9%） |
| 2 | 概念8 (c7) | 0.554 | -0.006 | |
| 3 | 概念23 (c22) | 0.502 | +0.045 | |
| 4 | 概念19 (c18) | 0.487 | -0.029 | |
| 5 | 概念24 (c23) | 0.437 | -0.031 | |

- 概念 c6 占绝对主导：S_h=1.106、drop=+0.532、面积 32%，远超出其他概念
- 初步视觉观察显示 c6 主要覆盖大片黏膜区域，但范围较宽，部分代表图包含界面文字；其医学含义及是否存在伪相关因素仍需医生审核

SSC/SDC 曲线：
- SDC：97.6% → **36.0%**（Δ=-61.6pp，移除 c6 后非癌分类崩溃）
- SSC：仅靠概念区域上色 → 57.1%（充分性有限，正常组织需要上下文）

#### 两模型 MOCE 核心对比

| 维度 | ResNet50 | EfficientNet-B0 |
| --- | --- | --- |
| **癌候选区域数** | **33,981** | 60,061 |
| **癌 SDC Δ** | **-9.9 pp** | -5.3 pp |
| **癌概念效应量** | drop 0.06–0.08 | drop 0.02–0.05 |
| **癌 SSC** | 无法评估（全黑基线癌偏置） | 无法评估（全黑基线癌偏置） |
| **非癌 SDC** | 移除 c6 → 大幅下降（Δ=-61.6pp） | 移除全部 → 几乎不变（Δ=-1.7pp） |
| **非癌策略** | 单概念主导（c6，面积 32%） | 多概念分散 |
| **非医学伪影** | class 0 概念7 部分代表图含界面文字，尚不能排除伪相关 | 概念3 排名第 9 |
| **当前定位** | 可优先作为 MOCE 深入分析对象 | 作为架构对照参考 |

**注意**：两模型在相同外院测试子集上的 AUC 基本相同（ResNet50 0.926 vs EfficientNet-B0 0.921），MOCE 的 SDC 差异衡量的是概念忠实性而非分类性能优劣。最终是否选定论文主模型，需结合医生命名、伪相关审核和独立验证结果决定。

#### ResNet50 两类的概念分工（有待临床验证的初步解释假设）

| | 非癌 | 癌 |
|---|---|---|
| 核心概念 | 大面积区域（面积 32%，覆盖 99.7% 患者） | 多个较小区域（面积 7-9%） |
| SDC 表现 | 移除 c6 → 准确率大幅下降 | 逐概念移除 → 逐步下降 |
| 初步观察 | 主要覆盖大片黏膜，范围较宽，部分含界面文字 | 代表图以狭长或较宽泛黏膜组织为主，未呈现单一早癌形态 |
| 待验证 | 是否为正常黏膜、图像背景、界面共性特征的混合 | 簇内一致性及病灶定位需医生复核 |

### 多中心独立外部验证（2026-07-21）

数据来源为 `数据/胃镜多中心早癌图片挑选`，来自多个不同中心的胃镜图像，与训练数据无患者重叠。当前仅包含癌数据（121 患者，542 张），非癌数据待补充。评估脚本为 `程序/模型训练/正式代码/evaluate_multicenter.py`。

#### 核心结果

| 指标 | ResNet50 (th=0.30) | EfficientNet-B0 (th=0.22) |
| --- | ---: | ---: |
| 图片级 Sensitivity | 34.3% (186/542) | 45.2% (245/542) |
| **患者级 Sensitivity** | **60.3% (73/121)** | **69.4% (84/121)** |
| 患者级 Sensitivity @0.50 | 32.2% (39/121) | 31.4% (38/121) |

对比原有内部测试集的患者级 Sens（ResNet50 0.978, EffNet 0.987），外部验证患者级敏感度骤降 30–38 个百分点。

#### 概率分布

两模型的癌概率均被大幅压低：

| 分位数 | ResNet50 | EfficientNet-B0 |
| --- | ---: | ---: |
| 图片均值 | 0.273 | 0.277 |
| 图片中位数 | 0.224 | 0.194 |
| 患者 max 中位数 | 0.367 | 0.364 |
| 患者 max >0.9 | 1.7% (2/121) | 5.8% (7/121) |

不是少数图片被压低，而是整体分布向下平移。模型对新数据的癌概率输出系统性地低于训练时的预期范围。

#### 两模型对比

- EfficientNet-B0 患者级敏感度比 ResNet50 高 9 个百分点，但其 locked 阈值也更低（0.22 vs 0.30）
- 两模型患者级 max_prob 相关性 r=0.683——趋势大致一致，但存在分歧
- **26 位患者（21.5%）两模型均漏诊**，属于这批中最难判别的病例
- 仅 ResNet50 漏诊 22 人，仅 EffNet 漏诊 11 人

#### 衰退原因分析

这批数据来自多个外部中心（患者 ID 前缀多样），与训练集（以武大省人民为主）存在明显的域偏移。可能因素：

1. **内镜设备/光源/色调**不同——模型依赖的低层颜色纹理特征跨设备泛化不足
2. **拍摄习惯/构图差异**——病灶在画面中的比例、位置分布不同
3. **病变特征分布差异**——外部中心的早癌病例可能包含训练集中未充分覆盖的形态亚型
4. **非医学伪特征**——训练集可能学到了日期文字、界面元素等域相关 shortcut，在新数据上失效

当前仅癌数据缺乏阴性对照，无法判断模型是将癌误判为非癌（低敏感度）还是整体概率标度偏移（需要非癌数据计算 AUC 区分）。非癌数据到齐后可通过 AUC 区分"排序能力尚可但阈值不适配"与"排序能力本身崩坏"两种情况。

#### 评估脚本设计

`evaluate_multicenter.py` 支持癌/非癌两种目录，当前非癌目录未提供时自动跳过 AUC、特异度等指标，只计算敏感度与 Wilson 95% CI。后续补充非癌数据后无需修改脚本，传 `--noncancer-dir` 即可输出完整指标。脚本同时输出 locked 阈值（模型注册表中的调优阈值）和 natural 阈值（0.50）的结果便于对比。患者级使用 max_probability 汇总（同时保留 mean/median 作为辅助信息），漏诊患者单独输出 CSV。

#### 后续计划

- 非癌数据到齐后重新评估，计算 AUC、特异度以区分排序衰退与阈值偏移
- 考虑用这批多中心数据做 MOCE 概念分解，观察跨中心概念激活模式是否一致
- 如 AUC 尚可（如 >0.85），可考虑微调适配；如 AUC 接近 0.5，说明当前模型对新中心基本不具备泛化能力，需从数据层面重新设计训练策略

### 待做

- **重新训练已完成**：已使用第二批整理后数据完成 ResNet50 与 EfficientNet-B0 的患者级划分训练
- **阈值重标定已完成**：ResNet50 选定阈值 0.30（测试集 Sens=0.978, Spec=0.867），EfficientNet-B0 选定阈值 0.22（测试集 Sens=0.987, Spec=0.799）；`inference.py` 的 `MODEL_REGISTRY` 已同步更新
- **MOCE 聚类与分析已完成**：两模型均已跑完 S_R 修正后的完整聚类与分析链
- **多中心外部验证初步完成**：癌数据已评估，患者级敏感度大幅衰退（ResNet50 60.3%, EffNet 69.4%）；等待非癌数据补全
- **医生命名**：当前概念仍需医生审查命名，材料（Excel 命名表 + 概念可视化）已生成。优先审核 ResNet50 class 0 概念7 和 class 1 Top 5；随后审核 EfficientNet-B0 的重要概念作为架构对照
- 仍需复核 EfficientNet-B0 的阶段间最佳权重选择（S1 Val AUC=0.9552 > S2 Val AUC=0.9432）
- MOCE 验证阶段（独立验证集 SSC/SDC + 1-NN 匹配到发现阶段的 KMeans，不重训聚类）
- 路线 B：ProtoMIL（SAE 概念发现）精读 → 复现

### 关键词口径

- activation map / 激活图 → **特征图**
- 公式用纯文本 + 代码块，不用 LaTeX
- 候选区域 ≠ 概念 —— 候选区域聚类后才形成概念簇

## 旧模型在v1.1裁剪数据上的无重训练验证（2026-07-22）

- 旧权重本身使用第二批5229张数据训练，不能把整套v1.1裁剪数据称为独立新测试集。
- 使用训练时保存的 `结果/数据划分/image_split_seed42.csv`，只将原349位test患者作为有效未见患者；先在原图上复现旧结果，再替换为同一图片的v1.1裁剪版本。
- ResNet50患者级AUC由0.979（95% CI 0.948-0.998）降至0.772（0.702-0.832），配对差值-0.207（95% CI -0.275至-0.147，p<0.001）。旧阈值0.30下患者敏感度由0.968降至0.339。
- EfficientNet-B0患者级AUC由0.983（95% CI 0.960-0.998）降至0.893（0.855-0.928），配对差值-0.091（95% CI -0.132至-0.051，p<0.001）。旧阈值0.22下患者敏感度由0.984降至0.742。
- 每患者限1-3张后，ResNet50/EfficientNet-B0患者AUC为0.780/0.896；相对裁剪全图集的差值区间均跨0，说明本次下降主要来自裁剪后的输入分布改变，而非限图数量。
- 严格共同支持集中只有24位患者属于旧test（11癌、13非癌）；ResNet50/EfficientNet-B0患者AUC为0.853/1.000，但样本极小。其余156位属于旧train/val，整套strict上的旧模型结果存在暴露污染，不能作为独立验证结论。
- 该结果证明旧模型对画面裁剪和构图变化敏感，支持其利用边缘/UI/分辨率等非病灶线索的风险判断；但裁剪本身也是输入域变化，不能仅凭AUC下降定位某一种具体捷径。

完整记录见 `数据去偏重训练_参数与报告/旧模型在v1_1裁剪数据验证结果_v1.md`，逐样本预测位于 `结果/去偏重训练_v1/旧模型新预处理验证_v1/`。

## 本地脚本编码约定

- 首次编写或修改脚本时保持现有主体结构，优先做局部、必要的修改，不主动进行大规模抽象或拆分。
- 本项目脚本主要在已配置好的本地环境运行，默认数据路径、CSV 字段、模型权重、字体和依赖均正确可用。
- 不添加针对已固定本地环境的多层路径检查、依赖回退和重复参数校验；但必须保留会影响实验正确性、避免静默错误或处理合理空候选情况的必要判断（如空候选跳过、聚类样本不足跳过等）。
- 数据筛选、训练阶段切换、早停、模型选择、阈值判断等会影响实验结果的核心逻辑必须保留。
