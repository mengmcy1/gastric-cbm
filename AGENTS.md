# gastric-cbm 项目协作说明

## 1. 核心 Markdown 的分工

本项目始终只维护一份长期说明和一份当前阶段进度文档：

1. `AGENTS.md`
   - 记录项目背景、长期目标、稳定技术路线、正式目录、统一实验口径、文件安全规则和已经确认的重要阶段成果。
   - 只在长期规则、稳定事实、正式入口或重要阶段结论发生变化时修改。
   - 不记录逐次运行日志、临时命令、单个 debug 结果和未经确认的候选判断。
2. 当前进度：`YOLO26实验进度与结果讨论.md`
   - 记录当前阶段、实时进度、正式结果、异常、待人工复核事项、候选结论和下一步。
   - 文件开头固定保留“新对话恢复项目的提示词”，提示词只讲恢复上下文和汇报的方法，不写具体即时进度。
   - 每完成一项正式实验或改变当前安排后及时更新，保留必要历史，不用新结论静默覆盖旧结论。

`实验进度与结果讨论.md`已于2026-08-13结束维护，作为第二批、M0-M5、MOCE与SAE阶段
历史保留。后续不得继续向其中追加YOLO26逐轮记录；阶段切换时可按同样方式结束旧进度
文档并启用新的当前进度文档，但任一时刻只能有一份实时进度文档。

详细实验数字以正式 CSV、JSON 和结果目录为准；即时状态以
`YOLO26实验进度与结果讨论.md` 为准；历史阶段事实可追溯已结束的
`实验进度与结果讨论.md`；文件安全、实验边界和协作规则以本文件为准。

重构前的完整历史记录保存在：

`文档/归档/AGENTS_重构前_20260731.md`

该备份仅供追溯，不再继续追加。

## 2. 项目背景与长期目标

课题围绕“基于概念提取技术的胃早癌诊断模型透明化研究”展开。研究对象为白光胃镜
图像，分类标签为：

- `1`：早癌或高级别瘤变；
- `0`：非癌。

长期目标不只是输出癌概率，还要回答：

1. 模型在图像中关注哪里；
2. 模型反复利用了哪些视觉模式；
3. 这些模式对癌/非癌判断是正向还是负向贡献；
4. 模型是否依赖设备界面、分辨率、来源、反光、黑边等伪特征；
5. 能否在医生确认概念后形成可干预、可追溯的概念瓶颈模型。

稳定技术路线为：

`数据审计与去偏 → CNN分类器 → Grad-CAM → MOCE → SAE → 医学命名 → M-CBM/CBL`

其中：

- Grad-CAM回答“模型看哪里”；
- MOCE回答“局部候选区域可聚成哪些模型导向概念”；
- SAE回答“深层特征可分解成哪些稀疏方向，以及它们如何贡献于预测”；
- M-CBM/CBL必须等待临床概念命名达到可用质量后再进入正式训练。

## 3. 数据与正式实验基线

### 3.1 正式数据目录

除`README.md`和`归档/`外，`数据/`顶层只保留正式流程直接使用的数据：

- `第二批整理后`：第二批原始整理数据，5229张，415位癌患者、1906位非癌患者；
- `第二批裁剪后_v1_1`：分类器去偏重训练的正式输入；
- `胃早癌概念提取训练集_裁剪后_v1_1`：医学生整理概念集的正式裁剪版本；
- `胃镜多中心测试集`：独立外部多中心测试数据。

旧数据、历史版本和不再作为正式入口的数据放在`数据/归档/`，不得删除。冻结清单可能
含绝对路径；移动正式数据前必须检查并同步所有清单，禁止只移动目录而不验证引用。

### 3.2 数据版本谱系

项目中的“第一批”“第二批”“第二批整理后”和“第二批裁剪后”不是可互换的名称：

| 版本 | 规模与结构 | 是否直接训练 | 当前定位 |
| --- | --- | --- | --- |
| 第一批 | CSV 3612行；可匹配图片3314张，癌1918/非癌1396 | 是，2026-07-08图像随机划分基线 | 标签和划分存在问题，仅保留历史结果 |
| 第二批 | 院方原始目录，按类别、中心和患者分层但含待清理项 | 否；作为整理脚本输入 | 只读归档于`数据/归档/原始数据_只读/第二批` |
| 第二批整理后 | 5229张、2321位患者；癌1600张/415人，非癌3629张/1906人 | 是，2026-07-15患者级划分旧模型 | 未裁剪历史基线及历史MOCE输入 |
| 第二批裁剪后_v1_1 | 对同一5229张执行统一裁边、界面处理和resize | 是，实验A/A0/B/C/D | 当前分类器和解释流程的正式训练母版 |
| 医学生概念集v1.1 | 3601张整理图的裁剪版；正式MOCE严格子集为502张/314人 | 否，不用于当前分类器训练 | MOCE概念发现与临床审核 |
| 外部多中心集 | 1941张、1329人；癌121人、非癌1208人 | 否 | 锁定模型的独立外部测试 |

仓库记录表明，原始`第二批`先经
`数据整理脚本/归档/第二批_v1_1/prepare_second_batch.py`形成`第二批整理后`，训练脚本
直接读取后者的`dataset_manifest.csv`。因此二者是同一批数据的“交付态”和“清理态”，
不是两次独立训练或两个可直接比较的实验队列。

### 3.3 数据原则

- 所有训练、验证、测试划分以`patient_id`为单位，禁止按图片随机拆分同一患者。
- 同一患者不得跨split；跨标签患者必须明确采用图像级还是患者级标签口径。
- 完全重复图、近重复帧、跨标签副本和训练/测试SHA重叠必须审计。
- 数据筛选和匹配优先通过manifest实现，不物理删除原图。
- 训练集用于拟合参数，验证集用于模型选择、阈值、聚合方式和解释器超参数选择；
  测试集及外部集只在方案锁定后投影和评估。
- 患者级指标是临床主要口径；图像级指标作为补充。多图患者不能在患者级评估中被重复
  加权。
- AUC、Sensitivity、Specificity和Accuracy必须同时注明数据集、统计层级、阈值来源和
  聚合方式。不同划分、不同匹配协议或不同患者集合的结果不能直接做配对显著性结论。

### 3.4 隐私与资产边界

患者图像、视频、患者级清单、图片级预测、特征缓存、模型解释图和可识别患者信息禁止
上传公共仓库。仓库应保持私有；模型权重通过私有Release或服务器受控路径管理。

`研究结果摘要/`只能保存经过列白名单和隐私检查的聚合结果。大体积数据与结果默认不
进入普通Git提交。

## 4. 正式目录与代码入口

### 4.1 目录职责

- `程序/模型训练/正式代码/`：分类器训练、推理、集成和外部评估；
- `程序/注意力热图/正式代码/`：Grad-CAM正式生成；
- `程序/MOCE/正式代码/`：MOCE聚类、严格概念集和K敏感性分析；
- `程序/SAE/正式代码/`：SAE训练、冻结投影、自动分析、图像中心可视化和CBL备用入口；
- `程序/项目维护/正式代码/`：安全摘要和模型资产维护；
- `程序/归档/`：旧基线、demo、一次性审计和历史实现，不作为新实验入口；
- `数据整理脚本/`：预处理、裁剪、筛图、匹配和数据审计；
- `数据整理记录/`：预处理验收、数据审计、清理日志和过程检查记录；
- `数据去偏重训练_参数与报告/`：冻结参数与正式实验报告；
- `结果/`：正式输出和本地大体积结果；
- `文档/`：标注规范、迁移说明和历史方法文档。

正式脚本的具体用法优先阅读各模块`README.md`。

### 4.2 当前主要入口

- ResNet50训练：`程序/模型训练/正式代码/resnet_train_debiased.py`
- EfficientNet-B0训练：`程序/模型训练/正式代码/efficientnet_train_debiased.py`
- 推理：`程序/模型训练/正式代码/inference.py`
- 集成与TTA：`程序/模型训练/正式代码/evaluate_ensemble_tta.py`
- 完整外部评估：`程序/模型训练/正式代码/evaluate_debiased_multicenter.py`
- M0全量矩阵汇总：`程序/模型训练/正式代码/analyze_m0_full_matrix.py`
- M0锁定模型外部评估：`程序/模型训练/正式代码/evaluate_m0_external_matrix.py`
- M0平衡矩阵启动：`程序/模型训练/正式代码/run_m0_balanced_matrix.sh`
- M1癌图辅助定位：`程序/模型训练/正式代码/efficientnet_m1_localization.py`
- M1平衡/全量矩阵启动：`程序/模型训练/正式代码/run_m1_matrix.sh`
- Grad-CAM：`程序/注意力热图/正式代码/gradcam_batch.py`
- 严格概念集MOCE：`程序/MOCE/正式代码/moce_curated_concept.py`
- MOCE K敏感性：`程序/MOCE/正式代码/run_kmeans_sensitivity.py`
- SAE训练：`程序/SAE/正式代码/sae_discovery.py`
- 锁定SAE投影：`程序/SAE/正式代码/sae_project_analyze.py`
- SAE跨数据集稳定性：`程序/SAE/正式代码/sae_cross_dataset_stability.py`
- 图像中心SAE：`程序/SAE/正式代码/sae_image_centered.py`
- M-CBM/CBL备用入口：`程序/SAE/正式代码/mcbm_cbl_train.py`

### 4.3 运行环境

- 正式Conda环境名为`gastric-cbm`。
- 当前服务器Python解释器为
  `/home/mcy/miniconda3/envs/gastric-cbm/bin/python`；迁移服务器后必须重新确认实际路径。
- 未经用户确认，不重装或大版本升级PyTorch、torchvision、CUDA等核心依赖。

## 5. 稳定实验口径

### 5.1 分类器

- 第二批v1.1已完成阶段的主模型为ResNet50，对照及性能模型为EfficientNet-B0。
- 新数据M0-M5路线不预设永久主骨干：ResNet50与EfficientNet-B0先在同一M0患者划分
  和train/val协议下比较，再只根据预先冻结的val规则选择主骨干；test和外部test不得
  参与该选择。
- 均使用ImageNet预训练，采用两阶段迁移学习和患者级冻结划分。
- 模型选择只看验证集；测试集不参与epoch、阈值、TTA、聚合或集成权重选择。
- 筛查场景重点报告Sensitivity，但不能脱离Specificity、AUC、混淆矩阵和置信区间。
- 内部高AUC必须与来源、尺寸、风格元数据基线和严格共同支持结果一起解释。

### 5.2 Grad-CAM

- 热图是最后卷积层的粗粒度响应上采样，不是病灶分割。
- 生成结果必须保留真实标签、预测标签和癌概率，并能追溯原图顺序。
- 医学审核要同时看TP、TN、FP、FN，并记录反光、黏液、器械、暗腔、边缘和界面等
  伪特征。

### 5.3 MOCE

- 候选区域不等于概念；候选区域经特征聚类后才形成概念簇。
- 正式概念发现使用严格平衡概念集，两个模型使用同一冻结清单。
- SSC/SDC的黑色基线是分布外输入，step0首先反映黑图类别偏置，不能直接解释为医学
  概念一致性或癌形态异质性。
- 概念重要性、覆盖率、条件概率下降和医生观察必须分开报告。
- K-Means数量是敏感性变量；不能仅因某个K的自动指标更高就宣布概念更医学化。

### 5.4 SAE

- 当前正式ResNet50 SAE输入是layer4全局平均池化后的2048维特征。
- 编码器为`Linear + ReLU`，解码器重构回2048维，以便复用原分类头并评价保真度。
- 当前基线字典为512维，损失为逐元素MSE加`lambda * L1`；患者平衡采样保留。
- SAE超参数、剪枝和概念筛选只能由训练/验证集确定；内部test和外部test只能使用锁定
  SAE投影。
- SAE Feature是模型内部方向，不自动等同医学概念。命名必须结合跨患者Top图、空间
  响应、分类贡献、消融、来源相关性和临床复核。
- 图像中心彩色结果中，红色表示促癌logit margin贡献，蓝色表示负向贡献；颜色不是
  像素级癌概率，7x7响应上采样也不是病灶分割。

## 6. 已确认的重要阶段成果

以下仅保留影响长期路线的里程碑；完整数字和当时讨论见正式报告及实时进度文档。

### 6.1 第一批图像随机划分基线

- 该阶段证明两类CNN和完整训练、阈值、推理流程可以运行。
- 因划分按图片而非患者进行，且原数据存在标签和划分问题，结果只作为历史流程基线，
  不再作为正式模型或与后续患者级结果直接比较。

### 6.2 第二批整理后未裁剪旧模型

- 患者级划分修复了第一批最直接的同患者泄漏风险，但输入仍保留黑边、设备界面、原始
  分辨率和来源风格。
- 约0.99的内部AUC受来源构成显著影响，不能解释为跨医院病灶识别能力；相关权重只用于
  裁剪前后诊断对照和历史解释。

### 6.3 数据偏倚与v1.1去偏重训练

- 第二批整理后未裁剪数据存在明显来源、分辨率、设备界面和患者图片数量混杂。
- 旧模型直接输入v1.1裁剪图时性能显著下降，证明模型对构图和边缘信息敏感，但该结果
  不能单独定位某一种shortcut。
- A/A0/B/C/D实验矩阵已经完成；不同协议结果不能混作同一泛化结论。

实验A内部患者AUC约0.93，严格共同支持OOF约0.8；完整与松弛数据的内部高AUC仍需
结合残余混杂解释，严格集则是控制已知画面因素后的敏感性分析。完整结果见实时进度文档。

### 6.4 集成与独立外部测试

- 验证集锁定的正式方案为ResNet50与EfficientNet-B0等权集成、`top2_mean`聚合、
  阈值0.518973且不使用TTA。
- 集成在内部冻结test的患者AUC为0.9578，在独立外部多中心集为0.802，存在明显域偏移。
- 外部癌患者图片数显著多于非癌患者，患者聚合收益必须与mean和随机单图敏感性分析
  一起披露。

### 6.5 MOCE

- 严格平衡概念集为314位患者、157对、502张图，两类各251张。
- ResNet50与EfficientNet-B0均已完成正式MOCE、自动分析和医学生材料。
- `K=10,15,...,50`的双模型、双类别聚类数量敏感性分析已完成。
- 两模型共同趋势是癌侧少数概念对输出贡献更集中，非癌侧单概念影响较弱；医学含义仍
  需临床审核，不能仅靠黑色遮挡SSC/SDC定论。

### 6.6 SAE

- 正式ResNet50 SAE使用3704张训练图、1624位训练患者，验证集793张、348位患者。
- 结构为`2048 → 512稀疏激活 → 2048`，`lambda_l1=5e-4`，最佳epoch为104。
- 512个Feature剪枝后保留450个；内部验证患者AUC由原模型0.913变为0.903。
- 锁定SAE在内部test的原模型/完整SAE/剪枝SAE患者AUC为
  0.9283/0.9247/0.9234，剪枝后下降0.0049；同一锁定阈值下患者Sensitivity由
  0.8889降至0.8254，说明排序保真较好但分类阈值存在漂移。
- 独立外部患者AUC由原ResNet50 0.69336变为剪枝SAE 0.69292，说明外部患者级排序
  保真度较高，但解释对象ResNet50自身的外部泛化能力有限。
- train/val预选的30个跨数据集候选Feature中，21个在train、val、内部test和外部test
  保持相同标签激活方向；癌相关15/15一致，非癌相关6/15一致。该结果只表示统计稳定性，
  仍需空间响应和临床语义审核。
- 外部1941张图已完成图像中心Top-5彩色SAE可视化，并整理100位患者的医学生提交版。

### 6.7 0804新数据全量M0诊断

- 冻结开发集为3348张、1732位患者；图片标签近似平衡，但患者标签为1286非癌对
  446癌，因此本阶段只用于全量诊断，不替代来源内平衡主实验。
- Keep/Notch、ResNet50/EfficientNet-B0和3个种子的12组正式val矩阵已完成；模型、
  阈值和预处理选择均未使用internal test或外部test。
- 全量阶段暂选Keep与EfficientNet-B0。Keep EfficientNet-B0跨种子患者AUC为
  `0.9091±0.0041`，患者Accuracy为`0.7885`；图片AUC为`0.8485±0.0090`，图片
  Accuracy为`0.7606`。完整Sensitivity、Specificity和逐种子结果见
  `实验进度与结果讨论.md`。
- Notch的平均AUC与Keep接近，但最差种子的患者Specificity下降超过预注册门槛；因此
  暂不进入下一轮主训练。上述骨干与预处理选择仍须由来源内平衡M0确认，不能据此声明
  已消除来源或风格shortcut。
- Keep锁定模型已在独立外部多中心集1329人/1941张上完成3种子评估。EfficientNet-B0
  患者AUC为`0.7399±0.0075`、图片AUC为`0.7238±0.0079`，患者/图片Accuracy为
  `0.6353/0.6452`；ResNet50患者AUC为`0.7158±0.0205`、图片AUC为
  `0.6885±0.0228`。外部明显低于内部val，但EfficientNet-B0仍较优且更稳定；该结果只
  评价泛化，不得反向用于模型或阈值选择。
- 下一轮来源内平衡M0的主比例为患者级癌:非癌约`1:1.3`，严格`1:1`只作为最终入选
  骨干的敏感性分析；两者都必须限制每患者图片数并在来源内部匹配主要画面风格。
- 来源内1:1.3主清单已冻结为1373张/1020人，癌:非癌患者446:574、图片597:776；
  患者/图片风格TV为0.0192/0.0461。严格1:1敏感性清单为1246张/892人。正式平衡M0
  的6组val矩阵已完成，且未评估internal test。EfficientNet-B0跨种子患者AUC为
  `0.8523±0.0070`、图片AUC为`0.8187±0.0098`，患者/图片Accuracy为
  `0.7734/0.7283`；ResNet50对应为`0.8273±0.0092`、`0.7946±0.0115`和
  `0.6667/0.6550`。按预注册val规则，M1-M5主骨干锁定为EfficientNet-B0，ResNet50
  保留为受控对照。独立外部多中心评估中，EfficientNet-B0患者/图片AUC为
  `0.6807±0.0107/0.6563±0.0131`，ResNet50为`0.6828±0.0193/0.6569±0.0132`；
  平衡训练未改善此前全量模型的外部AUC，说明域偏移仍明显。外部评估只用于泛化报告，
  不得反向改变val骨干选择或阈值。

### 6.8 YOLO26专用病灶定位

- 来源内平衡主实验已冻结`YOLO26s-640`，Balanced seed42/202/503三种子均通过预注册
  验证门槛；internal test和external在该阶段均未读取。
- 在val癌图Sensitivity固定为`0.9080`时，三种子mean IoU平均由同seed M1的约`0.442`
  提高到`0.563`，IoU>=0.50比例平均由约`0.402`提高到`0.690`，正式结论为
  `stable_success_3_of_3`。因此专用检测器相较轻量M1定位头具有稳定的框几何优势。
- 同一阈值下非癌图触发框率仍平均约`0.711`；YOLO26解决的是癌图框几何质量，不代表
  非癌展示特异性已经解决。完整集Y3-F仍是预注册诊断分支，不能反向修改Balanced方案。
- 完整集Y3-F诊断三种子同样`3/3`通过：在各自val癌图Sensitivity固定为`0.9000`时，
  mean IoU平均由同seed M1-F约`0.462`提高到`0.634`，IoU>=0.50比例由约`0.447`
  提高到`0.768`；非癌触发率平均约`0.495`。Y3-F使用不同且更大的开发队列，只能作为
  更多框监督的诊断证据，不能与Balanced作严格配对优劣结论或反向选参。
- Y4已在同一`198张/153人`锁定内部test上一次性揭盲。Balanced三seed的IoU50相对M1
  平均提升`+0.233`，患者bootstrap 95%CI为`[+0.148,+0.322]`，但seed42/202的
  Sensitivity差值为`-0.023/-0.093`，未通过预注册`>=-0.02`安全门槛，因此Y4主检验
  正式未通过。Full诊断线三seed均通过，IoU50平均提升`+0.252`且Sensitivity平均
  `+0.031`；它支持“更多框监督可能提高稳定性”，但不能补救Balanced主检验。
- Y4之后该内部test已消耗，禁止用其继续选模型、阈值、epoch、后处理或融合方案；按原
  预注册Y5/Y6原主线不再成立。用户随后明确决定以Full三seed启动Y4后探索路线：继续使用
  各seed自身val冻结阈值，不按Y4/external删选seed或返调模型；Y5外部无bbox，只能报告
  检测响应，不能宣称外部定位准确率。完整协议、逐seed结果和后续边界见
  `YOLO26实验进度与结果讨论.md`；Y4正式产物见
  `结果/YOLO26定位_0804/Y4锁定内部测试_20260814/`。
- Full探索路线Y5已投影到外部`1941张/1329人`：三seed癌患者任一图触发率为
  `0.893-0.942`，非癌患者误触发为`0.487-0.560`；患者级三seed多数触发的癌/非癌率为
  `0.926/0.537`。外部无bbox，这些数字只能说明是否产生候选框，不能解释为外部病灶
  定位Sensitivity或Specificity，也不能据此选择seed503或重定阈值。

## 7. 文件操作与代码修改规则

### 7.1 文件安全

- 禁止批量删除数据、结果、脚本或目录。
- 整理时优先移动到明确的`归档/`，不得以“无关”为由直接删除。
- 原始数据、冻结manifest、权重、正式CSV/JSON和历史结果不得覆盖。
- 正式运行使用独立输出目录；目标目录已存在时先停止核对。
- 修改清单路径、患者ID、标签、split、阈值、聚合或匹配关系前，必须说明影响并验证
  行数、患者数、标签分布和SHA关系。

### 7.2 代码修改

- 优先沿用现有模块和局部实现，不主动进行与任务无关的大规模重构。
- 核心逻辑必须保留显式校验，避免数据泄漏、患者跨split、阈值错用、空候选和权重错载
  等静默错误。
- 不为已固定本地环境堆叠多层无意义回退，但依赖、路径或字段错误会影响实验正确性时
  必须快速失败并给出明确提示。
- 训练、评估、Grad-CAM、MOCE、SAE和M-CBM等主要正式代码必须在模块开头说明整体
  用途，并为每个主要函数和类补充简洁的职责说明（优先使用docstring）；说明应概括
  输入、输出、实验角色或关键边界，便于逐模块审查，避免只复述单行代码的无效注释。
- 主要函数docstring还必须简要列出每个输入参数的名称、类型和作用，以及返回值的类型
  和含义；无参数或无返回值时明确写明。涉及张量、bbox、特征图或概率时，应注明关键
  shape、坐标范围、单位或统计层级。类说明至少覆盖构造参数、主要状态和`forward`输出。
- 复杂流程应按“数据与变换、模型结构、目标与损失、评估与可视化、训练与输出”等职责
  分段；关键数学定义、冻结边界、监督掩码和防泄漏约束必须能从代码注释中定位。
- 正式代码放入对应`正式代码/`；一次性检查和历史实现放入`程序/归档/`或适当过程目录。

### 7.3 GPU使用

- 每次运行GPU任务前先执行`nvidia-smi`，检查显存、利用率和正在运行的进程，再选择
  当时空闲的物理GPU。
- 不长期假设某个固定GPU始终可用，不终止或干扰其他用户的任务。
- 每次向用户提供GPU运行命令前，也必须在该时点重新检查GPU空闲情况；命令中的
  `CUDA_VISIBLE_DEVICES`或`CUDA_DEVICE`应使用刚确认的空闲物理编号，或明确写成待替换
  占位符。禁止因为此前使用过1号卡就在后续命令中继续默认写1。
- 使用`CUDA_VISIBLE_DEVICES=<物理GPU编号>`限制可见设备；映射后程序中的`cuda:0`
  表示第一张可见GPU，不等于物理0号卡。
- 长时间全量任务必须先完成少量样本debug并验收输出；用户明确自行运行时，只提供
  已验证命令，不擅自启动任务。
- 提供训练、MOCE、SAE、批量热图等长任务运行命令时，必须同时提供可直接执行的实时
  日志监测命令，并明确日志文件路径；Python长任务使用`-u`或`PYTHONUNBUFFERED=1`，
  启动脚本使用`tee`或等价方式持续落盘，避免终端有输出但日志为空。
- 实时监测优先给出`tail -F <log>`，必要时同时给出GPU状态和进程检查命令。说明
  `Ctrl+C`退出`tail/watch`只停止监视，不会停止另一个终端或后台运行的实验；若运行
  命令本身在当前前台终端执行，则`Ctrl+C`会终止实验，二者必须区分。

### 7.4 Git

- 未经用户明确要求，不提交、不推送、不改写历史。
- 提交前检查患者数据、大图片、特征缓存、权重和本地结果是否被错误纳入。
- 不撤销用户已有修改；遇到并行修改时先读懂并在其基础上工作。

## 8. 结果解释与汇报规则

每轮正式工作结束后，按以下顺序汇报并写入实时进度：

1. 做了什么：输入、冻结参数、改变变量、脚本和输出位置；
2. 结果如何：完成量、失败量、关键指标、自动检查和人工复核状态；
3. 能得出什么：区分已确认结论、候选判断和不能下的结论；
4. 对后续的影响：是否通过、是否冻结、是否需要补跑；
5. 下一步：立即任务、预计产物和决策标准；
6. 文件情况：新增或修改内容、是否覆盖源文件、验证是否通过。

汇报时必须区分：

- 训练、验证、内部test和独立外部test；
- 图像级和患者级；
- 自动指标和临床人工判断；
- 分类性能和解释保真度；
- 相关性证据和因果结论；
- 正式结果、敏感性分析和历史探索。

## 9. 文档维护规则

- `AGENTS.md`轻易不改；只有长期规则、稳定事实、正式入口或重要里程碑改变时更新。
- 当前阶段的实时文档为`YOLO26实验进度与结果讨论.md`；提示词始终位于当前进度文档
  开头且不写具体即时进度。`实验进度与结果讨论.md`已经结束维护，只作历史查询。
- 完整阶段表格和总结集中保留在对应阶段进度文档；`AGENTS.md`只保留稳定结论，
  不重复粘贴完整表格或逐次运行日志。
- 旧结论被推翻时保留原结论及日期，新增“已修正”说明和证据，不静默改写历史。
- 新对话先读`AGENTS.md`和当前阶段进度文档，再按需读取历史进度、相关代码、配置和正式结果；不要默认
  对话摘要比本地文件更新。
