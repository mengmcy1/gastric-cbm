# 项目协作说明

## 1. 文件与文档操作规则

### 1.1 文件安全

禁止批量删除文件或目录，不得使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

确需删除时，只能一次删除一个明确路径的文件，例如：

`Remove-Item "C:\path\to\file.txt"`

如果需要批量删除，应停止操作并请求用户手动处理。目录整理优先采用移动或归档，不得随意删除。

### 1.2 文件生成与目录整理

后续创建、下载或整理的项目文件，默认放在当前 `2D转3D` / `2Dto3D` 仓库中，不依赖固定用户名或桌面绝对路径。

- 最终交付文档、老师提供的材料、论文目录和项目说明可以保留在根目录或既有业务目录。
- 过程脚本、下载报告、渲染缓存和临时检查文件统一放入 `99_过程文件_可删除或归档/`。
- 过程文件按用途放入 `生成脚本`、`下载与检查报告`、`渲染检查缓存`、`临时文件` 等子目录。
- 新建资料或数据目录时使用清晰名称，如 `01_文献调研`、`02_方案设计`、`03_实验记录`。
- 不把项目文件散落到桌面、下载目录或其他无关位置。

`99_过程文件_可删除或归档/` 专门保存不需要用户后续实际运行的过程性内容，例如验证/检查脚本、Word 或 PPT 编辑脚本、渲染检查脚本、一次性下载脚本及临时缓存；该目录整体仅本地保留。

正式、可复现的 SHARP 浅 3D 实验代码应随实验包保存于 `03_实验记录/SHARP浅3D实验/scripts/`；冻结输入、清单和小型配置保存在同一实验包中，所有渲染图、视频、深度、Alpha、指标等大输出统一保存于 `03_实验记录/SHARP浅3D实验/outputs/` 并忽略提交。

### 1.3 文档写入审批

创建或修改 Markdown、Word、PDF、PPT、Excel 等说明性文档或交付文档前，必须先向用户提交拟写入的正文或内容提纲。

只有用户明确表示“确认”“可以写入”或作出同等授权后，才能实际写入。如果用户提出修改意见，应更新草稿并再次提交审阅。

代码和配置文件是否需要提前审阅，根据用户当次要求判断。

### 1.4 已生成 PPT 的局部迭代例外

对于已经生成的 PPT，如果用户已经明确指出需要修改的页面、文字、布局、配色或素材，可以直接修改，无需再次等待内容审批。

完成后必须逐项说明实际改动、检查结果和输出路径。

新建 PPT、重做整体结构、显著改变汇报逻辑，或用户明确要求先审阅时，仍须遵守文档写入审批规则。

### 1.5 长期规则与实时进度文档分工

`AGENTS.md` 是项目长期协作规则和稳定事实基线，主要记录：

- 项目背景、目标和长期技术路线；
- 文件安全、文档审批、代码风格、提交、删除和目录要求；
- 论文带读与实验协作的统一方法；
- 已经验证的重要阶段性成果；
- 不应随单次实验频繁变化的稳定事实。

具体任务的实时进度文档是该任务的即时交接记录。当前 SHARP 实验使用：

`03_实验记录/SHARP浅3D实验/实验进度与结果讨论.md`

实时进度文档主要记录：

- 当前实验阶段和即时进度；
- 每轮输入、配置、产物和结果；
- 自动指标、人工观察、异常和待复核事项；
- 候选结论、冻结结论及其证据；
- 下一步立即执行的任务；
- 帮助新对话恢复项目上下文的固定提示词。

每轮实验的琐碎运行记录不写入 `AGENTS.md`。只有长期规则、正式目录、统一实验口径、工具稳定能力或重要阶段性成果发生变化时才更新 `AGENTS.md`。

实时进度文档应随任务持续更新并保留历史，不得用尚未确认的候选结论覆盖旧记录。若两份文件表述不一致，文件安全和协作约束以 `AGENTS.md` 为准；实际任务进度和即时安排以正式输出及实时进度文档为准。

## 2. 项目目标与沟通方式

### 2.1 课题目标

**课题名称**：单张 2D 图像转浅 3D 表达 / 单图新视角合成。

**核心目标**：输入一张普通 2D 照片，生成可在移动端通过小幅晃动手机或滑动屏幕产生立体感、视角变化和运动视差的“浅 3D 表达”，效果类似 Apple 3D 照片或微信透视窗。

“浅 3D”不要求完整 360° 重建；当前质量研究目标为水平总视角范围 30°，即中心左右各 ±15°（不保证所有场景都能达到）。隐藏区域追求视觉合理与跨视角一致，不宣称真实恢复。

完整管线：

`单张图片 → 深度图 → 3DGS / 三维点云 / 分层表示 → 附近新视角渲染`

主要约束：

- 输入只有一张普通照片，没有多视图和已知相机位姿。
- 最终面向移动端部署。
- 交互渲染阶段不依赖 GPU 网络推理。
- 课题要求图片位于根目录 `课题要求.png`。

### 2.2 面向初学者的沟通要求

用户刚开始接触深度学习、三维视觉和神经渲染，协助时必须：

- 专业术语首次出现时，先用通俗语言建立直觉，再说明其在本项目中的作用。
- 缩写首次出现时给出中文名称、英文全称和缩写。
- 对复杂概念使用比喻、流程、示意或简单例子，不默认用户已经理解深度估计、相机模型、MPI、LDI、Mesh、NeRF、3DGS、去遮挡和新视角合成。
- 技术路线从基础到进阶组织，说明每种方法解决的问题、输入输出、基本原理、优缺点及其与本课题的关系。
- 推荐论文时说明重要程度、阅读难度、建议阅读章节和开源情况。
- 不只罗列名词或论文，应帮助用户建立方法之间的联系。

### 2.3 实验开始前的讲解要求

进行 SHARP 实验时，每一轮开始执行前，必须先向用户简要说明：

1. 本轮实验要解决什么问题。
2. 本轮使用什么输入，会生成什么输出。
3. 为什么当前阶段需要做这项实验。
4. 本轮通过后将进入哪一步。

讲解应先使用形象、直观的类比建立理解，再补充必要的技术参数。例如，实验工具冒烟测试可以解释为“正式测量前，先校准尺子、相机和记录表”。

除非用户要求暂停或确认，完成简要讲解后可以继续执行已经确定的实验计划。执行结束后应说明实际结果、发现的问题以及它对下一轮参数选择的影响。

### 2.4 Review、实验与汇报的统一工作方式

后续研究采用“Review 提出问题 → 公开证据形成假设 → 本地实验验证 → 失败案例界定边界 → 汇报形成结论”的闭环方式。

- Review 不能只罗列论文。每项技术应说明前置方法、原有问题、核心创新、实验依据、局限以及与本课题的关系。
- 实验证据分为论文公开结果、本地官方代码复现和自建 Benchmark 三类，三者必须分别标注，不能混用评价口径。
- 引用论文性能时必须同时记录数据集、输入分辨率、硬件、指标定义、裁剪或掩码规则、是否排除失败样本，以及是否包含预处理和后处理。
- 自建实验必须明确研究问题、基线、唯一改变变量、输入清单、自动指标、人工评价、失败案例和结论边界。
- 后续汇报应优先展示技术演变路径、方法对比、实验结果、失败案例和对下一步的影响，避免只有流程框图而缺少证据。

课题提出的端到端生成时间小于 5 秒、峰值内存小于 3 GB，当前作为长期产品目标和后续优化方向，不作为 Review、先导实验和基础 Benchmark 的阻塞性通过条件。

- 当前阶段优先获得完整、可复现和可分析的实验结果，包括质量、时间、内存、失败案例和瓶颈拆分。即使尚未达到最终指标，也应保留真实结果并说明差距，不得因性能不足而删除失败实验或停止质量评价。
- 性能报告必须区分冷启动、模型加载、预处理、网络前向、CPU/GPU 后处理、PLY 读写、GPU 上传、纯渲染和视频编码，并报告当前值、目标值、差距及相对基线改善。
- 不得使用不同硬件、分辨率和计时边界下的论文数字直接宣称已经达到产品要求。
- 当基础表示、质量参数和 Benchmark 基本稳定后，再依次研究模型蒸馏、FP16/INT8、后处理加速、高斯剪枝与量化、移动端渲染和内存复用，逐步逼近最终工程目标。

## 3. 当前核心任务：SHARP

### 3.1 当前目标与优先级

当前研究采用两条并行主线：

1. 以 Apple SHARP（Single-image High-Accuracy Real-time Parallax）为核心，完成可复现的质量、运动、裁剪、压缩和性能 Benchmark，建立失败案例并分析质量与工程瓶颈。
2. 系统梳理单图 3D 照片从 LDI、MPI、TMPI 到前馈 3DGS/SHARP 的技术演变，并分析轻量表示和移动端部署路线。

当前核心任务已从“继续完成 SHARP Benchmark”转为**自研可补全轻量 3DGS Framework**，方案以 `02_方案设计/总体Framework讨论总结_自适应3DGS.md` 第 14 章“30° 显露补全优先版”为准。SHARP Benchmark v1 已冻结，仅作为对照基线；不再把 SHARP 性能优化作为当前主线。

下一篇重点精读论文为：

`文献/02_单图3D照片与分层表达/05_TMPI_Tiled_Multiplane_Images_for_Practical_3D_Photography.pdf`

TMPI 用于理解“局部自适应分层表示”如何减少全局 MPI 的深度冗余，并作为压缩 3DGS 之外的移动端候选路线。论文 Review 与 SHARP 实验可以并行推进，但不得因为文献方向扩展而破坏 SHARP 既定的单变量实验顺序。

当前本地文献新增分类包括：

- `文献/07_3DGS压缩剪枝与结构化/`
- `文献/08_移动端渲染与部署/`
- `文献/09_前馈生成与显露区域补全/`

Framework 中的材质感知模块暂不作为当前主链路或近期实验变量。反射、透明、水面、高光和夜景仍作为场景标签与失败案例保留，但当前优先分析深度、遮挡、显露区域、表示精简和渲染稳定性问题。只有在基础 Benchmark 完成、材质相关问题被证明具有较高发生率，并且能够明确材质模块的输入、输出、评价指标和独立消融方式后，再考虑恢复材质感知分支。

### 3.2 SHARP 事实基线

- 论文：`文献/04_单图与前馈3DGS/09_SHARP_Sharp_Monocular_View_Synthesis.pdf`
- 版本：arXiv:2512.10685v2，2025 年 12 月首发，2026 年 2 月 27 日更新 v2。
- 源码：`源码/SHARP_APPLE注释/`
- 事实依据优先级：本地论文正文 > 实际可执行源码 > 本地复现结果 > Word 总结。
- 官方代码和权重已公开；权重由 Apple CDN 提供，采用 Apple 研究用途许可证。
- 官方仓库主要包含模型与推理代码，没有公开完整训练循环、训练数据集和损失实现。
- 发布模型默认 `sorting_monodepth=False`，双层深度没有硬性近远排序。
- 第一层主要表示可见表面；第二层没有隐藏深度真值，主要通过新视角渲染损失、感知损失、Alpha 损失和 TV 正则学习。
- Gaussian Decoder 最终输出头分为位置 3 维，以及尺度 3、旋转 4、颜色 3、不透明度 1，共 11 维。
- SHARP 在归一化设备坐标（Normalized Device Coordinates, NDC）中预测，但仍使用焦距和相机内参完成尺度转换及反投影。
- SHARP 使用固定 RGB，等价于零阶球谐，不使用高阶球谐。
- 论文按相机基线描述运动范围，目标主要是小于 0.5 米的附近视角，不换算成固定角度。
- SHARP 专题 PPT 必须复制现有模板生成新文件，不覆盖模板；正文采用简洁学术表达，可保留少量有助于理解和现场讲解的自然语言。

### 3.3 已完成工作

已完成：

- SHARP 方法总览和重点章节精读，包括完整数据流、Depth Pro、双层深度、Depth Adjustment、Gaussian Initializer、Gaussian Decoder、Composer、NDC 反投影、训练策略、损失、实验、运动范围和失败案例。
- 官方源码预测主链路与关键模块梳理。
- 独立 Conda 环境 `sharp` 配置；当前机器仅暴露物理 GPU 0（RTX 5060 Laptop GPU），进程内为 `cuda:0`。执行前必须以环境基线为准，不得擅自重装或污染其他环境。
- 官方权重加载、单图预测、PLY 导出、彩色视频和深度视频渲染。
- SHARP 专题 PPT 和讲稿初稿制作，当前处于检查与迭代阶段。

权重路径：

`99_过程文件_可删除或归档/临时文件/SHARP环境验证/torch_cache/hub/checkpoints/sharp_2572gikvuh.pt`

正式实验以 `03_实验记录/SHARP浅3D实验/00_方案与表格/environment_baseline.txt` 中记录的权重 SHA256 为准；当前源码 `checkpoints/` 目录未保存该权重。

复现输出目录：

`99_过程文件_可删除或归档/SHARP复现测试/`

当前实测口径：

- 输出约 `2 × 768 × 768 = 1,179,648` 个高斯。
- 单个 PLY 文件约 64 MB。
- 网络前向约 1.26 秒，CPU 后处理约 4.23 秒，冷启动预测约 28 秒。
- 首次 60 帧渲染约 91 秒，其中 gsplat CUDA 扩展初始化约 77.6 秒。
- 热启动 60 帧渲染约 13.98 秒，官方 CLI 端到端约 4.29 FPS。
- 论文约 100 FPS 是高斯常驻 GPU 时的纯渲染指标，不与包含加载、CPU 处理、设备搬运和视频编码的 CLI 速度直接比较。

### 3.4 当前瓶颈与下一步

主要工程瓶颈：

- `unproject_gaussians()` 对约 118 万个协方差矩阵执行 CPU float64 SVD。
- 官方 CLI 在每一渲染帧调用 `gaussians.to(device)`。
- 首次运行需要编译或加载 gsplat CUDA 扩展。

下一步：

1. 按“角度协议 → 显露补全 → 补充高斯 → 双端点一致性 → 细结构 → 自研基础表示 → Gate 与压缩 → 移动端”顺序推进自研 Framework；第一轮为 Stage 0+1 单变量 A/B（固定同一基础高斯，仅加显露补全与补充高斯，比较 5°/15°/30°）。
2. 第一轮不修改 SHARP 官方源码，通过适配器调用冻结 SHARP 基线。
3. 可复用的自研模型与核心库放 `源码/Adaptive3DGS/`；按用户要求，Stage 0+1 的正式实验脚本、配置和清单保存在 `03_实验记录/自研Framework实验/01_Stage0_1_角度与显露补全/`，大型输出、模型权重和运行日志仅本地保留，不上传 GitHub。
4. 5°/15°/30° 均指总扫视角；保留小于 5 秒、小于 3 GB 为长期目标，不阻塞第一阶段质量验证。
5. 在基础结果完整后，再比较移动端路线（压缩 3DGS 的 Metal/Vulkan/OpenGL ES/WebGPU 渲染，或蒸馏为 Mesh/LDI 等轻量表示）。

### 3.5 SHARP 浅 3D 实验当前进度

实验执行方案：

`SHARP实验TodoList与执行方案.docx`

实验最终需要形成四项结论：

1. 最大可接受屏幕视差。
2. 推荐四边单边安全裁剪比例。
3. 推荐高斯保留率。
4. SHARP 适用与不适用的场景范围。

实验结果目录：

`03_实验记录/SHARP浅3D实验/`

正式可复现实验脚本目录：

`03_实验记录/SHARP浅3D实验/scripts/`

`99_过程文件_可删除或归档/` 只保存无需用户实际运行的临时验证、检查及 Word/PPT 编辑脚本，不作为正式实验入口。

当前实验工具版本为 v2.1，已完成：

- 独立于 SHARP 官方源码的参数化三视图渲染。
- RGB、浮点 3DGS 渲染深度和 16 位累计 Alpha 导出。
- 按实际渲染分辨率统计高斯投影位移，P95 为主，保留 P99、最大值及画宽占比。
- 有效高斯同时要求中心/当前投影在实际画布内、中心/当前相机空间深度为正且位移有限。
- 静止重渲染差异图、PSNR、SSIM、LPIPS 及 Alpha 掩码指标。
- 完整 60 帧零运动/非零运动接口，逐帧 Alpha、相邻帧 SSIM、自动指标 CSV 和人工标记模板。
- H.264/yuv420p 视频编码，记录编码后实际分辨率和帧数；输出目录默认防覆盖。
- 四边单边 0%、3%、5%、10% 后裁剪并恢复显示尺寸。
- 正式剪枝工具支持按不透明度降序保留 100%、75%、50%、25% 高斯，应用冻结的 3% 单边裁剪并恢复显示尺寸，额外输出严格零位移静止帧、60 帧彩色/深度视频及 `2.1-prune` 人工评分模板。
- SHARP 微调后的两层单目深度和两层视差诊断导出。
- 当前使用物理 GPU 0，进程内显示为 `cuda:0`。
- 没有修改 SHARP 官方源码，没有覆盖原有真实图片测试结果。

已冻结 P01～P05 五张先导图片及对应完整 PLY，并记录输入/PLY SHA256。环境、权重和 LPIPS 已验证；五样本静止基线已经完成。

五样本正式静止基线结果：

- P01：PSNR 26.83 dB，SSIM 0.870，LPIPS 0.0268，Alpha≥0.99 覆盖率 96.53%。
- P02：PSNR 32.68 dB，SSIM 0.949，LPIPS 0.0132，Alpha≥0.99 覆盖率 99.47%。
- P03：PSNR 25.98 dB，SSIM 0.921，LPIPS 0.0192，Alpha≥0.99 覆盖率 96.52%。
- P04：PSNR 36.84 dB，SSIM 0.970，LPIPS 0.0198，Alpha≥0.99 覆盖率 99.89%；其输入分辨率为 810×1080，横向比较时必须单独标注。
- P05：PSNR 24.68 dB，SSIM 0.922，LPIPS 0.0193，Alpha≥0.99 覆盖率 95.02%；反射和近似平面深度是后续运动压力点。

工具校准 v2.1 已通过：P04 编码视频保持 810×1080；P01 在 `max_disparity=0.02` 的 3 帧短测试中左右端 P95 均约 43.10 px，占 2048 画宽约 2.10%。该短测试只验证统计与编码口径，不作为正式运动结论。

五样本四档正式运动矩阵已经完成：固定完整高斯、0%裁剪和 swipe 轨迹，共20个配置、1200帧。彩色/深度视频均为60帧，编码尺寸与渲染尺寸一致；汇总位于 `outputs/03_运动实验/motion_results.csv` 和 `motion_results.json`。

正式运动结果的端点 P95 画宽占比：

- P01：0.02/0.04/0.08 分别约 2.10% / 4.17% / 8.23%。
- P02：约 1.75% / 3.51% / 7.00%。
- P03：约 1.42% / 2.84% / 5.62%。
- P04：约 1.92% / 3.84% / 7.65%；其分辨率为 810×1080。
- P05：约 1.59% / 3.16% / 6.24%。

五样本四档运动矩阵和 20 个视频人工评分已经完成。`max_disparity=0.04` 于 2026 年 7 月 21 日正式冻结，五样本最低整体质量分为 2；`0.02` 为保守安全档，`0.08` 不适合作为统一默认值。相同 `max_disparity` 在不同样本上的实测 P95 不同，最终报告仍应同时给出像素值和占实际渲染画宽的比例。

早期校园建筑 PSNR 28.38 dB、`max_disparity=0.04` P99 约 94～95 px 等数据属于旧工具冒烟结果，其中投影统计曾使用 PLY 原始画布边界；只保留作历史校准记录，不得混入正式参数结论。

当前尚未完成：

- 规范化性能测试和显存测量。
- 10～20 张独立验证和最终移动端路线决策。

已冻结参数：`max_disparity=0.04`（2026-07-21）、`crop_single_side_percent=3`（2026-08-01）、`gaussian_keep_percent=100`（2026-08-03）。

阶段 4 正式剪枝矩阵和 20 个视频的完整人工评分已完成：固定 `max_disparity=0.04`、`crop_single_side_percent=3` 和 `swipe60`，只比较 100%、75%、50%、25% 高斯保留率。五样本的 keep100 整体质量分均为 3，keep075 均为 1，keep050 和 keep025 均为 0。按预设的“所有样本整体质量分不低于 2”标准，已冻结 `gaussian_keep_percent=100`。这只说明按不透明度降序的 Top-K 剪枝在 75% 及以下未通过先导质量标准，不能推广为所有剪枝、合并或量化方法都无效。下一阶段固定三项参数进行规范化性能测试，不提前混合独立验证或新的表示优化变量。

屏幕视差正式统计应以统一定义的 P95 为主要指标，同时保留 P99 和最大值用于异常诊断。当前高斯投影位移属于初步代理指标，不能直接等同于图像特征光流或固定角度。

SHARP Benchmark v1 已定稿（2026-08-05，见 `03_实验记录/SHARP浅3D实验/10_最终结论/SHARP_Benchmark_v1.md`），作为冻结基线；后续只用于对照，不再做 SHARP 性能优化。

## 4. 论文阅读与总结流程

### 4.1 阅读前整体概览

每篇论文开始逐段带读前，先说明：

1. 论文解决的问题、核心方法和输入输出链路。
2. 3–5 个关键创新点，每点说清“是什么”和“与什么对比”。
3. 重点章节及优先级（⭐⭐⭐/⭐⭐/⭐），并说明原因。
4. 与本课题的匹配点、差异和可借鉴内容。

用户确认整体脉络清晰后，再进入重点精读。

### 4.2 重点精读与三步讲解法

对与本课题最相关的内容优先精读：摘要、方法总览、核心模块、关键公式、实验结论、失败案例和移动端浅 3D 相关讨论。

逐段讲解采用：

1. **原文翻译**：忠实翻译，不增不减。
2. **大意梳理**：说明该段在讲什么以及在全文中的作用。
3. **概念拆解**：用直觉、比喻或例子解释关键概念。

当用户希望直接跳到代码或深入某个问题时，可以跳过后续步骤。

阅读过程中发现可用于论文、开题报告、技术路线或汇报的内容，应整理成“可写入论文/报告的表述”；实际写入前遵守文档审批规则。

### 4.3 统一论文总结规则

不单独维护论文带读笔记。整篇论文读完后，将重点一次性汇总到根目录 `论文总结.docx`，每篇论文追加一节，不再为每篇论文新建统一总结文件。

当前 `汇报/论文总结_SHARP_7.17.docx` 和 `汇报/课题启发_SHARP_7.17.docx` 属于 SHARP 阶段材料，不替代长期统一总结文件。

每篇总结采用统一结构：

1. 动机与核心问题。
2. 方法核心：整体思路、结构、流程和输入输出。
3. 关键概念：术语的通俗解释。
4. 技术创新：3–5 个“是什么 + 和谁对比”的创新点。
5. 核心公式与设计：直观含义和关键参数。
6. 关键实验结论：消融、重要数据和对比结果。
7. 优势与局限。
8. 与本课题的关系：匹配度、可借鉴点、差异和后续衔接。

节首给出：作者 / 机构 / 会议或年份 / 重要性★ / 难度 / 开源 / 本地路径。

按照本规则写入根目录 `论文总结.docx` 时，不需要重复申请审批；用户可以随时要求查看或修改。

整篇尚未读完时，可将临时记录放入 `99_过程文件_可删除或归档/临时文件/`，汇总后再归档；删除仍须遵守文件安全规则。

## 5. Git 与 GitHub 规则

### 5.1 提交约定

用户说“提交”即表示执行 `commit + push`：先提交，再立即推送，不需要额外确认。

仓库地址：

`https://github.com/mengmcy1/2Dto3D.git`

### 5.2 推送前检查

每次推送前必须：

1. 检查 `.git` 是否相对既有基线异常增长；如果本次操作导致明显增长，应排查是否误追踪大文件。
2. 检查未被 `.gitignore` 忽略且大于 5 MB 的文件是否合理。
3. 确认 remote URL 不包含 `ghp_...` 等明文令牌。

当前已知可以合理提交的大文件包括根目录的 `Review of ICCV 2025 Papers on 3D Gaussian Splatting.pptx` 以及 `汇报/` 中的含图文档和图片；仍应逐次核对。

### 5.3 严禁上传的目录

以下目录只保留在本地，不得从 `.gitignore` 中移除，也不得上传 GitHub：

- `papers/`
- `99_过程文件_可删除或归档/工具/`
- `99_过程文件_可删除或归档/渲染检查缓存/`
- `99_过程文件_可删除或归档/下载与检查报告/`

新电脑需要这些文件时，应通过 U 盘、网盘或重新下载获取。

### 5.4 认证与历史重写

- 认证优先使用 Windows 凭据管理器（Git Credential Manager），不得把令牌写入 remote URL、脚本或文档。
- 如果推送认证失败，应引导用户重新完成 GitHub 认证，不得输出或保存用户令牌。
- 发现大文件已进入远程历史时，先报告影响和拟执行方案，并获得用户确认。
- 历史清理和强制推送属于破坏性操作；未经明确确认不得执行。
- 历史改写后，其他本地副本需要重新 `git fetch` 并同步。
