# 项目协作说明

## 1. 文件与文档操作规则

### 1.1 文件安全

禁止批量删除文件或目录，不得使用：

- `del /s`
- `rd /s`
- `rmdir /s`
- `Remove-Item -Recurse`
- `rm -rf`

确需删除时，只能一次删除一个明确路径的文件，例如：

`Remove-Item "C:\path\to\file.txt"`

如果需要批量删除，应停止操作并请求用户手动处理。目录整理优先采用移动或归档，不得随意删除。

### 1.2 文件生成与目录整理

后续创建、下载或整理的项目文件，默认放在当前 `2D转3D` / `2Dto3D` 仓库中，不依赖固定用户名或桌面绝对路径。

- 最终交付文档、老师提供的材料、论文目录和项目说明可以保留在根目录或既有业务目录。
- 过程脚本、下载报告、渲染缓存和临时检查文件统一放入 `99_过程文件_可删除或归档/`。
- 过程文件按用途放入 `生成脚本`、`下载与检查报告`、`渲染检查缓存`、`临时文件` 等子目录。
- 新建资料或数据目录时使用清晰名称，如 `01_文献调研`、`02_方案设计`、`03_实验记录`。
- 不把项目文件散落到桌面、下载目录或其他无关位置。

`99_过程文件_可删除或归档/` 专门保存不需要用户后续实际运行的过程性内容，例如验证/检查脚本、Word 或 PPT 编辑脚本、渲染检查脚本、一次性下载脚本及临时缓存；该目录整体仅本地保留。

正式、可复现的 SHARP 浅 3D 实验代码应随实验包保存于 `03_实验记录/SHARP浅3D实验/scripts/`；冻结输入、清单和小型配置保存在同一实验包中，所有渲染图、视频、深度、Alpha、指标等大输出统一保存于 `03_实验记录/SHARP浅3D实验/outputs/` 并忽略提交。

### 1.3 文档写入审批

创建或修改 Markdown、Word、PDF、PPT、Excel 等说明性文档或交付文档前，必须先向用户提交拟写入的正文或内容提纲。

只有用户明确表示“确认”“可以写入”或作出同等授权后，才能实际写入。如果用户提出修改意见，应更新草稿并再次提交审阅。

代码和配置文件是否需要提前审阅，根据用户当次要求判断。

### 1.4 已生成 PPT 的局部迭代例外

对于已经生成的 PPT，如果用户已经明确指出需要修改的页面、文字、布局、配色或素材，可以直接修改，无需再次等待内容审批。

完成后必须逐项说明实际改动、检查结果和输出路径。

新建 PPT、重做整体结构、显著改变汇报逻辑，或用户明确要求先审阅时，仍须遵守文档写入审批规则。

## 2. 项目目标与沟通方式

### 2.1 课题目标

**课题名称**：单张 2D 图像转浅 3D 表达 / 单图新视角合成。

**核心目标**：输入一张普通 2D 照片，生成可在移动端通过小幅晃动手机或滑动屏幕产生立体感、视角变化和运动视差的“浅 3D 表达”，效果类似 Apple 3D 照片或微信透视窗。

“浅 3D”不要求完整 360° 重建，只需支持几度到十几度的小范围视角变化。

完整管线：

`单张图片 → 深度图 → 3DGS / 三维点云 / 分层表示 → 附近新视角渲染`

主要约束：

- 输入只有一张普通照片，没有多视图和已知相机位姿。
- 最终面向移动端部署。
- 交互渲染阶段不依赖 GPU 网络推理。
- 课题要求图片位于根目录 `课题要求.png`。

### 2.2 面向初学者的沟通要求

用户刚开始接触深度学习、三维视觉和神经渲染，协助时必须：

- 专业术语首次出现时，先用通俗语言建立直觉，再说明其在本项目中的作用。
- 缩写首次出现时给出中文名称、英文全称和缩写。
- 对复杂概念使用比喻、流程、示意或简单例子，不默认用户已经理解深度估计、相机模型、MPI、LDI、Mesh、NeRF、3DGS、去遮挡和新视角合成。
- 技术路线从基础到进阶组织，说明每种方法解决的问题、输入输出、基本原理、优缺点及其与本课题的关系。
- 推荐论文时说明重要程度、阅读难度、建议阅读章节和开源情况。
- 不只罗列名词或论文，应帮助用户建立方法之间的联系。

### 2.3 实验开始前的讲解要求

进行 SHARP 实验时，每一轮开始执行前，必须先向用户简要说明：

1. 本轮实验要解决什么问题。
2. 本轮使用什么输入，会生成什么输出。
3. 为什么当前阶段需要做这项实验。
4. 本轮通过后将进入哪一步。

讲解应先使用形象、直观的类比建立理解，再补充必要的技术参数。例如，实验工具冒烟测试可以解释为“正式测量前，先校准尺子、相机和记录表”。

除非用户要求暂停或确认，完成简要讲解后可以继续执行已经确定的实验计划。执行结束后应说明实际结果、发现的问题以及它对下一轮参数选择的影响。

## 3. 当前核心任务：SHARP

### 3.1 当前目标与优先级

当前最高优先级是复现和分析 Apple SHARP（Single-image High-Accuracy Real-time Parallax）：

1. 精读论文并理解方法。
2. 跑通官方代码和权重。
3. 使用官方示例与真实照片验证结果。
4. 建立失败案例并分析质量和工程瓶颈。
5. 尝试面向移动端浅 3D 的压缩、替换或改进方案。

次要重点是 3DGS 相关工作（论文分类 04/05/06）。原计划继续阅读的 LDI/MPI 路线暂缓；深度估计基础模型可作为 SHARP 复现阶段的参考。

### 3.2 SHARP 事实基线

- 论文：`papers/04_单图与前馈3DGS/09_SHARP_Sharp_Monocular_View_Synthesis.pdf`
- 版本：arXiv:2512.10685v2，2025 年 12 月首发，2026 年 2 月 27 日更新 v2。
- 源码：`源码/SHARP_APPLE注释/`
- 事实依据优先级：本地论文正文 > 实际可执行源码 > 本地复现结果 > Word 总结。
- 官方代码和权重已公开；权重由 Apple CDN 提供，采用 Apple 研究用途许可证。
- 官方仓库主要包含模型与推理代码，没有公开完整训练循环、训练数据集和损失实现。
- 发布模型默认 `sorting_monodepth=False`，双层深度没有硬性近远排序。
- 第一层主要表示可见表面；第二层没有隐藏深度真值，主要通过新视角渲染损失、感知损失、Alpha 损失和 TV 正则学习。
- Gaussian Decoder 最终输出头分为位置 3 维，以及尺度 3、旋转 4、颜色 3、不透明度 1，共 11 维。
- SHARP 在归一化设备坐标（Normalized Device Coordinates, NDC）中预测，但仍使用焦距和相机内参完成尺度转换及反投影。
- SHARP 使用固定 RGB，等价于零阶球谐，不使用高阶球谐。
- 论文按相机基线描述运动范围，目标主要是小于 0.5 米的附近视角，不换算成固定角度。
- SHARP 专题 PPT 必须复制现有模板生成新文件，不覆盖模板；正文采用简洁学术表达，可保留少量有助于理解和现场讲解的自然语言。

### 3.3 已完成工作

已完成：

- SHARP 方法总览和重点章节精读，包括完整数据流、Depth Pro、双层深度、Depth Adjustment、Gaussian Initializer、Gaussian Decoder、Composer、NDC 反投影、训练策略、损失、实验、运动范围和失败案例。
- 官方源码预测主链路与关键模块梳理。
- 独立 Conda 环境 `sharp` 配置，固定使用物理 GPU 1；执行前应核对当前环境，不得擅自重装或污染其他环境。
- 官方权重加载、单图预测、PLY 导出、彩色视频和深度视频渲染。
- SHARP 专题 PPT 和讲稿初稿制作，当前处于检查与迭代阶段。

权重路径：

`源码/SHARP_APPLE注释/checkpoints/sharp_2572gikvuh.pt`

复现输出目录：

`99_过程文件_可删除或归档/SHARP复现测试/`

当前实测口径：

- 输出约 `2 × 768 × 768 = 1,179,648` 个高斯。
- 单个 PLY 文件约 64 MB。
- 网络前向约 1.26 秒，CPU 后处理约 4.23 秒，冷启动预测约 28 秒。
- 首次 60 帧渲染约 91 秒，其中 gsplat CUDA 扩展初始化约 77.6 秒。
- 热启动 60 帧渲染约 13.98 秒，官方 CLI 端到端约 4.29 FPS。
- 论文约 100 FPS 是高斯常驻 GPU 时的纯渲染指标，不与包含加载、CPU 处理、设备搬运和视频编码的 CLI 速度直接比较。

### 3.4 当前瓶颈与下一步

主要工程瓶颈：

- `unproject_gaussians()` 对约 118 万个协方差矩阵执行 CPU float64 SVD。
- 官方 CLI 在每一渲染帧调用 `gaussians.to(device)`。
- 首次运行需要编译或加载 gsplat CUDA 扩展。

下一步：

1. 继续完成 SHARP 源码逐模块精读。
2. 使用真实单张照片建立质量测试集。
3. 分析深度错误、漂浮高斯、遮挡空洞、反射和景深等失败案例。
4. 测量高斯数量、PLY 大小、后处理时间和渲染速度的权衡。
5. 比较两条移动端路线：
   - 压缩 3DGS，并实现 Metal、Vulkan、OpenGL ES 或 WebGPU 渲染。
   - 将 3DGS 蒸馏或转换为 Mesh、LDI 等轻量表示。

### 3.5 SHARP 浅 3D 实验当前进度

实验执行方案：

`SHARP实验TodoList与执行方案.docx`

实验最终需要形成四项结论：

1. 最大可接受屏幕视差。
2. 推荐四边单边安全裁剪比例。
3. 推荐高斯保留率。
4. SHARP 适用与不适用的场景范围。

实验结果目录：

`03_实验记录/SHARP浅3D实验/`

实验过程脚本目录：

`99_过程文件_可删除或归档/生成脚本/SHARP实验工具/`

已完成实验工具 V1 的初步实现：

- 独立于 SHARP 官方源码的参数化三视图渲染。
- RGB、浮点 3DGS 渲染深度和 16 位累计 Alpha 导出。
- 相机位移及高斯投影屏幕位移统计。
- 静止重渲染差异图、PSNR 和 SSIM 计算。
- 四边单边 0%、3%、5%、10% 后裁剪并恢复显示尺寸。
- SHARP 微调后的两层单目深度和两层视差诊断导出。
- 全程固定使用物理 GPU 1，进程内显示为 `cuda:0`。
- 没有修改 SHARP 官方源码，没有覆盖原有真实图片测试结果。

已使用校园建筑完成静止和 `max_disparity=0.04` 的工具冒烟测试。按照正式实验方案，`0.04` 属于中运动档。

校园建筑静止基线初步结果：

- 全图 PSNR 约 28.38 dB，SSIM 约 0.895。
- Alpha≥0.99 区域 PSNR 约 28.73 dB，SSIM 约 0.897。
- Alpha≥0.99 有效覆盖区域约 96.5%。
- 3% 和 5% 裁剪暂时表现较好，但单一样本不能形成推荐结论。
- `max_disparity=0.04` 时，高斯投影位移 P99 约 94～95 像素，约占 4096 像素画宽的 2.3%。

当前结果只表示实验工具链已经能够运行，不代表 5 张先导实验已经完成。尚未完成：

- 5 张先导图片及 P05 反射/水面样本的最终冻结。
- 0/0.02/0.04/0.08 完整 60 帧运动矩阵。
- LPIPS 依赖与指标验证。
- 视频逐帧稳定性和伪影首次出现帧记录。
- 高斯剪枝、规范化性能测试和显存测量。
- 10～20 张独立验证和最终移动端路线决策。

屏幕视差正式统计应以统一定义的 P95 为主要指标，同时保留 P99 和最大值用于异常诊断。当前高斯投影位移属于初步代理指标，不能直接等同于图像特征光流或固定角度。

## 4. 论文阅读与总结流程

### 4.1 阅读前整体概览

每篇论文开始逐段带读前，先说明：

1. 论文解决的问题、核心方法和输入输出链路。
2. 3–5 个关键创新点，每点说清“是什么”和“与什么对比”。
3. 重点章节及优先级（⭐⭐⭐/⭐⭐/⭐），并说明原因。
4. 与本课题的匹配点、差异和可借鉴内容。

用户确认整体脉络清晰后，再进入重点精读。

### 4.2 重点精读与三步讲解法

对与本课题最相关的内容优先精读：摘要、方法总览、核心模块、关键公式、实验结论、失败案例和移动端浅 3D 相关讨论。

逐段讲解采用：

1. **原文翻译**：忠实翻译，不增不减。
2. **大意梳理**：说明该段在讲什么以及在全文中的作用。
3. **概念拆解**：用直觉、比喻或例子解释关键概念。

当用户希望直接跳到代码或深入某个问题时，可以跳过后续步骤。

阅读过程中发现可用于论文、开题报告、技术路线或汇报的内容，应整理成“可写入论文/报告的表述”；实际写入前遵守文档审批规则。

### 4.3 统一论文总结规则

不单独维护论文带读笔记。整篇论文读完后，将重点一次性汇总到根目录 `论文总结.docx`，每篇论文追加一节，不再为每篇论文新建统一总结文件。

当前 `汇报/论文总结_SHARP_7.17.docx` 和 `汇报/课题启发_SHARP_7.17.docx` 属于 SHARP 阶段材料，不替代长期统一总结文件。

每篇总结采用统一结构：

1. 动机与核心问题。
2. 方法核心：整体思路、结构、流程和输入输出。
3. 关键概念：术语的通俗解释。
4. 技术创新：3–5 个“是什么 + 和谁对比”的创新点。
5. 核心公式与设计：直观含义和关键参数。
6. 关键实验结论：消融、重要数据和对比结果。
7. 优势与局限。
8. 与本课题的关系：匹配度、可借鉴点、差异和后续衔接。

节首给出：作者 / 机构 / 会议或年份 / 重要性★ / 难度 / 开源 / 本地路径。

按照本规则写入根目录 `论文总结.docx` 时，不需要重复申请审批；用户可以随时要求查看或修改。

整篇尚未读完时，可将临时记录放入 `99_过程文件_可删除或归档/临时文件/`，汇总后再归档；删除仍须遵守文件安全规则。

## 5. Git 与 GitHub 规则

### 5.1 提交约定

用户说“提交”即表示执行 `commit + push`：先提交，再立即推送，不需要额外确认。

仓库地址：

`https://github.com/mengmcy1/2Dto3D.git`

### 5.2 推送前检查

每次推送前必须：

1. 检查 `.git` 是否相对既有基线异常增长；如果本次操作导致明显增长，应排查是否误追踪大文件。
2. 检查未被 `.gitignore` 忽略且大于 5 MB 的文件是否合理。
3. 确认 remote URL 不包含 `ghp_...` 等明文令牌。

当前已知可以合理提交的大文件包括根目录的 `Review of ICCV 2025 Papers on 3D Gaussian Splatting.pptx` 以及 `汇报/` 中的含图文档和图片；仍应逐次核对。

### 5.3 严禁上传的目录

以下目录只保留在本地，不得从 `.gitignore` 中移除，也不得上传 GitHub：

- `papers/`
- `99_过程文件_可删除或归档/工具/`
- `99_过程文件_可删除或归档/渲染检查缓存/`
- `99_过程文件_可删除或归档/下载与检查报告/`

新电脑需要这些文件时，应通过 U 盘、网盘或重新下载获取。

### 5.4 认证与历史重写

- 认证优先使用 Windows 凭据管理器（Git Credential Manager），不得把令牌写入 remote URL、脚本或文档。
- 如果推送认证失败，应引导用户重新完成 GitHub 认证，不得输出或保存用户令牌。
- 发现大文件已进入远程历史时，先报告影响和拟执行方案，并获得用户确认。
- 历史清理和强制推送属于破坏性操作；未经明确确认不得执行。
- 历史改写后，其他本地副本需要重新 `git fetch` 并同步。
