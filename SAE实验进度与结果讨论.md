# gastric-cbm SAE实验进度与结果讨论

## 新对话恢复项目的固定提示词

> 该提示词只规定恢复上下文与协作方法；即时状态以本文后续章节和正式输出为准。

```text
请继续协助我开展 gastric-cbm 的全局/局部 EfficientNet-B0 SAE解释研究。

先确认项目根目录为 /home/mcy/gastric-cbm，并按顺序阅读：

1. AGENTS.md：长期背景、数据/test边界、目录、文件安全、Git和注释规则；
2. SAE实验进度与结果讨论.md：当前协议、即时状态、结果、阻塞项和下一步；
3. YOLO26实验进度与结果讨论.md末尾：冻结M0-F全局模型、Y3-F检测器、Y6局部模型和
   工程采用边界；
4. 实验进度与结果讨论.md的“最终SAE任务路线”和旧ResNet50 SAE结果；
5. 本轮相关正式代码、CSV、JSON、checkpoint SHA和冻结manifest。数字以正式产物为准。

开始操作前先只读检查git status、目标输出目录和现有运行状态，不重复生成正式产物。
GPU任务先运行nvidia-smi，同时检查显存、利用率和进程，不默认GPU编号。

每轮先用通俗语言说明：解释哪个冻结分支、输入是什么、从哪层提取什么特征、是否只接触
train/val、产物如何验收。SAE不得修改被解释的分类器、检测器或其输出。

实验原则：
- 全局分支解释完整胃镜图；局部分支只解释冻结Y3-F阈值实际触发并进入Y6的预测ROI；
- SAE只用train拟合，val选择epoch、lambda、字典宽度、剪枝和Feature候选；
- internal test/external已经揭盲，只能在完整规则冻结后作描述性投影，不能反向选择方案；
- 不把全局/局部概率均值虚构成第三个“融合特征向量”；联合分析发生在Feature统计和概率归因层；
- Feature是模型内部方向，不自动等于医学概念；必须结合跨患者Top图、空间响应、来源关联、
  消融和临床审核；
- 先self-test和debug，再正式矩阵；不覆盖历史权重、冻结清单或人工材料；
- 长任务同时提供启动命令和实时日志命令。

正式阶段完成后，按“做了什么、结果如何、能确认什么、不能确认什么、风险、下一步、产物”
详细且易懂地汇报，并更新本文。只有长期规则或稳定里程碑变化时才更新AGENTS.md。
```

## 文档边界

本文从2026-08-17开始维护当前EfficientNet-B0全局/局部SAE路线。旧ResNet50 SAE历史见
`实验进度与结果讨论.md`；Y0-Y6模型训练、检测和ROI融合历史见
`YOLO26实验进度与结果讨论.md`。两份历史文档均不再追加实时进度。

## 当前状态

更新时间：2026-08-17

| 项目 | 状态 | 当前结论或阻塞项 |
| --- | --- | --- |
| 冻结解释对象 | 已确认 | 每seed对应M0-F全局EfficientNet-B0和Y6局部EfficientNet-B0，特征维度均为1280 |
| 全局SAE输入 | 已冻结 | Y0F mapping的train/val完整图；解释`features[8]`输出经GAP后的1280维向量 |
| 局部SAE输入 | 已冻结 | 同seed Y3-F Top-1框达到冻结阈值的train/val图；按Y6 `margin=0.20`裁为224，不分析未触发图 |
| 融合SAE | 不建立 | Y6最终融合是概率均值，不存在可冻结的融合特征向量；改做全局/局部Feature联合统计 |
| EfficientNet SAE代码 | A1c实现与正式运行完成 | 支持Top-K与无标签分类margin保真损失；旧`sae_discovery.py`仍为ResNet50历史入口 |
| SAE-A0/A1/A1b | 已完成，均未形成双分支合格配置 | Linear-ReLU+L1与纯MSE Top-K均存在稀疏度和个体预测保真的冲突 |
| SAE-A1c seed42 | 已按预注册停止 | 全局`K=1024,gamma=0.1`合格；局部`K=1024/1536`均不合格，故不进入A2或临床命名 |
| SAE-B空间Token seed42 | 已按预注册停止 | 局部K=128/256均未通过；K=256仅token cosine差0.00007，但整图约启用5025个Feature，不事后放宽 |
| SAE-C全局多种子复现 | 已完成，2/3通过 | seed42/202通过全部冻结门槛；seed503因recovered CE=0.9209未通过 |
| 全局SAE医学生探索材料 | 已生成 | 两个合格seed严格匹配出3对稳定Feature；只作全局分支探索性临床审核 |
| internal test/external | 保持锁定 | 首轮开发不得读取；全部选择冻结后才能作一次描述性投影 |

## 冻结解释对象与数据流

### 全局分支

```text
完整胃镜图224x224
  -> 冻结M0-F EfficientNet-B0 features[8] [1280,7,7]
  -> GAP 1280维
  -> SAE编码/重构
  -> 复用冻结M0-F分类头评价保真度
```

全局分支使用Y0F同seed患者划分的全部train和val。模型、分类头和BN统计始终冻结。

### 局部分支

```text
完整胃镜图
  -> 冻结Y3-F YOLO26 Top-1框与同seed冻结阈值
  -> 仅保留实际触发局部分支的图
  -> 框按margin=0.20扩为图内正方形并缩放至224x224
  -> 冻结Y6局部EfficientNet-B0 features[8] [1280,7,7]
  -> GAP 1280维
  -> SAE编码/重构
  -> 复用冻结Y6局部分类头评价保真度
```

局部SAE解释的是部署时真正送入局部分类器的预测ROI。Y6训练时的癌GT+jitter和非癌随机
匹配裁块仅用于训练局部分类器，不作为正式SAE解释输入。train上的Y3-F预测属于检测器的
训练集内输出，因此局部train Feature只用于拟合SAE，不用于声称定位泛化；局部val才用于
选择和报告开发性能。

## SAE结构与首轮协议

输入维度固定`D=1280`。字典宽度逐级推进：

| 编号 | 结构 | 用途 |
| --- | --- | --- |
| S0 | `1280 -> 512 -> 1280` | 与旧M-CBM 512维基线兼容的工程参照 |
| S1 | `1280 -> 1280 -> 1280` | 完整宽度参照 |
| S2 | `1280 -> 2560 -> 1280` | 首个正式过完备字典 |
| S3 | `1280 -> 5120 -> 1280` | 仅S2质量与稳定性合格后运行 |
| S4 | `1280 -> 10240 -> 1280` | 暂不运行；仅S3死亡/重复Feature可控时再预注册 |

统一实现：Linear-ReLU encoder、线性decoder、逐元素MSE、隐藏激活L1、decoder逐方向单位
范数、decoder梯度正交投影、患者/类别平衡采样、batch size 32。学习率和L1在前5% epoch
从0线性升至目标值，最大1000 epoch，patience 50。

首轮分两层：

1. `SAE-A0`工程验证：seed42、global/local、S0、`lambda=5e-4`，只验证数据血缘、特征
   提取、训练、重构分类头、剪枝和空间概览完整闭环；该轮不选择最终宽度。
2. `SAE-A1`开发比较：A0通过后，仅用seed42 train/val比较S0/S1/S2与
   `lambda={2e-4,5e-4,1e-3}`。不得因test/external结果改网格。
3. `SAE-A2`复现：选定配置后在seed202/503独立重跑；Feature编号不假定跨seed对齐，
   后续用decoder方向、患者激活和空间语义进行匹配。

## 质量指标与选择边界

每个split至少报告：逐元素MSE、cosine、mean L0、覆盖90%激活质量所需Feature数、死亡
Feature率、decoder重复方向率、原/重构图像AUC、原/重构患者AUC、分类一致率、癌概率MAE、
recovered CE。剪枝只按train激活患者数产生候选，用val recovered CE下降不超过0.01选择。

候选进入医学分析前必须同时满足：

- val患者AUC相对冻结原头下降不超过0.01；
- val预测一致率不少于0.95；
- val平均cosine不少于0.90；
- val recovered CE不少于0.95；
- 结果不存在大面积死亡Feature或近重复decoder方向；具体比例在A0输出分布后、A1运行前冻结，
  不得事后按喜欢的模型修改。

不同lambda/宽度不只按总损失最小选择。先过保真门槛，再比较稀疏性、死亡/重复Feature、
跨患者覆盖和临床可读性；若没有单一候选支配其他候选，保留Pareto结果而不强行宣布唯一最佳。

A0观察到两分支死亡Feature率和decoder重复方向率均为0。为在A1运行前冻结而非事后调整，
正式候选附加质量上限设为：train死亡Feature率不超过10%，绝对decoder余弦不低于0.95的
重复Feature比例不超过10%。这两项是排除明显退化字典的质量门，不替代上述分类保真门槛。

## SAE-A0正式结果（seed42，S0）

两条分支均使用`1280 -> 512 -> 1280`、`lambda=5e-4`，最佳epoch均为95。冻结分类器、
mapping和局部ROI清单SHA均已记录，未读取internal test或external。

| 分支 | train/val图像 | val cosine | val L0 | val NCC90 | 原患者AUC | 重构患者AUC | 患者一致率 | recovered CE | 核心门槛 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 全局 | 2350/497 | 0.7621 | 108.5 | 67.2 | 0.9133 | 0.8925 | 0.9269 | 0.9192 | 未通过 |
| 局部触发ROI | 1740/345 | 0.7242 | 121.3 | 75.5 | 0.8406 | 0.8524 | 0.8765 | 1.1399 | 未通过 |

全局患者AUC下降0.0208，超过允许的0.01；局部AUC点估计没有下降，但患者决策一致率仅
0.8765。两分支cosine均明显低于0.90。train死亡Feature率和decoder重复Feature率均为0，
说明SAE已经形成稀疏方向，但512维字典在当前约束下不足以高保真表示两套1280维特征。
剪枝只保留511/512和508/512，不能解决主要问题。因此A0只证明工程闭环成立，当前Feature
不得交给临床命名；按预注册进入S1/S2与lambda网格比较。

## 联合Feature与伪特征分析

全局和局部SAE分别训练。后续联合分析不要求二者Feature编号一一对应，而是比较decoder
余弦、患者级共激活、标签方向、来源/设备关联、代表图和空间响应，形成：癌相关、非癌相关、
共享黏膜/解剖、良性与癌共享形态、来源/设备、反光/气泡/黏液/镜体等候选簇。

共有Feature不能因为癌和非癌都激活就直接删除。只有来源关联、空间响应、临床审核和冻结
Feature簇屏蔽消融共同支持其为伪特征时，才列为排除候选。

## SAE-A1正式结果（seed42，S1/S2）

A1已完成全局/局部两分支、S1/S2和三档`lambda={2e-4,5e-4,1e-3}`共12组比较。
所有运行均复用A0冻结GAP缓存，只读取train/val；未读取internal test或external。完整机器可读
汇总见`结果/SAE/EfficientNet全局局部_0804/SAE-A1_宽度L1比较/sae_a0_a1_summary.csv`。

| 分支 | 当前最接近保真门槛的配置 | val cosine | val L0 | 原患者AUC | 重构患者AUC | 患者一致率 | recovered CE | 结论 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| 全局 | S2，`h=2560, lambda=2e-4` | 0.8778 | 610.4 | 0.9133 | 0.8957 | 0.9385 | 0.8877 | 未通过 |
| 局部触发ROI | S2，`h=2560, lambda=2e-4` | 0.8540 | 603.5 | 0.8406 | 0.8457 | 0.9383 | 1.1134 | 未通过 |

12组均未同时通过四项核心保真门槛。增大字典和减小L1总体能提高cosine，但同时显著提高
L0：S2、`lambda=2e-4`平均每张图约激活600/2560个Feature。提高L1可降低L0，却会明显
损害cosine、患者一致率或recovered CE。所有A1候选的死亡Feature率和decoder重复Feature率
仍为0，说明尚未出现字典退化，但当前S1/S2不存在可交给临床命名的合格折中点。

该结果不支持事后放宽冻结门槛，也不进入A2跨种子复现。按照预注册顺序进入S3容量诊断，
检验4倍膨胀能否在保持稀疏约束的同时改善重构；若S3仍不合格，再依据死亡/重复Feature
情况决定是否预注册S4，而不是直接宣布当前稠密表示为有效SAE。

## SAE-A1-S3预注册（seed42，4倍膨胀）

- 结构固定为`1280 -> 5120 -> 1280`，全局和局部仍分别训练；
- lambda沿用A1冻结的`{2e-4,5e-4,1e-3}`，不因A1结果新增更小lambda；
- 训练、缓存、患者/类别平衡采样、warmup、早停和所有质量门槛与A1完全一致；
- 仍只用seed42 train拟合、val选择，不读取internal test或external；
- 先过四项核心保真门槛及死亡/重复Feature上限，再与S1/S2形成Pareto比较；
- 若没有候选通过，不启动A2，不把Feature交给临床命名。

## 下一步

## SAE-A1-S3正式结果（seed42，4倍膨胀）

S3六组均完整结束，未读取internal test或external，结果已纳入同一汇总CSV。两分支在
`lambda=2e-4`时保真最好：

| 分支 | val cosine | val L0 | 原患者AUC | 重构患者AUC | AUC下降 | 患者一致率 | recovered CE | 核心门槛 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 全局 | 0.8891 | 744.5 | 0.9133 | 0.9036 | 0.0097 | 0.9231 | 0.9168 | 未通过 |
| 局部触发ROI | 0.8685 | 747.4 | 0.8406 | 0.8393 | 0.0013 | 0.8951 | 1.0222 | 未通过 |

全局S3首次把患者AUC下降压到0.01以内，但cosine、患者一致率和recovered CE仍未达标；局部
S3的AUC和recovered CE合格，但cosine和患者一致率仍不足。较大的lambda能把L0降到约
153--383，却进一步损害重构保真。S3死亡Feature率最高仅0.20%，decoder重复Feature率仍为
0，满足原协议中“死亡/重复可控才进入S4”的条件。因此S3不进入A2，但允许进行最后一档
8倍膨胀诊断。

## SAE-A1-S4预注册（seed42，8倍膨胀）

- 结构固定为`1280 -> 10240 -> 1280`，全局和局部独立训练；
- 继续沿用`lambda={2e-4,5e-4,1e-3}`，其余训练与质量门槛保持不变；
- S4是本轮单纯扩大字典宽度的终点，不再追加更宽字典；
- 若S4仍无合格候选，停止A1/A2并把结论记为“Linear-ReLU+L1形式未达冻结保真门槛”，
  后续需预注册新的稀疏编码形式，而不是放宽门槛或继续扩维；
- 仍只读取seed42 train/val，internal test与external继续锁定。

## 下一步

## SAE-A1-S4正式结果（seed42，8倍膨胀）

S4六组均正常结束。全局`lambda=2e-4`已达到患者AUC下降0.0012和recovered CE 0.9617，
但cosine 0.8957、患者一致率0.9308仍未通过；局部同配置的cosine为0.8741、患者一致率
0.9198，且患者AUC下降0.0196。其他lambda更稀疏，但保真更差。因此S4仍为0个合格候选，
按预注册结束单纯扩宽Linear-ReLU+L1路线，不进入A2，也不继续扩到16倍。

该系列说明主要瓶颈不是字典没有足够槽位，而是L1软惩罚本身的折中：小lambda下重构改善，
但每图需要约835--854个激活；大lambda将激活压到约213个时，重构和冻结分类决策明显受损。

## SAE-A1b Top-K预注册（seed42）

为直接控制稀疏度，A1b改用逐样本Top-K SAE。encoder仍为Linear-ReLU，但每张图只保留K个
最大非负激活，decoder和中心偏置不变；训练损失仅为逐元素MSE，不再叠加L1。该实验不使用
分类标签损失，避免把SAE训练成监督分类器。

- 字典固定为8倍膨胀：`1280 -> 10240 -> 1280`；
- 比较`K={128,256,512,1024}`，对应字典的1.25%、2.5%、5%和10%；
- 全局与局部独立训练，沿用相同GAP缓存、患者/类别平衡采样、decoder约束和早停；
- 四项核心保真门槛、死亡Feature率和decoder重复率上限保持不变；
- 仍只用seed42 train拟合、val选择，不读取internal test或external；
- 若多个K通过，优先选择K更小的配置；若无配置通过，停止本轮SAE模型搜索并重新评估
  输入层、稀疏形式或保真门槛的研究定义，不做事后放宽。

## 下一步

## SAE-A1b Top-K正式结果（seed42）

全局和局部两分支的`K={128,256,512,1024}`共8组均正常结束。运行仅复用seed42的冻结
train/val GAP特征；internal test与external未读取。完整汇总见
`结果/SAE/EfficientNet全局局部_0804/SAE-A1b_TopK比较/sae_a0_a1b_summary.csv`。

| 分支 | K | val cosine | val L0 | 原患者AUC | 重构患者AUC | AUC下降 | 患者一致率 | recovered CE | 结论 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 全局 | 128 | 0.7834 | 128.0 | 0.9133 | 0.8938 | 0.0195 | 0.8846 | 0.8517 | 未通过 |
| 全局 | 256 | 0.8387 | 256.0 | 0.9133 | 0.8972 | 0.0161 | 0.9077 | 0.8804 | 未通过 |
| 全局 | 512 | 0.8789 | 512.0 | 0.9133 | 0.9018 | 0.0115 | 0.9308 | 0.9282 | 未通过 |
| 全局 | 1024 | 0.9044 | 994.9 | 0.9133 | 0.9043 | 0.0090 | 0.9385 | 0.9690 | 未通过 |
| 局部触发ROI | 128 | 0.7496 | 128.0 | 0.8406 | 0.8181 | 0.0225 | 0.8457 | 0.8687 | 未通过 |
| 局部触发ROI | 256 | 0.8148 | 256.0 | 0.8406 | 0.8080 | 0.0325 | 0.9074 | 0.8267 | 未通过 |
| 局部触发ROI | 512 | 0.8598 | 512.0 | 0.8406 | 0.8230 | 0.0175 | 0.9198 | 0.9296 | 未通过 |
| 局部触发ROI | 1024 | 0.8917 | 1002.4 | 0.8406 | 0.8398 | 0.0008 | 0.9012 | 1.0040 | 未通过 |

Top-K实现了预期的直接稀疏控制：除少量激活恰为零外，val L0基本等于K，且所有配置的
decoder重复Feature率为0；死亡Feature率也均低于冻结的10%上限。但稀疏与保真仍存在明确
冲突。小K足够稀疏，却显著改变特征方向和冻结分类决策；K增大到1024时，全局只因患者
一致率0.9385未达到0.95而失败，局部则同时未达到cosine和一致率门槛。局部K=1024虽然
患者AUC几乎不降，但约10%的患者阈值判定发生变化，不能仅凭AUC排序接近就视为高保真。

因此A1b两分支均为0个合格候选，不进入A2跨种子复现，也不把当前Feature交给临床命名。
这个结论不否定特征中存在可解释信号，而是说明目前“末层GAP 1280维 + 单层
Linear-ReLU/Top-K + 纯MSE重构”的组合，无法在冻结稀疏度和个体预测保真要求下同时合格。

## 下一步

1. 按预注册停止继续扩大字典或盲扫K，不事后放宽四项门槛；
2. 在启动新训练前，先做无标签的重构误差归因：比较分类头敏感方向与普通MSE方向，确认
   患者一致率下降是否主要来自少数高分类权重维度；
3. 若归因成立，预注册A1c“特征MSE + 冻结分类margin一致性”的保真增强SAE；该损失只使用
   冻结分类头输出，不使用癌/非癌真值标签，并先在seed42比较最小参数网格；
4. 只有全局和局部均通过冻结门槛后才进入A2；若A1c仍失败，本轮SAE停止在模型诊断结论，
   转向更早特征层或空间级稀疏编码，不继续对同一GAP表示调参。

## SAE-A1c分类margin保真预注册（seed42）

A1b误差归因只读取全局/局部`K=1024`的val结果。分类权重方向仅占总重构误差能量的
0.197%和0.191%，对应校正向量范数仅为原特征范数的1.95%和2.11%；但校正50%的该方向后，
全局患者一致率由0.9385升至0.9692，局部由0.9012升至0.9630。说明普通MSE忽略了能量很小
但影响阈值决策的分类敏感方向，允许进入A1c。归因结果见
`SAE-A1b_TopK比较/margin_error_attribution.json`和`margin_correction_curve.csv`。

A1c保持无真值标签训练。冻结分类头的癌/非癌logit差称为margin，损失定义为：

```text
L = feature_MSE + gamma * mean(((margin_reconstructed - margin_original) / sigma_train_margin)^2)
```

其中`sigma_train_margin`仅由同分支train原特征计算并冻结；分类头参数不更新。该项约束的是
SAE重构前后同一黑箱模型证据一致，不使用患者癌/非癌标签，不把SAE训练成新的监督分类器。

- 第一阶段固定`1280 -> 10240 -> 1280`和`K=1024`，比较
  `gamma={0.1,0.5,1.0}`；
- margin权重与学习率在前5% epoch同步warmup，其他采样、decoder约束、早停和缓存不变；
- checkpoint按完整val总损失选择，四项核心门槛和死亡/重复Feature上限保持不变；
- 仍只使用seed42 train/val，internal test和external锁定；
- 若多个gamma通过，先选gamma更小者，避免分类方向约束压过一般视觉重构；
- 若分类保真门槛均通过但局部仅cosine仍略低于0.90，允许用已选最小gamma做一次预注册的
  `K=1536`容量确认；除此之外不增加K或gamma网格；
- 只有全局和局部均正式通过，才冻结各自最小合格配置进入A2。

## SAE-A1c分类margin保真正式结果（seed42）

A1c六组主矩阵与一次预注册的局部`K=1536`容量确认均正常完成，只复用seed42冻结的
train/val特征缓存；internal test和external未读取。完整汇总见
`结果/SAE/EfficientNet全局局部_0804/SAE-A1c_Margin保真/sae_a0_a1b_a1c_summary.csv`。

| 分支 | K | gamma | val cosine | 原患者AUC | 重构患者AUC | AUC下降 | 患者一致率 | recovered CE | 核心门槛 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 全局 | 1024 | 0.1 | 0.9039 | 0.9133 | 0.9087 | 0.0046 | 0.9500 | 0.9552 | 通过 |
| 全局 | 1024 | 0.5 | 0.9009 | 0.9133 | 0.9080 | 0.0053 | 0.9538 | 0.9367 | 未通过 |
| 全局 | 1024 | 1.0 | 0.8988 | 0.9133 | 0.9091 | 0.0043 | 0.9808 | 0.9762 | 未通过 |
| 局部触发ROI | 1024 | 0.1 | 0.8834 | 0.8406 | 0.8439 | -0.0033 | 0.9506 | 1.0157 | 未通过 |
| 局部触发ROI | 1024 | 0.5 | 0.8805 | 0.8406 | 0.8444 | -0.0038 | 0.9506 | 1.0573 | 未通过 |
| 局部触发ROI | 1024 | 1.0 | 0.8784 | 0.8406 | 0.8490 | -0.0085 | 0.9630 | 1.0840 | 未通过 |
| 局部触发ROI | 1536 | 0.1 | 0.8897 | 0.8406 | 0.8418 | -0.0013 | 0.9444 | 0.9974 | 未通过 |

margin约束确实解决了部分分类保真问题。全局`gamma=0.1`是本轮第一个同时通过四项冻结门槛
的配置：AUC只下降0.0046，患者一致率达到0.95，且cosine和recovered CE均达标。它说明
普通MSE遗漏的分类敏感方向可以通过无真值标签的冻结margin约束补回。

局部分支没有形成合格候选。`K=1024,gamma=0.1`已经通过AUC、一致率和recovered CE，只差
cosine，因此按预注册追加唯一一次`K=1536`。扩大K后cosine仅从0.8834升至0.8897，仍低于
0.90，同时患者一致率从0.9506降至0.9444。该结果说明局部ROI的末层GAP表示并非单纯缺少
激活容量；继续扩大K不能稳定地同时恢复一般特征几何和个体分类决策。

剪枝结果不改变上述判断。部分配置在recovered CE容差内只保留5--7个Feature，是因为少数
高频方向足以近似总体分类损失，并不代表它们完整重构了局部视觉表示；剪枝后的val cosine
只有约0.36，不能将这些少量Feature直接解释为完整医学概念集合。

## A1c阶段结论与后续边界

1. A1c按预注册停止，不进入A2 seed202/503复现，不读取internal test/external，也不将当前
   Feature交给临床命名。
2. 全局A1c合格证明margin保真方法本身有效，但正式路线要求全局与局部同时合格，不能只凭
   单分支成功宣布整套双分支SAE成功。
3. 当前失败边界明确指向“局部ROI末层GAP + 单层Top-K SAE”。后续若继续SAE，应另行预注册
   更早且保留空间结构的特征层、空间级稀疏编码或分层字典，而不是继续扩大同一GAP字典、
   增加gamma网格或放宽既有门槛。
4. 当前可保留全局`K=1024,gamma=0.1`作为工程诊断候选，但在新的局部表示路线完成并独立
   复现前，不升级为最终临床解释产品。

## SAE-B空间Token SAE预注册（seed42）

### 研究问题

A1c失败可能来自先做GAP导致空间信息不可逆丢失。SAE-B不更换冻结黑箱模型和层级，仍读取
EfficientNet-B0 `features[8]`的`[1280,7,7]`输出，但把49个空间位置分别视为1280维token，
由同一个SAE共享编码和解码参数：

```text
[B,1280,7,7] -> [B,49,1280]
                 -> 共享Top-K SAE逐token编码/重构
                 -> [B,1280,7,7]
                 -> GAP 1280维
                 -> 冻结原分类头验收
```

每个SAE Feature在一张图上直接产生`7x7`激活图，不再通过GAP Feature和decoder方向间接反投影。

### 冻结训练协议

- 仍使用seed42同一全局/局部train和val，不读取internal test或external；
- 字典固定`1280 -> 10240 -> 1280`，激活模式固定为逐token Top-K；
- 只允许`K_token={128,256}`，按`128 -> 256`逐级容量确认；某分支K=128通过即选定，只有
  K=128失败才运行K=256，不增加字典宽度或追加K网格；
- 分类margin权重固定`gamma=0.1`，不重新扫描gamma；
- 训练采样单位仍是图像，保持患者/类别平衡；一张图的49个token必须在同一批次共同重构；
- SAE训练批量固定为16张图（每步784个空间token），兼顾完整图像margin计算和运行效率；
- 损失为逐token特征MSE，加重构空间图GAP后相对原GAP的标准化冻结分类margin MSE；
- 学习率和margin权重前5% epoch线性warmup，最大1000 epoch、patience 50；
- checkpoint按患者平衡的完整val总损失选择；冻结Encoder、分类头和BN状态必须校验不变；
- 空间缓存按float32落盘，避免低精度缓存改变阈值边界；config记录shape、dtype、
  checkpoint/manifest SHA和train/val计数，并要求缓存GAP复算概率与冻结模型原概率最大差
  不超过`1e-5`。

### 指标与成功判据

冻结分类头继续使用A1c四项核心门槛：患者AUC下降不超过0.01、患者阈值判定一致率不少于
0.95、重构GAP平均cosine不少于0.90、recovered CE不少于0.95。此外新增空间完整性门槛：

- val全部空间token的平均cosine不少于0.80；
- 缓存GAP复算概率与冻结模型原概率最大绝对差不超过`1e-5`；
- train死亡Feature率不超过10%，decoder重复方向率不超过10%；
- 同时报告每token L0、每图49个位置的Feature并集大小和覆盖90%激活质量所需Feature数。

只有全局和局部均通过全部门槛，才选择各分支最小合格K进入SAE-B1 seed202/503复现。任一
分支在`K=128/256`均无合格候选，则SAE-B停止，不放宽门槛、不追加K，并保留A1c全局候选
仅作工程诊断。剪枝与临床概览只对通过上述未剪枝门槛的候选执行，避免用少数分类方向掩盖
空间重构失败。

正式执行先验证当前阻塞项局部分支；局部K=128/256均失败时直接停止，不运行全局空间SAE。
局部通过后才按相同的K=128优先规则验证全局分支。这是预注册的节省算力顺序，不改变双分支
均须合格的最终判据。

## SAE-B空间Token正式结果（seed42）

局部分支两档容量均正常完成，只读取冻结train/val空间特征；internal test和external未读取。
K=128失败后按预注册运行K=256，K=256仍未通过，因此停止且未启动全局空间SAE。

| K/token | 最佳epoch | GAP cosine | token cosine | 原患者AUC | 重构患者AUC | 患者一致率 | recovered CE | 每图Feature并集 | 结论 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 128 | 109 | 0.8917 | 0.7339 | 0.8406 | 0.8441 | 0.9136 | 1.0422 | 3071.0 | 未通过 |
| 256 | 104 | 0.9246 | 0.79993 | 0.8406 | 0.8401 | 0.9506 | 1.0412 | 5024.7 | 未通过 |

K=128同时未达到GAP cosine、token cosine和患者一致率。K=256已经通过四项分类保真门槛：
患者AUC仅下降0.0005、患者一致率0.9506、GAP cosine 0.9246、recovered CE 1.0412；但
token cosine为0.79993，仍低于预注册的0.80。差值虽然只有约0.00007，也不能在看完结果后
改判成功。

更重要的可解释性限制是，K=256虽然逐位置只激活字典的2.5%，49个位置合并后每张图平均
使用约5025个不同Feature，接近10240维字典的一半。死亡Feature率和decoder重复率均为0，
说明字典被充分使用；但它没有形成适合临床审核的紧凑图像级概念集合。因此即使忽略极小的
token cosine差距，当前配置也只证明空间重构可接近分类保真，不足以成为可用的稀疏解释产品。

SAE-B按协议停止：不追加K、不放宽token cosine、不运行全局分支或B1多种子，不进入剪枝、
internal test/external投影和临床命名。当前两条实验共同表明，末层1280维表示可被高容量字典
近似重构，但要同时获得“个体预测保真、空间保真和少量可审核概念”仍未成功。下一步不再
继续本轮SAE结构搜索；如未来重启，应先提出能约束整图Feature并集或使用分层/组稀疏机制的
新协议，而不是继续调Top-K。

## SAE-C全局分支复现与临床探索交付预注册

### 定位与边界

临床协作当前需要一批可以实际审核的SAE结果。A1c全局`K=1024,gamma=0.1`已经通过全部冻结
保真和字典质量门槛，而局部GAP与空间Token路线均未形成合格产品。因此新增一条独立的全局
探索性交付线，验证全局结果能否跨随机种子复现，并在不夸大全套双分支结论的前提下向医学生
提供候选Feature。

本阶段不推翻A1c/SAE-B的结论：完整的全局+局部SAE路线仍未成功。这里交付的是“冻结M0-F
全局分类器的探索性概念分析”，不能称为Y6全局局部融合系统的完整解释，也不能用全局Feature
替代局部病灶解释。YOLO预测框可作为单独的空间定位证据与全局Feature并列展示。

### 固定复现协议

- seed42直接引用既有A1c正式产物，不重新训练；
- seed202/503分别加载同seed冻结M0-F EfficientNet-B0及Y0F同seed train/val划分；
- 固定`features[8] -> GAP`、`1280 -> 10240 -> 1280`、Top-K `K=1024`、
  `gamma=0.1`、batch size 32、1000 epoch、patience 50和5% warmup；
- 不扫描K、gamma、学习率或其他超参数；每个seed只运行一次正式配置；
- 每个seed独立使用原四项核心门槛：患者AUC下降不超过0.01、患者一致率不少于0.95、
  val cosine不少于0.90、recovered CE不少于0.95；同时要求死亡Feature率和decoder重复率
  均不超过10%；
- 完整复现按`3/3`、`2/3`或`1/3`如实报告，不因结果放宽门槛；
- 本阶段只读取train/val，不读取internal test或external，不用其结果选择Feature或参数。

### 医学生交付规则

只有通过全部冻结门槛的seed才能参与跨seed稳定性分析。不同seed的Feature编号没有直接对应
关系，后续须按decoder方向余弦、激活患者重叠和标签方向一致性进行匹配，不能把相同编号视为
同一概念。

自动匹配固定使用两个合格seed的共同train+val图像，以图像SHA对齐；候选须经各自剪枝保留且
至少覆盖20位开发患者。对每个候选的跨图像激活做秩变换，以Spearman相关衡量响应模式一致性，
仅保留癌/非癌margin方向和标签激活差方向均一致、相关系数不少于0.50、且在两边互为最近邻的
Feature对。排序分数由激活相关性与两边分类贡献分位数组合；癌方向和非癌方向各最多提交15对。
省人民/外院平均激活差异不作为静默删除条件，而以`source_bias_warning`显式标记供临床排查。

seed42剪枝保留的166个Feature仅表示在交叉熵容差内保留的“分类相关候选池”。剪枝后val
cosine约0.51，说明它们不能完整重构全部视觉信息。临床提交版将从合格seed中再筛选约20--30
个稳定候选，提供激活最高患者图、空间响应图、癌/非癌激活分布、来源相关性和消融贡献，并
附临床命名表。文档必须显式标注“全局分支探索性概念”，不得写成已验证医学概念。

### 后续局部研究

全局交付不阻塞局部方法研究。下一版局部SAE优先考虑图像级组Top-K：先为每个Feature生成
`7x7`响应图，再在整张图范围只保留有限数量的Feature ID，从机制上约束每图概念并集，而
不是对49个token分别Top-K。该路线需另行预注册和实现，不与本次固定全局复现混跑。

## SAE-C全局分支正式结果与首版临床材料

seed202/503均按固定配置正常结束，未扫描任何新参数，internal test和external未读取。seed42
直接引用既有A1c结果。完整机器可读汇总见
`结果/SAE/EfficientNet全局局部_0804/SAE-C全局多种子复现/sae_c_global_replication_summary.csv`。

| seed | 最佳epoch | val cosine | 原患者AUC | 重构患者AUC | AUC下降 | 患者一致率 | recovered CE | 剪枝保留Feature | 结论 |
| ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | :---: |
| 42 | 374 | 0.9039 | 0.9133 | 0.9087 | 0.0046 | 0.9500 | 0.9552 | 166 | 通过 |
| 202 | 379 | 0.9006 | 0.9090 | 0.9037 | 0.0053 | 0.9769 | 0.9692 | 144 | 通过 |
| 503 | 407 | 0.9001 | 0.9053 | 0.8971 | 0.0082 | 0.9615 | 0.9209 | 123 | 未通过 |

seed503的cosine、AUC下降和患者一致率均达标，但recovered CE低于0.95。这表示SAE大体保留了
患者排序和阈值决策，却没有充分恢复原分类概率对真值的交叉熵质量。因此按预注册判为失败，
不纳入概念匹配。固定配置最终为`2/3`通过：说明全局margin保真SAE具有一定跨seed稳定性，
但尚不能宣称三种子完全复现。

只使用seed42和202的剪枝候选池，按冻结的共同图像Spearman相关、诊断方向一致和双向最近邻
规则，得到3对稳定Feature：

| 候选 | 方向 | seed42 ID | seed202 ID | 激活相关 | 来源偏倚警告 |
| --- | --- | ---: | ---: | ---: | :---: |
| G01 | 癌方向 | 1902 | 7964 | 0.9162 | 否 |
| G02 | 非癌方向 | 5363 | 8952 | 0.9040 | 否 |
| G03 | 非癌方向 | 2948 | 4297 | 0.7398 | 否 |

严格规则下只有3对，而不是原计划上限20--30对。这里不为增加数量而放宽相关阈值或取消互为
最近邻。初步工程目视显示，G01在多张癌图上响应于局灶黏膜区域；G02/G03更多响应于腔口、
皱襞等结构。该观察只用于帮助医学生开始审核，不能在临床复核前命名为具体征象。

首版可直接复制打包的材料位于
`结果/SAE分析/EfficientNet全局分支_0804/医学生提交版_v1`，包含阅读说明、三种子保真表、
全部稳定匹配、临床候选表、临床命名表，以及seed42/202各自的原图与空间响应热图。该目录
明确标注为全局分支探索结果；局部SAE失败、剪枝后不能完整重构视觉表示、seed503未复现均
作为限制保留。

### 全量展示补充协议

首版仅展示3对严格跨seed稳定候选，适合优先审核，但不等于全量分析。为支持医学生自主浏览
和挑选，在不改变训练、剪枝或稳定匹配判据的前提下补充全量展示：

- 两个合格seed各自全部10240个Feature的原始统计表完整保留；
- 对剪枝保留的分类相关候选全部生成空间响应图，即seed42的166个和seed202的144个，共310
  个seed内候选；每个候选继续展示激活最高的6位不同患者；
- 全量表增加癌/非癌margin方向、标签激活差、来源激活差、来源风险提示、分类贡献和跨seed
  稳定匹配编号；同时生成Excel汇总、HTML图册及临床填写表；
- 额外做患者级癌/非癌共享性分析：每位患者先对其图片取Feature最大激活，再分别计算癌与非癌
  患者的激活率、平均最大激活和单Feature标签AUC。两类激活率均不少于20%、标签AUC位于
  `[0.40,0.60]`、且归一化平均激活差不超过0.20时标为“两类共有”；AUC不少于0.65且癌侧
  激活更高标为“癌富集”，AUC不超过0.35且非癌侧更高标为“非癌富集”，其余标为“混合/
  不确定”。该分组只用于描述和临床浏览，不参与训练或SAE门槛选择；
- 3对严格跨seed候选仍作为优先审核层，不因扩大全量展示而改变其地位；
- seed503未通过冻结保真门槛，只保留失败指标，不生成Feature热图或临床命名材料；
- “全量候选”指全部剪枝保留的分类相关候选，不代表把10240个潜变量都视为医学概念。未剪枝
  Feature仅在原始统计表中供工程追溯。

全量材料已生成并验收：seed42为166个、seed202为144个，共310个seed内候选和310张纵向
概览图；两个seed各自完整10240维统计也已复制。患者级共享性分析识别出73个“癌与非癌共有”
候选，其中seed42为33个、seed202为40个；这是seed内候选数，不代表73种互不重复的医学概念。
seed42共有候选Feature 7086的抽查图在癌与非癌患者中都主要响应胃腔开口，说明该分析确实能
捕获通用解剖结构；其余共有候选仍需临床逐一判断。来源风险阈值在seed42未触发，在seed202
仅触发2个低贡献候选（Feature 3434和5605），均已保留警告而未静默删除。

全量输出位于`医学生提交版_v1/05_全量候选分析`：`01_全量候选总表.xlsx`包含两个seed剪枝
候选、共有Feature和3对稳定候选；`06_癌与非癌共有Feature.csv`单列共有组；
`07_临床全量审核表.csv`提供310行填写入口；`08_全量候选图册.html`用于按稳定候选、共有、
癌富集、非癌富集和混合组浏览全部热图。

## SAE当前下一步

1. 先由医学生审核G01--G03，填写是否存在一致可见模式、建议名称、伪特征风险和病灶关系；
2. 工程侧根据临床反馈检查三对Feature与YOLO病灶框的空间重叠，并补充来源/中心分层；
3. 临床候选与规则完全冻结后，才允许对internal test和external作一次描述性投影，评价激活
   稳定性，不再据此修改Feature筛选；
4. 局部解释作为独立研究线，预注册图像级组Top-K空间SAE；它不阻塞当前3对全局候选交付，
   也不能通过放宽SAE-B既有门槛来续跑。
