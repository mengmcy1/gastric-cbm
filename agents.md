# 项目背景与初步规划

## 项目背景

本项目围绕“基于概念提取技术的胃早癌诊断模型透明化研究”展开。临床上，白光胃镜是胃癌筛查和诊断的重要基础手段，但早期胃癌病灶常表现为轻微黏膜改变、边界模糊、色调变化、凹陷或隆起等细微特征，容易受到医生经验、图像质量、反光、黏液、气泡和器械干扰等因素影响，存在漏诊风险。

深度学习模型可以辅助胃镜图像中的癌/非癌判别，但常规模型通常只输出分类结果或概率，缺少对“模型为什么这样判断”的解释。对于真实临床场景，仅有癌/非癌结论不足以建立医生信任，也不利于发现模型是否依赖反光、气泡、黏液、边缘阴影等伪相关特征。因此，本项目希望从单纯分类模型逐步推进到可解释诊断模型，使模型输出诊断结果的同时，也能展示其关注区域、关键视觉概念及其对诊断的贡献。

## 当前数据情况

当前数据位于 `数据` 文件夹下，包含原始胃镜图片文件夹和一个带标签的 CSV 表格。CSV 字段包括图片名字及多个特征标签，其中当前第一阶段只使用最后一列“瘤变标签”进行二分类建模：

- `1`：早癌/瘤变
- `0`：非癌

已初步统计：

- CSV 总行数：3612
- CSV 标签分布：早癌 1933，非癌 1679
- 图片文件数：3314
- CSV 与图片可匹配样本数：3314
- 匹配样本标签分布：早癌 1918，非癌 1396
- CSV 中无对应图片记录：298

建模时应以“CSV 中能与图片文件成功匹配的 3314 张图像”为有效样本，不应直接使用全部 CSV 行。

## 第一阶段目标

第一阶段目标不是直接完成完整概念瓶颈模型，而是先建立一个可靠的胃镜癌/非癌图像分类基线，并生成注意力热图，证明模型能够关注到可疑病灶区域。

阶段性产出应包括：

- 癌/非癌二分类模型
- 每张图像的癌概率输出
- Grad-CAM 或 Grad-CAM++ 注意力热图
- 原图与热图叠加图
- 模型关注区域的 Top-K 可视化示例
- 对热图合理性的初步人工评估

## 模型选择初步规划

当前有效样本约 3314 张，早癌与非癌比例约为 1918:1396，属于中小规模医学图像二分类任务。第一阶段建议采用稳健、可解释工具成熟、容易复现的 CNN 模型，而不是一开始就使用复杂 Transformer。

推荐模型路线：

1. 主模型：ResNet50
   - 使用 ImageNet 预训练权重。
   - 替换最后全连接层为二分类输出。
   - 作为第一阶段主模型，用于训练癌/非癌分类器。
   - 优点是结构经典、训练稳定、Grad-CAM 支持成熟，适合后续生成注意力热图和接入 MOCE。

2. 对照模型：EfficientNet-B0
   - 同样使用 ImageNet 预训练权重。
   - 作为轻量对照模型，比较其 AUC、敏感度、特异度和 F1。
   - 如果 EfficientNet-B0 明显优于 ResNet50，可作为性能增强版本。

3. 暂缓模型：Swin Transformer / ConvNeXt
   - 可作为后续增强实验。
   - 不建议第一阶段作为主线，因为样本规模不大，训练和解释成本更高，且注意力热图工作流不如 ResNet50 直接。

## 数据划分建议

训练、验证、测试集应尽量按患者或检查编号划分，避免同一患者或同一次检查的多张图片同时进入训练集和测试集，造成数据泄漏。

如果暂时无法可靠提取患者 ID，可先采用分层随机划分作为第一版实验，但正式实验中应补充患者级或检查级划分。

建议比例：

- 训练集：70%
- 验证集：15%
- 测试集：15%

划分时保持早癌/非癌比例基本一致。

## 训练与评估指标

训练目标为二分类，建议使用交叉熵损失或带类别权重的交叉熵损失。由于匹配样本中早癌略多于非癌，类别不平衡不算严重，但仍建议监控类别敏感性。

主要评估指标：

- AUC
- Accuracy
- Sensitivity / Recall
- Specificity
- Precision
- F1-score
- Confusion matrix

在胃早癌诊断场景中，应重点关注 sensitivity/recall，避免漏诊。

## 可解释性路线

第一阶段先使用 Grad-CAM / Grad-CAM++ 生成注意力热图，回答“模型看哪里”。

后续再基于 MOCE 进一步提取模型导向概念，回答“模型反复关注的视觉概念是什么”。三篇参考文献的优先级如下：

1. MOCE：优先精读并复现
   - 最贴近当前任务。
   - 适合解释已训练好的图像分类模型。
   - 可从模型自身视角提取概念区域。

2. ProtoMIL：第二阶段参考
   - 重点参考 SAE 概念发现、原型图像和人工干预伪相关概念的思路。
   - 原文面向病理 WSI/MIL，与当前白光胃镜单图分类不完全一致。

3. M-CBM：作为最终框架参考
   - 适合指导后续概念瓶颈模型构建。
   - 当前更适合作为理论框架，不建议作为第一阶段复现主线。

## 当前推荐的第一阶段技术路线

1. 清洗 CSV 与图片路径，仅保留 3314 张匹配图片。
2. 使用“瘤变标签”构建二分类标签。
3. 按患者级或检查级进行训练/验证/测试划分。
4. 使用 ImageNet 预训练 ResNet50 训练癌/非癌分类模型。
5. 使用 EfficientNet-B0 作为对照实验。
6. 在测试集上计算 AUC、sensitivity、specificity、F1 等指标。
7. 对预测正确、预测错误、早癌高置信度、非癌高置信度样本分别生成 Grad-CAM 热图。
8. 请医生初步判断热图是否关注病灶区域，记录伪相关关注案例。
9. 在已有分类模型基础上复现 MOCE，为第二阶段概念提取做准备。

## Phase 1 实验结果（2026-07-08）

### 共同配置

| 配置项 | 值 |
|--------|-----|
| 随机种子 | 42 |
| 数据划分 | 训练 2319 / 验证 497 / 测试 498（分层随机） |
| 类别分布 | 早癌 1918 / 非癌 1396（1.37:1） |
| Batch Size | 32 |
| 优化器 | AdamW, weight_decay=1e-4 |
| 第一阶段 | 冻结 backbone, LR=1e-3, 10 epochs |
| 第二阶段 | CosineAnnealing + EarlyStop(patience=8), LR=1e-4, 20 epochs |
| BN 冻结 | freeze_bn_stats()：冻结层 requires_grad=False 时同步设为 eval |

### ResNet50

| 配置项 | 值 |
|--------|-----|
| 预训练权重 | ImageNet1K_V2 |
| 第二阶段解冻范围 | layer4 + fc |
| Label Smoothing | **0.05**（0.1 过强压低了概率分布） |
| Best Val AUC | 0.8468 |
| Test AUC | 0.8427 |
| Test Accuracy | 0.7751 |
| Test Sensitivity | 0.8160 |
| Test Specificity | 0.7190 |
| Test Precision | 0.7993 |
| Test F1 | 0.8076 |
| **推荐阈值 @Sens≥0.90** | **0.20**（Sens=0.913, Spec=0.522） |

### EfficientNet-B0

| 配置项 | 值 |
|--------|-----|
| 预训练权重 | ImageNet1K_V1 |
| 第二阶段解冻范围 | features[5:] + classifier |
| Label Smoothing | **0.1**（训练动态健康，无过强压制迹象） |
| Best Val AUC | 0.8718 |
| Test AUC | **0.8806** |
| Test Accuracy | 0.8012 |
| Test Sensitivity | 0.8160 |
| Test Specificity | **0.7810** |
| Test Precision | 0.8363 |
| Test F1 | 0.8260 |
| **推荐阈值 @Sens≥0.90** | **0.22**（Sens=0.903, Spec=0.617） |

### 测试集最终评估（验证集调阈值 → 测试集锁定评估）

阈值选择策略：验证集上扫描，Sens≥0.90 约束下选 Spec 最高。用该阈值在测试集上做最终评估，测试集全程不参与阈值决策。

#### ResNet50（阈值 0.20）

| 指标 | 验证集 | 测试集 |
|------|--------|--------|
| Sensitivity | 0.913 | 0.892 |
| Specificity | 0.522 | 0.514 |
| Accuracy | 0.748 | 0.733 |
| Precision | 0.725 | 0.716 |
| F1 | 0.808 | 0.794 |
| Youden | 0.435 | 0.407 |
| CM | (109,100,25,263) | (108,102,31,257) |

#### EfficientNet-B0（阈值 0.22）

| 指标 | 验证集 | 测试集 |
|------|--------|--------|
| Sensitivity | 0.903 | 0.906 |
| Specificity | 0.617 | 0.595 |
| Accuracy | 0.783 | 0.775 |
| Precision | 0.765 | 0.754 |
| F1 | 0.828 | 0.823 |
| Youden | 0.520 | 0.501 |
| CM | (129,80,28,260) | (125,85,27,261) |

#### 结论

- 验证集→测试集泛化良好，未出现大幅衰减，阈值选择没有过拟合验证集
- EfficientNet-B0（5.3M）全面优于 ResNet50（25M）：Test AUC +3.8, Test Spec +6.2 个百分点
- 在 Sens≥0.90 约束下，EfficientNet 测试集 Spec 0.595 vs ResNet 0.514，每 100 例非癌少误报约 8 例
- 后续 Grad-CAM 和可解释性实验建议以 EfficientNet-B0 为主线
