# gastric-cbm 实验进度与结果讨论

## 新对话恢复项目的固定提示词

> 将下面代码块完整复制到新对话中。提示词只规定恢复上下文和协作方法，不代表当前
> 具体实验进度；即时状态必须以本文件后续章节和正式输出为准。

```text
请继续协助我开展 gastric-cbm 胃早癌分类与可解释性研究。

首先确认当前工作目录是项目根目录，并检查至少存在：

- AGENTS.md
- 实验进度与结果讨论.md
- README.md
- 程序/
- 数据去偏重训练_参数与报告/
- 结果/

如果目录结构不符合，请先定位项目根目录，不要在错误位置新建重复项目。

开始操作前按以下顺序恢复上下文：

1. 完整阅读 AGENTS.md。
   - 重点理解项目长期目标、数据边界、训练/验证/test口径、正式代码入口、文件安全、
     隐私、Git和文档维护规则。
2. 阅读 实验进度与结果讨论.md。
   - 先看“当前状态”“当前统一口径”“分类器正式结果”“当前已知问题与处理”
     “当前风险”和“下一步”，再按任务需要查历史记录。
3. 阅读相关模块README和正式报告。
   - 分类任务查看程序/模型训练/正式代码/和数据去偏重训练_参数与报告/；
   - Grad-CAM查看程序/注意力热图/正式代码/；
   - MOCE查看程序/MOCE/正式代码/和结果/MOCE分析/；
   - SAE查看程序/SAE/正式代码/、结果/SAE/和结果/SAE分析/。
4. 需要确认数字时直接读取正式CSV、JSON、config和冻结manifest，不要只依赖Markdown
   摘要或旧对话。
5. 只读检查git status和相关输出目录，确认是否存在用户尚未提交的修改、未完成运行、
   同名输出或待人工复核材料。

按信息类型使用不同的可信度优先级：

- 数值与运行事实：冻结manifest、run config、正式CSV/JSON > 正式专题报告 >
  实验进度与结果讨论.md > AGENTS.md中的阶段摘要 > 归档文档和旧对话；
- 程序实际行为：当前正式代码 > 对应README > 归档代码和旧对话；
- 文件安全、实验边界和协作规则：AGENTS.md最高。

开始新任务前先在内部核对以下六项，只向用户简要汇报与本轮相关的内容：

1. 当前任务属于分类器、数据、Grad-CAM、MOCE、SAE还是M-CBM；
2. 上一阶段是否已经完成并锁定；
3. 本轮输入、模型、split、阈值、聚合方式和输出目录是什么；
4. 本轮是否会接触test或外部test，是否存在调参泄漏风险；
5. 有哪些内容可以自动验证，哪些必须由临床人员判断；
6. 本轮完成后将影响哪个决策。

不要因为对话上下文不完整而重新训练或重复生成。先检查已有结果；若结果完整，只做
缺失的投影、分析、整理或复核。

实验执行原则：

- 一轮尽量只改变一个关键变量；
- 超参数和剪枝只由train/val确定，内部test和外部test只做锁定评估；
- 患者级划分和患者级指标优先，图像级作为补充；
- 运行GPU任务前先用`nvidia-smi`确认空闲设备，不默认固定GPU始终可用；
- 长任务先debug，输出验收后再全量；
- 不覆盖原始数据、冻结清单、权重、历史结果或人工标注；
- 不批量删除，旧内容移动到归档；
- 未经明确要求不提交、不推送Git；
- 不把患者图像、预测明细、特征缓存或解释图上传公共仓库。

每轮开始前用简短、通俗的话说明：

1. 本轮解决什么问题；
2. 使用什么输入并生成什么输出；
3. 为什么现在需要做；
4. 通过后进入哪一步。

每轮结束按以下格式汇报：

1. 本轮做了什么；
2. 结果如何；
3. 哪些结论已确认，哪些只是候选；
4. 对后续有什么影响；
5. 下一步计划；
6. 新增/修改文件和验证情况。

完成正式实验后更新本文件的当前状态、结果、风险和下一步。只有长期规则、稳定事实、
正式入口或重要里程碑变化时才更新AGENTS.md。固定提示词始终保留在本文件开头，不在
提示词中写具体即时进度。
```

---

## 文档用途

本文件是当前项目的实时交接记录，维护：

- 当前阶段和立即任务；
- 已完成实验及其正式结果；
- 已冻结参数、协议和输出；
- 自动检查、人工复核和异常；
- 候选结论、结论边界和风险；
- 下一步及其决策标准。

详细逐样本数据留在正式CSV/JSON，长篇方法和统计讨论留在专题报告。本文件完整保留
重要阶段表格和总结，同时避免粘贴逐次运行日志。

## 当前状态

更新时间：2026-08-10

| 模块 | 状态 | 当前说明 |
| --- | --- | --- |
| 数据审计与v1.1裁剪 | 已完成 | 第二批、概念集和完整外部集均已形成正式清单与验收结果 |
| 去偏分类器实验A/A0/B/C/D | 已完成 | 主实验矩阵、严格共同支持5折和比例敏感性均已完成 |
| 患者聚合、集成与TTA | 已完成并锁定 | `top2_mean`、两模型等权、无TTA |
| 完整外部多中心测试 | 已完成 | 1329人/1941张，单模型与集成均已锁定评估 |
| Grad-CAM | 已完成正式批量脚本 | 支持ResNet50/EfficientNet-B0切换和按清单顺序输出 |
| 严格概念集MOCE | 已完成 | 两模型、两类别正式结果和自动分析均已生成 |
| MOCE聚类数量敏感性 | 已完成 | `K=10,15,...,50`共36组及跨K材料已生成 |
| ResNet50 SAE训练 | 已完成并锁定 | `2048→512→2048`，剪枝后450个Feature |
| SAE外部投影与彩色图像中心分析 | 已完成 | 外部1941张全量；100位患者彩色提交版已整理 |
| ResNet50 SAE内部test投影 | 已完成 | 732张/349人；剪枝SAE患者AUC 0.9234 |
| SAE跨数据集Feature定量稳定性 | 已完成 | train/val预选30个；四域方向一致21个 |
| EfficientNet-B0 SAE | 暂缓/等待新模型 | 当前v1.1不再单独启动；待新数据最终分类器锁定后按实际特征维度重建 |
| 新一版平衡分类数据 | 已交付，初审未通过直接训练条件 | `胃早癌概念提取训练集0804`共3577张；图片数近似平衡，但患者数和分辨率/画幅明显不平衡，需先构建来源内患者与风格匹配清单 |
| 新图像裁剪与Keep/Notch | 已完成并可进入M0 | PIP复核确认149张/120人，147张自动框、2张人工框；Notch与Keep尺寸一致，26张框QC全部人工通过；每分3577张的配对manifest已生成，其中2张外置PIP在Notch分支按普通图片分组 |
| M0全量诊断基线 | val矩阵与外部评估完成 | 开发集3348张/1732人；跨种子暂选Keep + EfficientNet-B0；外部1329人/1941张患者AUC 0.7399±0.0075、图片AUC 0.7238±0.0079；internal test仍未评估，待来源内1:1.3平衡M0确认 |
| M0来源内1:1.3平衡 | 正式val、尺度消融与外部评估完成 | val锁定Keep + EfficientNet-B0 + A0；val患者/图片AUC 0.8523±0.0070/0.8187±0.0098，外部为0.6807±0.0107/0.6563±0.0131；严格1:1和A0/A1/A2均完成；internal test继续锁定 |
| 框辅助分类器 | 两套M1清单冻结、CUDA冒烟通过 | 平衡主清单1373张/1020人、全量诊断清单3348张/1732人，癌图bbox均覆盖100%且无跨患者SHA重复；两套清单均已用对应M0 seed42权重完成GPU调试，六组矩阵入口已准备，正式训练尚未开始 |
| 扩张型SAE实验 | 路线已冻结，尚未运行 | 先保留现有512维参照，再只用train/val逐级比较完整宽度和扩张字典 |
| 医学概念命名 | 进行中/待临床 | MOCE与SAE材料已交付医学生 |
| M-CBM/CBL | 暂未正式启动 | 等待稳定概念命名；代码入口已准备 |

## 当前统一口径

- 患者级指标是主要临床口径，图像级指标作为补充。
- train/val用于模型、阈值、聚合、解释器超参数和剪枝选择；内部test与外部test只做
  锁定投影和评估。
- 内部高AUC必须与严格共同支持实验和独立外部结果一起解释，不能单独引用为多中心
  泛化性能。
- MOCE的SSC step 0首先反映黑色遮挡输入的类别偏置，不代表黏膜特征一致性。
- SAE彩色热图是7x7空间响应上采样；红色表示促癌logit margin贡献，蓝色表示负向
  贡献，紫色通常来自红蓝叠加，不是病灶分割或第三种贡献类型。
- 外部癌患者图片数显著多于非癌，仅图片数预测标签AUC为0.980；所有患者聚合结果必须
  同时报告mean和随机单图等敏感性分析。
- 新扩张SAE与EfficientNet-B0 SAE必须先在本文档冻结候选网格和选择规则，只用
  train/val完成选择。
- 下一版开发数据以早癌大赛和武大省人医为主，但必须在每个来源内部同时平衡癌/非癌
  患者、图片数量和画面风格；不得用“一个来源的癌+另一个来源的非癌”形成表面平衡。
- 病灶框以冻结的高分辨率Keep图（空间裁剪+FOV后，保留PIP）坐标为标注基准，同时记录
  图像尺寸、SHA256、`geometry_id`和坐标约定（左闭右开）。Notch只改变像素且与Keep共享
  `geometry_id`，因此M0选定Notch后无需重新标注；必须保留Keep到Notch的恒等坐标映射，并对
  bbox与Notch黑块相交的图片单独复核。带可视化矩形框的图片只用于人工验收，不作为训练
  输入。原坐标保存规则仅适用于
  标注在裁剪前完成的旧流程，此时必须同步变换bbox并校验。（2026-08-03修正：新数据
  改为crop-first标注，见“新一版数据边界”。）

## 当前正式数据

### 第二批分类数据

- 原始整理数据：5229张；
- 癌/高级别：1600张、415位患者；
- 非癌：3629张、1906位患者；
- 正式分类器输入：`数据/第二批裁剪后_v1_1`；
- 实验A冻结划分：
  - train：1624位患者、3704张；
  - val：348位患者、793张；
  - test：349位患者、732张。

### 医学生整理概念集

- 原始审计：3601张，0解码失败；
- 早癌1814张/487人，非癌1787张/1393人；
- 原图图片数接近平衡，但分辨率与画面风格严重不平衡；
- 排除跨标签患者和完全重复副本后，每患者限1至3张；
- MOCE严格核心集：314位患者、157对、502张，两类各251张；
- 严格集用于概念发现，不替代分类器训练集。

### 独立外部多中心集

- 1329位患者、1941张；
- 早癌：121人、542张；
- 非癌：1208人、1399张；
- 与第二批训练原图无SHA重叠；
- 癌患者图片数明显多于非癌，患者聚合结果必须同时报告敏感性分析。

## 分类器正式结果

### 第一批图像随机划分历史基线

第一批实验使用3314张可匹配图像，按图片分层随机划分为2319/497/498张。验证集选择
`Sensitivity >= 0.90`阈值后，图片级test锁定结果如下：

| 模型 | 图片AUC | 阈值 | Sens | Spec | Acc | Precision | F1 | TN/FP/FN/TP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| ResNet50 | 0.8427 | 0.20 | 0.892 | 0.514 | 0.733 | 0.716 | 0.794 | 108/102/31/257 |
| EfficientNet-B0 | 0.8806 | 0.22 | 0.906 | 0.595 | 0.775 | 0.754 | 0.823 | 125/85/27/261 |

阶段总结：

- 该阶段证明两类CNN和完整训练、阈值、推理流程可以跑通，EfficientNet-B0点估计较高；
- 划分按图片而不是患者进行，且原数据存在标签和划分问题，同一患者泄漏风险未被排除；
- 这组数字只说明项目早期迭代，不能与后续患者级结果直接比较，也不再作为正式模型。

### 第二批整理后未裁剪旧模型

原始`第二批`清理为`第二批整理后`后，使用5229张、2321位患者完成首次患者级训练。
训练期自然阈值0.50的图片级test结果：

| 模型 | 图片AUC | Acc | Sens | Spec | Precision | F1 | TN/FP/FN/TP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| ResNet50 | 0.9900 | 0.9703 | 0.9526 | 0.9779 | 0.9485 | 0.9505 | 530/12/11/221 |
| EfficientNet-B0 | 0.9910 | 0.9677 | 0.9569 | 0.9723 | 0.9367 | 0.9467 | 527/15/10/222 |

验证集按`Sensitivity >= 0.90`选择阈值后，同一774张图片test的锁定结果：

| 模型 | 阈值 | 图片AUC | 患者AUC | Sens | Spec | Acc | Precision | F1 | TN/FP/FN/TP |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | --- |
| ResNet50 | 0.30 | 0.990 | 0.979 | 0.978 | 0.867 | 0.901 | 0.759 | 0.855 | 470/72/5/227 |
| EfficientNet-B0 | 0.22 | 0.991 | 0.983 | 0.987 | 0.799 | 0.855 | 0.678 | 0.804 | 433/109/3/229 |

阶段总结：

- 患者级划分修复了第一批最直接的同患者泄漏风险，但输入仍保留黑边、设备界面、原始
  分辨率和来源风格；
- ResNet50验证集外院子集图片AUC仅0.727，而全量验证AUC为0.960；EfficientNet-B0
  全量test图片AUC为0.991，外院子集为0.921，显示来源构成显著影响总体指标；
- 验证集癌图主要来自武大省人民、外院图主要为非癌，约0.99的内部AUC不能解释为
  跨医院病灶识别能力；
- 相关权重只用于裁剪前后诊断对照和历史解释，不再作为部署或后续正式解释模型。

### 实验A内部冻结结果

实验A使用v1.1完整裁剪数据和标签×来源分层患者划分。阈值只在验证集按
`Sensitivity >= 0.90`选择，再锁定到349位内部test患者：

| 模型 | 患者AUC（95% CI） | 阈值 | Sens | Spec | Acc | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ResNet50 | 0.928（0.899-0.954） | 0.385 | 0.889 | 0.864 | 0.868 | 0.709 |
| EfficientNet-B0 | 0.931（0.896-0.961） | 0.389 | 0.841 | 0.839 | 0.840 | 0.654 |

验证集患者AUC分别为0.913和0.931。同一患者配对比较的test AUC差为-0.003
（95% CI -0.029至0.024，`p=0.792`），不支持两模型在实验A中存在显著差异。

完整实验矩阵患者级主要结果：

| 实验 | 数据协议 | ResNet50 AUC | EfficientNet-B0 AUC | 模型差异p值 |
| --- | --- | ---: | ---: | ---: |
| A | 完整裁剪数据，新标签×来源划分 | 0.928 | 0.931 | 0.792 |
| A0 | 完整裁剪数据，复用旧患者划分 | 0.953 | 0.957 | 0.721 |
| B | 松弛匹配1:1 | 0.929 | 0.951 | 0.199 |
| C | 严格共同支持1:1，5折OOF | 0.814 | 0.785 | 0.313 |
| D | 松弛匹配1:2 | 0.944 | 0.957 | 0.194 |

严格共同支持实验C包含90对、180位患者和238张图。每折阈值只由该折验证患者确定，再
锁定到测试折：

| 模型 | 患者OOF AUC（95% CI） | 图片OOF AUC | Sens | Spec | Acc | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ResNet50 | 0.814（0.750-0.872） | 0.797 | 0.800 | 0.611 | 0.706 | 0.731 |
| EfficientNet-B0 | 0.785（0.712-0.849） | 0.782 | 0.744 | 0.522 | 0.633 | 0.670 |

阶段总结：严格集结果说明完整与松弛数据的内部高AUC仍受残余混杂影响；但严格集规模
较小、阈值波动较大，其结果是已知画面因素控制后的敏感性分析，不是全部患者总体性能。

内部集成方案由验证集选择：

- 聚合：`top2_mean`；
- 权重：ResNet50 0.5 + EfficientNet-B0 0.5；
- TTA：关闭；
- 阈值：0.518973；
- 冻结test患者AUC：0.9578；
- test Accuracy/Sensitivity/Specificity：0.8940/0.8889/0.8951。

349位内部冻结test患者的完整集成对比：

| 方案 | 锁定阈值 | 患者AUC | Acc | Sens | Spec | F1 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: |
| ResNet50，mean | 0.385 | 0.9283 | 0.8682 | 0.8889 | 0.8636 | 0.7089 |
| EfficientNet-B0，mean | 0.389 | 0.9314 | 0.8395 | 0.8413 | 0.8392 | 0.6543 |
| 等权集成，top2_mean | 0.518973 | 0.9578 | 0.8940 | 0.8889 | 0.8951 | 0.7517 |

集成相对ResNet50和EfficientNet-B0的患者AUC差分别为+0.0295
（95% CI +0.0100至+0.0492）和+0.0264（+0.0129至+0.0421）。水平翻转TTA未通过
验证集选择，正式方案保持关闭。

正式报告：

- `数据去偏重训练_参数与报告/完整裁剪数据重训练实验A结果_v1.md`
- `数据去偏重训练_参数与报告/患者聚合集成TTA评估结果_v1.md`

### 偏倚敏感性

- 旧权重直接输入v1.1裁剪test时，ResNet50患者AUC从0.979降至0.772，
  EfficientNet-B0从0.983降至0.893；
- 实验A0在同一旧split上重训后，患者AUC恢复到0.953/0.957；
- 严格共同支持集5折OOF患者AUC为ResNet50 0.814、EfficientNet-B0 0.785；
- 松弛1:1和1:2实验AUC较高，但元数据仍可强预测标签，只作覆盖和敏感性分析。

当前解释：v1.1重训恢复了内部判别能力，但完整与松弛数据仍有残余来源/风格混杂。
严格共同支持结果更能反映去除主要已知画面差异后的困难程度。

正式报告：

- `数据去偏重训练_参数与报告/旧模型在v1_1裁剪数据验证结果_v1.md`
- `数据去偏重训练_参数与报告/严格共同支持集5折结果_v1.md`
- `数据去偏重训练_参数与报告/P7正式实验汇总_v1.md`

### 完整外部多中心锁定测试

独立完整外部集包含1329位患者、1941张图，其中癌121人、非癌1208人。以下完整复用
实验A锁定的模型、阈值、聚合和集成参数：

| 方案 | 聚合 | 阈值 | 患者AUC（95% CI） | Sens | Spec | Balanced Acc | Precision | NPV | TP/FN | TN/FP |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| ResNet50 | mean | 0.385 | 0.693（0.649-0.736） | 0.628 | 0.660 | 0.644 | 0.156 | 0.947 | 76/45 | 797/411 |
| EfficientNet-B0 | mean | 0.389 | 0.738（0.696-0.780） | 0.793 | 0.620 | 0.707 | 0.173 | 0.968 | 96/25 | 749/459 |
| 等权集成 | top2_mean | 0.518973 | 0.802（0.761-0.840） | 0.719 | 0.734 | 0.727 | 0.213 | 0.963 | 87/34 | 887/321 |

重要边界：

- 外部性能明显低于内部test，支持存在跨中心域偏移；
- 仅患者图片数量预测标签AUC为0.980；
- 集成改用mean聚合时AUC为0.729；
- 每患者随机单图重复抽样时，ResNet50/EfficientNet-B0/集成平均AUC约
  0.669/0.716/0.709；
- 因此集成0.802不能全部解释为病灶判别或模型互补。

正式报告：

`数据去偏重训练_参数与报告/完整多中心外部测试结果_v1.md`

## Grad-CAM进度

- 正式入口：`程序/注意力热图/正式代码/gradcam_batch.py`；
- 一次只运行一个模型，可在ResNet50与EfficientNet-B0间切换；
- 输出按清单顺序保存，并记录真实标签、预测标签和癌概率；
- 三联图版式、标题字号和位置已固定；
- 正式结果按模型分目录，旧热图已归档；
- 热图用于定位模型关注，不作为病灶分割；
- 后续临床复核重点是共同漏诊、共同误报以及是否关注反光、暗腔、边缘和界面。

## MOCE进度

### 正式严格概念集

- 输入：502张/314人/157对；
- ResNet50候选区域：非癌23351、癌25066；
- EfficientNet-B0候选区域：非癌37007、癌37745；
- 两类均完成聚类、重要性、SSC/SDC、概念总览、扩展图和命名表；
- 正式分析目录：
  - `结果/MOCE分析/resnet50/`
  - `结果/MOCE分析/efficientnet_b0/`

### 当前稳定观察

- 两模型均表现为癌侧少数概念对输出贡献更集中；
- 非癌侧单一概念移除影响较弱；
- ResNet50癌侧移除前五概念的破坏更强；
- EfficientNet-B0癌侧前五概念在黑图上恢复输出的充分性更高；
- 这些差异可能受特征层、候选区域粒度、概念重叠和遮挡基线影响，不能直接转述为
  “某模型学到了更真实的癌概念”。

### 聚类数量敏感性

- 已完成`K=10,15,...,50`；
- 两模型、两类别共36组；
- 自动分析和医学生提交材料位于`结果/MOCE聚类数量分析/`；
- 选择K时需要同时看簇内一致性、簇间重复、临床可命名性、重要性稳定性和跨K对应，
  不能只看单一聚类指标。

### 待人工工作

- 优先检查癌侧高重要性概念是否落在病灶；
- 标记反光、黏液、器械、黑边、暗腔和设备界面等伪概念；
- 比较同类概念是否跨患者、跨模型、跨K稳定；
- 形成可进入M-CBM/CBL的共识命名。

## SAE进度

### 正式ResNet50 SAE

正式run：

`结果/SAE/去偏重训练_v1/resnet50/formal_expA_resnet50_sae_l1_0005_20260724`

配置：

- 输入：ResNet50 layer4 GAP，2048维；
- SAE：`2048 → 512 → 2048`；
- `lambda_l1=0.0005`；
- batch size 32；
- 最大1000 epoch，patience 50；
- 最佳epoch 104；
- train：3704张/1624人；
- val：793张/348人；
- 512个Feature剪枝后保留450个；
- train平均L0为17.64，val平均L0为27.63。

内部验证保真度：

| 层级 | 原模型AUC | 剪枝SAE AUC | 变化 |
| --- | ---: | ---: | ---: |
| 图像级 | 0.8961 | 0.8781 | -0.0180 |
| 患者级 | 0.9135 | 0.9031 | -0.0104 |

外部投影：

| 层级 | 原模型AUC | 剪枝SAE AUC | 变化 |
| --- | ---: | ---: | ---: |
| 图像级 | 0.6812 | 0.6726 | -0.0085 |
| 患者级 | 0.6934 | 0.6929 | -0.0004 |

解释：SAE外部患者级排序保真度较高，但不能把0.693解释为SAE造成的低性能；被解释的
ResNet50原模型外部AUC本身就是0.693。

内部test锁定投影：

| 层级 | 原模型AUC | 完整SAE AUC | 剪枝SAE AUC | 剪枝变化 |
| --- | ---: | ---: | ---: | ---: |
| 图像级 | 0.8919 | 0.8823 | 0.8806 | -0.0113 |
| 患者级 | 0.9283 | 0.9247 | 0.9234 | -0.0049 |

锁定患者阈值0.38494下：

| 表示 | Acc | Sens | Spec | F1 | TN/FP/FN/TP |
| --- | ---: | ---: | ---: | ---: | --- |
| 原模型 | 0.8682 | 0.8889 | 0.8636 | 0.7089 | 247/39/7/56 |
| 完整SAE | 0.8367 | 0.8254 | 0.8392 | 0.6460 | 240/46/11/52 |
| 剪枝SAE | 0.8682 | 0.8254 | 0.8776 | 0.6933 | 251/35/11/52 |

- 732张、349位患者全部完成，患者标签分布为非癌286/癌63；
- 原模型概率与实验A冻结输出逐图一致，最大绝对误差为`1.6e-7`；
- 剪枝SAE患者AUC只下降0.0049，排序保真度较高；
- 锁定阈值下患者Sensitivity下降0.0635，同时Specificity上升0.0140，说明SAE重构造成
  概率标度和决策边界漂移；不得用test重新调阈值修饰该结果；
- 输出：`结果/SAE/去偏重训练_v1/resnet50/internal_test_resnet50_sae_20260731/`。

### 图像中心彩色SAE

- 外部1941张全部完成Top-5图像中心可视化；
- 每图包含综合彩色图、5个独立Feature图、统计表和总览；
- 红色为促癌方向，蓝色为负向方向，紫色可由重叠产生；
- 全量目录约16GB：
  `结果/SAE图像中心可视化/external_multicenter_all_top5_vivid_bg_20260729/`；
- 医学生彩色提交版为TP/TN/FP/FN各25位患者，共100人：
  `结果/SAE分析/去偏重训练_v1/resnet50/SAE图像中心彩色热图_医学生提交版_v1/`。

### 跨数据集Feature稳定性

使用同一锁定SAE已有缓存，对齐train、val、内部test和外部test：

| 数据集 | 图片 | 患者 | 用途 |
| --- | ---: | ---: | --- |
| train | 3704 | 1624 | 候选筛选 |
| val | 793 | 348 | 候选筛选 |
| internal test | 732 | 349 | 锁定确认 |
| external test | 1941 | 1329 | 锁定确认 |

候选只根据train/val产生：保留剪枝Feature、两域至少各激活5位患者、标签激活方向一致，
再按两域最小标签区分度与覆盖率排序；癌相关和非癌相关各取15个。test与external没有
参与候选筛选。

定量结果：

- 30个候选全部满足激活方向与分类margin贡献方向一致；
- 21/30在四个数据集保持相同标签激活方向；
- 癌相关候选15/15方向一致，非癌相关候选6/15方向一致；
- 22/30在internal test和external test的最小标签区分度不低于0.05，14/30不低于0.10；
- Feature覆盖率的train/val/internal test Spearman为0.879/0.872/0.855量级，与external
  的相关性降至0.574-0.624；
- 标签激活差异的train-external Spearman仅0.141，说明外部域中Feature排序发生明显变化；
- 来源相关Feature 48的来源AUC在train/val/internal test为0.722/0.774/0.726，应优先
  作为domain Feature审核，不应直接命名为医学概念。

上述“稳定”只表示同一SAE方向在不同患者集合中的统计关系，不证明空间响应落在病灶，
也不证明医学语义一致。正式输出：

`结果/SAE跨数据集稳定性/去偏重训练_v1/resnet50/cross_dataset_feature_stability_20260731/`

### SAE当前待做

1. 对已由train/val冻结的30个候选制作跨数据集空间响应对照：
   - 优先展示四域方向一致的21个Feature；
   - 同时单列Feature 48等来源相关方向作为伪特征对照；
   - 检查热点是否稳定落在病灶、正常黏膜、反光、边缘或设备界面。
2. 设计扩张型SAE train/val实验：
   - 现有512维作为基线；
   - 优先比较2048维和4096维；
   - L1需重新扫描或采用warm-up；
   - 只用val比较L0、重构、预测保真度、死Feature、重复性和概念一致性；
   - 开始前冻结候选维度、L1或warm-up规则、允许的AUC下降、最低预测一致率，以及
     L0、死Feature和重复Feature的选择标准；
   - 方案锁定后才能运行一次内部test和外部投影；由于这些test结果已经被查看，新扩张
     实验应明确标为预先固定规则后的补充/探索性分析，不包装成完全未见的确认性实验。
3. 当前v1.1 EfficientNet-B0 SAE暂缓：
   - 不再为即将被新数据与新分类路线替代的旧EfficientNet-B0单独启动正式SAE；
   - 现有1280维适配需求和旧计划保留，避免误解为技术上不可实现；
   - 待新数据最终分类器和待解释层锁定后，再按实际特征维度独立训练，不能复用
     ResNet50 SAE；
   - 如最终仍选择EfficientNet-B0，则只用新模型train/val确定字典规模和L1，再锁定
     投影内部test与外部test。
4. 医学命名稳定后再运行`mcbm_cbl_train.py`。

## 下一阶段数据与技术路线

本节记录2026-07-31与医学生、其他工程师讨论后冻结的方法路线。以下内容是待执行协议，
不是已经取得的实验结果；实际参数仍须在运行前写入配置并只用train/val选择。

### 新一版数据边界

- 开发数据以早癌大赛和武大省人医为主；
- 两个来源分别在来源内部平衡癌/非癌患者数、图片数和主要画面风格，不能只在合并后
  达到总量1:1；
- 继续执行患者级划分、同检查近重复去除、每患者图片数控制和来源×标签审计；
- 外院数据原则上作为冻结外部测试，不参与模型、阈值、增强、聚合或解释器超参数选择；
- 医学生基于冻结后的高分辨率Keep图进行标注，交付无可视化框的Keep图、
  患者与标签信息以及结构化矩形框坐标；同时保留原图用于追溯和临床对照。带框预览图
  只用于人工验收，禁止作为训练输入；
- 癌图需要记录`lesion_visible`和有效矩形框；癌但病灶不可见的图不能计算定位损失。
  非癌中的局灶性良性病变如能获得框和病变类型应一并保留，完全正常图可为空框。

建议的最小框字段为：

```text
image_path, image_sha256, geometry_id, image_width, image_height, patient_id, label, lesion_visible, x1, y1, x2, y2, coordinate_convention
```

`image_path`指向冻结后的无可视化框Keep图，`coordinate_convention`首版固定为
`xyxy_left_closed_right_open`。原图到Keep、Keep到Notch的变换记录保留在数据manifest中，
不与病灶框坐标系混用。

#### 0804交付数据初审（2026-08-04）

新交付目录为`数据/胃早癌概念提取训练集0804`，原图保持只读。完整扫描结果：

| 范围 | 癌/高级别 | 非癌 | 初步判断 |
| --- | ---: | ---: | --- |
| 全部 | 1779张/487人 | 1798张/1369人 | 图片近似1:1，患者明显不平衡 |
| 武大省人民 | 1431张/375人 | 1447张/1188人 | 图片平衡，患者和画面风格严重不平衡 |
| 第一届早癌大赛 | 70张/27人 | 70张/46人 | 分辨率和画幅一致，患者不平衡 |
| 第二届早癌大赛 | 177张/46人 | 166张/54人 | 患者较接近，画幅/尺寸接近 |
| 外院合计 | 101张/39人 | 115张/81人 | 不进入主开发集，继续按外部用途隔离 |

- 3577张均可解码，0个零字节文件；共有2组完全重复、额外2张，均在癌侧；
- 武大省人民有2位跨标签患者：`01.0000000129422`、`01.0000000166648`，在临床确认前
  不得进入任何split；
- 癌图中`1920x1080`为1300张，非癌为695张；精确分辨率TV为0.4829、宽高比分组TV
  为0.3442、尺寸分组TV为0.3852，不能把全量3577张直接作为去偏训练集；
- 第一届大赛三项风格TV均为0；第二届大赛宽高比TV为0.0181、尺寸TV为0.0200；主要
  风格失衡来自武大省人民；
- 0804外院216张与锁定外部多中心测试1941张无完全SHA重叠，但仍从主训练候选中排除；
- 下一步应先生成来源内患者级癌:非癌约1:1.3、每位患者图片上限一致且画面风格匹配的
  冻结候选清单，再进入平衡M0。严格1:1保留为最终入选骨干的敏感性分析；筛选通过
  manifest完成，不移动或删除原图。

#### M0全量诊断基线（2026-08-05冻结）

在来源内平衡集之前，先跑一轮不做患者数平衡的全量M0，用于确认信号上限、
Keep/Notch影响、两种骨干差异和风格shortcut风险。该实验只是诊断基线，不取代
来源内平衡核心实验；无论全量结果是否显示明显风格依赖，平衡对照均必须继续。

全量的精确边界为：

- 只使用武大省人民、第一届早癌大赛和第二届早癌大赛；
- 0804中的216张外院图仍按补充外部队列隔离，不进入train/val/internal test；
- 整体排除2位跨标签患者`01.0000000129422`、`01.0000000166648`；
- 完全重复图按Keep SHA256确定性保留1张，禁止重复副本跨split；
- 预计合格开发集为3348张/1732人，图片约为1:1，患者明显不平衡；
- 用`source×label`做患者级分层，按70/15/15生成唯一冻结split，split seed为42；
- Keep与Notch共用同一患者split、图片ID、增强、训练参数和随机种子。

正式M0在查看任何训练结果前冻结：

```text
preprocess_delta_auc_noninferiority = 0.005
backbone_delta_auc_noninferiority = 0.005
delta_sensitivity_noninferiority = 0.02
delta_specificity_noninferiority = 0.02
training_seeds = [42, 202, 503]
split_seed = 42
split_ratio = 0.70 / 0.15 / 0.15
threshold_rule = val患者Sensitivity >= 0.90时Specificity最大
minimum_val_sensitivity = 0.90
minimum_val_specificity = 0.50
cross_seed_summary = 均值 + 标准差 + 最差种子
paired_comparison = 同一val患者配对bootstrap 2000次
```

主指标是跨种子val患者AUC均值；Sensitivity和Specificity使用上述val锁定阈值。
internal test只在方案锁定后评估，不参与Keep/Notch、骨干、epoch或阈值选择。
风格判断同时依据来源/尺寸/画幅子组、元数据基线、来源留出和Grad-CAM，
不以单一内部AUC作判断。

### 阶段0：现有v1.1尺度增强预实验

阶段0只验证训练增强行为和代码稳定性，不把旧数据上的最优参数直接宣布为新数据最优。
开始前先确认现有训练脚本实际使用的`RandomResizedCrop`参数。

候选单变量对照：

| 编号 | RandomResizedCrop scale | 说明 |
| --- | --- | --- |
| A0 | 当前正式参数 | 基线 |
| A1 | `(0.7, 1.0)` | 相对当前基线轻度放宽随机裁剪范围 |
| A2 | `(0.5, 1.0)` | 相对当前基线较强放宽随机裁剪范围 |

当前正式基线为`scale=(0.85, 1.0)`、`ratio=(0.90, 1.10)`。`scale`表示裁剪区域占
原图面积的比例；下界从0.85降到0.7或0.5，会扩大随机裁剪范围并增强随机放大程度，
同时增加无bbox癌图被裁掉病灶却保留癌标签的风险。阶段0不使用Torchvision默认
`(0.08, 1.0)`，也不优先使用`(0.3, 1.0)`。同时检查`ratio`，避免胃镜结构被过度拉伸。

阶段0使用完全相同的患者划分、预训练权重和训练配置，至少比较3个随机种子。只用val
比较患者AUC、Sensitivity、Specificity、最佳epoch和过拟合趋势；外部test不参与选择。
通过只表示该增强在旧数据上未造成稳定退化，新数据到位后仍须重新确认。

### M1：CenterNet式辅助定位正则

第一版不做ROI特征融合，也不把定位头作为可部署检测器。其唯一目的，是用癌图矩形框
迫使共享encoder保留病灶位置相关特征，再观察分类和外部泛化是否改善。

正式首版结构：

```text
完整图 → ResNet50/EfficientNet encoder
       ├─ 最终特征 → GAP → 分类头
       └─ layer3特征 → 3x3 Conv + ReLU
                      ├─ 1x1 Conv → center heatmap（1通道）
                      ├─ 1x1 Conv → width/height（2通道）
                      └─ 1x1 Conv → center offset（2通道）
```

ResNet50的`layer3`约为`14x14`空间分辨率。中心目标使用按bbox大小生成的二维高斯热图；
宽高和offset只在真实中心位置计算回归损失。offset用于补偿14x14网格的中心量化误差。

```text
L_loc = L_heatmap_focal
      + lambda_size * L_size
      + lambda_offset * L_offset

L_total = L_cls + lambda_loc * I_valid_box * L_loc
```

- `L_cls`由所有图片计算；
- `I_valid_box`只对有可见、合法病灶框的癌图为1；
- 非癌图和病灶不可见癌图在M1中只计算分类损失；
- M1没有独立图像级objectness；center heatmap仍是位置置信度，但不宣称能够判断非癌
  图中是否存在病灶。

定位验收不能只看分类AUC，还需报告中心命中率、中心距离、IoU或框覆盖率，并与固定预测
画面中心的朴素基线比较。M1即使暂未提高分类AUC，只要定位明显可学且分类未稳定退化，
仍可继续测试特征金字塔；定位完全不可学时先检查标注、病灶可见性和特征分辨率。

### 后续检测与全局/局部路线

M2在M1基础上融合`layer2/layer3/layer4`特征，形成内部特征金字塔，以改善小病灶定位。
M3再把非癌图作为全零中心热图参与heatmap损失，使模型学习“无癌病灶”；框回归仍只对
有效癌框计算。heatmap最大响应可作为病灶存在置信度，是否另加图像级objectness头由
train/val消融决定。

定位模型可用后再进入ROI融合：

- M4使用医生真值框裁出的高分辨率局部图，与低分辨率完整图融合，用于测量理论上限；
- M5使用定位模型预测框，代表真实可部署结果；
- 局部裁图从正式处理输入图的未缩放分辨率版本生成并保留适量框外黏膜，不能使用
  已画框图片；
- M4可对真值框做合理平移、缩放和边界扰动；真值框与预测框渐进混合只作为后续课程
  学习或联合微调候选，不并入第一版M4/M5；
- 无可靠候选框时必须退化为全局分类，不因空ROI导致静默错误。

正式实验矩阵：

| 编号 | 定位监督 | 多尺度方式 | ROI融合 | 决策问题 |
| --- | --- | --- | --- | --- |
| M0 | 无 | 常规增强 | 无 | 新数据CNN分类基线 |
| M1 | 癌图辅助定位 | layer3单层 | 无 | bbox监督是否改善表示 |
| M2 | 癌图辅助定位 | layer2/3/4特征金字塔 | 无 | 小病灶定位是否改善 |
| M3 | 癌/非癌热图监督 | 选定的M1或M2定位结构 | 无 | 自动检测是否可用 |
| M4 | 同M3 | 完整图+高分辨率局部 | 真值框 | 全局/局部理论上限 |
| M5 | 同M3 | 完整图+高分辨率局部 | 预测框 | 最终部署性能 |

不采用`112x112`缩小后黑边pad至`224x224`的双分支：两路没有新增互补内容，缩小图会
损失早癌细节，固定黑边还可能重新引入边界shortcut。无标注阶段优先使用保守尺度增强；
有框阶段的“完整低分辨率视野+处理输入未缩放分辨率ROI”才作为真正的信息金字塔。
本节的“未缩放分辨率”均指正式处理输入图在网络resize前的尺寸，不是采集原图尺寸。

小型Transformer不是M0-M5的替代。先用CNN验证监督方式，再将全局骨干替换为Swin-T等
小模型做受控对照，避免同时改变数据、监督、尺度和backbone而无法归因。

### 分类器分阶段训练与冻结协议

本节规定M0-M5各阶段的输入、初始化、损失、参数冻结、模型选择和阶段衔接。结构路线
已经确定，具体学习率、epoch、batch size和损失权重必须在每阶段运行前写入配置并只用
train/val冻结。

通用原则：

- 所有阶段复用同一患者级train/val/internal test划分；
- 外部数据不参与模型、损失权重、epoch、阈值和结构选择；
- 模型只读取无可视化框的正式处理输入图和独立bbox坐标，带可视化框的图片不作为输入；
- 新数据先完成高分辨率空间裁剪、人工验收和版本冻结，再交给医学生标bbox；不先缩到
  `224x224`。标注文件必须绑定裁剪图SHA256和尺寸，训练resize/增强时同步变换bbox；
- 所有裁剪、缩放和翻转必须同步变换bbox；
- 每阶段加载上一阶段的val最佳权重，不根据test选择checkpoint；
- 分类指标和定位指标分别报告，不能只用一个联合分数掩盖某一任务退化；
- 新增模块先单独warm-up，再解冻必要的Encoder后层联合训练；
- M4/M5第一版保持M3完整冻结，避免融合训练改变定位框。

#### M0：完整图分类基线

```text
无标记完整图 → Encoder → GAP → 全局分类头
```

损失：

```text
L_M0 = L_cls
L_cls = CrossEntropy(global_logits, image_label)
```

训练分两步：

1. 加载ImageNet预训练Encoder，冻结全部Encoder，只训练分类头；
2. 分类头稳定后，解冻Encoder后部，使用更低学习率联合微调。

ResNet50首版建议：

```text
阶段A训练：分类头
阶段B训练：layer4 + 分类头
可选阶段C：layer3 + layer4 + 分类头
保持冻结：conv1、layer1，原则上也先冻结layer2
```

M0分别输出：

```text
m0_best_resnet50.pth
m0_best_efficientnet_b0.pth
```

新数据画中画较常见，M0同时加入`Keep/Notch`预处理消融。首轮采用完整的
`2种骨干 × 2种预处理`矩阵：

| 预处理分支 | 画中画图 | 无画中画图 | 首轮定位 |
| --- | --- | --- | --- |
| `pip_keep` | 四边裁剪后原样保留 | 同一四边裁剪图 | 基线 |
| `pip_remove_notch` | 人工确认框及安全边距置黑 | 与Keep指向同一文件 | 主去除方案 |

Keep与Notch必须使用完全相同的患者、图片ID、split、随机种子、增强和训练参数。自动画中画
检测只生成候选，正式Notch必须基于人工确认框。两种manifest均从同一冻结母清单派生，
禁止扫描输出目录组装数据。

PIP处理必须按以下顺序完成：

1. 在按中心、标签和画面风格抽取的验证子集上人工标记`pip/non_pip`；
2. 仅使用该验证子集校准自动检测阈值，冻结阈值后才运行全量检测；
3. 人工复核全部高置信和灰区候选，生成冻结的`confirmed_pip_labels.csv`；
4. 生成Notch候选并完成框完整性QC，最后才派生Keep/Notch配对manifest。

PIP复核是M0前的设备界面任务，病灶bbox是基于冻结Keep图的临床标注任务。两者为独立任务，
但在Keep几何冻结后可以并行，不需要等M0选定Keep/Notch。项目管理中必须分开任务包、字段说明和
交付时间；PIP初标可由工程人员完成，医学生只复核歧义和抽样病例。

M0首轮固定`RandomResizedCrop(scale=(0.85, 1.0), ratio=(0.90, 1.10))`。阶段0的A0/A1/A2
尺度实验与M0的Keep/Notch消融分开；不把scale并入`2种骨干×2种预处理`矩阵。
待M0选定预处理和主骨干后，再在唯一入选组合上对scale做新数据的单变量消融。

2026-08-05已冻结M0全量诊断清单：

- 清单目录：`数据整理记录/图像裁剪/胃早癌概念提取训练集0804_预处理_v1/06_M0全量诊断清单_20260805/`；
- 正式开发集为3348张、1732位患者，图片标签为1678非癌对1670癌，
  患者标签为1286非癌对446癌；
- 排除229张：补充外院216张、2位跨标签患者10张、Keep SHA完全重复副本3张；
- 患者级`source×label`分层固定为train/val/test=`1212/260/260`人，对应
  `2350/497/501`张；
- Keep与Notch使用相同患者split和图片主键，只切换处理图路径；
- 训练工具已改为根据val患者AUC选择epoch和S1/S2，阈值仅在val患者上按
  `Sensitivity>=0.90时Specificity最高`选择；正式候选筛选使用`--defer-test`
  不产生internal test预测。

正式val矩阵已于2026-08-05完成12/12组；internal test和外部test未解锁。
下表的图片级Accuracy/Sensitivity/Specificity使用同一种子在val患者上锁定的
筛查阈值，未为图片级单独调阈值。

| 预处理 | 骨干 | seed | 患者AUC | 患者Acc | 患者Sens | 患者Spec | 图片AUC | 图片Acc | 图片Sens | 图片Spec | 阈值 |
| --- | --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| Keep | ResNet50 | 42 | 0.8713 | 0.7346 | 0.9254 | 0.6684 | 0.8182 | 0.7404 | 0.8708 | 0.6187 | 0.03251 |
| Keep | ResNet50 | 202 | 0.8773 | 0.7846 | 0.9104 | 0.7409 | 0.8367 | 0.7666 | 0.8417 | 0.6965 | 0.13807 |
| Keep | ResNet50 | 503 | 0.8964 | 0.8231 | 0.9104 | 0.7927 | 0.8439 | 0.7666 | 0.7792 | 0.7549 | 0.24956 |
| Notch | ResNet50 | 42 | 0.8695 | 0.7577 | 0.9104 | 0.7047 | 0.8318 | 0.7465 | 0.8500 | 0.6498 | 0.11316 |
| Notch | ResNet50 | 202 | 0.8783 | 0.7538 | 0.9104 | 0.6995 | 0.8398 | 0.7646 | 0.8583 | 0.6770 | 0.12858 |
| Notch | ResNet50 | 503 | 0.8898 | 0.7692 | 0.9104 | 0.7202 | 0.8443 | 0.7545 | 0.8417 | 0.6732 | 0.16293 |
| Keep | EfficientNet-B0 | 42 | 0.9133 | 0.7808 | 0.9254 | 0.7306 | 0.8567 | 0.7586 | 0.8417 | 0.6809 | 0.20750 |
| Keep | EfficientNet-B0 | 202 | 0.9089 | 0.8192 | 0.9104 | 0.7876 | 0.8389 | 0.7505 | 0.7542 | 0.7471 | 0.33769 |
| Keep | EfficientNet-B0 | 503 | 0.9052 | 0.7654 | 0.9104 | 0.7150 | 0.8500 | 0.7726 | 0.8583 | 0.6926 | 0.24868 |
| Notch | EfficientNet-B0 | 42 | 0.9118 | 0.7846 | 0.9254 | 0.7358 | 0.8594 | 0.7746 | 0.8500 | 0.7043 | 0.21105 |
| Notch | EfficientNet-B0 | 202 | 0.9084 | 0.8077 | 0.9104 | 0.7720 | 0.8377 | 0.7586 | 0.7917 | 0.7276 | 0.30390 |
| Notch | EfficientNet-B0 | 503 | 0.9067 | 0.7385 | 0.9254 | 0.6736 | 0.8518 | 0.7626 | 0.8792 | 0.6537 | 0.23170 |

跨种子汇总：

| 预处理 | 骨干 | 患者AUC mean±SD | 患者AUC最差 | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片AUC最差 | 图片Acc | 图片Sens | 图片Spec |
| --- | --- | --- | ---: | ---: | ---: | ---: | --- | ---: | ---: | ---: | ---: |
| Keep | ResNet50 | 0.8817±0.0131 | 0.8713 | 0.7808 | 0.9154 | 0.7340 | 0.8329±0.0133 | 0.8182 | 0.7579 | 0.8306 | 0.6900 |
| Notch | ResNet50 | 0.8792±0.0102 | 0.8695 | 0.7603 | 0.9104 | 0.7081 | 0.8386±0.0064 | 0.8318 | 0.7552 | 0.8500 | 0.6667 |
| Keep | EfficientNet-B0 | 0.9091±0.0041 | 0.9052 | 0.7885 | 0.9154 | 0.7444 | 0.8485±0.0090 | 0.8389 | 0.7606 | 0.8181 | 0.7069 |
| Notch | EfficientNet-B0 | 0.9090±0.0026 | 0.9067 | 0.7769 | 0.9204 | 0.7271 | 0.8496±0.0110 | 0.8377 | 0.7653 | 0.8403 | 0.6952 |

只使用val和预注册门槛得到的阶段判断：

- EfficientNet-B0在Keep/Notch下的患者AUC均值均约0.909，高于ResNet50的
  0.879至0.882；图片AUC均值也更高，且患者AUC标准差更小。因此全量M0
  的主骨干候选锁定为EfficientNet-B0；
- EfficientNet-B0下Notch相对Keep的跨种子患者AUC均值差约-0.0001，
  但Specificity均值下降约0.0173，seed503下降0.0415，超过预注册0.02门槛；
  ResNet50下Notch有两个种子未通过全部患者级门槛。因此全量M0暂选Keep，
  Notch保留为已完成消融，不进入下一轮主训练；
- val中武大省人民为234/260人，第一届/第二届大赛只有11/15人；PIP子组
  仅20人。这些子组可用于提示风险，不足以声明跨来源泛化或Notch去除了shortcut；
- 全量M0证明新数据上的性能不再是旧未裁剪模型约0.99 AUC的异常水平，
  但数据仍存在患者数和来源/风格失衡；来源内平衡M0必须继续，不能直接解锁test或进入M1。

自动汇总位于
`结果/M0全量诊断_0804/正式验证集筛选/自动汇总/`，包含逐运行、跨种子、
非劣门槛、2000次患者整簇bootstrap和全部子组CSV。

#### M0全量模型独立外部评估（2026-08-05）

在查看外部结果前，已根据val暂选Keep与EfficientNet-B0；外部数据不参与模型、阈值、
预处理或种子选择。独立多中心集重新使用当前Keep流程处理，并确认其与M0开发集
SHA256重叠为0、患者标识直接重叠为0。外部集共1329人/1941张，其中非癌1208人/
1399张，癌121人/542张。35张暗部风险图经用户人工确认采用保守扩框+FOV遮罩；另有
4张`secondary_fov_component`提示待人工复核，但不阻塞本轮锁定评估。

| 骨干 | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片Acc | 图片Sens | 图片Spec |
| --- | --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| ResNet50 | 0.7158±0.0205 | 0.5661 | 0.8044 | 0.5422 | 0.6885±0.0228 | 0.5932 | 0.7097 | 0.5480 |
| EfficientNet-B0 | **0.7399±0.0075** | **0.6353** | 0.7383 | **0.6250** | **0.7238±0.0079** | **0.6452** | 0.7005 | **0.6238** |

图片级和患者级均沿用每个种子在val患者上锁定的同一筛查阈值，未在外部数据重调。
与内部val相比，EfficientNet-B0患者AUC由0.9091降至0.7399，图片AUC由0.8485降至
0.7238；ResNet50患者AUC由0.8817降至0.7158，图片AUC由0.8329降至0.6885。
说明两模型都有明显域偏移，但EfficientNet-B0的外部排序性能、准确率和跨种子稳定性
仍整体优于ResNet50，支持其继续作为平衡M0的主候选骨干。外部Sensitivity没有维持val
设定的0.90，说明阈值校准也发生漂移；后续不能只追求内部AUC，必须继续做来源内平衡、
来源留出和最终锁定外部评估。

完整结果位于`结果/M0全量诊断_0804/外部多中心完整测试_Keep_v1/`；运行日志为
`结果/M0全量诊断_0804/m0_external_keep_evaluation_20260805.log`。

#### M0来源内1:1.3平衡清单（2026-08-05冻结）

主清单在既有患者split内、按`split×来源`分别构建：保留全部446位合格癌患者，依据癌侧
患者主导`style_group`配额抽取574位非癌患者；完全同风格不足时才在同一来源内选择最近
风格。每位患者只取主导风格中的1至3张确定性代表帧，并让两类每患者平均图片数尽量一致。

| 清单 | 癌患者 | 非癌患者 | 患者非癌:癌 | 癌图片 | 非癌图片 | 图片非癌:癌 | 患者风格TV | 图片风格TV | 最近风格回退 |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| 主版1:1.3 | 446 | 574 | 1.287 | 597 | 776 | 1.300 | 0.0192 | 0.0461 | 19人 |
| 敏感性1:1 | 446 | 446 | 1.000 | 623 | 623 | 1.000 | 0.0157 | 0.0289 | 13人 |

主版未精确达到患者1:1.3，是因为第二届早癌大赛部分split内可用非癌患者不足；禁止跨来源
补齐。所有处理图存在，无患者跨split、无相同SHA跨split，每患者最多3张。19位最近风格
回退集中在小样本或缺乏共同风格支持的分层，后续必须同时报告来源分层结果。严格1:1仅在
主版选出最终骨干后作为敏感性分析，不重复完整骨干矩阵。

清单与审计位于
`数据整理记录/图像裁剪/胃早癌概念提取训练集0804_预处理_v1/07_M0来源内平衡清单_20260805/`。
EfficientNet-B0和ResNet50的1+1 epoch小样本冒烟均通过；正式矩阵入口为
`程序/模型训练/正式代码/run_m0_balanced_matrix.sh`，默认物理1号GPU并为每组保存实时日志。

选择只使用val：主指标为患者AUC，并同时检查Sensitivity、Specificity、画中画/无画中画
子组、分中心结果以及Grad-CAM是否集中于黑块。Notch相对Keep的患者AUC非劣容忍范围
已于2026-08-05冻结为`0.005`，Sensitivity和Specificity的允许下降均为`0.02`。
若Notch非劣且来源稳定性与黑块关注审计不差，
优先Notch以减少画中画内容shortcut；否则保留Keep。只有Notch明显失效或形成黑块shortcut
时，返回Keep，不再增加会改变图像尺寸和bbox坐标系的Right分支。

选择同时看val患者AUC、Sensitivity、Specificity、多随机种子稳定性、两个主要来源的
分层表现和训练/验证损失差距。

M0同时训练ResNet50和EfficientNet-B0，但M1-M5不默认把两条完整路线全部重复。主骨干
只根据M0的val结果锁定：

1. 患者级AUC为主要分类指标；
2. Sensitivity、Specificity、两个来源分层表现和多随机种子稳定性必须通过预设门槛；
3. 不根据internal test或外部test选择骨干；
4. 若EfficientNet-B0的患者AUC更高，或在预设非劣容忍范围内与ResNet50基本持平且其他
   指标不差，则优先将EfficientNet-B0作为M1-M5主骨干；
5. 另一模型保留M0分类基线，并在主路线锁定后复现M3或最终入选阶段作为补充对照，不
   重复全部中间实验；
6. 若主骨干在确认bbox、坐标变换和损失实现无误后仍无法学会M1定位，可启动另一骨干
   的M1作为预先记录的回退，而不是为保留既定选择强行继续。

下列M0选择参数已于2026-08-05、任何正式训练日志产生前预注册并冻结：

```text
preprocess_delta_auc_noninferiority
backbone_delta_auc_noninferiority
delta_sensitivity_noninferiority
delta_specificity_noninferiority
Sensitivity/Specificity的val阈值选择策略和最低门槛
精确随机种子列表（42/202/503为候选）
跨种子汇总方式
同一val患者上的配对比较方法
```

`preprocess_delta_auc_noninferiority=0.005`、`backbone_delta_auc_noninferiority=0.005`、
`delta_sensitivity_noninferiority=0.02`、`delta_specificity_noninferiority=0.02`，随机种子为
`42/202/503`。冻结后不得根据M0结果回改容忍范围。

#### M0来源内1:1.3平衡val结果（2026-08-05）

6组正式训练均使用同一冻结清单、Keep预处理和`--defer-test`；checkpoint、患者筛查阈值
和骨干选择只使用val。图片级指标沿用同一模型的val患者阈值，没有另行调阈值。

| 模型 | 种子 | 患者AUC | 患者Acc | 患者Sens | 患者Spec | 图片AUC | 图片Acc | 图片Sens | 图片Spec |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| EfficientNet-B0 | 42 | 0.8447 | 0.7516 | 0.9254 | 0.6163 | 0.8076 | 0.6950 | 0.8391 | 0.5841 |
| EfficientNet-B0 | 202 | 0.8584 | 0.8039 | 0.9104 | 0.7209 | 0.8262 | 0.7600 | 0.8506 | 0.6903 |
| EfficientNet-B0 | 503 | 0.8539 | 0.7647 | 0.9104 | 0.6512 | 0.8223 | 0.7300 | 0.8736 | 0.6195 |
| ResNet50 | 42 | 0.8174 | 0.6536 | 0.9104 | 0.4535 | 0.7826 | 0.6450 | 0.8621 | 0.4779 |
| ResNet50 | 202 | 0.8356 | 0.6797 | 0.9104 | 0.5000 | 0.8056 | 0.6600 | 0.8851 | 0.4867 |
| ResNet50 | 503 | 0.8289 | 0.6667 | 0.9104 | 0.4767 | 0.7956 | 0.6600 | 0.8966 | 0.4779 |

| 模型 | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片Acc | 图片Sens | 图片Spec |
| --- | --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| EfficientNet-B0 | 0.8523±0.0070 | 0.7734 | 0.9154 | 0.6628 | 0.8187±0.0098 | 0.7283 | 0.8544 | 0.6313 |
| ResNet50 | 0.8273±0.0092 | 0.6667 | 0.9104 | 0.4767 | 0.7946±0.0115 | 0.6550 | 0.8812 | 0.4808 |

EfficientNet-B0在三个种子的患者与图片AUC均高于同种子ResNet50，患者AUC平均差约
`0.0250`，同时患者Specificity和Accuracy明显更高；配对bootstrap的单种子95%区间因
val样本量有限仍跨0，因此不宣称统计学显著优越。按预注册规则，M1-M5主骨干锁定为
EfficientNet-B0，ResNet50保留为受控对照。该结论只来自val；internal test仍未解锁，
外部多中心评估只用于泛化报告，不反向改变本选择。

自动汇总位于`结果/M0平衡_0804/正式验证集筛选/自动汇总/`，逐组训练日志位于
`结果/M0平衡_0804/正式验证集筛选/logs/`。

#### M0来源内1:1.3平衡模型外部多中心评估（2026-08-05）

val选择完成后，使用各运行已锁定的checkpoint、患者筛查阈值和患者均值聚合，在独立
外部多中心集1329人/1941张上评估一次。图片级仍沿用患者阈值；AUC置信区间进行2000次
患者整簇bootstrap。外部结果不参与主骨干选择或调参。

| 模型 | 种子 | 患者AUC | 患者Acc | 患者Sens | 患者Spec | 图片AUC | 图片Acc | 图片Sens | 图片Spec |
| --- | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: | ---: |
| EfficientNet-B0 | 42 | 0.6690 | 0.5433 | 0.7521 | 0.5224 | 0.6468 | 0.5708 | 0.6882 | 0.5254 |
| EfficientNet-B0 | 202 | 0.6830 | 0.5914 | 0.6694 | 0.5836 | 0.6508 | 0.5987 | 0.6255 | 0.5883 |
| EfficientNet-B0 | 503 | 0.6901 | 0.5801 | 0.7273 | 0.5654 | 0.6713 | 0.5981 | 0.6956 | 0.5604 |
| ResNet50 | 42 | 0.6672 | 0.4733 | 0.8347 | 0.4371 | 0.6458 | 0.5374 | 0.7749 | 0.4453 |
| ResNet50 | 202 | 0.7044 | 0.4883 | 0.8512 | 0.4520 | 0.6715 | 0.5451 | 0.7583 | 0.4625 |
| ResNet50 | 503 | 0.6766 | 0.4372 | 0.8926 | 0.3916 | 0.6534 | 0.5137 | 0.7970 | 0.4039 |

| 模型 | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片Acc | 图片Sens | 图片Spec |
| --- | --- | ---: | ---: | ---: | --- | ---: | ---: | ---: |
| EfficientNet-B0 | 0.6807±0.0107 | 0.5716 | 0.7163 | 0.5571 | 0.6563±0.0131 | 0.5892 | 0.6697 | 0.5580 |
| ResNet50 | 0.6828±0.0193 | 0.4663 | 0.8595 | 0.4269 | 0.6569±0.0132 | 0.5320 | 0.7768 | 0.4372 |

与此前全量Keep模型相比，平衡训练没有改善该外部集：EfficientNet-B0患者/图片AUC分别
下降约`0.0592/0.0675`，ResNet50下降约`0.0330/0.0316`。两骨干外部AUC基本持平，
但锁定阈值下EfficientNet-B0的Accuracy和Specificity更高，ResNet50的Sensitivity更高。
这说明来源内平衡降低了内部可利用信号，却没有自动消除开发来源到外部多中心的域偏移；
也不能据此反向改选骨干或阈值。M1病灶框监督仍有必要同时做平衡主实验与全量诊断对照，
以检验定位约束能否缩小而不是掩盖域差距。

完整结果位于`结果/M0平衡_0804/外部多中心完整测试_Keep_v1/`，运行日志为
`结果/M0平衡_0804/m0_balanced_external_keep_evaluation_20260805.log`。

#### M0尺度增强A0/A1/A2单变量消融（2026-08-05）

在锁定的EfficientNet-B0 + Keep + 1:1.3平衡清单上，仅改变RandomResizedCrop的
`scale`下界做单变量消融，3种子，`--defer-test`；A0即正式M0运行本身。
A0=(0.85,1.0)、A1=(0.7,1.0)、A2=(0.5,1.0)，ratio固定(0.90,1.10)。

| 配置 | scale | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec |
| --- | --- | ---: | ---: | ---: | ---: |
| A0 | (0.85,1.0) | 0.8523±0.0070 | 0.7734 | 0.9154 | 0.6628 |
| A1 | (0.7,1.0) | 0.8560±0.0054 | 0.7669 | 0.9104 | 0.6550 |
| A2 | (0.5,1.0) | 0.8437±0.0062 | 0.7277 | 0.9104 | 0.5853 |

按种子A0=0.8447/0.8584/0.8539，A1=0.8514/0.8636/0.8528，A2=0.8396/0.8391/0.8525。
A2明确更差（AUC低、Spec降至0.585），排除；A1与A0在种子噪声内持平（AUC略高0.0037，
但Acc/Spec略低）。锁定A0=(0.85,1.0)为正式scale：A1增益不构成稳定收益，且更保守的
增强降低后续bbox定位阶段裁掉病灶的风险。结果位于`结果/M0平衡_0804/尺度消融_A1A2/`。

以下M1-M5结构和冻结说明以ResNet50为例。EfficientNet-B0使用等价空间分辨率stage：

| ResNet50阶段 | EfficientNet-B0阶段 | `224x224`输入下输出 |
| --- | --- | --- |
| `layer2` | `features[3]`，40通道 | `28x28` |
| `layer3` | `features[5]`，112通道 | `14x14` |
| `layer4` | `features[8]`，1280通道 | `7x7` |

因此EfficientNet-B0的M1从`features[5]`引出定位头，M2使用
`features[3]/features[5]/features[8]`构建FPN。M1联合微调优先解冻`features[5:]`，
M2联合微调优先解冻`features[3:]`；实际边界仍须结合val稳定性冻结，不能直接照搬
ResNet50的`layer2/3/4`名称。

#### M1：layer3癌图辅助定位

初始化：

```text
Encoder和全局分类头：加载M0最佳权重
Heatmap/Size/Offset定位头：随机初始化
```

```text
完整图 → 共享Encoder
       ├─ layer4 → GAP → 全局分类头
       └─ layer3 14x14 → Heatmap、Size、Offset
```

定位与总损失：

```text
L_loc =
    L_heatmap_focal
  + lambda_size * L_size_smoothL1
  + lambda_offset * L_offset_smoothL1

有效癌bbox：
L_M1 = L_cls + lambda_loc * L_loc

非癌或无有效bbox：
L_M1 = L_cls
```

训练分两步：

1. 冻结Encoder和分类头，只训练随机初始化的定位头；
2. 定位头开始收敛后，解冻`layer3、layer4`，联合训练分类头和定位头；
3. `conv1、layer1、layer2`保持冻结。

`L_size`和`L_offset`只在真实病灶中心位置计算。定位损失按有效bbox数量归一化；
batch内没有有效框时令`L_loc=0`，不得产生除零或NaN。

建议同时保存：

```text
m1_best_classification.pth
m1_best_localization.pth
m1_best_joint.pth
```

2026-08-10在正式M1训练前冻结第一版实现和运行参数：

| 项目 | 冻结值 |
| --- | --- |
| 主骨干/定位分流 | EfficientNet-B0；`features[5]`的112通道14x14特征 |
| 数据角色 | 来源内1:1.3平衡主实验 + 全量诊断对照；均为Keep |
| 随机种子 | 42、202、503；每组加载同数据角色、同种子的M0 val最佳权重 |
| batch/worker | 32 / 4 |
| 定位头预热 | 5 epoch，AdamW，LR `1e-3`；仅定位头可训练 |
| 联合训练 | 最多20 epoch，LR `1e-4`，weight decay `1e-4`，patience 8 |
| 解冻范围 | EfficientNet `features[5:] + classifier + localization_head` |
| 损失权重 | `lambda_loc=1.0`、`lambda_size=0.1`、`lambda_offset=1.0` |
| bbox感知裁剪 | A0 scale `(0.85,1.0)`；病灶中心保留且框面积保留至少80%，失败回退整图 |
| 联合模型选择 | val患者AUC不低于对应M0 AUC减0.005后，先最大化中心命中率，再比较mean IoU |
| test安全 | 默认不评估；正式矩阵不传`--evaluate-test` |

平衡主清单为1373张/1020人（597癌图有框），全量诊断清单为3348张/1732人
（1670癌图有框）；两者癌图bbox覆盖率均为100%，无跨患者SHA重复。统一入口为
`程序/模型训练/正式代码/run_m1_matrix.sh`。为避免未验收即连续跑完六组，该入口要求
显式提供数据范围与种子；先运行`balanced 42`，验收完整训练曲线、分类非劣门槛、定位
指标和叠加图后，再运行剩余矩阵。

#### M2：可选FPN辅助定位

只有当M1主要受小病灶或`14x14`空间分辨率限制时才测试M2。若M1完全不能定位，应先
检查bbox、病灶可见性、坐标变换、损失和权重，不能直接假设FPN可以修复。

初始化和结构：

```text
Encoder和全局分类头：加载M1最佳联合权重
FPN：随机初始化
新定位头：随机初始化，或仅加载形状兼容的输出层

layer2 28x28
layer3 14x14
layer4 7x7
      ↓ FPN
P2 28x28
      ↓
Heatmap、Size、Offset
```

损失与M1相同：

```text
L_M2 = L_cls + lambda_loc * I_valid_box * L_loc
```

训练先冻结Encoder，只训练FPN和定位头；随后解冻`layer2、layer3、layer4`，使用分层
学习率联合微调。M1和M2只根据val比较中心命中率、中心距离、IoU、病灶覆盖率、小中大
病灶召回率、患者级分类性能和推理成本。M2没有稳定收益时回到M1。

进入M3前采用分层规则选择M1或M2，不使用量纲不清的临时加权总分：

1. 先通过分类非劣门槛：患者AUC和Sensitivity不得相对M0或另一候选稳定下降超过预设
   容忍值；
2. 通过分类门槛后，优先比较病灶中心命中率、小病灶召回率和病灶覆盖率；
3. 再以IoU、平均中心距离、中/大病灶召回率和跨来源稳定性作次级判断；
4. 若M2未达到预设最小有效提升，选择结构更简单的M1；
5. AUC/Sensitivity容忍值和定位最小有效提升幅度必须在看到train/val患者数及bbox分布
   后、开始M1/M2正式训练前冻结。

#### M3：加入非癌负监督

M3加载M1或M2中val表现更好的模型，并重新创建优化器和学习率调度器。

```text
癌图Heatmap目标：病灶中心高斯
非癌Heatmap目标：全零
Size/Offset目标：只在有效癌bbox上存在
```

损失：

```text
L_M3 =
    L_cls
  + lambda_heatmap * L_heatmap_all_images
  + I_valid_box * (
        lambda_size * L_size
      + lambda_offset * L_offset
    )
```

训练初期将非癌Heatmap损失权重从0逐渐增加到目标值，避免尚不稳定的定位头被大量负
位置直接压成全零。采用M1结构时解冻`layer3、layer4`；采用M2结构时解冻
`layer2、layer3、layer4`和FPN；Encoder前层继续冻结。

M3锁定后提供的输出包括全局特征、全局分类概率、Heatmap、Size、Offset、预测bbox和定位置信
分数。内部最高响应候选区域始终存在，但只有定位置信分数超过val锁定阈值时才作为正式
预测框展示。分类为非癌但定位高响应的样本应保留为分歧病例，不强制删除预测框。

#### M4：真值癌ROI的局部上限实验

M4第一版完整冻结M3：

```text
冻结：
全局Encoder
全局分类头
定位头/FPN

新增并训练：
局部Encoder
局部分类头
融合分类头
```

配对输入：

```text
癌图：完整图 + 医生真值bbox裁出的ROI
非癌图：完整图 + 冻结M3预测的困难负ROI
```

第一版困难负ROI使用可复现的每图Top-1规则：

1. 冻结M3，为每张非癌图生成Heatmap；
2. 无论分数是否达到对外展示阈值，都取最高响应位置Top-1作为训练候选，避免部分非癌
   图因低置信而没有局部分支输入；
3. 使用该位置的Size/Offset恢复预测框并记录定位置信分数；
4. 预测框无效时，以Heatmap峰值为中心使用预先冻结的固定尺寸回退框，不静默丢弃样本；
5. 与已标注良性病变重叠的ROI仍保留为困难负样本，但增加良性病变类型和重叠标记，供
   分层分析；
6. 是否扩展到Top-K以及高置信困难负样本的采样权重，只能作为后续train/val消融。

局部ROI统一从无可视化框的高分辨率正式处理输入图裁剪并输出为`224x224`。扩边使用
显式参数`m`：

```text
左、右各扩展 m * bbox_width
上、下各扩展 m * bbox_height
```

裁剪框扩边后调整为正方形，并尽量平移回原图有效边界内，再resize到`224x224`，不得
增加固定黑边。`m=0.10`和`m=0.20`作为首轮train/val候选；正式值、回退框尺寸及插值
方式必须在M4运行前冻结，M4和M5复用完全相同的ROI协议。

局部Encoder使用M3 Encoder权重复制初始化，但作为独立网络更新。局部标签为：

```text
癌病灶ROI：roi_label=1
非癌困难ROI：roi_label=0
不确定ROI：roi_label=ignore
```

完整图和融合头使用`image_label`，局部头使用`roi_label`：

```text
L_final = CE(fusion_logits, image_label)
L_local = CE(local_logits[valid_roi], roi_label[valid_roi])

L_M4 = L_final + beta * L_local
```

局部损失按有效ROI数量归一化；batch内没有有效ROI时令`L_local=0`。因为M3被冻结，
`global_logits`只监控，不把`L_global`加入第一版优化损失。

训练先冻结局部Encoder，只训练局部分类头和融合分类头；随后解冻局部Encoder的
`layer4`，必要时再解冻`layer3`，使用低学习率微调。M4存在癌侧真值ROI、非癌侧预测
ROI的不对称，只表示真值框辅助下的局部信息上限，不能作为部署性能。

#### M5：全部使用M3预测ROI

初始化：

```text
冻结M3：加载M3最佳权重
局部Encoder和融合头：加载M4最佳权重
```

癌图和非癌图全部使用冻结M3预测ROI。训练时根据预测ROI与医生bbox的关系生成局部标签：

```text
明确覆盖癌病灶：roi_label=1
明确未覆盖病灶：roi_label=0
部分覆盖或标注不确定：roi_label=ignore
非癌ROI：roi_label=0
```

匹配综合使用IoU、病灶覆盖率、预测中心是否落入医生框，以及医生框中心是否落入裁剪
ROI；具体阈值必须在运行前冻结。多病灶图与所有有效医生框匹配，不能只比较第一个框。

第一版损失：

```text
L_M5 = L_final + beta * L_local
```

第一版继续冻结M3，只训练局部Encoder后层、局部分类头和融合分类头。这样定位框在训练
期间保持固定，训练和推理使用同一个定位器。M5主要输出`P_final`，同时保留
`P_global`、`P_local`、预测bbox和定位置信分数用于回退、质控和解释。

#### 可选联合微调

只有M5相对M3取得稳定收益后，才单独开展联合微调。为避免定位器随融合训练漂移，保留
一个完全冻结的M3副本负责生成bbox，另复制一个全局分类Encoder参与特征融合：

```text
L_joint =
    L_final
  + alpha * L_global
  + beta * L_local
```

如果进一步希望更新定位器，必须重新加入`L_loc`并在线更新ROI，作为更复杂的独立实验，
不并入第一版M5。

#### 阶段决策

```text
M0：建立新数据分类基线
M1：验证癌图定位辅助
M1不足且原因是空间分辨率：测试M2
选择M1或M2：进入M3并加入非癌抑制
M3分类和定位已满足需求：可以直接作为最终模型
M4明显优于M3：说明真值局部信息有价值
M5明显优于M3：采用自动全局+局部融合
M5无稳定增益：返回更简单的M3
```

### 最终SAE任务路线

现有ResNet50 `2048→512→2048` SAE继续作为第二批v1.1阶段的冻结解释基线。新数据和
最终分类器确定后必须重新提取特征并重新训练SAE；旧Feature编号和医学命名不能直接
迁移到新模型。

最终模型优先分别提取和分析：

1. 全局分支特征；
2. 局部分支特征；
3. 全局/局部融合后的最终分类特征。

设待解释层维度为`D`，候选字典宽度按以下顺序逐级比较：

| 编号 | 结构 | 定位 |
| --- | --- | --- |
| S0 | `D → 512 → D` | 当前M-CBM兼容参照 |
| S1 | `D → D → D` | 完整宽度参照 |
| S2 | `D → 2D → D` | 首个正式过完备SAE |
| S3 | `D → 4D → D` | 主要扩张候选 |
| S4 | `D → 8D → D` | 仅S3稳定、死Feature和重复Feature可控时运行 |

不直接采用32倍扩张。当前数据规模远小于大模型SAE语境，过宽字典可能产生大量死亡、
重复和不稳定Feature。具体L1网格、warm-up、允许的AUC下降和稳定性门槛必须在运行前
再次写入本文档并冻结。

统一训练与选择原则：

- SAE只用train拟合，val选择epoch、L1、宽度和剪枝规则；
- 使用逐元素MSE、L1稀疏约束、decoder单位范数和decoder梯度正交投影；
- 保留患者平衡采样和batch size 32；
- 学习率前5% warm-up，L1从0线性增加至目标值；
- 同时报告MSE、cosine、L0、90%激活质量所需Feature数、死Feature率、重复Feature率、
  图像/患者预测一致率和原分类头保真度；
- test和外部test只投影一次，不用于选择宽度、L1、剪枝或Feature；
- 不仅按重构误差或AUC选SAE，还要比较随机种子稳定性、跨患者/跨来源稳定性和临床
  可命名性。

共享Feature分析不预先把Feature硬分为“癌Feature”和“非癌Feature”。应对全部保留
Feature联合构图，综合decoder余弦相似度、患者级共激活、癌/非癌激活差异、来源关联、
代表图视觉相似度和空间响应相似度进行聚类，再由临床人员标记：

- 癌相关概念；
- 非癌相关概念；
- 癌/非癌共享解剖或黏膜概念；
- 良性病变与癌共享形态；
- 医院、设备、分辨率和画面风格概念；
- 镜体、文字、黑边、反光、气泡、黏液等干扰概念；
- 稀有、重复或不稳定概念。

共有Feature不能仅因两类都激活而删除。只有在来源关联、空间响应和医学审核均支持其为
伪特征，并且锁定Feature簇屏蔽后内部性能不降或外部稳定性改善时，才可作为排除候选。
Feature干预使用`编码→屏蔽Feature/簇→解码→复用原分类头`，同时报告AUC、Sensitivity、
Specificity、决策一致率和分中心变化。

最终SAE交付包括宽度比较、训练质量、Feature级统计、共享Feature聚类、来源Feature、
Feature中心与图像中心空间图、内部/外部稳定性、Feature簇屏蔽消融、临床命名表和阅读
指南。稳定概念形成后再进入M-CBM/CBL。

## 当前已知问题与处理

| 问题 | 可能影响 | 当前处理 | 后续验证 |
| --- | --- | --- | --- |
| MOCE黑色遮挡基线偏置 | SSC/SDC step 0可能主要反映黑图类别先验 | 不将step 0直接解释为黏膜一致性或医学充分性 | 比较其他填充值或遮挡基线 |
| SAE彩色图出现紫色 | 红蓝Feature响应重叠后产生混色 | 结合5个单Feature图解释，不设为第三种贡献类别 | 必要时检查原始贡献矩阵 |
| K-Means结果随K变化 | 概念粒度、重复簇和小簇数量变化 | 已完成`K=10,15,...,50`敏感性分析，不凭单一指标选K | 医学生/医生结合跨K稳定性审核 |
| 外部患者图片数混杂 | `top2_mean`可能利用癌患者图片更多这一结构 | 正式结果同时披露mean和随机单图敏感性 | 后续补充限图聚合或分层分析 |
| SAE Feature医学语义未确认 | 高激活方向可能仍对应来源、反光或界面 | 保留来源相关性、Top患者图、空间响应与消融证据 | 跨数据集对照并由临床命名 |
| 新数据表面平衡但来源仍泄漏 | 合并后1:1仍可能是一个来源偏癌、另一来源偏非癌 | 要求早癌大赛和武大省人医各自内部平衡患者、图片和风格 | 训练前做来源×标签和元数据预测审计 |
| M1随机裁剪丢失癌病灶 | 癌图被裁成无病灶图但标签仍为癌 | 无框阶段只做保守尺度增强；有框后使用bbox感知变换 | 抽样可视化并统计裁剪后病灶保留率 |
| M1中心网格量化 | 14x14热图对小病灶中心定位较粗 | 增加center offset，并按bbox大小生成高斯目标 | 报告中心距离、命中率和IoU |
| 真值ROI到预测ROI分布差异 | M4可能高估实际部署性能 | M4只作上限；M5使用预测框并在训练中混入扰动框/预测框 | 分开报告M4与M5 |

## 当前风险与结论边界

1. 内部实验A高AUC与严格共同支持OOF、独立外部结果差距大，说明内部结果不能单独代表
   多中心泛化。
2. 外部患者图片数量本身高度预测标签，任何患者聚合收益都必须做单图和mean敏感性分析。
3. MOCE的黑色遮挡会引入类别偏置，SSC/SDC不能单独证明概念医学真实性。
4. SAE保真度只说明稀疏表示可近似原模型，不说明Feature自动具有医学意义。
5. 热图和SAE空间响应都是粗定位，不是病灶分割。
6. test和外部test已经用于正式报告，后续不得根据其结果反复选择超参数或结构。
7. 新扩张SAE和EfficientNet-B0 SAE属于已知test背景下的补充研究；必须先在本文档冻结
   候选网格和选择规则，再只用train/val完成选择。
8. 医学生命名属于人工证据；自动分析不能代替医生最终判断。
9. 新数据、病灶框、定位监督、特征金字塔、ROI融合和Transformer必须按M0-M5顺序做
   单变量或可归因消融，不能一次全部叠加后只报告最终最好结果。
10. 带可视化矩形框的图片含有人为边缘，不能作为模型输入；训练必须使用无可视化框的冻结
    正式处理输入图和其坐标系下的独立结构化标注。

## 下一步

### 立即任务

1. 已完成来源内患者级癌:非癌约1:1.3主清单和严格1:1敏感性清单冻结；补充外院216张
   继续排除在开发集外；
2. 已完成平衡M0的ResNet50与EfficientNet-B0各3个种子，全部使用`--defer-test`；
   只用val锁定EfficientNet-B0为M1-M5主骨干，ResNet50保留为受控对照；
3. 已完成1941张主外部多中心集评估；结果确认仍有明显域偏移，且不反向改变val选择；
4. 在平衡M0入选的唯一骨干与Keep分支上，单独运行A0/A1/A2尺度增强消融，不与
   Keep/Notch变量混跑；
5. 对全量和平衡候选补充来源、风格、PIP子组、来源留出和Grad-CAM审计，判断模型是否
   仍依赖设备界面、分辨率或来源风格；
6. A0/A1/A2、来源/风格审计和严格1:1敏感性分析完成后，才使用锁定checkpoint、阈值和
   聚合规则评估一次internal test；0804中的216张补充外院队列继续与1941张主外部集
   分开报告，并先完成患者与来源重叠审计；
7. 已完成医学生bbox接收与整合：`数据/标注与汇总/bbox_manifest_20260807.csv`，
   1779张癌图全部有bbox，命名映射/尺寸/界内审计通过（详见近期历史2026-08-07）；
8. 已冻结M1来源内1:1.3 Keep主清单：1373张/1020人，597张癌图全部带有效框，非癌图
   `localization_supervision=0`，无患者跨split、无跨患者SHA重复；此前0025730跨患者
   重复图未进入该主清单，因此不再阻塞M1主实验；
9. 已冻结M1全量诊断Keep清单：3348张/1732人，1670张癌图bbox覆盖100%；M0阶段已将
   0025730副本规范为只保留原患者目录一份，因此该清单跨患者SHA为0；
10. 21张bbox与Notch黑块相交只影响Notch旁路；M1主实验已锁定Keep，故不跳过这些癌图
   的定位损失。Notch后续诊断实验若重启，再单独冻结A/B处理；
11. 已实现EfficientNet-B0 M1正式脚本并在物理GPU 1完成两套清单的小样本1+1 epoch冒烟；增强后GT框联系表
    肉眼检查无坐标错位，分类/定位损失和三类checkpoint输出链路通过，internal test未
    推理。`run_m1_matrix.sh`已准备平衡/全量×3种子的统一入口，且不会传
    `--evaluate-test`；下一步先运行平衡seed42，首个完整结果验收后才扩展其余5组。

### 随后任务

1. bbox到位后先做标注合法性、病灶可见性和框分布验收，再实现并debug M1的
   `heatmap+size+offset`辅助定位；
2. M1同时保留“来源内平衡主实验”和“全量诊断实验”：前者判断框监督能否提升可靠
   分类，后者只用于比较定位监督是否缩小来源/风格差距；两者必须使用对应M0作为基线；
3. 根据M1分类与定位双重结果推进M2特征金字塔、M3自动检测、M4真值ROI上限和M5预测
   ROI部署实验；
4. CNN监督路线确认后，再将Swin-T等小型Transformer作为受控backbone对照；
5. 最终分类器冻结后，按S0-S4路线只用train/val完成SAE宽度和稀疏策略选择，再做共享
   Feature聚类、空间响应和Feature簇屏蔽消融；
6. 现有锁定ResNet50 SAE的30个四域候选空间响应对照继续保留为旧模型诊断任务，但不
   阻塞新数据和框辅助分类器主线；
7. 收集医学生对现有MOCE与彩色SAE图的标注；稳定概念形成后再进入M-CBM/CBL。

## 正式报告索引

- 数据与参数：
  - `数据去偏重训练_参数与报告/数据预处理参数_v1_1.md`
  - `数据去偏重训练_参数与报告/数据筛图参数_v1_1.md`
  - `数据去偏重训练_参数与报告/数据训练参数_v1.md`
- 分类实验：
  - `数据去偏重训练_参数与报告/完整裁剪数据重训练实验A结果_v1.md`
  - `数据去偏重训练_参数与报告/严格共同支持集5折结果_v1.md`
  - `数据去偏重训练_参数与报告/P7正式实验汇总_v1.md`
  - `数据去偏重训练_参数与报告/患者聚合集成TTA评估结果_v1.md`
  - `数据去偏重训练_参数与报告/完整多中心外部测试结果_v1.md`
- 概念集：
  - `数据去偏重训练_参数与报告/医学生整理概念集模型审计结果_v1.md`
  - `数据去偏重训练_参数与报告/两批v1_1裁剪数据对比与选用结论_v1.md`
- MOCE：
  - `结果/MOCE分析/`
  - `结果/MOCE聚类数量分析/`
- SAE：
  - `程序/SAE/正式代码/README.md`
  - `结果/SAE/去偏重训练_v1/resnet50/`
  - `结果/SAE分析/去偏重训练_v1/resnet50/`

## 近期历史

- 2026-07-22：完成v1.1实验A/A0/B/C/D、严格共同支持5折和旧模型同口径对照。
- 2026-07-23：完成概念集审计、严格502张概念集、双模型MOCE，并锁定患者聚合与集成。
- 2026-07-24至2026-07-28：修正SAE损失尺度，完成ResNet50 SAE、外部测试和外部投影。
- 2026-07-29至2026-07-30：完成外部1941张图像中心彩色SAE及100位患者提交版。
- 2026-07-31：备份旧`AGENTS.md`，建立双文档结构；完整表格和总结归入本进度文档，
  `AGENTS.md`保留稳定规则、数据谱系和精简里程碑；完成锁定ResNet50 SAE内部test
  投影及四域Feature定量稳定性分析；冻结下一版来源内平衡数据、CenterNet式辅助定位、
  M0-M5分类实验矩阵及S0-S4扩张SAE与共享Feature分析路线。
- 2026-08-03：完成现有第二批原图89张/60个抽样单元的四边裁剪、FOV、PIP和
  Keep/Notch小样本工程验收。人工总览确认6张PIP；固定OpenCV线程与RNG后两次
  PIP结果逐字节一致，候选召回为100%、需人工排除3个假阳性。6张Notch框初筛无异常；
  两分支各89张、83张共用同一文件、6张使用Notch像素版，尺寸与`geometry_id`全部一致。
  该结果只证明工程流水线可运行，不冻结新数据阈值或Keep/Notch模型选择结论。
- 2026-08-03：针对暗黏膜可能被亮度轮廓误裁的问题，增加原图FOV反查的
  `dark_crop_safety_review.py`。89张样本按“框外FOV比例/单边越界比例”生成四档联图：
  敏感档42张、中等档14张、保守档5张、强风险档4张。肉眼初查确认候选中既有应放宽的
  暗部误裁，也有放宽后会重新带回设备UI或视频进度条的情况，因此该步骤只负责筛查和
  生成保守候选，禁止整批自动替换。随后将候选改为按边定向放宽：已检出进度条时锁住
  当前底边，只恢复越界明显的顶部、左侧或右侧；保守档5张中3张恢复顶部、2张恢复右侧，
  均未重新放宽底边。用户肉眼比较四档后确认C保守档“不多也不少”，据此冻结正式复核
  阈值为框外FOV比例`>=0.05`或单边越界比例`>=0.06`；逐图仍需记录最终选择。
- 2026-08-04：收到`胃早癌概念提取训练集0804`并完成只读初审。3577张均可解码，图片
  标签近似平衡，但患者数为487癌对1369非癌，且分辨率、画幅和尺寸仍明显关联标签；
  暂停直接全量裁剪训练，先构建来源内患者与风格匹配清单。
- 2026-08-05：完成0804标注包、双路径Keep manifest和3577张冻结几何的一致性验收；
  zip SHA256为`62bb00f3d76107e83cedb0437a2eb869f5985eeb50f83be650ca8f211363e4ed`。
  四角PIP全量候选为177张，人工联系表复核确认30张假阳性，其余147张暂按真PIP记录；
  120张来源与标签平衡的阴性抽查中新发现1张假阴性，与既往底边融合漏检图不是同一张。
  两张假阴性均已进入待手动画框侧表，并已从冻结正式处理输入复制到
  `03_画中画筛查_全量_20260804/人工框选包_画中画假阴性_20260805/`。人工红框复核后，
  两张图均回填为`xyxy=(0,0,312,250)`（对应`xywh=(0,0,312,250)`）。已修正
  Notch生成脚本的人工框优先逻辑，并改为按冻结Keep manifest的
  `final_processed_path`逐图取图，避免第一届WL紧裁图退回旧FOV目录。正确版Notch
  已生成149张/120人，与Keep几何一致；错误图像根读取版已移入归档。
  框完整性初筛剩26张/25人待最终人眼确认，其中大多为尺寸簇提示；
  第一届WL的2张画中画已在紧裁坐标下完整去除，不需要再做改变bbox几何的空间裁剪。
  随后用户确认26张框QC全部通过；正式Keep/Notch配对manifest为每分3577张、
  0尺寸错误。Notch分支分组为3428张`normal`、147张`pip_notch`和2张
  `normal_after_external_pip_removal`；后两张是画中画完全位于主视野外的第一届WL图。
- 2026-08-05：冻结M0全量诊断开发集为3348张/1732人，排除补充外院216张、
  跨标签患者10张和重复SHA副本3张；生成共用患者split的Keep/Notch正式清单。
  训练工具已切换为val患者AUC选checkpoint，并加入val患者筛查阈值扫描和
  `--defer-test`。ResNet50 Keep/Notch与EfficientNet-B0 Keep的1+1 epoch小样本冒烟全部通过；
  调试指标仅用于验证工程链路，不参与正式结论。
- 2026-08-05：完成M0正式val矩阵第1/12组`Keep + ResNet50 + seed42`。最佳为
  S2 epoch 5，val患者AUC 0.8713；验证患者阈值0.03251下Sensitivity 0.9254、
  Specificity 0.6684。源码快照与配置SHA一致，完整训练日志已落盘，且未生成
  internal test预测。早先一次运行因运行期间报告字段代码快照变化，仅作pilot完整归档，
  不计入正式矩阵。
- 2026-08-05：完成M0全量诊断正式val矩阵12/12组及2000次患者整簇bootstrap。
  全量阶段暂选Keep与EfficientNet-B0：其跨种子患者AUC为`0.9091±0.0041`，患者
  Accuracy/Sensitivity/Specificity均值为`0.7885/0.9154/0.7444`；图片AUC为
  `0.8485±0.0090`，图片Accuracy/Sensitivity/Specificity均值为
  `0.7606/0.8181/0.7069`。Notch在平均AUC上与Keep接近，但最差种子的患者
  Specificity下降超过预注册0.02门槛，因此不进入下一轮主训练。该选择仍需来源内平衡
  M0确认；internal test和外部test均未参与选择、尚未在本轮评估。
- 2026-08-05：完成当前Keep流程下的独立外部多中心预处理与6组锁定模型评估。
  外部1329人/1941张与M0开发集无SHA或患者标识直接重叠；35张暗部风险图经人工确认
  使用保守回退。EfficientNet-B0跨种子患者AUC/图片AUC为`0.7399±0.0075`/
  `0.7238±0.0079`，患者/图片Accuracy为`0.6353/0.6452`；ResNet50对应为
  `0.7158±0.0205`/`0.6885±0.0228`和`0.5661/0.5932`。结果确认明显域偏移，同时
  支持EfficientNet-B0继续作为来源内1:1.3平衡M0的主候选；外部结果不用于反向调参。
- 2026-08-05：冻结来源内1:1.3主平衡清单1373张/1020人，癌:非癌患者为446:574、
  图片为597:776；患者/图片风格TV为0.0192/0.0461。同步生成严格1:1敏感性清单
  1246张/892人。两清单均无患者或SHA跨split、无缺图、每患者最多3张；双骨干1+1
  epoch冒烟通过，正式6组GPU训练入口与逐组日志已准备。
- 2026-08-05：完成来源内1:1.3平衡M0正式6组val矩阵并锁定EfficientNet-B0为M1-M5
  主骨干。其患者/图片AUC为`0.8523±0.0070/0.8187±0.0098`，ResNet50为
  `0.8273±0.0092/0.7946±0.0115`；internal test未评估。随后完成独立外部多中心
  1329人/1941张评估，EfficientNet-B0外部患者/图片AUC为
  `0.6807±0.0107/0.6563±0.0131`，ResNet50为`0.6828±0.0193/0.6569±0.0132`。
  平衡训练未改善外部AUC，域偏移仍明显；外部结果不用于反向调参或改变骨干选择。
- 2026-08-05：完成严格1:1敏感性（ENet+Keep，3种子，严格清单1246张/892人，
  癌:非癌623:623）。val患者AUC为`0.8389±0.0067`（1:1.3为0.8523，差0.013）、
  锁定阈值下Spec均值0.458（1:1.3为0.663）；独立外部患者AUC为`0.6846±0.0055`
  （1:1.3为0.6807，基本持平），外部Sens/Spec均值0.865/0.409。结论：类别比例只
  影响val（1:1.3多出的非癌患者提高val Spec）、不影响外部；全量→平衡的外部下降
  （0.74→0.68）非比例驱动，更倾向平衡移除可转移的shortcut、外部0.68接近真实泛化，
  数据量因素不能完全排除（需来源/风格审计佐证）。1:1.3主清单维持不变。
- 2026-08-05：完成M0尺度增强A0/A1/A2单变量消融（EfficientNet-B0+Keep，1:1.3平衡，
  3种子，仅改RandomResizedCrop scale，ratio固定）。A0=(0.85,1.0)患者AUC
  `0.8523±0.0070`、A1=(0.7,1.0)`0.8560±0.0054`、A2=(0.5,1.0)`0.8437±0.0062`；
  A2明确更差（Spec降至0.585）排除，A1与A0在种子噪声内持平。锁定A0(0.85,1.0)为
  正式scale：A1增益不构成稳定收益，且保守增强降低后续bbox定位阶段裁掉病灶的风险。
  训练工具已加入`--crop-scale-min/max`参数并快照记录。
- 2026-08-07：收到医学生病灶bbox标注（7个CVAT XML共1773张癌图每图1框，外院/第一/二届
  各1 + 武大省人民按年5；另重标annotations.xml 34张=29张重标+5张漏标补标）。完成
  只读审查与整合：
  - 命名映射验证：武大省人民/第一届用basename、第二届用`{患者}_{basename}`、外院用
    `{中心}_{患者}_{basename}`，重建后100%映射回0804癌图；标注尺寸100%一致、框100%界内；
  - 整合为`数据/标注与汇总/bbox_manifest_20260807.csv`：**1779张癌图全部有bbox**
    （含processed_relative_path、patient_id、label、bbox_xyxy、pip_present、notch_intersect）；
  - 发现1：跨患者重复图`01.0000000025730.0047.1499388698.jpg`出现在
    `01.0000000025730`与`01.0000000265169`两患者目录，处理图SHA完全相同（同一图复制到
    两患者），bbox已同时赋给两份，但划分审计需处理（防同图跨split泄漏）；
  - 发现2：29张bbox-PIP相交经重标后相交面积大幅缩小（最大从~5.5万降到~1.6万，占框
    ≤~9%），但仍有**21张残余相交**；A/B方案待定（Notch臂跳过定位损失 vs 逐张确认
    病灶可见），已在manifest标记`notch_intersect`；
  - 5张漏标（武大省人民2024患者`01.0000000393194`）已补。M1待定：A/B处理、跨患者
    重复图，随后启动CenterNet式辅助定位。
- 2026-08-10：冻结M1来源内1:1.3 Keep主清单
  `08_M1辅助定位清单_20260810/m1_balanced_keep_primary_1to1p3_split_seed42.csv`。
  清单复用M0患者split，共1373张/1020人；597张癌图bbox覆盖率100%，776张非癌图明确
  不计算M1定位损失；图片路径、实际尺寸、框界内、患者跨split/标签和跨患者SHA检查
  全部通过。此前0025730跨患者重复图未进入主清单；Notch相交标记不阻塞Keep主线。
  新增`efficientnet_m1_localization.py`，实现`features[5]`引出的14x14
  `heatmap+size+offset`定位头、bbox感知A0增强、warm-up与联合微调、分类/定位/联合三类
  checkpoint及验证定位指标。物理GPU 1上的6人小样本1+1 epoch冒烟完整通过，定位损失可下降，
  增强后GT框联系表肉眼未见坐标错位；调试指标不代表模型效果，internal test未推理。
  增强后GT框联系表肉眼未见坐标错位；internal test未推理，正式M1尚未启动。
- 2026-08-10：补齐M1全量诊断对照。M0全量Keep清单已在上游排除跨患者副本，实际仅
  保留0025730原患者路径一份；3348张中无跨患者SHA，1670张癌图全部匹配bbox。生成
  `09_M1全量诊断清单_20260810/m1_full_keep_split_seed42.csv`并通过路径、尺寸、框界内、
  患者split和checksums验收。全量清单加载同种子全量M0权重的CUDA 1+1 epoch冒烟通过，
  GT框增强联系表未见错位，未推理internal test。新增`run_m1_matrix.sh`，统一平衡/全量
  ×种子42/202/503的checkpoint、参数、输出和日志；脚本要求显式范围与种子，且不传
  `--evaluate-test`。
- 2026-08-10：更新正式代码审查规范。训练、评估、Grad-CAM、MOCE、SAE和M-CBM等
  主要模块除整体职责外，每个主要函数/类还需在docstring中简要说明输入参数名称、类型、
  作用以及返回值类型与含义；张量和bbox注明shape、坐标范围或统计层级。已先对M1训练
  脚本和M1清单构建脚本完成该格式补充，不改变模型结构、损失、冻结参数或既有结果。
