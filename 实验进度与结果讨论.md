# gastric-cbm 实验进度与结果讨论

> **文档状态：已于2026-08-13结束维护。** 本文档完整保留第二批数据、去偏分类器、
> M0-M5、MOCE与SAE阶段记录；后续专用检测器实验转入
> `YOLO26实验进度与结果讨论.md`，不得继续把新阶段逐轮日志追加到本文档。

## 新对话恢复项目的固定提示词（已停用）

> 本提示词随本文档一同停止维护，仅作历史留档。当前新对话提示词位于
> `YOLO26实验进度与结果讨论.md`开头。

```text
请继续协助我开展 gastric-cbm 胃早癌分类与可解释性研究。

首先确认当前工作目录是项目根目录，并检查至少存在：

- AGENTS.md
- 实验进度与结果讨论.md
- YOLO26实验进度与结果讨论.md
- README.md
- 程序/
- 数据去偏重训练_参数与报告/
- 结果/

如果目录结构不符合，请先定位项目根目录，不要在错误位置新建重复项目。

开始操作前按以下顺序恢复上下文：

1. 完整阅读 AGENTS.md。
   - 重点理解项目长期目标、数据边界、训练/验证/test口径、正式代码入口、文件安全、
     隐私、Git和文档维护规则。
2. 阅读 YOLO26实验进度与结果讨论.md，获取当前即时状态和下一步；仅在追溯M0-M5、
   MOCE或SAE历史时阅读已结束维护的 实验进度与结果讨论.md。
3. 阅读相关模块README和正式报告。
   - 分类任务查看程序/模型训练/正式代码/和数据去偏重训练_参数与报告/；
   - Grad-CAM查看程序/注意力热图/正式代码/；
   - MOCE查看程序/MOCE/正式代码/和结果/MOCE分析/；
   - SAE查看程序/SAE/正式代码/、结果/SAE/和结果/SAE分析/。
4. 需要确认数字时直接读取正式CSV、JSON、config和冻结manifest，不要只依赖Markdown
   摘要或旧对话。
5. 只读检查git status和相关输出目录，确认是否存在用户尚未提交的修改、未完成运行、
   同名输出或待人工复核材料。

按信息类型使用不同的可信度优先级：

- 数值与运行事实：冻结manifest、run config、正式CSV/JSON > 正式专题报告 >
  YOLO26实验进度与结果讨论.md > 已结束的实验进度与结果讨论.md >
  AGENTS.md中的阶段摘要 > 归档文档和旧对话；
- 程序实际行为：当前正式代码 > 对应README > 归档代码和旧对话；
- 文件安全、实验边界和协作规则：AGENTS.md最高。

开始新任务前先在内部核对以下六项，只向用户简要汇报与本轮相关的内容：

1. 当前任务属于分类器、数据、Grad-CAM、MOCE、SAE还是M-CBM；
2. 上一阶段是否已经完成并锁定；
3. 本轮输入、模型、split、阈值、聚合方式和输出目录是什么；
4. 本轮是否会接触test或外部test，是否存在调参泄漏风险；
5. 有哪些内容可以自动验证，哪些必须由临床人员判断；
6. 本轮完成后将影响哪个决策。

不要因为对话上下文不完整而重新训练或重复生成。先检查已有结果；若结果完整，只做
缺失的投影、分析、整理或复核。

实验执行原则：

- 一轮尽量只改变一个关键变量；
- 超参数和剪枝只由train/val确定，内部test和外部test只做锁定评估；
- 患者级划分和患者级指标优先，图像级作为补充；
- 运行GPU任务前先用`nvidia-smi`确认空闲设备，不默认固定GPU始终可用；
- 长任务先debug，输出验收后再全量；
- 不覆盖原始数据、冻结清单、权重、历史结果或人工标注；
- 不批量删除，旧内容移动到归档；
- 未经明确要求不提交、不推送Git；
- 不把患者图像、预测明细、特征缓存或解释图上传公共仓库。

每轮开始前用简短、通俗的话说明：

1. 本轮解决什么问题；
2. 使用什么输入并生成什么输出；
3. 为什么现在需要做；
4. 通过后进入哪一步。

每轮结束按以下格式汇报：

1. 本轮做了什么；
2. 结果如何；
3. 哪些结论已确认，哪些只是候选；
4. 对后续有什么影响；
5. 下一步计划；
6. 新增/修改文件和验证情况。

完成正式实验后更新YOLO26实验进度与结果讨论.md的当前状态、结果、风险和下一步。只有长期规则、稳定事实、
正式入口或重要里程碑变化时才更新AGENTS.md。当前固定提示词始终保留在
YOLO26实验进度与结果讨论.md开头，不在提示词中写具体即时进度。
```

---

## 文档用途

本文件曾作为项目的实时交接记录，现已冻结为历史阶段记录，保存：

- 当前阶段和立即任务；
- 已完成实验及其正式结果；
- 已冻结参数、协议和输出；
- 自动检查、人工复核和异常；
- 候选结论、结论边界和风险；
- 下一步及其决策标准。

详细逐样本数据留在正式CSV/JSON，长篇方法和统计讨论留在专题报告。本文件完整保留
重要阶段表格和总结，同时避免粘贴逐次运行日志。

## 当前状态

更新时间：2026-08-13

| 模块                          | 状态                                | 当前说明                                                                                                                                                                                                                                                                             |
| ----------------------------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ |
| 数据审计与v1.1裁剪            | 已完成                              | 第二批、概念集和完整外部集均已形成正式清单与验收结果                                                                                                                                                                                                                                 |
| 去偏分类器实验A/A0/B/C/D      | 已完成                              | 主实验矩阵、严格共同支持5折和比例敏感性均已完成                                                                                                                                                                                                                                      |
| 患者聚合、集成与TTA           | 已完成并锁定                        | `top2_mean`、两模型等权、无TTA                                                                                                                                                                                                                                                     |
| 完整外部多中心测试            | 已完成                              | 1329人/1941张，单模型与集成均已锁定评估                                                                                                                                                                                                                                              |
| Grad-CAM                      | 已完成正式批量脚本                  | 支持ResNet50/EfficientNet-B0切换和按清单顺序输出                                                                                                                                                                                                                                     |
| 严格概念集MOCE                | 已完成                              | 两模型、两类别正式结果和自动分析均已生成                                                                                                                                                                                                                                             |
| MOCE聚类数量敏感性            | 已完成                              | `K=10,15,...,50`共36组及跨K材料已生成                                                                                                                                                                                                                                              |
| ResNet50 SAE训练              | 已完成并锁定                        | `2048→512→2048`，剪枝后450个Feature                                                                                                                                                                                                                                              |
| SAE外部投影与彩色图像中心分析 | 已完成                              | 外部1941张全量；100位患者彩色提交版已整理                                                                                                                                                                                                                                            |
| ResNet50 SAE内部test投影      | 已完成                              | 732张/349人；剪枝SAE患者AUC 0.9234                                                                                                                                                                                                                                                   |
| SAE跨数据集Feature定量稳定性  | 已完成                              | train/val预选30个；四域方向一致21个                                                                                                                                                                                                                                                  |
| EfficientNet-B0 SAE           | 暂缓/等待新模型                     | 当前v1.1不再单独启动；待新数据最终分类器锁定后按实际特征维度重建                                                                                                                                                                                                                     |
| 新一版平衡分类数据            | 已交付，初审未通过直接训练条件      | `胃早癌概念提取训练集0804`共3577张；图片数近似平衡，但患者数和分辨率/画幅明显不平衡，需先构建来源内患者与风格匹配清单                                                                                                                                                              |
| 新图像裁剪与Keep/Notch        | 已完成并可进入M0                    | PIP复核确认149张/120人，147张自动框、2张人工框；Notch与Keep尺寸一致，26张框QC全部人工通过；每分3577张的配对manifest已生成，其中2张外置PIP在Notch分支按普通图片分组                                                                                                                   |
| M0全量诊断基线                | val矩阵与外部评估完成               | 开发集3348张/1732人；跨种子暂选Keep + EfficientNet-B0；外部1329人/1941张患者AUC 0.7399±0.0075、图片AUC 0.7238±0.0079；internal test仍未评估，待来源内1:1.3平衡M0确认                                                                                                               |
| M0来源内1:1.3平衡             | 正式val、尺度消融与外部评估完成     | val锁定Keep + EfficientNet-B0 + A0；val患者/图片AUC 0.8523±0.0070/0.8187±0.0098，外部为0.6807±0.0107/0.6563±0.0131；严格1:1和A0/A1/A2均完成；internal test继续锁定                                                                                                               |
| 框辅助分类器                  | M1定位保留；M3c-B硬门控test未确认 | M1框内部test几何稳定（mean IoU约0.446、center-hit 0.907）。M3c-B锁定内部test为0/3，降级为辅助评分；诊断性外部患者均值AUC为0.724（早期ResNet参照0.693），但不自动隐藏M1框。M4/M5/M5b/M5c局部分类干预路线均已结束 |
| M3非癌响应抑制                | balanced正式失败，full未运行        | 预注册要求balanced 3/3种子成功，实际仅seed42成功，seed202/503均无合格epoch，故结果为1/3并按门控停止。负监督可降低非癌FP，但不能跨种子稳定保持癌召回；M1仍为正式定位产品，internal test未评估                                                                                         |
| M3b温和定位抑制               | balanced正式失败                    | 全网格温和抑制M3b-A为2/3，Top-5稀疏抑制M3b-B为1/3，均未达预注册3/3；不运行full/test/external                                                                                                                                                                                         |
| M3c框展示门控                 | val成功，内部test正式未确认         | M3c-B在val为3/3，但锁定内部test为0/3；可保留`q_region`作为并列辅助评分或人工排序信号，不得自动隐藏M1框。外部投影仅为诊断，不改变内部test结论                                                                                                                                        |
| YOLO26专用检测器路线          | 已转入新的实时进度文档              | 后续状态、冻结协议和结果统一见`YOLO26实验进度与结果讨论.md`；本文档不再追加YOLO逐轮记录                                                                                                                                                                                              |
| 扩张型SAE实验                 | 路线已冻结，尚未运行                | 先保留现有512维参照，再只用train/val逐级比较完整宽度和扩张字典                                                                                                                                                                                                                       |
| 医学概念命名                  | 进行中/待临床                       | MOCE与SAE材料已交付医学生                                                                                                                                                                                                                                                            |
| M-CBM/CBL                     | 暂未正式启动                        | 等待稳定概念命名；代码入口已准备                                                                                                                                                                                                                                                     |

## 当前统一口径

- 患者级指标是主要临床口径，图像级指标作为补充。
- train/val用于模型、阈值、聚合、解释器超参数和剪枝选择；内部test与外部test只做
  锁定投影和评估。
- 内部高AUC必须与严格共同支持实验和独立外部结果一起解释，不能单独引用为多中心
  泛化性能。
- MOCE的SSC step 0首先反映黑色遮挡输入的类别偏置，不代表黏膜特征一致性。
- SAE彩色热图是7x7空间响应上采样；红色表示促癌logit margin贡献，蓝色表示负向
  贡献，紫色通常来自红蓝叠加，不是病灶分割或第三种贡献类型。
- 外部癌患者图片数显著多于非癌，仅图片数预测标签AUC为0.980；所有患者聚合结果必须
  同时报告mean和随机单图等敏感性分析。
- 新扩张SAE与EfficientNet-B0 SAE必须先在本文档冻结候选网格和选择规则，只用
  train/val完成选择。
- 下一版开发数据以早癌大赛和武大省人医为主，但必须在每个来源内部同时平衡癌/非癌
  患者、图片数量和画面风格；不得用“一个来源的癌+另一个来源的非癌”形成表面平衡。
- 病灶框以冻结的高分辨率Keep图（空间裁剪+FOV后，保留PIP）坐标为标注基准，同时记录
  图像尺寸、SHA256、`geometry_id`和坐标约定（左闭右开）。Notch只改变像素且与Keep共享
  `geometry_id`，因此M0选定Notch后无需重新标注；必须保留Keep到Notch的恒等坐标映射，并对
  bbox与Notch黑块相交的图片单独复核。带可视化矩形框的图片只用于人工验收，不作为训练
  输入。原坐标保存规则仅适用于
  标注在裁剪前完成的旧流程，此时必须同步变换bbox并校验。（2026-08-03修正：新数据
  改为crop-first标注，见“新一版数据边界”。）

## 当前正式数据

### 第二批分类数据

- 原始整理数据：5229张；
- 癌/高级别：1600张、415位患者；
- 非癌：3629张、1906位患者；
- 正式分类器输入：`数据/第二批裁剪后_v1_1`；
- 实验A冻结划分：
  - train：1624位患者、3704张；
  - val：348位患者、793张；
  - test：349位患者、732张。

### 医学生整理概念集

- 原始审计：3601张，0解码失败；
- 早癌1814张/487人，非癌1787张/1393人；
- 原图图片数接近平衡，但分辨率与画面风格严重不平衡；
- 排除跨标签患者和完全重复副本后，每患者限1至3张；
- MOCE严格核心集：314位患者、157对、502张，两类各251张；
- 严格集用于概念发现，不替代分类器训练集。

### 独立外部多中心集

- 1329位患者、1941张；
- 早癌：121人、542张；
- 非癌：1208人、1399张；
- 与第二批训练原图无SHA重叠；
- 癌患者图片数明显多于非癌，患者聚合结果必须同时报告敏感性分析。

## 分类器正式结果

### 第一批图像随机划分历史基线

第一批实验使用3314张可匹配图像，按图片分层随机划分为2319/497/498张。验证集选择
`Sensitivity >= 0.90`阈值后，图片级test锁定结果如下：

| 模型            | 图片AUC | 阈值 |  Sens |  Spec |   Acc | Precision |    F1 | TN/FP/FN/TP    |
| --------------- | ------: | ---: | ----: | ----: | ----: | --------: | ----: | -------------- |
| ResNet50        |  0.8427 | 0.20 | 0.892 | 0.514 | 0.733 |     0.716 | 0.794 | 108/102/31/257 |
| EfficientNet-B0 |  0.8806 | 0.22 | 0.906 | 0.595 | 0.775 |     0.754 | 0.823 | 125/85/27/261  |

阶段总结：

- 该阶段证明两类CNN和完整训练、阈值、推理流程可以跑通，EfficientNet-B0点估计较高；
- 划分按图片而不是患者进行，且原数据存在标签和划分问题，同一患者泄漏风险未被排除；
- 这组数字只说明项目早期迭代，不能与后续患者级结果直接比较，也不再作为正式模型。

### 第二批整理后未裁剪旧模型

原始`第二批`清理为`第二批整理后`后，使用5229张、2321位患者完成首次患者级训练。
训练期自然阈值0.50的图片级test结果：

| 模型            | 图片AUC |    Acc |   Sens |   Spec | Precision |     F1 | TN/FP/FN/TP   |
| --------------- | ------: | -----: | -----: | -----: | --------: | -----: | ------------- |
| ResNet50        |  0.9900 | 0.9703 | 0.9526 | 0.9779 |    0.9485 | 0.9505 | 530/12/11/221 |
| EfficientNet-B0 |  0.9910 | 0.9677 | 0.9569 | 0.9723 |    0.9367 | 0.9467 | 527/15/10/222 |

验证集按`Sensitivity >= 0.90`选择阈值后，同一774张图片test的锁定结果：

| 模型            | 阈值 | 图片AUC | 患者AUC |  Sens |  Spec |   Acc | Precision |    F1 | TN/FP/FN/TP   |
| --------------- | ---: | ------: | ------: | ----: | ----: | ----: | --------: | ----: | ------------- |
| ResNet50        | 0.30 |   0.990 |   0.979 | 0.978 | 0.867 | 0.901 |     0.759 | 0.855 | 470/72/5/227  |
| EfficientNet-B0 | 0.22 |   0.991 |   0.983 | 0.987 | 0.799 | 0.855 |     0.678 | 0.804 | 433/109/3/229 |

阶段总结：

- 患者级划分修复了第一批最直接的同患者泄漏风险，但输入仍保留黑边、设备界面、原始
  分辨率和来源风格；
- ResNet50验证集外院子集图片AUC仅0.727，而全量验证AUC为0.960；EfficientNet-B0
  全量test图片AUC为0.991，外院子集为0.921，显示来源构成显著影响总体指标；
- 验证集癌图主要来自武大省人民、外院图主要为非癌，约0.99的内部AUC不能解释为
  跨医院病灶识别能力；
- 相关权重只用于裁剪前后诊断对照和历史解释，不再作为部署或后续正式解释模型。

### 实验A内部冻结结果

实验A使用v1.1完整裁剪数据和标签×来源分层患者划分。阈值只在验证集按
`Sensitivity >= 0.90`选择，再锁定到349位内部test患者：

| 模型            |    患者AUC（95% CI） |  阈值 |  Sens |  Spec |   Acc |    F1 |
| --------------- | -------------------: | ----: | ----: | ----: | ----: | ----: |
| ResNet50        | 0.928（0.899-0.954） | 0.385 | 0.889 | 0.864 | 0.868 | 0.709 |
| EfficientNet-B0 | 0.931（0.896-0.961） | 0.389 | 0.841 | 0.839 | 0.840 | 0.654 |

验证集患者AUC分别为0.913和0.931。同一患者配对比较的test AUC差为-0.003
（95% CI -0.029至0.024，`p=0.792`），不支持两模型在实验A中存在显著差异。

完整实验矩阵患者级主要结果：

| 实验 | 数据协议                       | ResNet50 AUC | EfficientNet-B0 AUC | 模型差异p值 |
| ---- | ------------------------------ | -----------: | ------------------: | ----------: |
| A    | 完整裁剪数据，新标签×来源划分 |        0.928 |               0.931 |       0.792 |
| A0   | 完整裁剪数据，复用旧患者划分   |        0.953 |               0.957 |       0.721 |
| B    | 松弛匹配1:1                    |        0.929 |               0.951 |       0.199 |
| C    | 严格共同支持1:1，5折OOF        |        0.814 |               0.785 |       0.313 |
| D    | 松弛匹配1:2                    |        0.944 |               0.957 |       0.194 |

严格共同支持实验C包含90对、180位患者和238张图。每折阈值只由该折验证患者确定，再
锁定到测试折：

| 模型            | 患者OOF AUC（95% CI） | 图片OOF AUC |  Sens |  Spec |   Acc |    F1 |
| --------------- | --------------------: | ----------: | ----: | ----: | ----: | ----: |
| ResNet50        |  0.814（0.750-0.872） |       0.797 | 0.800 | 0.611 | 0.706 | 0.731 |
| EfficientNet-B0 |  0.785（0.712-0.849） |       0.782 | 0.744 | 0.522 | 0.633 | 0.670 |

阶段总结：严格集结果说明完整与松弛数据的内部高AUC仍受残余混杂影响；但严格集规模
较小、阈值波动较大，其结果是已知画面因素控制后的敏感性分析，不是全部患者总体性能。

内部集成方案由验证集选择：

- 聚合：`top2_mean`；
- 权重：ResNet50 0.5 + EfficientNet-B0 0.5；
- TTA：关闭；
- 阈值：0.518973；
- 冻结test患者AUC：0.9578；
- test Accuracy/Sensitivity/Specificity：0.8940/0.8889/0.8951。

349位内部冻结test患者的完整集成对比：

| 方案                  | 锁定阈值 | 患者AUC |    Acc |   Sens |   Spec |     F1 |
| --------------------- | -------: | ------: | -----: | -----: | -----: | -----: |
| ResNet50，mean        |    0.385 |  0.9283 | 0.8682 | 0.8889 | 0.8636 | 0.7089 |
| EfficientNet-B0，mean |    0.389 |  0.9314 | 0.8395 | 0.8413 | 0.8392 | 0.6543 |
| 等权集成，top2_mean   | 0.518973 |  0.9578 | 0.8940 | 0.8889 | 0.8951 | 0.7517 |

集成相对ResNet50和EfficientNet-B0的患者AUC差分别为+0.0295
（95% CI +0.0100至+0.0492）和+0.0264（+0.0129至+0.0421）。水平翻转TTA未通过
验证集选择，正式方案保持关闭。

正式报告：

- `数据去偏重训练_参数与报告/完整裁剪数据重训练实验A结果_v1.md`
- `数据去偏重训练_参数与报告/患者聚合集成TTA评估结果_v1.md`

### 偏倚敏感性

- 旧权重直接输入v1.1裁剪test时，ResNet50患者AUC从0.979降至0.772，
  EfficientNet-B0从0.983降至0.893；
- 实验A0在同一旧split上重训后，患者AUC恢复到0.953/0.957；
- 严格共同支持集5折OOF患者AUC为ResNet50 0.814、EfficientNet-B0 0.785；
- 松弛1:1和1:2实验AUC较高，但元数据仍可强预测标签，只作覆盖和敏感性分析。

当前解释：v1.1重训恢复了内部判别能力，但完整与松弛数据仍有残余来源/风格混杂。
严格共同支持结果更能反映去除主要已知画面差异后的困难程度。

正式报告：

- `数据去偏重训练_参数与报告/旧模型在v1_1裁剪数据验证结果_v1.md`
- `数据去偏重训练_参数与报告/严格共同支持集5折结果_v1.md`
- `数据去偏重训练_参数与报告/P7正式实验汇总_v1.md`

### 完整外部多中心锁定测试

独立完整外部集包含1329位患者、1941张图，其中癌121人、非癌1208人。以下完整复用
实验A锁定的模型、阈值、聚合和集成参数：

| 方案            | 聚合      |     阈值 |    患者AUC（95% CI） |  Sens |  Spec | Balanced Acc | Precision |   NPV | TP/FN |   TN/FP |
| --------------- | --------- | -------: | -------------------: | ----: | ----: | -----------: | --------: | ----: | ----: | ------: |
| ResNet50        | mean      |    0.385 | 0.693（0.649-0.736） | 0.628 | 0.660 |        0.644 |     0.156 | 0.947 | 76/45 | 797/411 |
| EfficientNet-B0 | mean      |    0.389 | 0.738（0.696-0.780） | 0.793 | 0.620 |        0.707 |     0.173 | 0.968 | 96/25 | 749/459 |
| 等权集成        | top2_mean | 0.518973 | 0.802（0.761-0.840） | 0.719 | 0.734 |        0.727 |     0.213 | 0.963 | 87/34 | 887/321 |

重要边界：

- 外部性能明显低于内部test，支持存在跨中心域偏移；
- 仅患者图片数量预测标签AUC为0.980；
- 集成改用mean聚合时AUC为0.729；
- 每患者随机单图重复抽样时，ResNet50/EfficientNet-B0/集成平均AUC约
  0.669/0.716/0.709；
- 因此集成0.802不能全部解释为病灶判别或模型互补。

正式报告：

`数据去偏重训练_参数与报告/完整多中心外部测试结果_v1.md`

## Grad-CAM进度

- 正式入口：`程序/注意力热图/正式代码/gradcam_batch.py`；
- 一次只运行一个模型，可在ResNet50与EfficientNet-B0间切换；
- 输出按清单顺序保存，并记录真实标签、预测标签和癌概率；
- 三联图版式、标题字号和位置已固定；
- 正式结果按模型分目录，旧热图已归档；
- 热图用于定位模型关注，不作为病灶分割；
- 后续临床复核重点是共同漏诊、共同误报以及是否关注反光、暗腔、边缘和界面。

## MOCE进度

### 正式严格概念集

- 输入：502张/314人/157对；
- ResNet50候选区域：非癌23351、癌25066；
- EfficientNet-B0候选区域：非癌37007、癌37745；
- 两类均完成聚类、重要性、SSC/SDC、概念总览、扩展图和命名表；
- 正式分析目录：
  - `结果/MOCE分析/resnet50/`
  - `结果/MOCE分析/efficientnet_b0/`

### 当前稳定观察

- 两模型均表现为癌侧少数概念对输出贡献更集中；
- 非癌侧单一概念移除影响较弱；
- ResNet50癌侧移除前五概念的破坏更强；
- EfficientNet-B0癌侧前五概念在黑图上恢复输出的充分性更高；
- 这些差异可能受特征层、候选区域粒度、概念重叠和遮挡基线影响，不能直接转述为
  “某模型学到了更真实的癌概念”。

### 聚类数量敏感性

- 已完成`K=10,15,...,50`；
- 两模型、两类别共36组；
- 自动分析和医学生提交材料位于`结果/MOCE聚类数量分析/`；
- 选择K时需要同时看簇内一致性、簇间重复、临床可命名性、重要性稳定性和跨K对应，
  不能只看单一聚类指标。

### 待人工工作

- 优先检查癌侧高重要性概念是否落在病灶；
- 标记反光、黏液、器械、黑边、暗腔和设备界面等伪概念；
- 比较同类概念是否跨患者、跨模型、跨K稳定；
- 形成可进入M-CBM/CBL的共识命名。

## SAE进度

### 正式ResNet50 SAE

正式run：

`结果/SAE/去偏重训练_v1/resnet50/formal_expA_resnet50_sae_l1_0005_20260724`

配置：

- 输入：ResNet50 layer4 GAP，2048维；
- SAE：`2048 → 512 → 2048`；
- `lambda_l1=0.0005`；
- batch size 32；
- 最大1000 epoch，patience 50；
- 最佳epoch 104；
- train：3704张/1624人；
- val：793张/348人；
- 512个Feature剪枝后保留450个；
- train平均L0为17.64，val平均L0为27.63。

内部验证保真度：

| 层级   | 原模型AUC | 剪枝SAE AUC |    变化 |
| ------ | --------: | ----------: | ------: |
| 图像级 |    0.8961 |      0.8781 | -0.0180 |
| 患者级 |    0.9135 |      0.9031 | -0.0104 |

外部投影：

| 层级   | 原模型AUC | 剪枝SAE AUC |    变化 |
| ------ | --------: | ----------: | ------: |
| 图像级 |    0.6812 |      0.6726 | -0.0085 |
| 患者级 |    0.6934 |      0.6929 | -0.0004 |

解释：SAE外部患者级排序保真度较高，但不能把0.693解释为SAE造成的低性能；被解释的
ResNet50原模型外部AUC本身就是0.693。

内部test锁定投影：

| 层级   | 原模型AUC | 完整SAE AUC | 剪枝SAE AUC | 剪枝变化 |
| ------ | --------: | ----------: | ----------: | -------: |
| 图像级 |    0.8919 |      0.8823 |      0.8806 |  -0.0113 |
| 患者级 |    0.9283 |      0.9247 |      0.9234 |  -0.0049 |

锁定患者阈值0.38494下：

| 表示    |    Acc |   Sens |   Spec |     F1 | TN/FP/FN/TP  |
| ------- | -----: | -----: | -----: | -----: | ------------ |
| 原模型  | 0.8682 | 0.8889 | 0.8636 | 0.7089 | 247/39/7/56  |
| 完整SAE | 0.8367 | 0.8254 | 0.8392 | 0.6460 | 240/46/11/52 |
| 剪枝SAE | 0.8682 | 0.8254 | 0.8776 | 0.6933 | 251/35/11/52 |

- 732张、349位患者全部完成，患者标签分布为非癌286/癌63；
- 原模型概率与实验A冻结输出逐图一致，最大绝对误差为`1.6e-7`；
- 剪枝SAE患者AUC只下降0.0049，排序保真度较高；
- 锁定阈值下患者Sensitivity下降0.0635，同时Specificity上升0.0140，说明SAE重构造成
  概率标度和决策边界漂移；不得用test重新调阈值修饰该结果；
- 输出：`结果/SAE/去偏重训练_v1/resnet50/internal_test_resnet50_sae_20260731/`。

### 图像中心彩色SAE

- 外部1941张全部完成Top-5图像中心可视化；
- 每图包含综合彩色图、5个独立Feature图、统计表和总览；
- 红色为促癌方向，蓝色为负向方向，紫色可由重叠产生；
- 全量目录约16GB：
  `结果/SAE图像中心可视化/external_multicenter_all_top5_vivid_bg_20260729/`；
- 医学生彩色提交版为TP/TN/FP/FN各25位患者，共100人：
  `结果/SAE分析/去偏重训练_v1/resnet50/SAE图像中心彩色热图_医学生提交版_v1/`。

### 跨数据集Feature稳定性

使用同一锁定SAE已有缓存，对齐train、val、内部test和外部test：

| 数据集        | 图片 | 患者 | 用途     |
| ------------- | ---: | ---: | -------- |
| train         | 3704 | 1624 | 候选筛选 |
| val           |  793 |  348 | 候选筛选 |
| internal test |  732 |  349 | 锁定确认 |
| external test | 1941 | 1329 | 锁定确认 |

候选只根据train/val产生：保留剪枝Feature、两域至少各激活5位患者、标签激活方向一致，
再按两域最小标签区分度与覆盖率排序；癌相关和非癌相关各取15个。test与external没有
参与候选筛选。

定量结果：

- 30个候选全部满足激活方向与分类margin贡献方向一致；
- 21/30在四个数据集保持相同标签激活方向；
- 癌相关候选15/15方向一致，非癌相关候选6/15方向一致；
- 22/30在internal test和external test的最小标签区分度不低于0.05，14/30不低于0.10；
- Feature覆盖率的train/val/internal test Spearman为0.879/0.872/0.855量级，与external
  的相关性降至0.574-0.624；
- 标签激活差异的train-external Spearman仅0.141，说明外部域中Feature排序发生明显变化；
- 来源相关Feature 48的来源AUC在train/val/internal test为0.722/0.774/0.726，应优先
  作为domain Feature审核，不应直接命名为医学概念。

上述“稳定”只表示同一SAE方向在不同患者集合中的统计关系，不证明空间响应落在病灶，
也不证明医学语义一致。正式输出：

`结果/SAE跨数据集稳定性/去偏重训练_v1/resnet50/cross_dataset_feature_stability_20260731/`

### SAE当前待做

1. 对已由train/val冻结的30个候选制作跨数据集空间响应对照：
   - 优先展示四域方向一致的21个Feature；
   - 同时单列Feature 48等来源相关方向作为伪特征对照；
   - 检查热点是否稳定落在病灶、正常黏膜、反光、边缘或设备界面。
2. 设计扩张型SAE train/val实验：
   - 现有512维作为基线；
   - 优先比较2048维和4096维；
   - L1需重新扫描或采用warm-up；
   - 只用val比较L0、重构、预测保真度、死Feature、重复性和概念一致性；
   - 开始前冻结候选维度、L1或warm-up规则、允许的AUC下降、最低预测一致率，以及
     L0、死Feature和重复Feature的选择标准；
   - 方案锁定后才能运行一次内部test和外部投影；由于这些test结果已经被查看，新扩张
     实验应明确标为预先固定规则后的补充/探索性分析，不包装成完全未见的确认性实验。
3. 当前v1.1 EfficientNet-B0 SAE暂缓：
   - 不再为即将被新数据与新分类路线替代的旧EfficientNet-B0单独启动正式SAE；
   - 现有1280维适配需求和旧计划保留，避免误解为技术上不可实现；
   - 待新数据最终分类器和待解释层锁定后，再按实际特征维度独立训练，不能复用
     ResNet50 SAE；
   - 如最终仍选择EfficientNet-B0，则只用新模型train/val确定字典规模和L1，再锁定
     投影内部test与外部test。
4. 医学命名稳定后再运行`mcbm_cbl_train.py`。

## 下一阶段数据与技术路线

本节记录2026-07-31与医学生、其他工程师讨论后冻结的方法路线。以下内容是待执行协议，
不是已经取得的实验结果；实际参数仍须在运行前写入配置并只用train/val选择。

### 新一版数据边界

- 开发数据以早癌大赛和武大省人医为主；
- 两个来源分别在来源内部平衡癌/非癌患者数、图片数和主要画面风格，不能只在合并后
  达到总量1:1；
- 继续执行患者级划分、同检查近重复去除、每患者图片数控制和来源×标签审计；
- 外院数据原则上作为冻结外部测试，不参与模型、阈值、增强、聚合或解释器超参数选择；
- 医学生基于冻结后的高分辨率Keep图进行标注，交付无可视化框的Keep图、
  患者与标签信息以及结构化矩形框坐标；同时保留原图用于追溯和临床对照。带框预览图
  只用于人工验收，禁止作为训练输入；
- 癌图需要记录`lesion_visible`和有效矩形框；癌但病灶不可见的图不能计算定位损失。
  非癌中的局灶性良性病变如能获得框和病变类型应一并保留，完全正常图可为空框。

建议的最小框字段为：

```text
image_path, image_sha256, geometry_id, image_width, image_height, patient_id, label, lesion_visible, x1, y1, x2, y2, coordinate_convention
```

`image_path`指向冻结后的无可视化框Keep图，`coordinate_convention`首版固定为
`xyxy_left_closed_right_open`。原图到Keep、Keep到Notch的变换记录保留在数据manifest中，
不与病灶框坐标系混用。

#### 0804交付数据初审（2026-08-04）

新交付目录为`数据/胃早癌概念提取训练集0804`，原图保持只读。完整扫描结果：

| 范围           |    癌/高级别 |          非癌 | 初步判断                           |
| -------------- | -----------: | ------------: | ---------------------------------- |
| 全部           | 1779张/487人 | 1798张/1369人 | 图片近似1:1，患者明显不平衡        |
| 武大省人民     | 1431张/375人 | 1447张/1188人 | 图片平衡，患者和画面风格严重不平衡 |
| 第一届早癌大赛 |    70张/27人 |     70张/46人 | 分辨率和画幅一致，患者不平衡       |
| 第二届早癌大赛 |   177张/46人 |    166张/54人 | 患者较接近，画幅/尺寸接近          |
| 外院合计       |   101张/39人 |    115张/81人 | 不进入主开发集，继续按外部用途隔离 |

- 3577张均可解码，0个零字节文件；共有2组完全重复、额外2张，均在癌侧；
- 武大省人民有2位跨标签患者：`01.0000000129422`、`01.0000000166648`，在临床确认前
  不得进入任何split；
- 癌图中`1920x1080`为1300张，非癌为695张；精确分辨率TV为0.4829、宽高比分组TV
  为0.3442、尺寸分组TV为0.3852，不能把全量3577张直接作为去偏训练集；
- 第一届大赛三项风格TV均为0；第二届大赛宽高比TV为0.0181、尺寸TV为0.0200；主要
  风格失衡来自武大省人民；
- 0804外院216张与锁定外部多中心测试1941张无完全SHA重叠，但仍从主训练候选中排除；
- 下一步应先生成来源内患者级癌:非癌约1:1.3、每位患者图片上限一致且画面风格匹配的
  冻结候选清单，再进入平衡M0。严格1:1保留为最终入选骨干的敏感性分析；筛选通过
  manifest完成，不移动或删除原图。

#### M0全量诊断基线（2026-08-05冻结）

在来源内平衡集之前，先跑一轮不做患者数平衡的全量M0，用于确认信号上限、
Keep/Notch影响、两种骨干差异和风格shortcut风险。该实验只是诊断基线，不取代
来源内平衡核心实验；无论全量结果是否显示明显风格依赖，平衡对照均必须继续。

全量的精确边界为：

- 只使用武大省人民、第一届早癌大赛和第二届早癌大赛；
- 0804中的216张外院图仍按补充外部队列隔离，不进入train/val/internal test；
- 整体排除2位跨标签患者`01.0000000129422`、`01.0000000166648`；
- 完全重复图按Keep SHA256确定性保留1张，禁止重复副本跨split；
- 预计合格开发集为3348张/1732人，图片约为1:1，患者明显不平衡；
- 用`source×label`做患者级分层，按70/15/15生成唯一冻结split，split seed为42；
- Keep与Notch共用同一患者split、图片ID、增强、训练参数和随机种子。

正式M0在查看任何训练结果前冻结：

```text
preprocess_delta_auc_noninferiority = 0.005
backbone_delta_auc_noninferiority = 0.005
delta_sensitivity_noninferiority = 0.02
delta_specificity_noninferiority = 0.02
training_seeds = [42, 202, 503]
split_seed = 42
split_ratio = 0.70 / 0.15 / 0.15
threshold_rule = val患者Sensitivity >= 0.90时Specificity最大
minimum_val_sensitivity = 0.90
minimum_val_specificity = 0.50
cross_seed_summary = 均值 + 标准差 + 最差种子
paired_comparison = 同一val患者配对bootstrap 2000次
```

主指标是跨种子val患者AUC均值；Sensitivity和Specificity使用上述val锁定阈值。
internal test只在方案锁定后评估，不参与Keep/Notch、骨干、epoch或阈值选择。
风格判断同时依据来源/尺寸/画幅子组、元数据基线、来源留出和Grad-CAM，
不以单一内部AUC作判断。

### 阶段0：现有v1.1尺度增强预实验

阶段0只验证训练增强行为和代码稳定性，不把旧数据上的最优参数直接宣布为新数据最优。
开始前先确认现有训练脚本实际使用的`RandomResizedCrop`参数。

候选单变量对照：

| 编号 | RandomResizedCrop scale | 说明                             |
| ---- | ----------------------- | -------------------------------- |
| A0   | 当前正式参数            | 基线                             |
| A1   | `(0.7, 1.0)`          | 相对当前基线轻度放宽随机裁剪范围 |
| A2   | `(0.5, 1.0)`          | 相对当前基线较强放宽随机裁剪范围 |

当前正式基线为`scale=(0.85, 1.0)`、`ratio=(0.90, 1.10)`。`scale`表示裁剪区域占
原图面积的比例；下界从0.85降到0.7或0.5，会扩大随机裁剪范围并增强随机放大程度，
同时增加无bbox癌图被裁掉病灶却保留癌标签的风险。阶段0不使用Torchvision默认
`(0.08, 1.0)`，也不优先使用`(0.3, 1.0)`。同时检查`ratio`，避免胃镜结构被过度拉伸。

阶段0使用完全相同的患者划分、预训练权重和训练配置，至少比较3个随机种子。只用val
比较患者AUC、Sensitivity、Specificity、最佳epoch和过拟合趋势；外部test不参与选择。
通过只表示该增强在旧数据上未造成稳定退化，新数据到位后仍须重新确认。

### M1：CenterNet式辅助定位正则

第一版不做ROI特征融合，也不把定位头作为可部署检测器。其唯一目的，是用癌图矩形框
迫使共享encoder保留病灶位置相关特征，再观察分类和外部泛化是否改善。

正式首版结构：

```text
完整图 → ResNet50/EfficientNet encoder
       ├─ 最终特征 → GAP → 分类头
       └─ layer3特征 → 3x3 Conv + ReLU
                      ├─ 1x1 Conv → center heatmap（1通道）
                      ├─ 1x1 Conv → width/height（2通道）
                      └─ 1x1 Conv → center offset（2通道）
```

ResNet50的`layer3`约为`14x14`空间分辨率。中心目标使用按bbox大小生成的二维高斯热图；
宽高和offset只在真实中心位置计算回归损失。offset用于补偿14x14网格的中心量化误差。

```text
L_loc = L_heatmap_focal
      + lambda_size * L_size
      + lambda_offset * L_offset

L_total = L_cls + lambda_loc * I_valid_box * L_loc
```

- `L_cls`由所有图片计算；
- `I_valid_box`只对有可见、合法病灶框的癌图为1；
- 非癌图和病灶不可见癌图在M1中只计算分类损失；
- M1没有独立图像级objectness；center heatmap仍是位置置信度，但不宣称能够判断非癌
  图中是否存在病灶。

定位验收不能只看分类AUC，还需报告中心命中率、中心距离、IoU或框覆盖率，并与固定预测
画面中心的朴素基线比较。M1即使暂未提高分类AUC，只要定位明显可学且分类未稳定退化，
仍可继续测试特征金字塔；定位完全不可学时先检查标注、病灶可见性和特征分辨率。

### 后续检测与全局/局部路线

M2在M1基础上融合`layer2/layer3/layer4`特征，形成内部特征金字塔，以改善小病灶定位。
M3的"非癌全零热图抑制"路线已尝试并按预注册判负（M3/M3b-A/B均未达3/3），被
M3c-B"冻结M1框 + 独立区域门控头"取代（3/3通过）。以下"定位模型可用后再进入ROI
融合"中的定位/门控基座均指正式管线`M1定位头 + M3c-B门控头`，不是已失效的M3热图
抑制头。

定位模型可用后再进入ROI融合：

- M4使用医生真值框裁出的高分辨率局部图，与低分辨率完整图融合，用于测量理论上限；
- M5使用定位模型预测框，代表真实可部署结果；
- 局部裁图从正式处理输入图的未缩放分辨率版本生成并保留适量框外黏膜，不能使用
  已画框图片；
- M4可对真值框做合理平移、缩放和边界扰动；真值框与预测框渐进混合只作为后续课程
  学习或联合微调候选，不并入第一版M4/M5；
- 无可靠候选框时必须退化为全局分类，不因空ROI导致静默错误。

正式实验矩阵：

| 编号 | 定位监督        | 多尺度方式           | ROI融合 | 决策问题             |
| ---- | --------------- | -------------------- | ------- | -------------------- |
| M0   | 无              | 常规增强             | 无      | 新数据CNN分类基线    |
| M1   | 癌图辅助定位    | layer3单层           | 无      | bbox监督是否改善表示 |
| M2   | 癌图辅助定位    | layer2/3/4特征金字塔 | 无      | 小病灶定位是否改善   |
| M3   | 癌/非癌热图监督 | 选定的M1或M2定位结构 | 无      | 自动检测是否可用     |
| M4   | 冻结M1+M3c-B    | 完整图+高分辨率局部  | 真值框  | 全局/局部理论上限    |
| M5   | 冻结M1+M3c-B    | 完整图+高分辨率局部  | 预测框  | 最终部署性能         |

不采用`112x112`缩小后黑边pad至`224x224`的双分支：两路没有新增互补内容，缩小图会
损失早癌细节，固定黑边还可能重新引入边界shortcut。无标注阶段优先使用保守尺度增强；
有框阶段的“完整低分辨率视野+处理输入未缩放分辨率ROI”才作为真正的信息金字塔。
本节的“未缩放分辨率”均指正式处理输入图在网络resize前的尺寸，不是采集原图尺寸。

小型Transformer不是M0-M5的替代。先用CNN验证监督方式，再将全局骨干替换为Swin-T等
小模型做受控对照，避免同时改变数据、监督、尺度和backbone而无法归因。

### 分类器分阶段训练与冻结协议

本节规定M0-M5各阶段的输入、初始化、损失、参数冻结、模型选择和阶段衔接。结构路线
已经确定，具体学习率、epoch、batch size和损失权重必须在每阶段运行前写入配置并只用
train/val冻结。

通用原则：

- 所有阶段复用同一患者级train/val/internal test划分；
- 外部数据不参与模型、损失权重、epoch、阈值和结构选择；
- 模型只读取无可视化框的正式处理输入图和独立bbox坐标，带可视化框的图片不作为输入；
- 新数据先完成高分辨率空间裁剪、人工验收和版本冻结，再交给医学生标bbox；不先缩到
  `224x224`。标注文件必须绑定裁剪图SHA256和尺寸，训练resize/增强时同步变换bbox；
- 所有裁剪、缩放和翻转必须同步变换bbox；
- 每阶段加载上一阶段的val最佳权重，不根据test选择checkpoint；
- 分类指标和定位指标分别报告，不能只用一个联合分数掩盖某一任务退化；
- 新增模块先单独warm-up，再解冻必要的Encoder后层联合训练；
- M4/M5第一版保持M1定位头与M3c-B门控头完整冻结，避免融合训练改变定位框（原"保持
  M3完整冻结"因M3失败已失效，正式基座为`M1 + M3c-B`）。

#### M0：完整图分类基线

```text
无标记完整图 → Encoder → GAP → 全局分类头
```

损失：

```text
L_M0 = L_cls
L_cls = CrossEntropy(global_logits, image_label)
```

训练分两步：

1. 加载ImageNet预训练Encoder，冻结全部Encoder，只训练分类头；
2. 分类头稳定后，解冻Encoder后部，使用更低学习率联合微调。

ResNet50首版建议：

```text
阶段A训练：分类头
阶段B训练：layer4 + 分类头
可选阶段C：layer3 + layer4 + 分类头
保持冻结：conv1、layer1，原则上也先冻结layer2
```

M0分别输出：

```text
m0_best_resnet50.pth
m0_best_efficientnet_b0.pth
```

新数据画中画较常见，M0同时加入`Keep/Notch`预处理消融。首轮采用完整的
`2种骨干 × 2种预处理`矩阵：

| 预处理分支           | 画中画图                 | 无画中画图         | 首轮定位   |
| -------------------- | ------------------------ | ------------------ | ---------- |
| `pip_keep`         | 四边裁剪后原样保留       | 同一四边裁剪图     | 基线       |
| `pip_remove_notch` | 人工确认框及安全边距置黑 | 与Keep指向同一文件 | 主去除方案 |

Keep与Notch必须使用完全相同的患者、图片ID、split、随机种子、增强和训练参数。自动画中画
检测只生成候选，正式Notch必须基于人工确认框。两种manifest均从同一冻结母清单派生，
禁止扫描输出目录组装数据。

PIP处理必须按以下顺序完成：

1. 在按中心、标签和画面风格抽取的验证子集上人工标记`pip/non_pip`；
2. 仅使用该验证子集校准自动检测阈值，冻结阈值后才运行全量检测；
3. 人工复核全部高置信和灰区候选，生成冻结的`confirmed_pip_labels.csv`；
4. 生成Notch候选并完成框完整性QC，最后才派生Keep/Notch配对manifest。

PIP复核是M0前的设备界面任务，病灶bbox是基于冻结Keep图的临床标注任务。两者为独立任务，
但在Keep几何冻结后可以并行，不需要等M0选定Keep/Notch。项目管理中必须分开任务包、字段说明和
交付时间；PIP初标可由工程人员完成，医学生只复核歧义和抽样病例。

M0首轮固定`RandomResizedCrop(scale=(0.85, 1.0), ratio=(0.90, 1.10))`。阶段0的A0/A1/A2
尺度实验与M0的Keep/Notch消融分开；不把scale并入`2种骨干×2种预处理`矩阵。
待M0选定预处理和主骨干后，再在唯一入选组合上对scale做新数据的单变量消融。

2026-08-05已冻结M0全量诊断清单：

- 清单目录：`数据整理记录/图像裁剪/胃早癌概念提取训练集0804_预处理_v1/06_M0全量诊断清单_20260805/`；
- 正式开发集为3348张、1732位患者，图片标签为1678非癌对1670癌，
  患者标签为1286非癌对446癌；
- 排除229张：补充外院216张、2位跨标签患者10张、Keep SHA完全重复副本3张；
- 患者级`source×label`分层固定为train/val/test=`1212/260/260`人，对应
  `2350/497/501`张；
- Keep与Notch使用相同患者split和图片主键，只切换处理图路径；
- 训练工具已改为根据val患者AUC选择epoch和S1/S2，阈值仅在val患者上按
  `Sensitivity>=0.90时Specificity最高`选择；正式候选筛选使用`--defer-test`
  不产生internal test预测。

正式val矩阵已于2026-08-05完成12/12组；internal test和外部test未解锁。
下表的图片级Accuracy/Sensitivity/Specificity使用同一种子在val患者上锁定的
筛查阈值，未为图片级单独调阈值。

| 预处理 | 骨干            | seed | 患者AUC | 患者Acc | 患者Sens | 患者Spec | 图片AUC | 图片Acc | 图片Sens | 图片Spec |    阈值 |
| ------ | --------------- | ---: | ------: | ------: | -------: | -------: | ------: | ------: | -------: | -------: | ------: |
| Keep   | ResNet50        |   42 |  0.8713 |  0.7346 |   0.9254 |   0.6684 |  0.8182 |  0.7404 |   0.8708 |   0.6187 | 0.03251 |
| Keep   | ResNet50        |  202 |  0.8773 |  0.7846 |   0.9104 |   0.7409 |  0.8367 |  0.7666 |   0.8417 |   0.6965 | 0.13807 |
| Keep   | ResNet50        |  503 |  0.8964 |  0.8231 |   0.9104 |   0.7927 |  0.8439 |  0.7666 |   0.7792 |   0.7549 | 0.24956 |
| Notch  | ResNet50        |   42 |  0.8695 |  0.7577 |   0.9104 |   0.7047 |  0.8318 |  0.7465 |   0.8500 |   0.6498 | 0.11316 |
| Notch  | ResNet50        |  202 |  0.8783 |  0.7538 |   0.9104 |   0.6995 |  0.8398 |  0.7646 |   0.8583 |   0.6770 | 0.12858 |
| Notch  | ResNet50        |  503 |  0.8898 |  0.7692 |   0.9104 |   0.7202 |  0.8443 |  0.7545 |   0.8417 |   0.6732 | 0.16293 |
| Keep   | EfficientNet-B0 |   42 |  0.9133 |  0.7808 |   0.9254 |   0.7306 |  0.8567 |  0.7586 |   0.8417 |   0.6809 | 0.20750 |
| Keep   | EfficientNet-B0 |  202 |  0.9089 |  0.8192 |   0.9104 |   0.7876 |  0.8389 |  0.7505 |   0.7542 |   0.7471 | 0.33769 |
| Keep   | EfficientNet-B0 |  503 |  0.9052 |  0.7654 |   0.9104 |   0.7150 |  0.8500 |  0.7726 |   0.8583 |   0.6926 | 0.24868 |
| Notch  | EfficientNet-B0 |   42 |  0.9118 |  0.7846 |   0.9254 |   0.7358 |  0.8594 |  0.7746 |   0.8500 |   0.7043 | 0.21105 |
| Notch  | EfficientNet-B0 |  202 |  0.9084 |  0.8077 |   0.9104 |   0.7720 |  0.8377 |  0.7586 |   0.7917 |   0.7276 | 0.30390 |
| Notch  | EfficientNet-B0 |  503 |  0.9067 |  0.7385 |   0.9254 |   0.6736 |  0.8518 |  0.7626 |   0.8792 |   0.6537 | 0.23170 |

跨种子汇总：

| 预处理 | 骨干            | 患者AUC mean±SD | 患者AUC最差 | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片AUC最差 | 图片Acc | 图片Sens | 图片Spec |
| ------ | --------------- | ---------------- | ----------: | ------: | -------: | -------: | ---------------- | ----------: | ------: | -------: | -------: |
| Keep   | ResNet50        | 0.8817±0.0131   |      0.8713 |  0.7808 |   0.9154 |   0.7340 | 0.8329±0.0133   |      0.8182 |  0.7579 |   0.8306 |   0.6900 |
| Notch  | ResNet50        | 0.8792±0.0102   |      0.8695 |  0.7603 |   0.9104 |   0.7081 | 0.8386±0.0064   |      0.8318 |  0.7552 |   0.8500 |   0.6667 |
| Keep   | EfficientNet-B0 | 0.9091±0.0041   |      0.9052 |  0.7885 |   0.9154 |   0.7444 | 0.8485±0.0090   |      0.8389 |  0.7606 |   0.8181 |   0.7069 |
| Notch  | EfficientNet-B0 | 0.9090±0.0026   |      0.9067 |  0.7769 |   0.9204 |   0.7271 | 0.8496±0.0110   |      0.8377 |  0.7653 |   0.8403 |   0.6952 |

只使用val和预注册门槛得到的阶段判断：

- EfficientNet-B0在Keep/Notch下的患者AUC均值均约0.909，高于ResNet50的
  0.879至0.882；图片AUC均值也更高，且患者AUC标准差更小。因此全量M0
  的主骨干候选锁定为EfficientNet-B0；
- EfficientNet-B0下Notch相对Keep的跨种子患者AUC均值差约-0.0001，
  但Specificity均值下降约0.0173，seed503下降0.0415，超过预注册0.02门槛；
  ResNet50下Notch有两个种子未通过全部患者级门槛。因此全量M0暂选Keep，
  Notch保留为已完成消融，不进入下一轮主训练；
- val中武大省人民为234/260人，第一届/第二届大赛只有11/15人；PIP子组
  仅20人。这些子组可用于提示风险，不足以声明跨来源泛化或Notch去除了shortcut；
- 全量M0证明新数据上的性能不再是旧未裁剪模型约0.99 AUC的异常水平，
  但数据仍存在患者数和来源/风格失衡；来源内平衡M0必须继续，不能直接解锁test或进入M1。

自动汇总位于
`结果/M0全量诊断_0804/正式验证集筛选/自动汇总/`，包含逐运行、跨种子、
非劣门槛、2000次患者整簇bootstrap和全部子组CSV。

#### M0全量模型独立外部评估（2026-08-05）

在查看外部结果前，已根据val暂选Keep与EfficientNet-B0；外部数据不参与模型、阈值、
预处理或种子选择。独立多中心集重新使用当前Keep流程处理，并确认其与M0开发集
SHA256重叠为0、患者标识直接重叠为0。外部集共1329人/1941张，其中非癌1208人/
1399张，癌121人/542张。35张暗部风险图经用户人工确认采用保守扩框+FOV遮罩；另有
4张`secondary_fov_component`提示待人工复核，但不阻塞本轮锁定评估。

| 骨干            | 患者AUC mean±SD         |          患者Acc | 患者Sens |         患者Spec | 图片AUC mean±SD         |          图片Acc | 图片Sens |         图片Spec |
| --------------- | ------------------------ | ---------------: | -------: | ---------------: | ------------------------ | ---------------: | -------: | ---------------: |
| ResNet50        | 0.7158±0.0205           |           0.5661 |   0.8044 |           0.5422 | 0.6885±0.0228           |           0.5932 |   0.7097 |           0.5480 |
| EfficientNet-B0 | **0.7399±0.0075** | **0.6353** |   0.7383 | **0.6250** | **0.7238±0.0079** | **0.6452** |   0.7005 | **0.6238** |

图片级和患者级均沿用每个种子在val患者上锁定的同一筛查阈值，未在外部数据重调。
与内部val相比，EfficientNet-B0患者AUC由0.9091降至0.7399，图片AUC由0.8485降至
0.7238；ResNet50患者AUC由0.8817降至0.7158，图片AUC由0.8329降至0.6885。
说明两模型都有明显域偏移，但EfficientNet-B0的外部排序性能、准确率和跨种子稳定性
仍整体优于ResNet50，支持其继续作为平衡M0的主候选骨干。外部Sensitivity没有维持val
设定的0.90，说明阈值校准也发生漂移；后续不能只追求内部AUC，必须继续做来源内平衡、
来源留出和最终锁定外部评估。

完整结果位于`结果/M0全量诊断_0804/外部多中心完整测试_Keep_v1/`；运行日志为
`结果/M0全量诊断_0804/m0_external_keep_evaluation_20260805.log`。

#### M0来源内1:1.3平衡清单（2026-08-05冻结）

主清单在既有患者split内、按`split×来源`分别构建：保留全部446位合格癌患者，依据癌侧
患者主导`style_group`配额抽取574位非癌患者；完全同风格不足时才在同一来源内选择最近
风格。每位患者只取主导风格中的1至3张确定性代表帧，并让两类每患者平均图片数尽量一致。

| 清单      | 癌患者 | 非癌患者 | 患者非癌:癌 | 癌图片 | 非癌图片 | 图片非癌:癌 | 患者风格TV | 图片风格TV | 最近风格回退 |
| --------- | -----: | -------: | ----------: | -----: | -------: | ----------: | ---------: | ---------: | -----------: |
| 主版1:1.3 |    446 |      574 |       1.287 |    597 |      776 |       1.300 |     0.0192 |     0.0461 |         19人 |
| 敏感性1:1 |    446 |      446 |       1.000 |    623 |      623 |       1.000 |     0.0157 |     0.0289 |         13人 |

主版未精确达到患者1:1.3，是因为第二届早癌大赛部分split内可用非癌患者不足；禁止跨来源
补齐。所有处理图存在，无患者跨split、无相同SHA跨split，每患者最多3张。19位最近风格
回退集中在小样本或缺乏共同风格支持的分层，后续必须同时报告来源分层结果。严格1:1仅在
主版选出最终骨干后作为敏感性分析，不重复完整骨干矩阵。

清单与审计位于
`数据整理记录/图像裁剪/胃早癌概念提取训练集0804_预处理_v1/07_M0来源内平衡清单_20260805/`。
EfficientNet-B0和ResNet50的1+1 epoch小样本冒烟均通过；正式矩阵入口为
`程序/模型训练/正式代码/run_m0_balanced_matrix.sh`，默认物理1号GPU并为每组保存实时日志。

选择只使用val：主指标为患者AUC，并同时检查Sensitivity、Specificity、画中画/无画中画
子组、分中心结果以及Grad-CAM是否集中于黑块。Notch相对Keep的患者AUC非劣容忍范围
已于2026-08-05冻结为`0.005`，Sensitivity和Specificity的允许下降均为`0.02`。
若Notch非劣且来源稳定性与黑块关注审计不差，
优先Notch以减少画中画内容shortcut；否则保留Keep。只有Notch明显失效或形成黑块shortcut
时，返回Keep，不再增加会改变图像尺寸和bbox坐标系的Right分支。

选择同时看val患者AUC、Sensitivity、Specificity、多随机种子稳定性、两个主要来源的
分层表现和训练/验证损失差距。

M0同时训练ResNet50和EfficientNet-B0，但M1-M5不默认把两条完整路线全部重复。主骨干
只根据M0的val结果锁定：

1. 患者级AUC为主要分类指标；
2. Sensitivity、Specificity、两个来源分层表现和多随机种子稳定性必须通过预设门槛；
3. 不根据internal test或外部test选择骨干；
4. 若EfficientNet-B0的患者AUC更高，或在预设非劣容忍范围内与ResNet50基本持平且其他
   指标不差，则优先将EfficientNet-B0作为M1-M5主骨干；
5. 另一模型保留M0分类基线，并在主路线锁定后复现M3或最终入选阶段作为补充对照，不
   重复全部中间实验；
6. 若主骨干在确认bbox、坐标变换和损失实现无误后仍无法学会M1定位，可启动另一骨干
   的M1作为预先记录的回退，而不是为保留既定选择强行继续。

下列M0选择参数已于2026-08-05、任何正式训练日志产生前预注册并冻结：

```text
preprocess_delta_auc_noninferiority
backbone_delta_auc_noninferiority
delta_sensitivity_noninferiority
delta_specificity_noninferiority
Sensitivity/Specificity的val阈值选择策略和最低门槛
精确随机种子列表（42/202/503为候选）
跨种子汇总方式
同一val患者上的配对比较方法
```

`preprocess_delta_auc_noninferiority=0.005`、`backbone_delta_auc_noninferiority=0.005`、
`delta_sensitivity_noninferiority=0.02`、`delta_specificity_noninferiority=0.02`，随机种子为
`42/202/503`。冻结后不得根据M0结果回改容忍范围。

#### M0来源内1:1.3平衡val结果（2026-08-05）

6组正式训练均使用同一冻结清单、Keep预处理和`--defer-test`；checkpoint、患者筛查阈值
和骨干选择只使用val。图片级指标沿用同一模型的val患者阈值，没有另行调阈值。

| 模型            | 种子 | 患者AUC | 患者Acc | 患者Sens | 患者Spec | 图片AUC | 图片Acc | 图片Sens | 图片Spec |
| --------------- | ---: | ------: | ------: | -------: | -------: | ------: | ------: | -------: | -------: |
| EfficientNet-B0 |   42 |  0.8447 |  0.7516 |   0.9254 |   0.6163 |  0.8076 |  0.6950 |   0.8391 |   0.5841 |
| EfficientNet-B0 |  202 |  0.8584 |  0.8039 |   0.9104 |   0.7209 |  0.8262 |  0.7600 |   0.8506 |   0.6903 |
| EfficientNet-B0 |  503 |  0.8539 |  0.7647 |   0.9104 |   0.6512 |  0.8223 |  0.7300 |   0.8736 |   0.6195 |
| ResNet50        |   42 |  0.8174 |  0.6536 |   0.9104 |   0.4535 |  0.7826 |  0.6450 |   0.8621 |   0.4779 |
| ResNet50        |  202 |  0.8356 |  0.6797 |   0.9104 |   0.5000 |  0.8056 |  0.6600 |   0.8851 |   0.4867 |
| ResNet50        |  503 |  0.8289 |  0.6667 |   0.9104 |   0.4767 |  0.7956 |  0.6600 |   0.8966 |   0.4779 |

| 模型            | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片Acc | 图片Sens | 图片Spec |
| --------------- | ---------------- | ------: | -------: | -------: | ---------------- | ------: | -------: | -------: |
| EfficientNet-B0 | 0.8523±0.0070   |  0.7734 |   0.9154 |   0.6628 | 0.8187±0.0098   |  0.7283 |   0.8544 |   0.6313 |
| ResNet50        | 0.8273±0.0092   |  0.6667 |   0.9104 |   0.4767 | 0.7946±0.0115   |  0.6550 |   0.8812 |   0.4808 |

EfficientNet-B0在三个种子的患者与图片AUC均高于同种子ResNet50，患者AUC平均差约
`0.0250`，同时患者Specificity和Accuracy明显更高；配对bootstrap的单种子95%区间因
val样本量有限仍跨0，因此不宣称统计学显著优越。按预注册规则，M1-M5主骨干锁定为
EfficientNet-B0，ResNet50保留为受控对照。该结论只来自val；internal test仍未解锁，
外部多中心评估只用于泛化报告，不反向改变本选择。

自动汇总位于`结果/M0平衡_0804/正式验证集筛选/自动汇总/`，逐组训练日志位于
`结果/M0平衡_0804/正式验证集筛选/logs/`。

#### M0来源内1:1.3平衡模型外部多中心评估（2026-08-05）

val选择完成后，使用各运行已锁定的checkpoint、患者筛查阈值和患者均值聚合，在独立
外部多中心集1329人/1941张上评估一次。图片级仍沿用患者阈值；AUC置信区间进行2000次
患者整簇bootstrap。外部结果不参与主骨干选择或调参。

| 模型            | 种子 | 患者AUC | 患者Acc | 患者Sens | 患者Spec | 图片AUC | 图片Acc | 图片Sens | 图片Spec |
| --------------- | ---: | ------: | ------: | -------: | -------: | ------: | ------: | -------: | -------: |
| EfficientNet-B0 |   42 |  0.6690 |  0.5433 |   0.7521 |   0.5224 |  0.6468 |  0.5708 |   0.6882 |   0.5254 |
| EfficientNet-B0 |  202 |  0.6830 |  0.5914 |   0.6694 |   0.5836 |  0.6508 |  0.5987 |   0.6255 |   0.5883 |
| EfficientNet-B0 |  503 |  0.6901 |  0.5801 |   0.7273 |   0.5654 |  0.6713 |  0.5981 |   0.6956 |   0.5604 |
| ResNet50        |   42 |  0.6672 |  0.4733 |   0.8347 |   0.4371 |  0.6458 |  0.5374 |   0.7749 |   0.4453 |
| ResNet50        |  202 |  0.7044 |  0.4883 |   0.8512 |   0.4520 |  0.6715 |  0.5451 |   0.7583 |   0.4625 |
| ResNet50        |  503 |  0.6766 |  0.4372 |   0.8926 |   0.3916 |  0.6534 |  0.5137 |   0.7970 |   0.4039 |

| 模型            | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec | 图片AUC mean±SD | 图片Acc | 图片Sens | 图片Spec |
| --------------- | ---------------- | ------: | -------: | -------: | ---------------- | ------: | -------: | -------: |
| EfficientNet-B0 | 0.6807±0.0107   |  0.5716 |   0.7163 |   0.5571 | 0.6563±0.0131   |  0.5892 |   0.6697 |   0.5580 |
| ResNet50        | 0.6828±0.0193   |  0.4663 |   0.8595 |   0.4269 | 0.6569±0.0132   |  0.5320 |   0.7768 |   0.4372 |

与此前全量Keep模型相比，平衡训练没有改善该外部集：EfficientNet-B0患者/图片AUC分别
下降约`0.0592/0.0675`，ResNet50下降约`0.0330/0.0316`。两骨干外部AUC基本持平，
但锁定阈值下EfficientNet-B0的Accuracy和Specificity更高，ResNet50的Sensitivity更高。
这说明来源内平衡降低了内部可利用信号，却没有自动消除开发来源到外部多中心的域偏移；
也不能据此反向改选骨干或阈值。M1病灶框监督仍有必要同时做平衡主实验与全量诊断对照，
以检验定位约束能否缩小而不是掩盖域差距。

完整结果位于`结果/M0平衡_0804/外部多中心完整测试_Keep_v1/`，运行日志为
`结果/M0平衡_0804/m0_balanced_external_keep_evaluation_20260805.log`。

#### M0尺度增强A0/A1/A2单变量消融（2026-08-05）

在锁定的EfficientNet-B0 + Keep + 1:1.3平衡清单上，仅改变RandomResizedCrop的
`scale`下界做单变量消融，3种子，`--defer-test`；A0即正式M0运行本身。
A0=(0.85,1.0)、A1=(0.7,1.0)、A2=(0.5,1.0)，ratio固定(0.90,1.10)。

| 配置 | scale      | 患者AUC mean±SD | 患者Acc | 患者Sens | 患者Spec |
| ---- | ---------- | ---------------: | ------: | -------: | -------: |
| A0   | (0.85,1.0) |   0.8523±0.0070 |  0.7734 |   0.9154 |   0.6628 |
| A1   | (0.7,1.0)  |   0.8560±0.0054 |  0.7669 |   0.9104 |   0.6550 |
| A2   | (0.5,1.0)  |   0.8437±0.0062 |  0.7277 |   0.9104 |   0.5853 |

按种子A0=0.8447/0.8584/0.8539，A1=0.8514/0.8636/0.8528，A2=0.8396/0.8391/0.8525。
A2明确更差（AUC低、Spec降至0.585），排除；A1与A0在种子噪声内持平（AUC略高0.0037，
但Acc/Spec略低）。锁定A0=(0.85,1.0)为正式scale：A1增益不构成稳定收益，且更保守的
增强降低后续bbox定位阶段裁掉病灶的风险。结果位于`结果/M0平衡_0804/尺度消融_A1A2/`。

以下M1-M5结构和冻结说明以ResNet50为例。EfficientNet-B0使用等价空间分辨率stage：

| ResNet50阶段 | EfficientNet-B0阶段       | `224x224`输入下输出 |
| ------------ | ------------------------- | --------------------- |
| `layer2`   | `features[3]`，40通道   | `28x28`             |
| `layer3`   | `features[5]`，112通道  | `14x14`             |
| `layer4`   | `features[8]`，1280通道 | `7x7`               |

因此EfficientNet-B0的M1从`features[5]`引出定位头，M2使用
`features[3]/features[5]/features[8]`构建FPN。M1联合微调优先解冻`features[5:]`，
M2联合微调优先解冻`features[3:]`；实际边界仍须结合val稳定性冻结，不能直接照搬
ResNet50的`layer2/3/4`名称。

#### M1：layer3癌图辅助定位

初始化：

```text
Encoder和全局分类头：加载M0最佳权重
Heatmap/Size/Offset定位头：随机初始化
```

```text
完整图 → 共享Encoder
       ├─ layer4 → GAP → 全局分类头
       └─ layer3 14x14 → Heatmap、Size、Offset
```

定位与总损失：

```text
L_loc =
    L_heatmap_focal
  + lambda_size * L_size_smoothL1
  + lambda_offset * L_offset_smoothL1

有效癌bbox：
L_M1 = L_cls + lambda_loc * L_loc

非癌或无有效bbox：
L_M1 = L_cls
```

训练分两步：

1. 冻结Encoder和分类头，只训练随机初始化的定位头；
2. 定位头开始收敛后，解冻`layer3、layer4`，联合训练分类头和定位头；
3. `conv1、layer1、layer2`保持冻结。

`L_size`和`L_offset`只在真实病灶中心位置计算。定位损失按有效bbox数量归一化；
batch内没有有效框时令`L_loc=0`，不得产生除零或NaN。

建议同时保存：

```text
m1_best_classification.pth
m1_best_localization.pth
m1_best_joint.pth
```

2026-08-10在正式M1训练前冻结第一版实现和运行参数：

| 项目            | 冻结值                                                                                   |
| --------------- | ---------------------------------------------------------------------------------------- |
| 主骨干/定位分流 | EfficientNet-B0；`features[5]`的112通道14x14特征                                       |
| 数据角色        | 来源内1:1.3平衡主实验 + 全量诊断对照；均为Keep                                           |
| 随机种子        | 42、202、503；每组加载同数据角色、同种子的M0 val最佳权重                                 |
| batch/worker    | 32 / 4                                                                                   |
| 定位头预热      | 5 epoch，AdamW，LR`1e-3`；仅定位头可训练                                               |
| 联合训练        | 最多20 epoch，LR`1e-4`，weight decay `1e-4`；第一版patience 8，早停修正版patience 12 |
| 解冻范围        | EfficientNet`features[5:] + classifier + localization_head`                            |
| 损失权重        | `lambda_loc=1.0`、`lambda_size=0.1`、`lambda_offset=1.0`                           |
| bbox感知裁剪    | A0 scale`(0.85,1.0)`；病灶中心保留且框面积保留至少80%，失败回退整图                    |
| 联合模型选择    | val患者AUC不低于对应M0 AUC减0.005后，先最大化中心命中率，再比较mean IoU                  |
| test安全        | 默认不评估；正式矩阵不传`--evaluate-test`                                              |

平衡主清单为1373张/1020人（597癌图有框），全量诊断清单为3348张/1732人
（1670癌图有框）；两者癌图bbox覆盖率均为100%，无跨患者SHA重复。统一入口为
`程序/模型训练/正式代码/run_m1_matrix.sh`。为避免未验收即连续跑完六组，该入口要求
显式提供数据范围与种子；先运行`balanced 42`，验收完整训练曲线、分类非劣门槛、定位
指标和叠加图后，再运行剩余矩阵。

2026-08-10完成平衡第一版3种子后，发现第一版早停计数把“尚未通过分类非劣门槛”直接
视为“无联合改善”：即使joint阶段患者AUC仍在上升，也会在第8轮固定停止。seed202的
joint AUC在第5/7/8轮为`0.8480/0.8501/0.8511`（非劣下限`0.8534`），seed503在
第7/8轮为`0.8438/0.8468`（下限`0.8489`），均是在接近门槛且上升时停止。因此登记
M1早停修正版，保持数据、初始化、损失、学习率、20个joint上限和非劣门槛不变，只修改：

1. 非劣门槛仅决定某个epoch能否进入`m1_best_joint.pth`候选；
2. 早停独立监测joint阶段`val_patient_AUC`是否创新高；
3. patience由8提高到12；
4. 三种子从头复跑并使用`_pat12`结果/日志后缀，不覆盖第一版。

这属于根据train/val训练轨迹修正训练控制，不使用internal test或外部test。修正版仍须
三种子统一复跑，不能只延长seed202/503后与第一版seed42混合汇总。

2026-08-10完成`_pat12`平衡主实验3种子。三组都跑满5轮warm-up和20轮joint，
无内部test预测。下表的“joint最高”严格限定为joint阶段，用于判断联合微调本身是否有效：

| 种子 | M0 val患者AUC | 非劣下限 | joint最高AUC（轮次） |  相对M0 | 中心命中率 | mean IoU | 通过门槛的joint轮数 |
| ---: | ------------: | -------: | -------------------: | ------: | ---------: | -------: | ------------------: |
|   42 |        0.8447 |   0.8397 |         0.8487（17） | +0.0040 |     0.9080 |   0.4584 |                   9 |
|  202 |        0.8584 |   0.8534 |         0.8605（13） | +0.0021 |     0.8966 |   0.4724 |                  11 |
|  503 |        0.8539 |   0.8489 |         0.8473（10） | -0.0066 |     0.9080 |   0.4558 |                   0 |
| 均值 |        0.8523 |        - |               0.8521 | -0.0002 |     0.9042 |   0.4622 |                   - |

早停修正结论为“有效且必要”：seed42和202均在旧的第8轮停止点之后通过非劣
门槛，因此第一版的三种子失败结论不再成立。但seed503在20轮内仍未过门槛，所以
不能宣称M1在分类上已稳定非劣。三种子的定位都显著高于固定画面中心基线`0.6437`，
说明当前主要矛盾是联合优化下的分类保真，而不是14x14定位头完全学不会定位。

`m1_best_joint.pth`仍按预注册规则在warm-up与joint的所有非劣候选中，先比中心命中率、
再比mean IoU。因此seed202最终回选warm-up epoch2（中心命中率0.9080），而没有选择
joint epoch13（AUC 0.8605、mean IoU 0.4724，但中心命中率0.8966）。这不是权重损坏，
但会让终端最终摘要掩盖joint阶段的最好分类表现；后续汇报必须同时列出“预注册最佳
联合checkpoint”和“joint阶段最高AUC”，不得只看终端最后一行。

2026-08-10在M1b运行前预注册低学习率对照。M1b只将joint LR从`1e-4`降为
`3e-5`；数据、M0初始权重、种子、warm-up、20轮joint上限、patience 12、损失权重、
解冻范围、非劣容忍`0.005`和checkpoint选择规则全部不变。不因低LR收敛较慢而
临时增加epoch，不读取internal test或外部test。

M1b的主成功判据严格限定为“真joint阶段”，不使用warm-up回退充当成功：

```text
对每个种子：max(val_patient_AUC | stage=joint) >= M0 val患者AUC - 0.005
整体成功：seed42、202、503全部满足（3/3）
```

三个固定下限分别为seed42 `0.8397`、seed202 `0.8534`、seed503 `0.8489`。其中
seed503是主要检验点；seed42/202也必须重新满足，不得只报告seed503。每个种子还须
单独报告：通过非劣线的joint epoch数、joint最高AUC相对M0的差值、合格joint epoch中按
中心命中率/mean IoU选出的定位结果，以及全局`m1_best_joint.pth`最终来自warm-up
还是joint。

低LR可能保住分类却减慢定位适应；因此“3/3真joint非劣，但全局交付checkpoint仍
回选warm-up”不直接判为失败，必须如实报告为分类保真成功、联合定位选择未胜出。
反之，若seed503的joint最高AUC仍低于`0.8489`，则低LR未解决三种子稳定性；不得
用warm-up回退的M0 AUC宣称M1b成功。

代码为M1b增加`JOINT_LR`环境变量（默认`1e-4`，向后兼容），并新增：

- `m1_best_joint_stage_auc.pth`：仅在joint阶段按患者AUC选择；
- `m1_best_joint_stage_eligible.pth`：仅在通过非劣线的joint epoch中按定位规则选择；
- `config.json/joint_stage_selection`：记录真joint最高AUC、合格epoch数、非劣是否通过和
  全局交付checkpoint所属阶段。若无合格joint epoch，第二个checkpoint不生成。

M1b 3种子完成后按预注册判定失败：三组的真joint最高AUC均未通过各自非劣线，
合格joint epoch均为0，全局交付checkpoint均回选warm-up。与原`1e-4 + patience12`的
受控对比如下，所有定位指标均取各种子“真joint最高AUC epoch”的对应值：

| 参数             | seed42 AUC | seed202 AUC | seed503 AUC | 真joint AUC均值 | 中心命中率均值 | mean IoU均值 | 非劣通过 |
| ---------------- | ---------: | ----------: | ----------: | --------------: | -------------: | -----------: | -------: |
| `1e-4 + pat12` |     0.8487 |      0.8605 |      0.8473 |          0.8521 |         0.9042 |       0.4622 |      2/3 |
| `3e-5 + pat12` |     0.8374 |      0.8520 |      0.8436 |          0.8443 |         0.9080 |       0.4492 |      0/3 |
| M1b - 原M1       |    -0.0113 |     -0.0085 |     -0.0036 |         -0.0078 |        +0.0038 |      -0.0130 | -2个种子 |

M1b虽让中心命中率均值小幅上升`0.0038`，但该指标已在约0.90附近饱和；同时患者AUC
和mean IoU均明显下降。因此否决`3e-5`，不再增加epoch或用其跑全量数据。后续全量
诊断锁定较优的`joint_lr=1e-4`、`patience=12`、`joint_epochs=20`，其余参数不变。
该全量运行的目的是诊断更大且更复杂数据下的分类/定位变化，不是继续调参；由于平衡
主实验仍只有2/3真joint非劣，不得据此宣称M1已稳定成功。全量3种子的结果
和日志后缀固定为`_frozen1e4_pat12`，启动时仍显式传入`JOINT_LR=1e-4`和
`EARLY_STOP_PATIENCE=12`，避免依赖隐式默认值。

2026-08-11验收全量诊断3种子。服务器异常发生在三组结果全部落盘之后；每组均有
`config.json`、`training_history.csv`、val预测和完整checkpoint。seed202按预注册早停于
joint epoch13，seed42/503跑满20轮；这些都是正常完成，不是中途缺失。三组均无
internal test文件，因此无需删除或重跑。真joint最高AUC epoch汇总如下：

| 种子 | M0 val患者AUC | 非劣线 | 真joint最高AUC（epoch） |  相对M0 | center-hit | mean IoU | IoU≥0.5 | 合格joint轮数 | 全局交付 |
| ---: | ------------: | -----: | ----------------------: | ------: | ---------: | -------: | -------: | ------------: | -------- |
|   42 |        0.9133 | 0.9083 |            0.9091（20） | -0.0042 |     0.9292 |   0.5252 |   0.6167 |             1 | warm-up  |
|  202 |        0.9089 | 0.9039 |             0.9052（1） | -0.0037 |     0.9125 |   0.4659 |   0.4458 |             1 | warm-up  |
|  503 |        0.9052 | 0.9002 |            0.8944（17） | -0.0107 |     0.9250 |   0.5095 |   0.5708 |             0 | warm-up  |
| 均值 |        0.9091 |      - |                  0.9029 | -0.0062 |     0.9222 |   0.5002 |   0.5444 |             - | -        |

全量结果复现了平衡主实验的2/3非劣模式：seed42/202各只有1个真joint epoch过线，
seed503未过线。相比固定画面中心基线`0.6292`，center-hit达`0.9222`，且mean IoU均值
`0.5002`，说明更大数据下定位能力更清晰；但joint AUC均值仍比M0低`0.0062`，且三个
全局交付checkpoint都回选warm-up。结论锁定为：现有M1定位头可以从冻结M0特征中学到病灶
位置，但让共享Encoder同时承受分类和定位梯度尚不能稳定改善或保持分类性能。

#### M1正式产品：warmup-only定位头

M1的阶段声明由“bbox监督改善共享Encoder”收窄为“定位头可以在冻结M0特征上学到
病灶位置”。正式产品不允许混入任何joint epoch，定义如下：

1. 每个数据角色和种子都从对应M0 val最佳权重重新初始化；
2. EfficientNet-B0 Encoder和分类头全部冻结，只训练`features[5]`后的14x14定位头；
3. 训练5轮，AdamW，LR `1e-3`，数据、bbox增强和定位损失与原M1 warm-up完全一致；
4. 候选epoch必须同时满足`center-hit >= fixed-center-hit + 0.10`且`mean IoU >= 0.40`；
5. 合格epoch中先最大化mean IoU，平手时依次最小化平均中心距离、最大化center-hit；
6. 输出统一命名为`m1_best_warmup_localization.pth`，并在`config.json/warmup_product_selection`
   记录门槛、排序规则和入选epoch；
7. 不运行joint，不读取internal test或外部test。

选择mean IoU作为主指标的原因是center-hit在14x14网格上已早期饱和，且后续M3/M4需要
用预测框生成ROI，框重合质量比“中心是否落在真值框里”更有直接价值。历史六组中有3组的
center-hit最佳和mean IoU最佳epoch不同，因此不复用语义不统一的旧`m1_best_joint.pth`，
而是用同一入口对平衡/全量×3种子各重跑5轮warm-up。这不是重跑全量joint实验。

2026-08-11完成六组统一提取。六组均有`m1_best_warmup_localization.pth`、
`training_history.csv`、`config.json/warmup_product_selection`、val预测和QC图；全部
`test_evaluated=false`。分类路径冻结后，同一次运行的5轮患者AUC保持不变，定位头训练
不会造成分类AUC先下降再恢复。正式入选结果如下：

| 数据角色      | 种子 | 入选epoch |      val患者AUC |      center-hit |        mean IoU |        IoU>=0.3 |        IoU>=0.5 |      病灶覆盖率 |
| ------------- | ---: | --------: | --------------: | --------------: | --------------: | --------------: | --------------: | --------------: |
| 平衡主实验    |   42 |         3 |          0.8447 |          0.9080 |          0.4437 |          0.7586 |          0.4368 |          0.7460 |
| 平衡主实验    |  202 |         2 |          0.8584 |          0.9080 |          0.4466 |          0.8046 |          0.3678 |          0.7512 |
| 平衡主实验    |  503 |         4 |          0.8539 |          0.8851 |          0.4346 |          0.7701 |          0.4023 |          0.7221 |
| 平衡均值+/-SD |    - |         - | 0.8523+/-0.0057 | 0.9004+/-0.0108 | 0.4417+/-0.0051 | 0.7778+/-0.0195 | 0.4023+/-0.0282 | 0.7397+/-0.0127 |
| 全量诊断      |   42 |         5 |          0.9133 |          0.9333 |          0.4777 |          0.8542 |          0.4500 |          0.7801 |
| 全量诊断      |  202 |         4 |          0.9088 |          0.8958 |          0.4541 |          0.8042 |          0.4542 |          0.7495 |
| 全量诊断      |  503 |         5 |          0.9054 |          0.8833 |          0.4550 |          0.7792 |          0.4375 |          0.7338 |
| 全量均值+/-SD |    - |         - | 0.9092+/-0.0032 | 0.9042+/-0.0212 | 0.4623+/-0.0109 | 0.8125+/-0.0312 | 0.4472+/-0.0071 | 0.7545+/-0.0192 |

平衡主实验的最佳epoch为`3/2/4`，均未触及第5轮，且三种子mean IoU标准差仅`0.0051`；
因此当前证据不支持为平衡正式产品增加warm-up轮数。全量诊断的最佳epoch为`5/4/5`，
两组触及预设上限，说明更大数据上的定位头可能尚有少量训练空间。该现象不改变已冻结的
5轮产品；若后续M3/M4确认14x14框精度成为瓶颈，可预先固定规则，对全量3种子补做统一
10轮训练长度敏感性实验。不得只延长结果较差或第5轮较好的单个种子，也不得用延长后的
有利结果替换当前主结果。

M3初始化时加载同数据角色、同种子的`m1_best_warmup_localization.pth`：M0 Encoder和
分类头继续冻结，定位头接收癌图中心高斯和非癌图全零热图监督。这样M3先回答
“定位头能否学会抑制非癌假阳性区域”，不再同时引入分类Encoder退化这个混杂变量。

M1b与pat12各运行中观察到：验证集`center-hit`在warm-up前2-3轮即达到约0.90的饱和
水平，之后仅小幅波动；joint阶段中超过该warm-up峰值的epoch（偶现0.92）均未通过
非劣门槛，过线的joint轮center-hit普遍不超过约0.90。joint阶段的定位增益主要体现在
IoU（均值约0.41升至0.46），而非center-hit。该观察说明：中心命中指标在14x14定位头
上早期饱和，不宜作为延长训练的判据；后续若要提升定位质量，方向是更高分辨率
（如M2的P2 28x28）或size回归改进，而不是增加warm-up轮数。

#### M2：可选FPN辅助定位

M2没有取消，而是随M1结论重构为“冻结Encoder/分类头前提下的高分辨率定位”。
只有当M3/M4表明14x14框精度不足，尤其是小病灶召回或ROI边界受限时才测试M2。

初始化和结构：

```text
Encoder和全局分类头：加载对应M0权重并全程冻结
FPN：随机初始化
新定位头：随机初始化，或仅加载warmup-only中形状兼容的输出层

layer2 28x28
layer3 14x14
layer4 7x7
      ↓ FPN
P2 28x28
      ↓
Heatmap、Size、Offset
```

由于Encoder和分类头冻结，优化目标只需定位损失：

```text
L_M2 = I_valid_box * L_loc
```

全程只训练FPN和定位头，不解冻Encoder。M1和M2根据val比较mean IoU、小病灶召回、
中心距离、病灶覆盖率、center-hit和推理成本；分类输出应与M0完全一致，只作为实现
不变性检查。M2没有达到预先冻结的定位最小改善幅度时回到M1 warmup-only产品。

进入M3前采用分层规则选择M1或M2，不使用量纲不清的临时加权总分：

1. 先确认M1/M2的Encoder和分类头checksum与对应M0完全一致，val分类概率只允许
   浮点精度差异；
2. 在分类不变前提下，优先比较mean IoU、小病灶召回率和平均中心距离；
3. 再以病灶覆盖率、center-hit、中/大病灶召回率和跨来源稳定性作次级判断；
4. 若M2未达到预设最小有效提升，选择结构更简单的M1；
5. 定位最小有效提升幅度必须在M2正式训练前冻结。

#### M3：加入非癌负监督

M3加载同数据角色、同种子的M1 warmup-only产品，或在后续验证胜出的frozen-encoder
M2产品。Encoder和分类头继续冻结，只为定位头/FPN重新创建优化器和调度器。

```text
癌图Heatmap目标：病灶中心高斯
非癌Heatmap目标：全零
Size/Offset目标：只在有效癌bbox上存在
```

损失（正负分离、独立归一化，避免batch癌非癌比例漂移损失尺度）：

```text
L_heatmap_pos = 癌图CenterNet focal loss（对每张癌图在14x14空间求和，按癌图真实中心数取平均）
L_heatmap_neg = 非癌图负响应focal loss
                = 对每张非癌图在14x14空间求和 -log(1-p)*p^2，再对非癌图片取平均；
                  无非癌图时为可微的0

L_M3 = L_heatmap_pos + w_neg(epoch) * L_heatmap_neg
     + lambda_size * L_size + lambda_offset * L_offset
```

训练初期将非癌Heatmap损失权重从0逐渐增加到目标值，避免定位头被大量负位置
直接压成全零。采用M1结构时只训练定位头；采用M2结构时只训练FPN和定位头。
分类损失和分类指标只作为不变性检查，不参与梯度更新。

M3锁定后提供的输出包括全局特征、全局分类概率、Heatmap、Size、Offset、预测bbox和定位置信
分数。内部最高响应候选区域始终存在，但只有定位置信分数超过val锁定阈值时才作为正式
预测框展示。分类为非癌但定位高响应的样本应保留为分歧病例，不强制删除预测框。

#### M3预注册协议（修订版2026-08-11，运行前冻结）

口径声明：M3是“癌相关可疑区域定位器”，不是通用病灶检测器。非癌全零监督会同时要求
抑制良性隆起、炎症等非癌区域；当前结构每图只输出最高响应框，不做多病灶检测。

```text
输入：平衡主实验（先）与全量诊断（后）各×3种子，与M1同一冻结清单与患者split
初始化：同数据角色、同种子的 m1_best_warmup_localization.pth；M1初始状态记为epoch 0基线
冻结：EfficientNet-B0 Encoder（features全部）与全局分类头，全程eval模式（BN用running统计）
可训练：仅 features[5] 后的 14x14 定位头
全程 --defer-test；不读取internal test或外部test
```

冻结的程序化校验（防 model.train() 意外改动冻结层BN）：

```text
训练前后分别保存并比对：
  Encoder参数 checksum、分类头参数 checksum、
  BN running mean / running var checksum
同时记录 M0/M1 与 M3 的 val分类概率最大绝对差（冻结正确时应 <= 1e-6）
任一不一致 → 运行失败，禁止把M3当产品
```

目标与损失（正负分离、独立归一化，避免batch癌非癌比例漂移损失尺度）：

```text
癌图Heatmap目标：病灶中心高斯；非癌图Heatmap目标：全零
Size/Offset：只在有效癌bbox中心

L_heatmap_pos = 癌图 CenterNet focal loss
                = 对每张癌图在14x14空间求和，再按癌图真实中心数取平均
L_heatmap_neg = 非癌图负响应 focal loss
                = 对每张非癌图在14x14空间求和 -log(1-p)*p^2，再对非癌图片取平均

L_M3 = L_heatmap_pos + w_neg(epoch) * L_heatmap_neg
     + lambda_size * L_size + lambda_offset * L_offset
```

实现提示：正负损失都是“每张图的空间总损失”，同一尺度，不随batch癌非癌比例
变化。batch无非癌图时 `L_heatmap_neg` 返回可微的0；无癌图时 `L_heatmap_pos`、
`L_size`、`L_offset` 同样返回0。

分类损失不参与梯度；val分类指标作为不变性检查（应恒等于M0）。

候选冻结参数（与M1定位头一致，减少变量）：

```text
lambda_size = 0.1，lambda_offset = 1.0（pos热图权重固定1.0）
w_neg(epoch) = min(1.0, max(0.0, (epoch - 1) / 4))   # e1→0，e5→1.0，之后恒为1.0
LR = 1e-3，AdamW，weight_decay = 1e-4，batch = 32
epochs = 20
早停：前5轮ramp内不早停；从第6轮起，val主指标（固定M1阈值下的非癌FP率）
  连续patience=8轮未改善才停；只有合格epoch能计入"改善"
```

阈值算法（可执行定义，比较始终用 `>=`）：

```text
固定M1阈值：在M1产品的val癌图所有候选置信度中，
  选择满足 sensitivity >= 0.90 的最高阈值
```

双阈值报告（每个epoch都输出两套）：

```text
a. 固定M1阈值：癌图检测召回率、非癌FP率 —— 判断绝对置信度是否被整体压低
b. M3自身阈值 FPR@Sensitivity>=0.90 —— 判断癌/非癌区分能力是否真的提高
```

模型选择（每role x seed，只由val；三条合格门槛必须同时满足）：

```text
基线 = M1 warmup产品在val上的 mean IoU / center-hit /
       固定M1阈值下的癌图检测召回率 / 非癌FP率

合格门槛：
  1) mean IoU >= 基线IoU - 0.02
  2) center-hit >= 基线cenhit - 0.05
  3) 固定M1阈值下癌图检测召回率 >= 基线癌召回 - 0.02

主选择指标：合格epoch中最小化 固定M1阈值下的非癌FP率
平手依次比：FPR@Sensitivity>=0.90更低 → mean IoU更高 → 非癌最大响应均值更低
```

失败与回退：没有任何epoch同时通过三条门槛时，判定M3失败并回退M1产品，
禁止强行保存一个M3产品。

成功判据（预注册，禁止事后回改）：

```text
每role的3/3种子：入选epoch均通过IoU、center-hit、癌召回三条合格门槛，
  且非癌FP率相对基线绝对下降 >= 0.10
辅助判据（不单独作成功标准）：非癌heatmap最大响应均值相对基线下降 >= 25%
  且绝对下降 >= 0.02
平衡腿通过后才进入全量腿；全量腿判据相同、独立判定
```

分层诊断（val，随产品一起报告）：

```text
- 小/中/大病灶（按bbox面积分桶）的IoU、召回率
- 各来源中心的癌召回与非癌FP
- PIP、画幅、风格子组
- 患者级bootstrap置信区间（癌召回、非癌FP）
否则总体FP下降可能只来自某个容易的来源子组
```

输出与记录：

- checkpoint：`m3_best_localization.pth`（按role x seed独立目录）
- config：冻结参数 + 冻结校验结果 + `m3_selection`
  （门槛、入选epoch、双阈值两套指标、基线vs入选、分层诊断表）
- 每图heatmap最大响应与预测框QC、非癌高响应分歧病例清单（分类非癌但定位高响应）

#### M3 balanced正式结果（2026-08-11）

按冻结协议完成seed42/202/503。seed42有合格epoch且非癌FP绝对下降超过`0.10`；
seed202/503均无任何epoch同时通过mean IoU、center-hit和固定阈值癌召回三条门槛，
训练脚本按设计抛出失败并未保存M3 checkpoint。balanced结果为`1/3`，未达到预注册
`3/3`成功标准，因此full腿不运行，M3不能替代M1正式产品。

| 种子 | M1基线 IoU / cenhit / 癌召回 / 非癌FP | M3入选      | 入选 IoU / cenhit / 癌召回 / 非癌FP | seed成功 |
| ---: | ------------------------------------- | ----------- | ----------------------------------- | -------- |
|   42 | 0.4437 / 0.9080 / 0.9080 / 0.8142     | epoch 11    | 0.4334 / 0.8851 / 0.8966 / 0.5398   | 是       |
|  202 | 0.4466 / 0.9080 / 0.9080 / 0.8053     | 无合格epoch | -                                   | 否       |
|  503 | 0.4346 / 0.8851 / 0.9080 / 0.8053     | 无合格epoch | -                                   | 否       |

失败模式跨seed一致：非癌负监督可以明显压低FP，但多数epoch同时把癌图定位置信度
压到固定阈值以下；几何IoU在部分epoch仍可保持，主要矛盾是“空间位置”和“病灶存在性”
共用同一Heatmap输出后的置信度拉扯，而不是分类Encoder或BN漂移。三组冻结校验的分类
概率差均为0，internal test未评估。

仅使用既有M1 val预测做后验诊断（不计入M3正式结果）发现：若直接用冻结M0分类概率
按图像级癌召回`>=0.90`锁阈值，再决定是否展示原M1预测框，三个种子的癌召回均为
`0.9080`，非癌FP分别为`0.5752/0.4956/0.4867`；相比M1定位置信度门控的
`0.8142/0.8053/0.8053`更低且不改变框几何。下一候选应优先验证“冻结M1框 + 独立
病灶存在性门控”，而不是直接继续增大或简单重调Heatmap负损失。该候选必须另行
预注册；在此之前不得把它写成正式结果。

由于现有M4草稿假定M3已锁定，M3失败后M4不能原样启动。需先决定门控来源并修订M4
初始化与困难负ROI定义；M1继续作为当前正式定位产品。

#### M3b：温和且稀疏的非癌抑制（预注册，2026-08-11）

M3 balanced仅1/3种子成功后，对唯一成功的seed42做了一次事后外部多中心投影。该结果
只用于定位失败模式，不参与M3b模型选择：固定内部val阈值`0.2300`时，图像级非癌FP
由`0.7055`降至`0.4510`，癌召回由`0.8487`降至`0.7454`；患者级非癌FP由
`0.7326`降至`0.4694`，癌召回由`0.9339`降至`0.9091`。高分辨率癌图召回由
`0.7872`降至`0.4681`，提示现有负监督可能同时造成癌侧Heatmap整体降温。外部清单中
`legacy_crop_reused`又与标签完全重合，因此这次投影只能说明“外部非癌响应被明显压低”，
不能证明已经消除设备或画面风格偏倚。

从本节冻结起，M3b-A/B的epoch选择、早停、A/B取舍和3种子验收严格只使用内部val；
运行期间不再读取、汇总或肉眼比较外部集。上述外部数字作为“已查看的诊断记录”归档，
不得成为M3b调参或验收指标。

实现采用现有`efficientnet_m3_suppression.py`的向后兼容参数，不覆盖原M3：

```text
--w-neg-max：默认1.0，原M3值
--neg-topk：默认0，表示14x14共196个位置全部求和；传5时只对最高5个响应求和
--w-neg-ramp-epochs：默认5，原M3值
--max-cancer-detection-loss：默认-1，沿用原召回率容差；M3b传1
--resolution-gate-min-cancer：默认0，关闭分辨率门槛；M3b传15
--resolution-gate-max-detection-loss：默认1
```

M3b继续使用每图空间“求和”而非均值。`neg_topk=5`会同时改变负损失的作用区域和
原始量级，因此M3b-B不是严格的单变量损失形态消融；它回答的是“温和权重叠加稀疏
困难负响应约束”整体是否更稳定。训练日志逐epoch记录：

```text
heatmap_pos
heatmap_neg_raw
w_neg
heatmap_neg_weighted = w_neg * heatmap_neg_raw
size
offset
total = heatmap_pos + heatmap_neg_weighted + 0.1*size + 1.0*offset
```

冻结实验：

| 实验  | `w_neg_max` |     `neg_topk` | ramp | 其余参数 |
| ----- | ------------: | ---------------: | ---: | -------- |
| M3b-A |           0.5 |      0（全网格） | 10轮 | 与M3相同 |
| M3b-B |           0.5 | 5（最高5点求和） | 10轮 | 与M3相同 |

癌侧保护门槛全部使用固定M1阈值，并与同role、同seed的M1基线逐一比较：

1. 总体val癌图检出数最多减少1张；balanced val共87张癌图，即最低允许M1检出数减1，
   不再使用模糊的`0.02`召回率容差；
2. 分辨率使用清单既有`size_group`并冻结合并为：
   `large = large_1025_1600`，
   `non_large = medium_641_1024 + small_le640`；
3. 仅癌图数不少于15的分组执行门槛，每个有效组最多比M1少检1张；
4. balanced val两组分别为`large=72`、`non_large=15`；full val分别为
   `large=204`、`non_large=36`，均可复现；
5. mean IoU仍须不低于M1基线`0.02`，center-hit仍须不低于M1基线`0.05`；
6. 非癌FP相对M1基线继续要求绝对下降至少`0.10`，不因M3b重新设基线或放宽。

执行顺序固定为：先跑M3b-A balanced 3种子并完成汇总，再跑M3b-B balanced 3种子。
每个变体只有3/3种子同时通过总体癌计数、两个有效分辨率组、IoU、center-hit和FP下降
门槛时才判成功。A/B均失败则回退M1；只有一个成功则选择成功者；二者均成功时先比较
3种子的非癌FP下降稳定性，再比较癌检出、分辨率子组、mean IoU和损失贡献，不读取
external或internal test。显式癌响应保护损失暂不加入；仅当M3b-B仍稳定压低癌响应时，
再另行预注册M3c。

矩阵执行器采用逐seed容错：单个训练因无合格epoch返回非零时，保留该目录和日志并继续
后续seed；全部指定seed结束后统一列出完成与失败组，并以非零状态提示矩阵不完整。
续跑同一命令时，有完整`config.json`的组自动跳过，已有目录但无config的已知失败组
只登记不覆盖，缺失组继续运行。该机制只防止计算队列被单组失败中断，不改变3/3成功
门槛；balanced未达3/3时full腿仍必须拒绝启动。

2026-08-11完成M3b-A balanced 3种子。seed42和503均有通过全部合格门槛且FP绝对下降
不少于`0.10`的产品，seed202无合格epoch，结果为`2/3`，未达到预注册成功标准，禁止
进入full腿。与原M3的`1/3`相比，`w_neg_max=0.5 + ramp=10 + 严格癌侧计数门槛`
改善了跨种子稳定性，但尚未完全解决癌侧置信度与几何定位的拉扯。

| 种子 | M1基线 IoU / cenhit / 癌检出 / 非癌FP | M3b-A入选   | 入选 IoU / cenhit / 癌检出 / 非癌FP | FP绝对下降 | seed成功 |
| ---: | ------------------------------------- | ----------- | ----------------------------------- | ---------: | -------- |
|   42 | 0.4437 / 0.9080 / 79/87 / 0.8142      | epoch 11    | 0.4249 / 0.8736 / 81/87 / 0.6903    |     0.1239 | 是       |
|  202 | 0.4466 / 0.9080 / 79/87 / 0.8053      | 无合格epoch | -                                   |          - | 否       |
|  503 | 0.4346 / 0.8851 / 79/87 / 0.8053      | epoch 9     | 0.4359 / 0.8736 / 78/87 / 0.6814    |     0.1239 | 是       |

分辨率癌侧门槛也按预注册通过：seed42的`large/non_large`检出由`66/13`变为
`67/14`；seed503由`66/13`变为`66/12`，后者恰好达到non_large最多减少1张的下限。
seed202最接近的epoch9虽有IoU`0.4458`、center-hit`0.8736`、总体癌检出`78/87`和
非癌FP`0.6195`，但分辨率门槛失败，说明总体仅少检1张掩盖了小样本分辨率组内更集中
的癌检出损失；该epoch按预注册拒绝是正确行为。epoch1和18能通过总体及分辨率癌计数，
但分别因IoU或IoU+center-hit不合格而被拒绝。

两个成功seed的辅助“非癌最大响应均值下降”均未通过：seed42相对下降`9.2%`
（绝对`0.0284`），seed503仅下降`1.3%`（绝对`0.0038`）。因此固定阈值FP改善主要
表现为一批样本跨过既定阈值，尚不能解释为整体现象充分分离。M3b-A结论冻结为：
比原M3更温和、更稳定，但仍失败；下一步按预注册只运行M3b-B balanced 3种子，测试
top-5困难负响应约束能否让seed202在保护non_large癌图的同时降低非癌FP。期间继续
禁止查看external和internal test。

2026-08-11完成M3b-B balanced 3种子。seed503通过全部门槛；seed42存在合格epoch，
但入选epoch的非癌FP仅下降`0.0796`，未达到`0.10`；seed202仍无任何合格epoch。
M3b-B结果为`1/3`，预注册判定失败，不运行full腿。

| 种子 | M1基线 IoU / cenhit / 癌检出 / 非癌FP | M3b-B入选   | 入选 IoU / cenhit / 癌检出 / 非癌FP | FP绝对下降 | seed成功 |
| ---: | ------------------------------------- | ----------- | ----------------------------------- | ---------: | -------- |
|   42 | 0.4437 / 0.9080 / 79/87 / 0.8142      | epoch 6     | 0.4250 / 0.8736 / 79/87 / 0.7345    |     0.0796 | 否       |
|  202 | 0.4466 / 0.9080 / 79/87 / 0.8053      | 无合格epoch | -                                   |          - | 否       |
|  503 | 0.4346 / 0.8851 / 79/87 / 0.8053      | epoch 16    | 0.4265 / 0.8391 / 79/87 / 0.6991    |     0.1062 | 是       |

seed42表明top-5在癌侧合格epoch上可能抑制不足，但不能用`5/196`直接推断负损失弱
40倍：同epoch日志中M3b-B的`neg_raw`约为M3b-A的55%-67%，因为top-5选择的是损失
最大的响应点。seed42在epoch9可把FP降至`0.6460`，但癌检出降为`77/87`且分辨率
门槛失败，说明top-5并非没有抑制能力，而是仍未找到稳定的癌/非癌平衡点。

seed202在M3b-A和B的最接近epoch均为epoch9：A为IoU`0.4458`、癌检出`78/87`、
FP`0.6195`；B为IoU`0.4409`、癌检出`78/87`、FP`0.6726`。两者都因分辨率门槛
失败，说明top-5没有解决non_large癌图的集中损失。seed503上A的IoU`0.4359`、FP
`0.6814`也优于B的IoU`0.4265`、FP`0.6991`。

三套配方最终稳定性为：原M3 `1/3`、M3b-A `2/3`、M3b-B `1/3`。M3b-A是当前
最平衡的诊断配方，但仍未达到正式成功标准，不能替代M1。按预注册回退M1 warmup-only
定位产品，并停止直接通过增大`w_neg`或继续扫描top-k来优化同一个Heatmap输出。
若继续M3路线，应另行预注册“冻结M1框几何 + 独立病灶存在性门控”，把“在哪里”和
“是否存在癌相关区域”拆成两个输出；不得查看external或internal test来选择该方案。

#### M3c：冻结M1框 + 独立病灶存在性门控（预注册，2026-08-11）

M3c把“病灶在哪里”（冻结M1定位头）与“该热点是否值得展示”（独立存在性门控）拆成
两个输出，避免共享Heatmap在抑制非癌时压掉癌图置信度。首选零训练候选：用冻结M0
分类器概率作为门控信号，只决定是否展示M1预测框，不修改框几何。

后验诊断（不计入正式结果，2026-08-11）已用冻结M0分类概率按图像级癌召回>=0.90
锁阈值做过一次：三个种子癌召回均为`0.9080`，非癌FP为`0.5752/0.4956/0.4867`，
均低于M1定位置信门控的`0.8142/0.8053/0.8053`。本预注册将该候选形式化为正式测试；
诊断仅作可行性证据，不作为M3c的验收结果。

输入与数据（零训练，纯后处理）：

```text
平衡腿（先）x3种子（42/202/503），与M1同一冻结清单与患者split
模型：冻结M1 warmup-only产品（框+定位置信）与冻结M0分类器（图像级癌概率）
全程不训练；全程--defer-test；不读取internal test或external
```

门控决策（可执行定义）：

```text
对每张图：M1给出预测框与定位置信度（几何固定），M0分类器给出癌概率p_cls
门控阈值t_gate：在val癌图的p_cls上，取满足图像级癌召回>=0.90的最高阈值
  （复用lock_recall_threshold，比较用>=）
展示该框当且仅当 p_cls >= t_gate；不展示的框计为“未检出”
```

指标（val，图像级+患者级，均对比M1定位置信门控基线）：

```text
门控癌召回 = 癌图中被展示框的比例（按阈值构造约等于0.90）
门控非癌FP = 非癌图中被展示框的比例（基线约0.81）
large/non_large分组的门控癌召回与非癌FP
患者级聚合与bootstrap CI
展示框的IoU/cenhit作为“几何不变”的核对报告
```

成功判据（预注册，禁止事后回改）：

```text
操作点约束（由阈值规则保证，不构成独立证据）：
  总体门控癌召回 >= 0.90 —— t_gate就是在同一val癌图上按召回>=0.90锁定的最高阈值

真正验收（3/3种子全部通过才接受）：
1. 非癌FP下降：FP_drop = M1定位置信门控FP - M3c门控FP >= 0.10
   对应逐seed上限：seed42 <= 0.7142、seed202 <= 0.7053、seed503 <= 0.7053
   （≤0.71仅作统一展示口径；逐seed的FP_drop>=0.10为精确核验）
2. 分组召回：large/non_large（各自癌图数>=15）的召回均 >= 总体召回 - 0.05
   （离散计数示例：总体79/87=0.9080，下限0.8580 -> large>=62/72、non_large>=13/15；
    non_large仅15张，1张=6.67个百分点，故实际要求较严）
3. 3/3种子通过才接受；平衡腿通过后才考虑全量腿
```

候选扩展（不并入本预注册，不得用本测试结果反向调参）：

- 若M3c-A（分类器门控）达标但需进一步压FP，另预注册M3c-B：训练独立区域门控头，
  输入为冻结features[5]在预测框处的特征 + 热图峰值 + 框尺寸，仍冻结M1定位头。
- 若展示框在癌图上出现低定位置信却p_cls高的情况，可将“定位置信下限”作为后续
  细化候选，单独预注册。

前置性声明（必须如实标注，不得省略）：

M3c-A是对已经查看过总体诊断结果的形式化内部验证，不能称为完全前瞻性的预注册实验。
其价值是冻结规则并生成可复现文件；真正的泛化结论仍需来自尚未查看的internal test或
external，但当前阶段继续保持二者锁定。运行前已依据既有val预测预判：seed42的
non_large组召回约`0.800`，预计低于分组下限`0.858`，故M3c-A预计为2/3；该预判不构成
放宽门槛的理由，若运行结果一致则接受M3c-A为2/3失败，再另行决定是否预注册M3c-B。

输出与记录：

- 不产生新checkpoint（冻结模型 + 阈值）；config记录t_gate、门控癌召回/非癌FP、
  分组与患者级指标、展示框IoU
- 每图p_cls、门控决策、预测框；QC含“癌图被展示/被拒绝”与“非癌被展示/被拒绝”样例

#### M3c-A正式结果（2026-08-11）

使用冻结M1 warmup-only产品已保存的val预测完成零训练门控；没有重新前向推理，没有读取
internal test或external，也没有生成新checkpoint。三组预测框坐标前后最大绝对差均为0，
说明M3c-A只决定是否展示框，未改变M1框几何。正式结果为`2/3`，与运行前记录的预判
一致，未达到预注册3/3成功标准，因此M3c-A整体失败，不进入full腿。

| 种子 | `t_gate` |      图像癌检出 | 图像非癌FP（M1基线） | FP绝对下降 |       large召回 |   non_large召回 | seed成功 |
| ---: | ---------: | --------------: | -------------------: | ---------: | --------------: | --------------: | -------- |
|   42 |     0.1566 | 79/87（0.9080） |     0.5752（0.8142） |     0.2389 | 67/72（0.9306） | 12/15（0.8000） | 否       |
|  202 |     0.1442 | 79/87（0.9080） |     0.4956（0.8053） |     0.3097 | 65/72（0.9028） | 14/15（0.9333） | 是       |
|  503 |     0.2234 | 79/87（0.9080） |     0.4867（0.8053） |     0.3186 | 66/72（0.9167） | 13/15（0.8667） | 是       |

三个种子的总体癌召回均按操作点固定为`0.9080`，非癌FP均下降远超`0.10`；M3c-A确实
能比定位置信度更有效地过滤非癌框。失败只来自seed42的non_large召回`0.8000`低于
预注册下限`0.8580`，不得用总体FP改善掩盖该集中损失，也不事后放宽门槛。

患者级任一图展示框的结果同样显示明显FP下降：

| 种子 | 患者癌召回（M1基线） | 患者非癌FP（M1基线） | 门控非癌FP 95% CI |
| ---: | -------------------: | -------------------: | ----------------: |
|   42 |     0.9403（0.9403） |     0.6047（0.8488） |     0.5000-0.7093 |
|  202 |     0.9403（0.9552） |     0.5349（0.8605） |     0.4302-0.6395 |
|  503 |     0.9552（0.9403） |     0.5116（0.8372） |     0.4186-0.6163 |

M3c-A结论冻结为：分类概率是有价值的框展示门控信号，并且不损伤框几何；但seed42暴露
了低分辨率癌图的集中漏检，故不能作为正式3种子稳定产品。正式定位产品继续回退M1。
若继续，应先决定是否值得预注册真正独立训练的M3c-B区域门控头；M3c-A结果不得用于
修改现有阈值或分组标准，internal test与external继续锁定。

M3c-A分组门槛的补充敏感性解释（不修改原正式结论）：non_large仅15张，1张对应
`6.67`个百分点。seed42中M1定位置信门控检出`13/15`，M3c-A检出`12/15`，相对M1
只多漏1张；同时large由`66/72`变为`67/72`，总体仍为`79/87`。因此M3c-A的2/3
正式失败结论保持不变，但“每组相对M1最多多漏1张”的计数口径可作为小样本敏感性
分析。该口径在M3/M3b中已经使用，M3c-B在训练前正式采用，避免继续用15张小组上的
固定5个百分点造成不连续判定。

#### M3c-B：冻结M1框的独立区域门控头（预注册，2026-08-11）

目的：继续把“在哪里”和“是否值得展示”解耦。冻结M1 Encoder、分类头、定位头和
预测框，只训练一个很小的区域二分类头。M3c-B不能移动、缩放或重新生成框，只输出
`q_region`，决定冻结M1框是否展示。

结构：

```text
输入图 -> 冻结EfficientNet-B0 features[5] -> F [112,14,14]
同一冻结M1定位头 -> Top-1预测框 b、定位峰值 p_loc、框宽高(w,h)
F与b -> ROIAlign(3x3) -> 空间均值 -> 112维区域特征
[区域特征, p_loc, w, h] -> Linear(115->64) -> SiLU -> Dropout(0.2)
                              -> Linear(64->1) -> 区域存在性logit z
q_region = sigmoid(z)
```

第一版不输入全局分类概率`p_cls`，避免简单复现M3c-A；不输入中心坐标、中心/设备/年份
等元数据，减少位置和来源shortcut。ROIAlign使用冻结M1预测框，而不是部署时不存在的
真值框。

训练样本与监督：

```text
癌图：若冻结M1预测框中心落在增强同步后的真值bbox内，作为区域正例 y=1
      若预测框未命中真值病灶，不计算门控loss；该图仍在val正式验收中计为可能漏检，
      不允许把画错位置的ROI硬标成癌区域
非癌图：冻结M1 Top-1预测框全部作为困难负例 y=0
```

训练集使用患者/标签平衡采样：每个标签期望权重相同，同一标签内每位患者期望权重相同，
患者多图时其单图权重相应降低。Encoder与M1三个头全程`eval()`，BN使用冻结running统计；
区域门控头独立初始化，只训练其参数。

损失：

```text
L_pos = mean softplus(-z_pos)   # 等价于正例BCEWithLogits
L_neg = mean softplus( z_neg)   # 等价于负例BCEWithLogits
L_gate = 0.5 * L_pos + 0.5 * L_neg
```

癌/非癌分别求均值后等权，batch缺某一类时该项返回可微0；不加入分类损失、定位损失、
IoU损失或M3热图负损失。第一版不使用focal、额外margin或蒸馏，避免一次引入多个变量。

冻结训练参数：

```text
balanced主实验 x 3配对seed（42/202/503）
batch=32，AdamW，lr=1e-3，weight_decay=1e-4
epochs=30，early-stop patience=8
internal test与external全程不建立DataLoader、不前向、不调阈值
```

每个epoch只在val锁门控阈值：在val癌图`q_region`中取满足图像级癌召回`>=0.90`的
最高实际阈值，比较始终用`>=`。操作点癌召回由阈值规则保证，不作为独立成功证据。

模型选择与成功门槛：

```text
M1基线：同seed冻结M1定位置信度在val癌图召回>=0.90时的最高阈值及对应非癌FP
总体：M3c-B非癌FP相对M1基线绝对下降>=0.10
分组：large/non_large中癌图数>=15的组，M3c-B检出数 >= M1基线检出数-1
几何：M1框坐标最大绝对差必须为0；Encoder/分类头/定位头参数和BN checksum不变
主选择：通过总体与分组门槛的epoch中最小化非癌FP
平手：患者级非癌FP更低 -> 区域门控AUC更高
单seed没有合格epoch则失败；balanced 3/3种子成功才接受，不运行full或test兜底
```

输出：`m3c_gate_best.pth`只含区域门控头参数及冻结输入checksum；逐epoch训练损失、
正例命中比例、val阈值/总体/分组/患者指标、逐图门控概率与冻结框、三类QC和config。
M3c-A继续保留2/3正式失败；上述新计数门槛只适用于运行前冻结的M3c-B，不反向改写
M3c-A结果。

M3c-B实现状态（2026-08-11）：新增
`efficientnet_m3c_region_gate.py`和`run_m3c_region_gate_matrix.sh`。入口只建立
train/val Dataset；M1参数、BN running统计、分类概率和框坐标均有冻结校验；
单个seed失败会写入失败config并返回非零，矩阵启动器会保留现场并继续其他seed。
Python编译、内置回归自测、Shell语法和CPU端到端冒烟已通过；冒烟中冻结M1的
分类概率最大差和框坐标最大差均为`0.00e+00`，且config记录
`test_evaluated=false`、`external_evaluated=false`。下一步仅运行balanced
seed42/202/503，以3/3通过为正式成功，不用full/test/external补救。

M3c-B balanced正式结果（2026-08-11）：3个配对seed全部通过预注册成功门槛，
因此M3c-B可作为当前正式的“冻结M1框+独立ROI展示门控”方案。

| seed | 入选epoch | 门控阈值 | 区域AUC |          癌检出 |         M1非癌FP |      M3c-B非癌FP | FP绝对下降 |      癌患者检出 |      非癌患者FP |
| ---: | --------: | -------: | ------: | --------------: | ---------------: | ---------------: | ---------: | --------------: | --------------: |
|   42 |         3 |   0.3027 |  0.7793 | 79/87（0.9080） | 92/113（0.8142） | 53/113（0.4690） |     0.3451 | 62/67（0.9254） | 40/86（0.4651） |
|  202 |        11 |   0.1196 |  0.7913 | 79/87（0.9080） | 91/113（0.8053） | 66/113（0.5841） |     0.2212 | 63/67（0.9403） | 53/86（0.6163） |
|  503 |         7 |   0.1772 |  0.7740 | 79/87（0.9080） | 91/113（0.8053） | 60/113（0.5310） |     0.2743 | 62/67（0.9254） | 46/86（0.5349） |

跨种子均值+/-样本标准差：区域门控AUC为`0.7815+/-0.0089`；图像级癌召回
`0.9080+/-0.0000`；非癌FP由M1的`0.8083+/-0.0051`降至`0.5280+/-0.0576`，
绝对下降`0.2802+/-0.0622`；患者级癌检出为`0.9303+/-0.0086`，非癌患者FP
为`0.5388+/-0.0757`。三个seed的large均从M1的`66/72`变为`65/72`，
non_large均从`13/15`变为`14/15`，完整通过“每组相对M1最多多漏1张”的
计数门槛。这说明ROI内容本身能区分“值得展示的可疑区域”与非癌假响应，
而且相比直接压制定位热图的M3/M3b更稳定。

三组的Encoder、分类头、定位头和BN checksum均保持不变，分类概率最大差和
框坐标最大差均为`0.00e+00`；checkpoint、config、history、逐图/患者预测、
冻结清单快照与三类QC图完整。`test_evaluated=false`、
`external_evaluated=false`，本阶段结论只适用于内部val模型选择，不表示已证明外部泛化。

#### M4：真值癌ROI的局部上限实验（修订预注册，2026-08-11）

M4检验高质量病灶局部信息能否稳定提高患者级分类AUC：冻结现有分类、定位和门控系统，
癌图使用医生真值ROI，非癌图使用M1预测ROI，只训练独立局部分支和融合头。M4是真值框
辅助下的局部信息上限，不是部署性能。

角色声明（M3c-B的准确角色）：

```text
M4冻结M1全局/定位基座，并冻结M3c-B作为ROI展示门控和困难负样本分层器。
M3c-B不参与全局分类前向，只负责：读取M1预测框、计算q_region、
把非癌ROI标记为hard/easy、在部署流程中决定是否展示框。
```

冻结与新增：

```text
冻结：M1（全局Encoder + 分类头 + 定位头） + M3c-B门控头
新增并训练：局部Encoder（从同seed的M1 checkpoint复制权重；M1 Encoder权重即M0
  Encoder权重，因warmup-only M1未改Encoder；独立副本，不与全局分支共享参数对象）
            局部分类头 + 融合分类头
```

配对输入：

```text
癌图：完整图 + 医生真值bbox裁出的ROI
非癌图：完整图 + M1 Top-1预测ROI
```

非癌ROI分层（第一版只用于分层分析，不改采样权重与损失权重）：

```text
q_region >= 该seed门控阈值  -> hard negative（部署中会真展示的假阳性）
q_region <  该seed门控阈值  -> easy negative（已被门控拒绝）
所有非癌ROI一律以 roi_label=0 等权参与训练
后续消融 M4-A(等权) vs M4-B(提高hard采样权重)，第一版只运行M4-A
```

冻结ROI清单（每个seed训练前离线生成，禁止训练时随机生成）：

```text
图像路径；patient_id；split；label
ROI来源：gt / predicted / fallback
三套坐标（均存原图归一化坐标，分开保存，不混用）：
  m1_pred_bbox：M1原始预测框
  roi_base_bbox：癌图GT框 / 非癌图M1框 / 回退框（局部分支输入依据）
  roi_crop_bbox：扩边0.20并正方形化后的最终裁剪框（可复现裁图）
q_region；hard/easy；门控阈值
M1与M3c-B checkpoint SHA256；图像SHA256
```

- M1与M3c-B推理使用确定性评估变换，不使用训练随机增强；
- **当前只为train/val生成M1与M3c-B结果**；internal test/external不读取图像、不生成
  预测框、不计算q_region；
- 归一化预测框映射回正式处理输入图后，按`m=0.20`裁高分辨率ROI；
- 之后只对已裁出的ROI做颜色/翻转/压缩等增强，每个epoch的ROI来源固定不变。

ROI协议（正式冻结，M4与M5复用）：

```text
扩边比例 m = 0.20
输出 = 224x224
先扩边，再调整为正方形，尽量平移回原图有效边界内
不补黑边；插值 = bilinear
```

选择`m=0.20`是因为早癌诊断可能依赖病灶周围黏膜，且医生矩形框不一定精确覆盖完整
边界。第一版直接固定，`m=0.10`仅作M4失败且怀疑上下文过多时的消融，不并行选参。
回退框尺寸 = 对应seed train癌图中、M1框命中病灶的预测框宽高中位数；只用train计算
并冻结进manifest/config，不从val/test统计，也不运行时临时决定。
存在良性病变框时才统计"与良性病变重叠"（当前非癌数据无良性病变bbox，仅在有该字段
时执行）。

损失与融合：

```text
L_final = CE(fusion_logits, image_label)
L_local = CE(local_logits[valid_roi], roi_label[valid_roi])
L_M4 = L_final + 0.5 * L_local

融合：concat(冻结全局特征 feat_global[1280], 局部特征 feat_local[1280])
       -> 融合分类头 Linear(2560->512) -> SiLU -> Dropout(0.2)
       -> Linear(512->2) -> P_final
局部头：feat_local[1280] -> Dropout(0.2) -> Linear(1280, 2) -> local_logits
```

梯度关系（重要）：

```text
L_final  -> 更新融合头和未冻结的局部Encoder（不经过局部分类头）
L_local  -> 更新局部分类头和未冻结的局部Encoder
阶段A中局部Encoder冻结，故 L_final只更新融合头、L_local只更新局部分类头
```

- 全局Encoder与全局分类头冻结，`L_global`只监控不进梯度；
- 局部损失按有效ROI数归一化，batch无有效ROI时`L_local=0`；
- M4第一版每图都有GT/预测/回退ROI，故`valid_roi=True`；`ignore`主要属于后续M5，
  M4不留模糊口径；
- M3c-B的`q_region`和hard/easy标记**不得输入融合分类头**，避免把门控概率当标签捷径。

训练调度（EfficientNet-B0表述）：

```text
阶段A：冻结全局分支、M1定位头、M3c-B和局部Encoder；
       只训练局部分类头 + 融合分类头
阶段B：继续冻结全局分支、M1和M3c-B；
       解冻局部Encoder最后特征阶段 features[7:]，低学习率微调
阶段C（仅阶段B不足时）：进一步解冻局部Encoder features[6:]，作为单独消融，不自动执行
```

冻结训练参数与checkpoint选择（2026-08-11，正式冻结）：

```text
batch = 32；AdamW；weight_decay = 1e-4
阶段A：5 epoch，两个头的 lr = 1e-3
阶段B：最多20 epoch，features[7:] lr = 3e-5，两个头 lr = 1e-4
early-stop patience = 8（阶段B，按val患者AUC）
随机种子 = seed；采样器 = 患者/标签平衡（沿用M3c-B的WeightedRandomSampler）
checkpoint选择：val患者AUC最高
平手：图像AUC更高 -> val L_final更低
```

元数据审计（必须，拆三档，检测ROI来源差异捷径）：

```text
A. 纯几何：center x/y、宽、高、面积、长宽比 预测癌/非癌
B. q_region-only：门控信号基线（q_region本身是训练出的视觉区域分类信号，约0.78AUC，
   其预测力高是正常现象，不能证明框几何捷径）
C. 几何 + q_region：联合元数据上限

只有 A 的AUC很高，才能说明“真值框与预测框的几何差异可能泄露标签”。
这些元数据不得输入局部分类器，只用于审计报告。
```

冻结校验（沿用M3c-B做法）：

```text
M1参数/BN、M3c-B门控参数、全局分类头 训练前后checksum一致
M1预测框坐标最大差 <= 1e-6；q_region不变
```

成功判据（预注册，禁止事后回改）：

```text
主成功：3/3 seed 的 Δ患者AUC 均 > 0，且三seed平均 Δ患者AUC >= 0.02
其中 Δ患者AUC = M4融合患者AUC - 同seed冻结全局患者AUC
患者聚合复用冻结的 top2_mean（与M0一致），保证患者AUC可比
患者级配对bootstrap：同一次重采样同时计算全局与融合AUC，给出ΔAUC的95%CI

分级解释：
  正式成功：3/3都提高，平均提升 >= 0.02
  强成功：  3/3每个seed都提高 >= 0.02
  提示性收益：3/3都提高，但平均只提升 [0.01, 0.02)
  失败：    存在seed下降，或平均提升不足0.01
```

辅助指标（随产品报告）：

```text
图像级AUC；患者级Sensitivity/Specificity
局部头整体AUC；癌ROI vs hard negative 的局部AUC；癌ROI vs easy negative 的局部AUC
hard/easy分别的准确率
来源、分辨率、病灶大小分层
元数据审计三档（A/B/C）结果
全局与融合预测一致率；融合新增的正确病例与新增错误病例
```

尤其注意：若只在easy negative上表现好、面对hard negative没有区分能力，局部分支的
实际价值有限——hard negative的区分力是判断局部信息价值的关键。

输出：`m4_best.pth`（局部Encoder + 局部分类头 + 融合分类头）、config（全部冻结参数、
冻结校验、选择记录、分层、元数据审计、患者级bootstrap）；`test_evaluated=false`、
`external_evaluated=false`。M5在M4完成后沿用"冻结M1 + M3c-B预测ROI"管线单独修订。

M4实现状态（2026-08-11，三轮审阅修复闭环，可正式放行）：

- `数据整理脚本/build_m4_roi_manifest.py`：冻结ROI清单构建（只train/val确定性推理、
  三套坐标、hard/easy分层、回退框=仅train命中中位数、病灶大小三分位冻结、
  debug与正式隔离、CSV/config同时检查、SHA与阈值同源）。
- `程序/模型训练/正式代码/efficientnet_m4_fusion.py`：M4训练（冻结M1全局分支+局部
  Encoder副本+局部/融合头；患者/标签平衡采样；阶段A/B正确BN冻结；选择键
  患者AUC→图像AUC→val融合损失；阶段B独立patience；bootstrap保留重复权重；
  元数据审计train拟合/val评估；病灶大小/来源/分辨率分层；一致率与新增对错患者；
  产物链SHA校验）。
- `run_m4_roi_manifest_matrix.sh` / `run_m4_fusion_matrix.sh`：三组启动器（幂等验证、
  残缺快速失败、失败汇总非零退出、tee退出码）。
- 全部验证通过：py_compile、bash -n、两个自测、debug单seed冒烟、git diff --check。
- 运行顺序：先ROI清单矩阵42,202,503，再M4训练矩阵；正式成功判据3/3 Δ患者AUC>0
  且平均≥0.02。

M4 balanced正式结果（2026-08-12）：三种子矩阵全部正常结束，达到预注册"正式成功"
判据（3/3均>0且平均≥0.02），未达到"强成功"——seed202精确值0.01996<0.02，仅为命名
边界，不影响正式成功结论。

| seed | 最佳阶段/epoch | 全局患者AUC | 融合患者AUC | Δ患者AUC | Bootstrap 95% CI |
| ---: | -------------: | ----------: | ----------: | -------: | ---------------- |
|   42 |    Stage A / 4 |      0.8436 |      0.8875 |  +0.0439 | [0.0145, 0.0771] |
|  202 |    Stage A / 2 |      0.8554 |      0.8754 |  +0.0200 | [-0.0120, 0.0535] |
|  503 |    Stage A / 2 |      0.8499 |      0.8863 |  +0.0364 | [0.0079, 0.0672] |
| 均值 |                |      0.8496 |      0.8831 |  +0.0334 |                 |

统计稳定性：三个seed方向完全一致；seed42与seed503的配对bootstrap 95%CI完全高于0，
提升较稳定；seed202区间跨0，单seed统计证据较弱。合理表述为：真值病灶ROI与完整图
融合在三个随机种子上均提高患者级AUC，平均绝对提升0.0334；其中两个种子的配对
Bootstrap区间完全高于0。

Stage A/B：三个最佳产品全部来自Stage A（冻结全局Encoder+冻结局部Encoder，只训练
局部分类头与融合分类头）；Stage B解冻局部Encoder最后阶段后无一个seed超过Stage A。
说明已训练EfficientNet-B0特征足以表示病灶ROI，收益主要来自"看到放大的局部区域并
正确融合"而非重新微调Encoder。该结论与下述元数据几何风险并存——几何捷径也可能贡献
部分收益，两者不是二选一。

局部头：局部头整体AUC为0.858/0.867/0.867；癌ROI vs hard negative为0.744/0.796/0.775，
癌ROI vs easy negative为0.959/0.967/0.971。局部分支能很好区分癌ROI与easy非癌ROI，
但对困难假阳性ROI的区分力仅约0.74-0.80，难点仍是局部形态像病灶的非癌区域。

病灶大小（小/中/大三档n=37/30/20图、30/27/19患者，样本量小，仅作诊断信号）：
小病灶融合相对全局的平均概率提升三seed均为正（+0.294/+0.124/+0.171），是本阶段最有
价值的临床信号——完整图缩放后易丢失的小病灶细节可由放大真值ROI补回；中病灶在
seed202/503略降（-0.039/-0.032），大病灶收益较小。中病灶下降需在M5继续观察。

筛查指标（每seed在val上锁"患者召回>=0.90"阈值，spec为同val匹配点、属乐观估计）：
三seed患者Sens均为0.9104，Spec为0.709/0.721/0.767；但阈值差异很大（0.268/0.034/0.161），
概率校准不稳定，不能跨模型/跨数据直接套用阈值；现阶段主要比较AUC，部署阈值需单独校准。

元数据风险：纯ROI几何AUC（A档）三seed均为0.710-0.723，说明癌图医生真值框与非癌图
预测框在位置/面积/长宽比上存在可识别差异；且B档q_region-only为0.77-0.79，
C档几何+q_region为0.81-0.85，表明门控信号与框几何结合后标签预测力更强。
注意C档仍然只是元数据审计，不包含ROI图像纹理。因此M4的提升可能同时包含真正的
病灶局部纹理信息与真值/预测框几何差异带来的捷径。M4的正确定位是：证明高质量局部
病灶信息具有分类价值的理论上限实验，而非可直接部署的最终性能。

分层信号（三种子平均患者AUC）：武大省人民0.890（n=129患者）、第一届早癌大赛0.917
（n=9）、第二届早癌大赛0.762（n=15）。第二届仍是明显弱项；第一届n仅9，0.917不可
过度解读；中分辨率组仅4位患者，AUC只作诊断不作结论。

一致率与净变化：全局与融合在0.5阈值一致率0.902/0.922/0.915，融合新增正确患者
11/9/9、新增错误4/3/4，融合主要靠调概率而非大改决策。

冻结校验与安全边界：三组冻结M1全局分支的参数与BN running统计checksum训练
前后一致（freeze_ok=true），未触发"冻结校验失败"。局部Encoder在Stage B按协议解冻，
不属于freeze_ok的校验对象；但三个最终入选checkpoint均来自局部Encoder未解冻的Stage A。
`test_evaluated=false`、
`external_evaluated=false`，未读取internal test/external，结论只适用于内部val模型选择。

M5修订方向：M4用真值框测理论上限；M5改为癌图与非癌图全部使用冻结M1+M3c-B预测ROI，
测实际部署收益，看+0.0334平均提升能保留多少。注意几何泄露在M5不会自动消失，只是
换成"预测框vs预测框"的分布差异，元数据审计A/B/C应原样搬入M5作为门槛。

#### M5：预测ROI融合分类部署实验（预注册，2026-08-12）

> 取代旧稿"M5：全部使用M3预测ROI"（M3热图抑制已失效）。M5定义为**预测ROI融合分类
> 部署实验**：癌图与非癌图**全部**使用冻结M1 Top-1预测框生成的ROI，不提供真值框；
> M3c-B门控只控制是否向医生展示框，**不控制分类分支**。分类输出回答"患者是否疑似癌"，
> 门控回答"该框是否可靠到足以展示"，两个任务分开。最终系统允许"癌概率较高但定位框
> 未展示"，这不是矛盾。

背景：M4用癌图真值框测局部融合理论上限，正式成功，平均Δ患者AUC=+0.0334。M5去掉
真值框，回答两个问题：①预测ROI融合相对冻结全局还剩多少收益；②几何泄露是否因
"GT vs 预测"变成"预测 vs 预测"而降低。

##### 管线与冻结

```text
所有图像（癌+非癌）：
  冻结M1 Top-1预测框 m1_pred_bbox
  → margin=0.20扩边并正方形化（expand_and_square）
  → roi_crop_bbox 局部图 → 局部Encoder

M3c-B门控（冻结，只控制展示，不屏蔽融合输入）：
  q_region >= threshold → 对医生展示预测框
  q_region <  threshold → 不展示预测框
  无论门控是否通过，分类器都计算局部ROI

只对train/val生成ROI清单；不读internal test/external。
```

##### 初始化（冷启动，与M4完全一致）

```text
全局Encoder：加载对应seed的M1 warmup产品并冻结
局部Encoder：deepcopy对应M1 Encoder（独立参数对象）
局部分类头：随机初始化
融合分类头：随机初始化
```

M4 warm-start不作为第一版（M4头已适应"癌侧真值框、非癌侧预测框"输入分布，直接迁移
会带入M4几何捷径、使M5与M4不再干净对照）；留作后续M5b（M4→M5 domain adaptation）
单独预注册。

##### roi_label匹配规则（基于扩边后roi_crop_bbox，运行前冻结）

M5的局部头学习"该ROI是否含病灶"，不再直接取图像标签。以最终输入局部分支的
roi_crop_bbox与医生GT框计算病灶覆盖率：

```text
lesion_coverage = intersection(crop, GT) / area(GT)

非癌图：                          roi_label = 0
癌图，无有效医生框：              roi_label = ignore
癌图，有有效医生框：
  对所有GT框计算coverage，取最大coverage的GT框
  GT中心位于crop内 且 coverage >= 0.50  → roi_label = 1（明确含病灶）
  coverage = 0（与全部GT均无重叠）      → roi_label = 0（ROI内无病灶）
  其余（部分覆盖/擦边/不确定）          → roi_label = ignore

多病灶图：任一GT满足正例条件→1；全部coverage=0→0；其余→ignore
```

0.50理由：至少包含病灶中心和一半病灶才适合作为明确癌ROI；完整包含小病灶的大ROI
不会被IoU偏低误判；与M1已观察到的约0.75平均病灶覆盖率相容。IoU>=0.3仅作辅助诊断
指标保留，不作为roi_label=1主规则。阈值0.50运行前冻结；若要修改，须留预注册修订
记录后重新生成全部清单，不得事后调节。

##### 损失

```text
L_M5 = L_fusion + beta * L_local        （beta=0.5，同M4）

L_fusion：所有图像计算，真值=图像/患者癌标签
L_local ：只在roi_label=0/1的图像上计算；ignore行不参与局部CE
  valid = roi_labels != IGNORE
  local_loss = CE(local_logits[valid], roi_labels[valid])  （valid空则取0）
```

癌图预测框没拍到病灶时：局部头被监督为非癌ROI，融合头仍被监督为癌——融合头需学会
"局部ROI无病灶证据时仍依赖全局特征完成分类"，这正是合理行为。

##### ROI标签分布验收（正式训练前sanity check，不用于调阈值）

```text
train/val分别报告：
  roi_label=1/0/ignore 的图片数与患者数
  癌图lesion_coverage分布
  癌图GT-center-hit比例
  癌图gate-pass/gate-fail
  roi_label × gate-pass 交叉表
  回退框数量

必要检查：
  roi_label=1的癌图数量不得少到局部头几乎没有正例
  ignore比例若异常高（如超过癌图约50%），先检查匹配坐标是否正确
```

这些只检查实现是否合理，不用于事后调节0.50阈值。

##### 模型与训练

- 架构同M4Model：冻结M1全局分支 + deepcopy局部Encoder + 局部头(1280→2) +
  融合头(2560→512→2)。
- 超参锁定为M4值：stage A 5轮/lr 1e-3、stage B 20轮/lr 1e-4/backbone 3e-5、
  weight_decay 1e-4、beta 0.5、patience 8、bootstrap 2000、batch 32、margin 0.20。
- 保留Stage A/B：预测ROI更噪，局部Encoder微调可能适应不精准框（有益）或进一步
  拟合错误ROI（有害），不预删B。
- 选择键同M4：患者AUC最高 → 图像AUC更高 → val融合损失更低。
- 第一版不解冻定位器（M1/M3c-B全程冻结）。

##### 主终点与成功判据（与M4一致，补齐"提示性收益"档）

```text
Δ患者AUC = M5融合患者AUC − 同seed冻结全局患者AUC
患者聚合top2_mean；配对bootstrap（同一次重采样同时算全局与融合）

正式成功：   3/3 Δ>0 且 平均Δ >= 0.02
强成功：     3/3 每个seed Δ >= 0.02
提示性收益： 3/3 Δ>0 但 平均Δ ∈ [0.01, 0.02)
失败：       存在seed下降，或平均Δ < 0.01
```

##### M4收益存活率（诊断指标，不作判据）

```text
整体存活率 = mean(ΔM5) / mean(ΔM4) = mean(ΔM5) / 0.0334
每seed：ΔM5_seed / ΔM4_seed
患者级配对AUC_M5 − AUC_M4（同一val患者集合bootstrap），回答
"从真值ROI换成预测ROI后性能损失是否显著"
```

以上均不参与M5 checkpoint选择。

##### 元数据审计

```text
A       ：预测框几何（roi_base_bbox，与M4可比）
B       ：q_region-only
C       ：预测框几何 + q_region
A_crop  ：（辅助）最终roi_crop_bbox的中心/面积/长宽比 —— 更贴近局部Encoder
          实际看到的裁图尺度
```

解释数值口径（0.03–0.05是解释区间，不是成败门槛）：

```text
A_M5 <= A_M4 − 0.05   ：几何可预测性明显降低
|A_M5 − A_M4| < 0.03  ：几何风险基本相当
介于其间               ：小幅变化，谨慎描述
```

措辞规则：仅凭A档预测力高**不能证明**融合模型主要使用了几何捷径（几何变量没有直接
输入融合头）。严谨表述为——A高且Δ大时，**无法排除**预测框几何及其造成的裁剪尺度、
上下文差异对融合收益的贡献，因此不能将提升完全归因于病灶纹理。

##### 冻结校验口径

```text
ROI清单生成阶段：M1/M3c-B checkpoint SHA、门控阈值与checkpoint同源、确定性推理、
  ROI CSV SHA、框坐标/q_region重复推理一致性
M5训练阶段：冻结M1全局分支参数与BN running统计训练前后SHA一致（freeze_ok）
  —— M1定位头与M3c-B门控不在M5训练图中，不由训练前后校验，而由ROI产物链SHA保证来源
```

##### 后续（不在第一版，单独预注册）

- M5b：M4→M5 warm-start domain adaptation。
- 联合微调：解冻定位器的独立实验（须重新加入L_loc并在线更新ROI），不并入第一版M5。

##### 实现状态（2026-08-12，可进入验证放行）

- `数据整理脚本/build_m5_roi_manifest.py`：两类都用预测框ROI；roi_label以扩边后
  roi_crop_bbox与GT病灶覆盖率匹配（0/1/2，0.50冻结）；gate_pass/max_lesion_coverage
  字段；sanity验收报告（roi_label计数/coverage分位/GT中心命中/门控交叉/回退框数）；
  debug与正式隔离；CSV/config同时检查；SHA与阈值同源。
- `程序/模型训练/正式代码/efficientnet_m5_fusion.py`：冷启动训练同M4配方；局部CE
  用ignore掩码（全ignore取0）；审计A/B/C同名+新增A_crop；存活率诊断（患者级配对
  AUC_M5−AUC_M4 bootstrap + 单seed Δ比值，读M4产物，缺失则跳过）；冻结校验只表述
  M1全局分支参数/BN不变。
- `run_m5_roi_manifest_matrix.sh` / `run_m5_fusion_matrix.sh`：幂等验证、残缺快速
  失败、分级结论（强成功/正式成功/提示性收益/失败）+整体存活率诊断。
- 验证通过：py_compile、bash -n、构建与训练两个自测、CPU debug冒烟（ROI来源全
  predicted、roi_label/gate_pass/sanity结构正确、GT中心命中1.0）、git diff --check。
- 2026-08-12二次审阅修复：补齐`M5Dataset.eval_transform`（否则正式训练首次
  取全局图会报错）；修正M4/M5患者表合并后`label_m5/label_m4`的存活率统计；
  ROI构建增加M3c-B seed/debug/M1 SHA同源校验，并真正执行框坐标与
  `q_region`重复推理一致性（实测最大差异均为0）。GPU 1上完成单seed小样本
  端到端debug，并用M4 seed42正式153位val患者验证配对存活率统计可运行。
- 2026-08-12正式一致性复核：M3c-B正式评估使用`batch_size=32`，原M4/M5 ROI
  构建器默认`64`。CUDA批大小改变会产生约`6e-4`的`q_region`差异和约`1.3e-4`
  的框坐标差异；seed202恰有阈值边界样本，导致M5清单门控癌召回由M3c-B原始
  `79/87`变为`78/87`。现已将两个构建器默认值和启动器显式参数统一为`32`，并
  强制校验其与M3c-B config中的`batch_size`一致，同时在ROI config记录实际与参照
  批大小。原M5 batch64三种子CSV/config/log已移至
  `结果/M5预测ROI融合_0804/归档/冻结ROI清单_batch64_作废_20260812/`，不得用于训练。
  M4既有正式结果保留为已完成阶段，仅记录其非癌预测ROI可能存在最多1像素的历史
  边界差异；未来重建统一使用32，不据此重跑或改写既有M4结论。
- 数据边界：当前M1冻结清单为1373张唯一图像、每图一个有效医生框；
  `compute_roi_label`已支持多GT列表，但若未来一图提供多个病灶框，需先将多框表示接入
  ROI构建器，不能默认当前单框字段已覆盖多病灶。
- 2026-08-12 M5正式ROI清单已按batch size 32重建并验收放行：三种子均为
  `1175`张（train `975`、val `200`），实际/参照批大小均为`32/32`；三组val癌图
  门控均恢复为`79/87`。seed42/202/503的癌图中心命中率分别为
  `0.9922/0.9883/0.9961`，回退框均为`0`；val癌图局部标签`1/ignore`分别为
  `84/3、86/1、86/1`，无癌图被标为局部阴性。重复推理框坐标及`q_region`最大差异
  均为`0`，CSV SHA一致，`test_evaluated=false`、`external_evaluated=false`。
  结论：ROI产物链验收通过。GPU debug与正式ROI清单矩阵均已完成。
- 正式M5训练矩阵（batch32清单）已跑完，结论见下"M5 balanced正式结果"。

M5 balanced正式结果（2026-08-12）：**正式失败**。

| seed | 全局患者AUC | M5融合患者AUC | Δ患者AUC | 入选 |
| ---: | ----------: | ------------: | -------: | ---- |
|   42 |      0.8436 |        0.8553 |  +0.0116 | Stage A / ep1 |
|  202 |      0.8554 |        0.8442 |  -0.0113 | Stage A / ep1 |
|  503 |      0.8499 |        0.8455 |  -0.0043 | Stage A / ep2 |
| 均值 |      0.8496 |        0.8483 |  -0.0013 |           |

预注册判据：2/3 seed下降 + 平均Δ=-0.0013<0.01 → **正式失败，无回旋**。配对
bootstrap的AUC_M5−AUC_M4 95%CI三种子均完全低于0（[-0.061,-0.004]、
[-0.063,-0.002]、[-0.067,-0.018]），GT→预测ROI的性能损失显著。整体存活率
`mean(ΔM5)/0.0334≈-0.04`，M4的+0.0334平均提升基本不存活。冻结校验全通过，
`test_evaluated=false`、`external_evaluated=false`。

元数据审计：A档（预测框几何）三种子均明显低于M4（0.713→0.574、0.723→0.510、
0.710→0.569，降幅全≥0.05）→ 按预注册口径"几何可预测性明显降低"；且M5中C≈B
（0.78 vs 0.77-0.79），几何对q_region无增量，元数据信号由q_region单独承载。

融合失败定位（免训练诊断，用既有val预测）：**局部信息有真实互补信号，但冷启动的
concat-MLP融合头是瓶颈**，而非局部特征无用。

| seed | 患者AUC 全局 | 局部 | 训练融合头 | 简单均值 | 2特征LR(in-sample) |
| ---: | ----------: | ---: | --------: | -------: | -----------------: |
|   42 |      0.8436 | 0.8320 |    0.8553 |   0.8506 |             0.8501 |
|  202 |      0.8554 | 0.8565 |    0.8442 |   0.8693 |             0.8697 |
|  503 |      0.8499 | 0.8546 |    0.8455 |   0.8594 |             0.8591 |

全局与局部患者概率的**简单均值在三种子全部超过全局**（+0.007/+0.014/+0.010）；
而训练出的2560→512→2拼接融合头在seed202/503**明显差于无参数简单均值甚至全局**。
Spearman(全局,局部)≈0.89-0.91，边际信息小但真实存在。结论：组合本身可行，
失败在"组合器学不出来"——局部头与全局头各自有效，融合环节主动破坏了排序。

阶段决策：M5正式失败 → 按决策树回退到 **"M1全局分类 + M3c-B框展示"** 正式管线；
M5融合不作为正式分类器，只留作诊断工具。"预测ROI局部融合"作为部署路径在
concat-MLP融合方式下被关闭；保留M5b（换融合方式）为下一候选。

#### M5b：残差锚定融合——全局稳定锚点+有界局部修正（预注册，2026-08-12）

动机：M5诊断表明局部头有真实互补信息（患者级AUC 0.83-0.86，hard负例0.71-0.75），
但冷启动concat-MLP融合头破坏排序（seed202/503融合<全局，而简单均值>全局）。M5b
改为**残差锚定**：全局分类器作为稳定锚点，局部信息只做有限、受控的修正。

数据：**复用M5冻结ROI清单**（batch32，已验收），不重新生成；含
`image/roi/label/roi_label/gate_pass/q_region`。不读internal test/external。

架构（数据流）：

```text
原始图 ──→ 全局图224 → 冻结全局Encoder → 全局特征 f_global → 冻结全局头 → m_global(癌−非癌logit差)
原始图 ──→ M1预测ROI裁剪224 → 冻结局部Encoder → 局部特征 f_local → 局部头(阶段L训) → m_local / p_local
similarity = cos(f_global, f_local)
残差输入固定为 [m_global, m_local, similarity]
   → 小型MLP(3→16→1，最后一层init 0) → tanh → Δm∈[-1,1]
m_final = m_global + α × Δm        （α∈[0,1]，训练开始时Δm=0 → m_final=m_global）
p_final = sigmoid(m_final)；损失用 binary_cross_entropy_with_logits(m_final, label)
```

α定义（`--alpha-mode`参数化，A/B共用同一套数据、局部头、残差结构与损失，只换α）：
- **M5b-A（硬门控）**：`α = gate_pass`；
- **M5b-B（门控×局部置信，推荐主方案）**：`α = gate_pass × p_local`（M3c-B先判框是否
  值得展示，局部头再判框内是否有病灶证据，两者共同控制修正幅度）。

第一版残差输入**明确排除**：`roi_area`、`q_region原始值`、框坐标、框宽高——
避免重新引入几何捷径。`roi_area`留作M5b-C/V1.5单独消融。

模块冻结表：

```text
全局Encoder/全局分类头/M1定位头/M3c-B门控头：冻结（M1、M3c由ROI产物链SHA保证来源）
局部Encoder：从全局Encoder deepcopy并冻结
局部分类头：阶段L训练，之后冻结
残差修正MLP：阶段R训练
α：确定性公式（A/B），不训练额外门控头（避免975张训练图上再次过拟合）
```

训练（两阶段）：

```text
阶段L（训局部头）：局部Encoder冻结，只训局部头；训练ROI沿用M5的水平翻转与轻量颜色
  增强，val使用确定性变换；L=CE(z_local, roi_label, ignore=2)；
  按val局部头AUC选checkpoint后冻结局部分类头。
阶段R（训残差头）：全部冻结，预计算特征后只训残差MLP；
  batch=32，沿用患者/标签平衡采样，避免多图患者和多数标签主导残差方向；
  L_M5b = L_final + λ_reg × L_reg
  L_final = BCE_with_logits(m_final, label)
  L_reg = mean((α×Δm)²)        λ_reg = 0.1（冻结，不扫描）
日志分别记录：L_final / L_reg_raw / 0.1×L_reg / mean(|Δm|) / mean(|α×Δm|) / mean(α)
阶段R选择键：val患者AUC最高 → val图像AUC更高 → val BCE更低（不得使用train loss平手）。
```

产品保护与判据（重述版，与M5同门槛）：

```text
候选融合产品 = val患者AUC最高的真实训练epoch（阶段R）
最终产品     = argmax(全局基线, 候选融合产品)      ← 全局回退保护
Δ = 最终产品患者AUC − 全局患者AUC（≥0）

正式成功：   3/3 seed均由真实融合epoch胜出（未回退）且 平均Δ ≥ 0.02
强成功：     3/3 seed各自 Δ ≥ 0.02
提示性收益： 3/3 seed均由真实融合epoch胜出 且 平均Δ ∈ [0.01, 0.02)
失败：       任一seed最终回退全局模型（Δ=0），或 平均Δ < 0.01
```

报告时同时给出"最佳融合checkpoint的原始Δ"（未含回退地板），避免只看到非负数而误判
融合稳定。
若回退：`m5b_best.pth`与`val_*_predictions.csv`必须严格对应全局产品；最佳融合候选另存
`m5b_best_fusion_candidate.pth`和candidate预测，供rawΔ与失败诊断使用。产品与候选的
bootstrap分别报告，不得用候选预测冒充回退后的正式产品。

α通道归因审计（M5b-B必须；A/B都报告）：单独报告以下信号的图像级与患者级AUC：

```text
gate_pass / p_local / α=gate_pass×p_local / similarity / 最终p_final
```

并报告残差实际工作量：α分布、Δm分布、α×Δm分布、融合导致的患者排序变化数、
全局错→融合对、全局对→融合错。归因结论口径：

```text
α的AUC很高 且 α×Δm接近零   → 收益主要来自可靠性门控，不称"内容残差融合成功"
Δm明显工作 且 提高排序      → 支持局部内容残差融合有效
两者共同贡献                → 表述为"门控与内容修正联合获益"
```

similarity单独图像/患者级标签AUC必须报告（余弦虽属内容关系，仍查是否意外携带标签捷径）。
元数据审计A/B/C、局部头vs hard/easy、病灶大小、来源/分辨率分层、修正前后新增对错患者、
M4收益存活率=mean(ΔM5b)/0.0334均继续报告。`test_evaluated=false`、
`external_evaluated=false`。

实现状态（2026-08-12，已实现可验证）：

- `程序/模型训练/正式代码/efficientnet_m5b_fusion.py`：`--alpha-mode {gate,gate_local}`
  共用一套代码（A=α=gate_pass，B=α=gate_pass×p_local）；残差头最后一层init 0；
  阶段L训局部头（CE roi_label ignore=2，按val局部头AUC选）+ 阶段R只训残差MLP
  （BCE_with_logits(m_final,label)+0.1×mean((α×Δm)²)，输入固定[m_global,m_local,
  similarity]）；产品保护=argmax(全局,候选融合)，记录fell_back与rawΔ；α归因审计
  （gate_pass/p_local/α/similarity/p_final图像+患者AUC、α/Δm/α×Δm分布、排序变化、
  对错翻转）；similarity单独标签AUC；元数据审计A/B/C+A_crop；M4存活率诊断；
  阶段L/R历史分别落盘；`test_evaluated=false`、`external_evaluated=false`。
- 2026-08-12正式放行复核修复：残差补`tanh`硬界`[-1,1]`；阶段L恢复M5局部增强，
  阶段R统一batch32与患者/标签平衡mini-batch，选择平手改用val BCE；产品回退时正式
  checkpoint/预测/bootstrap严格切回全局，融合候选独立归档；修复M4配对存活率标签列、
  M5b预计算硬编码batch64、患者排序变化误命名，并加强ROI行数/batch32产物链校验。
- 2026-08-12后处理性能修复：旧患者bootstrap每轮重复扫描/拼接图像DataFrame，正式产品、
  候选和M4对照共6000轮导致单seed后处理超过20分钟。现改为先固定患者`top2_mean`，再按
  患者首次出现顺序做有放回索引抽样；回退产品差异解析为0，未回退产品复用候选bootstrap；
  M4配对同样改为患者NumPy数组索引。用已完成seed42正式预测做新旧2000次逐值对照：
  Δ均值、CI上下界、有效轮数差异全部为0（容差`1e-12`），候选约6.9秒、M4约6.0秒。
  当前慢版矩阵已在seed202训练完成、后处理未保存时中断；空输出目录和日志归档至
  `结果/M5b残差融合_0804/归档/seed202_后处理中断_慢bootstrap_20260812/`。seed42完整
  结果保留，后续只重跑seed202/503，不改变模型训练或统计口径。
- `程序/模型训练/正式代码/run_m5b_fusion_matrix.sh`：`<gate|gate_local> <seeds>`；
  幂等验证、残缺快速失败、分级结论（强成功/正式成功/提示性收益/失败，含回退标记）。
- 验证通过：py_compile、bash -n、git diff --check、内置自测（残差init-0、BCE与
  2类CE等价）、CPU端到端debug冒烟（gate与gate_local两条路径，阶段L→R→产品保护
  →归因/审计/存活率全流程，回退保护在平局时正确选全局）。
- 2026-08-12已完成正式M5b-A/B各3种子。M5b-A硬门控逐seed产品Δ为
  `+0.0050/0/+0.0012`（seed202回退），平均约`+0.0021`；M5b-B软门控为
  `+0.0031/0/+0.0012`（seed202回退），平均约`+0.0014`。两者均因存在seed回退且
  平均Δ低于`0.01`判定失败，不解锁internal test/external。硬门控只在seed42略优，
  说明`p_local`衰减不是整体失败主因；当前预测ROI残差融合路线结束，结果保留作诊断。

#### M5c：患者概率空间简单融合（后验假设、运行前冻结，2026-08-12）

M5b-A/B正式矩阵均失败：硬门控产品平均患者AUC增益约`+0.0021`，软门控约
`+0.0014`，两者均有seed202回退全局且平均增益低于`0.01`。但既有val诊断中，分别对
全局与局部图片概率执行`top2_mean`后再做患者概率简单均值，3/3 seed均高于全局
（约`+0.007/+0.014/+0.010`）。因此新增M5c检验“局部信息是否只需要简单概率级组合”。

科学边界：该假设由M5/M5b的val结果形成，不宣称是未看val的原始预注册；以下协议在
正式运行M5c前冻结，运行后不改公式、不按val挑选最佳方法。internal test/external继续
锁定，M5c在val上的成功只代表候选方案通过验证，最终泛化仍需方案锁定后一次性确认。

统一输入与聚合：复用同seed正式M5b-B（`gate_local`）冻结的全局Encoder、局部Encoder
和局部头；不训练CNN。全局和局部图片概率分别按既有`top2_mean`聚合为患者概率后再融合，
不得改成“先融合图片概率再聚合”。不使用`gate_pass/q_region/框面积/坐标/相似度`。

三种方法的主次关系预先固定：

```text
M5c-A（主检验）: p = 0.5*p_global + 0.5*p_local
M5c-B（辅助）  : LR([logit(p_global), logit(p_local)])；仅train患者拟合，C=1，class_weight=balanced
M5c-C（辅助）  : 两路概率分别按train患者ECDF映射为分位数，再固定等权平均
```

禁止跑完A/B/C后按val择优并冒充一个正式产品。主成功判据只看A：3/3 seed均严格高于
同seed全局患者AUC且平均`ΔAUC>=0.01`为验证成功；3/3提高且平均在`[0.005,0.01)`为
提示性收益；否则失败。三种方法均报告逐seed AUC/Δ及患者配对bootstrap 95% CI。

实现入口：`efficientnet_m5c_probability_fusion.py`；矩阵入口：
`run_m5c_probability_fusion_matrix.sh`。输出只含train/val患者预测、LR系数、冻结来源SHA、
三方法结果和代码快照；`test_evaluated=false`、`external_evaluated=false`。

正式结果（2026-08-13，平衡主实验val，714位train患者/153位val患者，3 seeds）：

| seed | 全局患者AUC | A固定均值AUC（Δ） | B train-LR AUC（Δ） | C rank均值AUC（Δ） |
| ---: | ---: | ---: | ---: | ---: |
| 42  | 0.8436 | **0.8556（+0.0120）** | 0.8549（+0.0113） | 0.8542（+0.0106） |
| 202 | 0.8554 | **0.8700（+0.0146）** | 0.8530（-0.0024） | 0.8665（+0.0111） |
| 503 | 0.8499 | **0.8591（+0.0092）** | 0.8497（-0.0002） | 0.8597（+0.0098） |
| 平均Δ | — | **+0.0119；3/3提升** | +0.0029；非3/3 | +0.0105；3/3提升 |

主检验A按冻结判据验证成功：3/3 seed均严格高于同seed全局基线，平均患者AUC提升
`0.0119`。C也显示同方向辅助证据，说明收益并非完全依赖原始概率标度。B不稳定：其
train拟合局部logit系数在seed42/202/503约为`0.979/-0.208/-0.010`，seed202/503几乎
丢弃或反向使用局部信息，支持“小样本下学习融合权重不稳，固定等权组合更稳健”的解释。

统计边界：A的三组患者配对bootstrap 95% CI均跨0（约`[-0.010,0.036]`、
`[-0.005,0.039]`、`[-0.006,0.027]`），因此当前结论是**跨初始化方向一致且达到预设
点估计门槛**，不是“单个seed已获得统计显著提升”。三个seed共享同一val患者，不能把
它们当作三份独立临床队列合并放大显著性；最终泛化必须由锁定后的test/external验证。

阶段决策：冻结M5c-A为唯一后续分类候选，公式固定为患者级
`0.5*p_global + 0.5*p_local`，两路图片概率先各自`top2_mean`再融合；B/C只作辅助消融，
不得在test上重新择优。下一步先冻结val上M5c-A的患者级操作阈值及完整产物SHA，再为
内部test生成同源M1预测ROI并一次性投影；不得用test调整均值权重、聚合、阈值或ROI协议。

内部test投影实现状态（2026-08-13，尚未正式读取test指标）：

- 已生成冻结协议：`结果/M5c概率融合_0804/冻结内部test协议/
  m5c_internal_test_protocol.json`。协议锁定`0.5/0.5`、双路先`top2_mean`、M1 Top-1框、
  `margin=0.20`、batch32、三seed模型SHA、确认判据和val阈值。M5c患者阈值分别为
  `0.235449/0.223614/0.288931`；对应全局阈值为`0.282320/0.234907/0.280485`。
- test ROI入口：`build_m5c_internal_test_roi.py`及
  `run_m5c_internal_test_roi_matrix.sh`。只用冻结M1和train阶段回退尺寸生成ROI；推理副本
  强制`localization_supervision=0`，test真值框不被Dataset读取、变换或用于回退/裁剪。
- 锁定评价入口：`evaluate_m5c_locked_internal_test.py`及
  `run_m5c_locked_internal_test_matrix.sh`。一次性输出患者主指标、图像辅助指标、固定阈值
  混淆矩阵、全局/M5c AUC及配对差异bootstrap CI、来源/中心/尺寸分层；不提供调参入口。
- 静态检查、三个内置自测和CPU每类1位患者端到端冒烟均通过。冒烟仅验证代码路径，
  输出在`/tmp`，不计作正式test评价；正式三seed GPU矩阵仍未启动。
- 2026-08-13已完成正式内部test ROI三seed构建并通过数值验收：每组均为`198张/153人`
  （图片标签0/1=`112/86`，患者标签0/1=`86/67`），198张全部来自M1预测框、回退为0；
  198/198坐标有效、图像文件完整、重复推理最大差为0，CSV与协议SHA一致。输出清单不含
  GT/bbox/localization_supervision字段，config明确`labels_used_for_roi_generation=false`、
  `test_gt_used_for_roi_generation=false`。三个seed的框在198张上全部有差异，确认各自加载
  对应模型。test ROI面积中位数约`0.650-0.657`，定位置信度中位数约`0.297-0.309`，与
  train/val正式ROI的面积`0.639-0.658`、置信度`0.306-0.316`接近，无明显裁剪漂移。
  ROI清单已放行；锁定内部test指标尚未运行。
- 2026-08-13已按冻结协议完成M5c-A内部test一次性评价，三组产物均为`198张/153人`、
  协议/ROI/checkpoint SHA一致，`thresholds_from_val_only=true`、`test_evaluated=true`、
  `external_evaluated=false`。正式结果如下：

| seed | 全局test患者AUC | M5c-A test患者AUC | ΔAUC | Δ的患者配对bootstrap 95% CI |
| ---: | ---: | ---: | ---: | ---: |
| 42  | 0.8565 | 0.8469 | -0.0095 | [-0.0336, 0.0139] |
| 202 | 0.8473 | 0.8461 | -0.0012 | [-0.0263, 0.0259] |
| 503 | 0.8617 | 0.8728 | +0.0111 | [-0.0117, 0.0353] |
| 平均 | 0.8551 | 0.8553 | **+0.0001** | — |

冻结确认判据结论为**未确认**：只有seed503提高，seed42/202下降，非3/3提升且平均Δ远低于
`0.005`。三组配对CI均跨0，seed503的单组正向点估计也不足以推翻总体结论。val上的
3/3、平均`+0.0119`未在独立内部test复现，说明简单均值优势仍受val偶然排序影响。

固定val Sens>=0.90阈值下，M5c test三seed平均Accuracy/Sensitivity/Specificity为
`0.736/0.871/0.632`；对应全局模型为`0.754/0.871/0.663`。M5c没有提高平均灵敏度，
反而降低约3.1个百分点特异度和1.7个百分点准确率。分层仅作诊断：small_le640组仅
14位患者且三seed Δ均为负（约`-0.021/-0.042/-0.021`），提示预测ROI局部分支在小尺寸
图像上可能更不稳，但样本过小，禁止据此用test定向修改ROI或融合公式。

最终阶段决策：M5c-A内部test未确认，按预先冻结规则结束M4/M5/M5b/M5c的“局部ROI干预
最终分类”路线；不得基于本次test重新调0.5/0.5权重、改用rank均值、重扫阈值或挑seed。
M5c不进入外部多中心正式产品评价。当前分类产品回退为冻结M0全局分类器；M1定位器与
M3c-B区域门控继续作为病灶定位/框展示与解释组件，不干预最终癌概率。

#### M3c-B内部test冻结确认（2026-08-13，协议已冻结、尚未揭盲）

M5c内部test未确认后，最终产品仍保留`冻结M1预测框 + 冻结M3c-B区域展示门控`，因此该
定位/展示组件需要独立内部test确认。现有M5c test ROI清单只包含M1预测框，构建时未加载
M3c-B门控头、没有生成`q_region`，不能替代M3c-B test。

已新增并冻结：

- `freeze_m3c_internal_test_protocol.py`：只读取正式val配置和split清单，冻结三seed的
  M1/M3c-B checkpoint SHA256、M1定位阈值、M3c-B门控阈值、batch=32、test队列SHA与
  确认判据；冻结时不生成test预测、不计算test指标。
- `build_m3c_internal_test_predictions.py`：用冻结M1+M3c-B对test确定性前向，输出M1框、
  定位置信度、`q_region`及两个冻结门控决定。推理副本把test bbox清零并关闭定位监督，
  真值框不参与框或门控概率生成，预测CSV也不携带标签；重复前向框与`q_region`差异必须
  不超过`1e-6`。
- `evaluate_m3c_locked_internal_test.py`：预测完成后才合并冻结test真值，报告图像/患者级
  癌召回、非癌FP、M1到M3c-B的FP下降、`q_region` AUC、患者bootstrap、M1框IoU、中心
  命中、病灶覆盖率和large/non_large计数。代码不提供test重选阈值入口。
- `run_m3c_internal_test_matrix.sh`：三seed串行构建与一次性评价，完整产物幂等跳过、残缺
  产物拒绝覆盖、单seed失败保留现场并继续。

正式冻结产物：

```text
结果/M3c区域门控_0804/冻结内部test协议/m3c_internal_test_protocol.json
结果/M3c区域门控_0804/冻结内部test协议/m3c_internal_test_queue.csv
test队列SHA256 = 600889f089cbeb8da90518cbd822161fb0552633af5b88d7d919dee7e5168088
队列规模 = 198张 / 153人；癌图bbox监督 = 86张
推理batch = 32
```

冻结阈值：

| seed | M1定位置信阈值 | M3c-B门控阈值 |
| ---: | -------------: | ---------------: |
| 42   |       0.230048 |         0.302738 |
| 202  |       0.230969 |         0.119552 |
| 503  |       0.230419 |         0.177208 |

单seed内部test确认规则：

```text
癌召回：M3c-B >= max(0.85, 同seed M1癌召回 - 0.05)
非癌FP：同seed M1非癌FP - M3c-B非癌FP >= 0.10
分辨率组：癌图数>=15的large/non_large组，M3c-B检出数 >= M1检出数-1
总判定：42/202/503三seed均通过上述全部适用门槛
```

M3c-B不能改变M1框，因此IoU、中心命中和病灶覆盖率用于说明冻结M1几何在test上的表现，
不作为M3c-B可移动框的证据。冻结后禁止修改模型、阈值、test队列和判据，禁止按test挑
seed或调参。

M3c-B锁定内部test结果（2026-08-13）：**0/3 seed通过，正式确认失败**。外部数据仍未
读取。本结论不反向修改val结果：M3c-B在val为3/3成功，但该成功没有按冻结的高敏感
保护规则完整复现到独立内部test。

| seed | q_region图像AUC | M1癌召回 | M3c-B癌召回 | M1非癌FP | M3c-B非癌FP | FP下降 | large检出 M1→M3c | 正式通过 |
| ---: | ---------------: | --------: | ------------: | --------: | ------------: | -----: | ------------------: | :------: |
|   42 |           0.8113 |    0.9302 |        0.8721 |    0.8214 |        0.4196 | 0.4018 |             72→68 | 否 |
|  202 |           0.8216 |    0.9535 |        0.9186 |    0.8482 |        0.5982 | 0.2500 |             74→72 | 否 |
|  503 |           0.8568 |    0.9535 |        0.9302 |    0.7857 |        0.5000 | 0.2857 |             74→72 | 否 |
| 均值 |           0.8299 |    0.9457 |        0.9070 |    0.8185 |        0.5060 | 0.3125 |                 - | 0/3 |

失败原因逐项解释：

- 非癌FP下降门槛三seed全部通过，而且平均绝对下降`0.3125`，证明`q_region`确实能识别
  大量非癌假响应，不是无效随机分数。
- 癌召回保护：seed202与503通过；seed42由`80/86`降为`75/86`，召回`0.8721`低于其
  冻结下限`0.8802`，差1张即未通过。
- large组是三seed共同失败点：seed42相对M1少检4张，seed202/503各少检2张，均超过
  “最多多漏1张”的冻结门槛。non_large仅9张，不参与正式门槛。
- 患者级均值同样表现为取舍：癌召回由`0.9453`降至`0.9104`，非癌FP由`0.8605`降至
  `0.5271`。非癌误框显著减少，但代价是额外漏掉部分癌患者。
- 三seed共同拒绝5张癌图，其中2张是三个seed都把原本M1已展示的框关掉，提示存在稳定
  的癌侧盲点，并非仅由随机种子造成。

冻结M1框几何在内部test稳定：三seed平均IoU约`0.4464`、中心命中率均为`0.9070`、平均
病灶覆盖率约`0.6701`。这说明主要问题不在M1框整体失效，而在M3c-B把部分有效癌框作为
假响应拒绝。

阶段决策：M3c-B保留为“降低非癌误框的研究性/辅助置信信号”，但不再作为正式产品中
自动隐藏框的硬门控，尤其不能按test表现挑选seed503。正式定位输出回退为冻结M1框与
定位置信度；如界面需要使用`q_region`，只能并列展示为辅助评分或用于人工排序，不能在
没有新验证集的情况下重新调阈值、做多数投票或恢复为硬开关。M3c-B未通过内部test，
因此不继续以“正式产品确认”为目的运行外部test；后续外部投影如开展，只能标记为诊断性
分析，不用于挽救本次内部test结论。

M3c-B诊断性外部多中心投影（2026-08-13）：应研究需求，在内部test正式未确认之后，仍将
冻结M1+M3c-B三seed投影到同一套完整外部多中心集`1941张/1329人`。本次不训练、不重选
阈值、不选择seed，结果只回答`q_region`是否携带跨中心癌/非癌排序信号，不改变内部test
`0/3`和“不得作为自动隐藏框硬门控”的结论。外部集没有bbox，不能评价框是否覆盖病灶。

| seed | M1分类患者AUC（mean） | q_region患者AUC（mean） | 配对ΔAUC及95% CI | q_region图像AUC | q_region患者AUC（max） |
| ---: | ---------------------: | ----------------------: | ----------------: | ---------------: | ---------------------: |
|   42 |                 0.6691 |                  0.7379 | +0.0689 [0.0325, 0.1048] |           0.7323 |                 0.8240 |
|  202 |                 0.6830 |                  0.7257 | +0.0427 [0.0084, 0.0757] |           0.7106 |                 0.8197 |
|  503 |                 0.6901 |                  0.7090 | +0.0188 [-0.0139, 0.0514] |           0.6953 |                 0.8111 |
| 均值 |                 0.6807 |                  0.7242 |                 +0.0435 |           0.7127 |                 0.8183 |

与早期外部结果的正确比较：

- 早期ResNet50外部患者AUC为`0.6934`；当前`q_region`患者均值AUC为`0.7242`，描述性高
  `0.0308`。但二者不是同一模型、同一输出头，不能把差值写成严格配对模型提升。
- 同一M1模型内，分类概率患者AUC均值为`0.6807`，与此前来源内平衡EfficientNet外部结果
  `0.6807`一致，证明本次数据和分类前向口径正确；`q_region`均值比同源分类概率高
  `0.0435`。配对bootstrap中seed42/202差值区间高于0，seed503区间跨0。
- 全量诊断EfficientNet外部患者AUC均值为`0.7399`，仍略高于`q_region`均值`0.7242`；
  因训练队列和角色不同，同样只作参照。

`q_region max`的`0.8183`不能作为主要性能结论：外部癌患者平均`4.48`张图，非癌患者仅
`1.16`张；仅使用患者图片数量预测标签，AUC就达到`0.9798`。对多图取最大值会天然抬高
癌患者分数，因此主要口径锁定为患者均值AUC和图像AUC，max只用于描述实际“任一图触发”
行为。

冻结val门槛下，三seed图像级平均癌召回/特异度为`0.8235/0.4825`；M1定位置信门槛为
`0.8376/0.3012`。M3c-B平均少约`1.4`个百分点癌召回，换来约`18.1`个百分点特异度，
再次证明它擅长压低非癌假响应，但不是无代价的高敏感硬门控。患者max口径癌召回/特异度
为`0.9449/0.4608`，受图片数量严重不平衡影响，只能解释为当前队列上的操作表现。

外部诊断最终解释：`q_region`是有价值的跨中心辅助风险分数，患者均值排序优于当前平衡
M1分类概率并高于早期`0.693`参照；但内部test癌侧保护失败、外部无bbox、max聚合又受图片
数量混杂，因此产品角色保持不变：保留为并列评分/人工排序信号，不自动隐藏M1框，不替代
正式癌症分类概率。

M5c正式结果（2026-08-13）：**主检验A验证成功**。三种子患者级固定均值
`0.5*p_global+0.5*p_local`（复用冻结M5b-B全局/局部头）的Δ患者AUC：

| seed | 全局患者AUC | A融合AUC | Δ患者AUC | 配对bootstrap 95%CI |
| ---: | ----------: | -------: | -------: | ------------------- |
|   42 |      0.8436 |   0.8556 |  +0.01198 | [-0.0101, 0.0358] |
|  202 |      0.8554 |   0.8700 |  +0.01458 | [-0.0053, 0.0388] |
|  503 |      0.8499 |   0.8591 |  +0.00920 | [-0.0064, 0.0267] |
| 均值 |             |          |  +0.01192 |                    |

预注册主检验判据：3/3 seed均严格高于全局 且 平均Δ>=0.01 → **验证成功**。辅助方法：
C（train ECDF rank均值）均Δ`+0.0105`接近A；B（train-LR）`+0.0029`但seed202/503为负
（logits空间LR未泛化）。`test_evaluated=false`、`external_evaluated=false`。

融合系列对比（均Δ患者AUC）：M5 concat-MLP `-0.0013`（失败）< M5b-B `+0.0014` <
M5b-A `+0.0021` < M5c-B LR `+0.0029` < M5c-C rank均值 `+0.0105` < **M5c-A固定均值
`+0.0119`（验证成功）**。结论：局部概率的互补价值需要**全权重、无门控**地使用——
α门控压制局部信号、LR/MLP在975张图上过拟合，均为减法；无参数的固定均值最稳。

边界（预注册口径）：假设由M5/M5b的val诊断形成（后验），M5c在val上的成功=候选方案
通过验证；**最终泛化仍需方案锁定后对internal test/external一次性确认**。单seed CI均
跨0，效应温和，3/3方向一致+均Δ是判据信号。

候选方案（待锁定后一次性确认）：`p_final = 0.5×top2_mean(全局概率) + 0.5×top2_mean(局部概率)`，
冻结M5b-B（gate_local）全局/局部Encoder+局部头，不引入门控/几何/相似度。

#### 阶段决策（2026-08-13最终修订，M5c与M3c-B均完成内部test）

```text
M0：建立新数据分类基线
M1：正式定位器（warmup-only产品，已冻结）
M3/M3b：热图抑制失败，归档为诊断证据
M3c-B：val 3/3通过，但锁定内部test 0/3；仅保留为辅助评分，不作隐藏框硬门控
M1不足且原因是空间分辨率：M2特征金字塔仍为候选（未运行）
M4优于冻结M1全局分类基线：说明真值ROI局部信息有价值（已结束）
M5/M5b（concat-MLP、α残差）：预测ROI融合路线失败，归档为诊断证据
M5c（患者概率固定均值）：val成功但内部test未确认，停止，不进入external
最终分类产品：冻结M0全局分类器
最终定位输出：冻结M1预测框；M3c-B q_region可并列显示或人工排序，但不得自动隐藏框
```

后续YOLO26路线的研究问题、Y0-Y6预注册协议和实时结果统一见
`YOLO26实验进度与结果讨论.md`。本文档只保留M0-M5阶段结论作为该路线的前置依据。

### 最终SAE任务路线

现有ResNet50 `2048→512→2048` SAE继续作为第二批v1.1阶段的冻结解释基线。新数据和
最终分类器确定后必须重新提取特征并重新训练SAE；旧Feature编号和医学命名不能直接
迁移到新模型。

最终模型优先分别提取和分析：

1. 全局分支特征；
2. 局部分支特征；
3. 全局/局部融合后的最终分类特征。

设待解释层维度为`D`，候选字典宽度按以下顺序逐级比较：

| 编号 | 结构              | 定位                                       |
| ---- | ----------------- | ------------------------------------------ |
| S0   | `D → 512 → D` | 当前M-CBM兼容参照                          |
| S1   | `D → D → D`   | 完整宽度参照                               |
| S2   | `D → 2D → D`  | 首个正式过完备SAE                          |
| S3   | `D → 4D → D`  | 主要扩张候选                               |
| S4   | `D → 8D → D`  | 仅S3稳定、死Feature和重复Feature可控时运行 |

不直接采用32倍扩张。当前数据规模远小于大模型SAE语境，过宽字典可能产生大量死亡、
重复和不稳定Feature。具体L1网格、warm-up、允许的AUC下降和稳定性门槛必须在运行前
再次写入本文档并冻结。

统一训练与选择原则：

- SAE只用train拟合，val选择epoch、L1、宽度和剪枝规则；
- 使用逐元素MSE、L1稀疏约束、decoder单位范数和decoder梯度正交投影；
- 保留患者平衡采样和batch size 32；
- 学习率前5% warm-up，L1从0线性增加至目标值；
- 同时报告MSE、cosine、L0、90%激活质量所需Feature数、死Feature率、重复Feature率、
  图像/患者预测一致率和原分类头保真度；
- test和外部test只投影一次，不用于选择宽度、L1、剪枝或Feature；
- 不仅按重构误差或AUC选SAE，还要比较随机种子稳定性、跨患者/跨来源稳定性和临床
  可命名性。

共享Feature分析不预先把Feature硬分为“癌Feature”和“非癌Feature”。应对全部保留
Feature联合构图，综合decoder余弦相似度、患者级共激活、癌/非癌激活差异、来源关联、
代表图视觉相似度和空间响应相似度进行聚类，再由临床人员标记：

- 癌相关概念；
- 非癌相关概念；
- 癌/非癌共享解剖或黏膜概念；
- 良性病变与癌共享形态；
- 医院、设备、分辨率和画面风格概念；
- 镜体、文字、黑边、反光、气泡、黏液等干扰概念；
- 稀有、重复或不稳定概念。

共有Feature不能仅因两类都激活而删除。只有在来源关联、空间响应和医学审核均支持其为
伪特征，并且锁定Feature簇屏蔽后内部性能不降或外部稳定性改善时，才可作为排除候选。
Feature干预使用`编码→屏蔽Feature/簇→解码→复用原分类头`，同时报告AUC、Sensitivity、
Specificity、决策一致率和分中心变化。

最终SAE交付包括宽度比较、训练质量、Feature级统计、共享Feature聚类、来源Feature、
Feature中心与图像中心空间图、内部/外部稳定性、Feature簇屏蔽消融、临床命名表和阅读
指南。稳定概念形成后再进入M-CBM/CBL。

## 当前已知问题与处理

| 问题                       | 可能影响                                      | 当前处理                                             | 后续验证                           |
| -------------------------- | --------------------------------------------- | ---------------------------------------------------- | ---------------------------------- |
| MOCE黑色遮挡基线偏置       | SSC/SDC step 0可能主要反映黑图类别先验        | 不将step 0直接解释为黏膜一致性或医学充分性           | 比较其他填充值或遮挡基线           |
| SAE彩色图出现紫色          | 红蓝Feature响应重叠后产生混色                 | 结合5个单Feature图解释，不设为第三种贡献类别         | 必要时检查原始贡献矩阵             |
| K-Means结果随K变化         | 概念粒度、重复簇和小簇数量变化                | 已完成`K=10,15,...,50`敏感性分析，不凭单一指标选K  | 医学生/医生结合跨K稳定性审核       |
| 外部患者图片数混杂         | `top2_mean`可能利用癌患者图片更多这一结构   | 正式结果同时披露mean和随机单图敏感性                 | 后续补充限图聚合或分层分析         |
| SAE Feature医学语义未确认  | 高激活方向可能仍对应来源、反光或界面          | 保留来源相关性、Top患者图、空间响应与消融证据        | 跨数据集对照并由临床命名           |
| 新数据表面平衡但来源仍泄漏 | 合并后1:1仍可能是一个来源偏癌、另一来源偏非癌 | 要求早癌大赛和武大省人医各自内部平衡患者、图片和风格 | 训练前做来源×标签和元数据预测审计 |
| M1随机裁剪丢失癌病灶       | 癌图被裁成无病灶图但标签仍为癌                | 无框阶段只做保守尺度增强；有框后使用bbox感知变换     | 抽样可视化并统计裁剪后病灶保留率   |
| M1中心网格量化             | 14x14热图对小病灶中心定位较粗                 | 增加center offset，并按bbox大小生成高斯目标          | 报告中心距离、命中率和IoU          |
| 真值ROI到预测ROI分布差异   | M4可能高估实际部署性能                        | M4只作上限；M5使用预测框并在训练中混入扰动框/预测框  | 分开报告M4与M5                     |

## 当前风险与结论边界

1. 内部实验A高AUC与严格共同支持OOF、独立外部结果差距大，说明内部结果不能单独代表
   多中心泛化。
2. 外部患者图片数量本身高度预测标签，任何患者聚合收益都必须做单图和mean敏感性分析。
3. MOCE的黑色遮挡会引入类别偏置，SSC/SDC不能单独证明概念医学真实性。
4. SAE保真度只说明稀疏表示可近似原模型，不说明Feature自动具有医学意义。
5. 热图和SAE空间响应都是粗定位，不是病灶分割。
6. test和外部test已经用于正式报告，后续不得根据其结果反复选择超参数或结构。
7. 新扩张SAE和EfficientNet-B0 SAE属于已知test背景下的补充研究；必须先在本文档冻结
   候选网格和选择规则，再只用train/val完成选择。
8. 医学生命名属于人工证据；自动分析不能代替医生最终判断。
9. 新数据、病灶框、定位监督、特征金字塔、ROI融合和Transformer必须按M0-M5顺序做
   单变量或可归因消融，不能一次全部叠加后只报告最终最好结果。
10. 带可视化矩形框的图片含有人为边缘，不能作为模型输入；训练必须使用无可视化框的冻结
    正式处理输入图和其坐标系下的独立结构化标注。

## 下一步

### 当前下一步：SAE解释与MAGE改进双路线并行（2026-08-14）

M0-M5局部干预路线和YOLO26 Y0-Y6路线均已完成当前轮次。后续不再沿用“先继续调ROI
融合，再做解释”的串行安排，改为以下两条互不阻塞的路线并行：

```text
路线A：冻结现有模型 → SAE解释 → 全局/局部/共享Feature及伪特征分析
路线B：bbox局部教师 → MAGE-like双目标蒸馏 → 推理时仅保留全图学生
```

#### 路线A：现有模型SAE分析

目的：回答当前工程系统已经学到了什么，不修改当前模型参数或分类结果。优先分别解释：

1. M0-F全局EfficientNet-B0 Encoder：输入完整胃镜图，提取GAP前后冻结的1280维全局
   表征，分析病灶、周围黏膜、颜色、解剖结构、来源和设备Feature；
2. Y6局部EfficientNet-B0 Encoder：输入YOLO预测框按冻结`margin=0.20`生成的局部ROI，
   提取1280维局部表征，分析颜色、纹理、边界、凹陷、隆起及困难非癌区域Feature；
3. 全局/局部联合分析：不预先把Feature硬分为癌和非癌，联合比较decoder方向相似度、
   患者级共激活、空间响应、来源相关性和代表图视觉语义，标记癌相关、非癌相关、共享
   解剖/黏膜、良性与癌共享形态以及反光、黏液、设备和分辨率等伪特征。

SAE宽度沿既有S0-S4原则逐级推进。当前两路待解释特征维度均为`D=1280`，候选为
`1280→512→1280`、`1280→1280→1280`、`1280→2560→1280`、
`1280→5120→1280`；只有前一级在重构、稀疏性、死亡/重复Feature和预测保真度上合格，
才继续扩大，不直接运行8倍或32倍字典。正式运行前还需冻结L1网格、warm-up、允许的
AUC下降、最低一致率和剪枝规则。

数据隔离保持不变：仅用train训练SAE、val选择宽度/L1/epoch/剪枝和Feature候选；已经
查看过的internal test和external只能在全部规则冻结后作一次描述性投影，不能参与SAE
选择。首版主seed及跨seed稳定性策略在运行前单独冻结，不根据external AUC挑seed。

路线A最终交付：宽度比较、全局与局部Feature统计、共享Feature聚类、来源/设备Feature、
图像中心与Feature中心空间图、Feature簇屏蔽消融、临床命名表及医学生阅读指南。Feature
只有在来源、空间响应、医学审核和屏蔽消融共同支持其为伪特征时，才可作为排除候选。

#### 路线B：MAGE-like bbox引导蒸馏

目的：回答能否把病灶局部结构知识稳定地教给全图分类器，同时避免推理阶段继续承受YOLO
框误差。原MAGE使用精细病灶掩膜和消色差局部专家；本项目只有矩形bbox，且任务为
癌/HGD与非癌，故正式名称使用`MAGE-like bbox-guided knowledge distillation`，不得声称
完整复现原论文。

计划数据流：

```text
训练阶段
癌图GT bbox → 灰度/弱颜色局部病灶图 ─┐
                                       ├→ 局部教师 → logits与空间注意力
非癌OOF YOLO困难负ROI ────────────────┘                  │
完整彩色胃镜图 → 全图学生 ← 分类蒸馏 + 注意力蒸馏 ──────┘

推理阶段
完整彩色胃镜图 → 全图学生 → 癌概率
```

癌图使用无可视化框的原图和结构化GT bbox；非癌图优先使用患者级OOF YOLO产生的困难
假阳性ROI，无有效候选时才按预先冻结规则使用尺寸匹配回退ROI。不得直接使用在同一train
图像上拟合的检测器输出冒充OOF困难负，也不得把“是否有GT框”变成标签捷径。

首版总损失预定为：

```text
L_total = L_cls(student, y)
        + lambda_logit * L_KD(student_logits, teacher_logits)
        + lambda_attention * L_att(student_map, teacher_map)
```

其中`L_cls`保证全图学生完成癌/非癌主任务，`L_KD`转移局部教师的类别知识，`L_att`把
局部病灶空间关注映射到全图学生。具体温度、两个lambda、bbox软掩膜/抖动、局部教师
负样本比例和checkpoint选择必须在训练前预注册，不得在test/external上调整。

路线B按以下顺序执行：

1. 构建并审计癌GT局部图、非癌OOF困难负ROI和完整图之间的一一对应关系；
2. 先训练局部教师，确认其能区分癌ROI与真实部署中的非癌困难ROI；
3. 固定局部教师，训练接收完整彩色图像的全图学生；
4. 与相同数据、骨干、seed和患者聚合口径的普通M0-F全图基线受控比较；
5. 推理只保留全图学生，不依赖YOLO或局部教师；
6. 若MAGE-like学生稳定超过基线，再单独训练其SAE；若未超过，则保留路线A作为当前系统
   的正式解释结果，不将现有Feature编号迁移给MAGE学生。

现有internal test和external均已被查看，不能作为MAGE超参数或结构选择依据。train/val
可用于该探索路线的开发和预注册比较，但最终确证性性能必须由新的独立数据完成。

#### 两路线汇合与近期执行顺序

1. 冻结路线A的全局/局部checkpoint、目标层、train/val清单和seed策略，开始特征提取；
2. 同时起草路线B的正式预注册协议，优先完成患者级OOF YOLO困难负ROI清单；
3. SAE先完成S0参照及1280/2560宽度可行性比较，MAGE先完成局部教师可行性检查；
4. 医学生可并行复核YOLO正确框、漏检框、非癌误框及全局/局部Feature代表图；
5. 最终形成“现有模型Feature诊断”和“MAGE-like新模型改进验证”两套独立结果，再决定
   是否让MAGE学生替代当前全图分类器并进入新的MOCE/SAE分析。

### 历史任务清单（已大部分完成，仅供追溯）

#### 立即任务

1. 已完成来源内患者级癌:非癌约1:1.3主清单和严格1:1敏感性清单冻结；补充外院216张
   继续排除在开发集外；
2. 已完成平衡M0的ResNet50与EfficientNet-B0各3个种子，全部使用`--defer-test`；
   只用val锁定EfficientNet-B0为M1-M5主骨干，ResNet50保留为受控对照；
3. 已完成1941张主外部多中心集评估；结果确认仍有明显域偏移，且不反向改变val选择；
4. 在平衡M0入选的唯一骨干与Keep分支上，单独运行A0/A1/A2尺度增强消融，不与
   Keep/Notch变量混跑；
5. 对全量和平衡候选补充来源、风格、PIP子组、来源留出和Grad-CAM审计，判断模型是否
   仍依赖设备界面、分辨率或来源风格；
6. A0/A1/A2、来源/风格审计和严格1:1敏感性分析完成后，才使用锁定checkpoint、阈值和
   聚合规则评估一次internal test；0804中的216张补充外院队列继续与1941张主外部集
   分开报告，并先完成患者与来源重叠审计；
7. 已完成医学生bbox接收与整合：`数据/标注与汇总/bbox_manifest_20260807.csv`，
   1779张癌图全部有bbox，命名映射/尺寸/界内审计通过（详见近期历史2026-08-07）；
8. 已冻结M1来源内1:1.3 Keep主清单：1373张/1020人，597张癌图全部带有效框，非癌图
   `localization_supervision=0`，无患者跨split、无跨患者SHA重复；此前0025730跨患者
   重复图未进入该主清单，因此不再阻塞M1主实验；
9. 已冻结M1全量诊断Keep清单：3348张/1732人，1670张癌图bbox覆盖100%；M0阶段已将
   0025730副本规范为只保留原患者目录一份，因此该清单跨患者SHA为0；
10. 21张bbox与Notch黑块相交只影响Notch旁路；M1主实验已锁定Keep，故不跳过这些癌图
    的定位损失。Notch后续诊断实验若重启，再单独冻结A/B处理；
11. 已完成M1平衡`_pat12`与M1b (`3e-5`)各3种子。原`1e-4`真joint AUC/
    mean IoU均值为`0.8521/0.4622`，非劣2/3；M1b为`0.8443/0.4492`，非劣0/3。
    M1b已否决。锁定`1e-4 + patience12`的全量诊断3种子也已完成：真joint
    AUC/mean IoU均值`0.9029/0.5002`，非劣2/3；服务器中断前三组均已完整落盘，
    无需重跑。warmup-only正式产品六组也已完成：平衡3种子center-hit/mean IoU均值
    `0.9004/0.4417`，全量诊断为`0.9042/0.4623`；平衡最佳epoch未触及第5轮，全量
    为`5/4/5`。当前直接进入M3；10轮warm-up仅保留为全量定位训练长度敏感性候选，
    M2保留为frozen-encoder FPN精度候选；internal test继续锁定。
12. M0-M5局部干预路线已完成并按锁定internal test停止。当前进入YOLO26的Y0数据转换与
    审计；Y0临床完整性确认通过后才运行Y1 smoke，Y1不得产生性能结论。

#### 随后任务

1. bbox到位后先做标注合法性、病灶可见性和框分布验收，再实现并debug M1的
   `heatmap+size+offset`辅助定位；
2. M1同时保留“来源内平衡主实验”和“全量诊断实验”：前者判断框监督能否提升可靠
   分类，后者只用于比较定位监督是否缩小来源/风格差距；两者必须使用对应M0作为基线；
3. 根据M1分类与定位双重结果推进M2特征金字塔、M3自动检测、M4真值ROI上限和M5预测
   ROI部署实验；
4. CNN监督路线确认后，再将Swin-T等小型Transformer作为受控backbone对照；
5. 最终分类器冻结后，按S0-S4路线只用train/val完成SAE宽度和稀疏策略选择，再做共享
   Feature聚类、空间响应和Feature簇屏蔽消融；
6. 现有锁定ResNet50 SAE的30个四域候选空间响应对照继续保留为旧模型诊断任务，但不
   阻塞新数据和框辅助分类器主线；
7. 收集医学生对现有MOCE与彩色SAE图的标注；稳定概念形成后再进入M-CBM/CBL。

## 正式报告索引

- 数据与参数：
  - `数据去偏重训练_参数与报告/数据预处理参数_v1_1.md`
  - `数据去偏重训练_参数与报告/数据筛图参数_v1_1.md`
  - `数据去偏重训练_参数与报告/数据训练参数_v1.md`
- 分类实验：
  - `数据去偏重训练_参数与报告/完整裁剪数据重训练实验A结果_v1.md`
  - `数据去偏重训练_参数与报告/严格共同支持集5折结果_v1.md`
  - `数据去偏重训练_参数与报告/P7正式实验汇总_v1.md`
  - `数据去偏重训练_参数与报告/患者聚合集成TTA评估结果_v1.md`
  - `数据去偏重训练_参数与报告/完整多中心外部测试结果_v1.md`
- 概念集：
  - `数据去偏重训练_参数与报告/医学生整理概念集模型审计结果_v1.md`
  - `数据去偏重训练_参数与报告/两批v1_1裁剪数据对比与选用结论_v1.md`
- MOCE：
  - `结果/MOCE分析/`
  - `结果/MOCE聚类数量分析/`
- SAE：
  - `程序/SAE/正式代码/README.md`
  - `结果/SAE/去偏重训练_v1/resnet50/`
  - `结果/SAE分析/去偏重训练_v1/resnet50/`

## 近期历史

- 2026-07-22：完成v1.1实验A/A0/B/C/D、严格共同支持5折和旧模型同口径对照。
- 2026-07-23：完成概念集审计、严格502张概念集、双模型MOCE，并锁定患者聚合与集成。
- 2026-07-24至2026-07-28：修正SAE损失尺度，完成ResNet50 SAE、外部测试和外部投影。
- 2026-07-29至2026-07-30：完成外部1941张图像中心彩色SAE及100位患者提交版。
- 2026-07-31：备份旧`AGENTS.md`，建立双文档结构；完整表格和总结归入本进度文档，
  `AGENTS.md`保留稳定规则、数据谱系和精简里程碑；完成锁定ResNet50 SAE内部test
  投影及四域Feature定量稳定性分析；冻结下一版来源内平衡数据、CenterNet式辅助定位、
  M0-M5分类实验矩阵及S0-S4扩张SAE与共享Feature分析路线。
- 2026-08-03：完成现有第二批原图89张/60个抽样单元的四边裁剪、FOV、PIP和
  Keep/Notch小样本工程验收。人工总览确认6张PIP；固定OpenCV线程与RNG后两次
  PIP结果逐字节一致，候选召回为100%、需人工排除3个假阳性。6张Notch框初筛无异常；
  两分支各89张、83张共用同一文件、6张使用Notch像素版，尺寸与`geometry_id`全部一致。
  该结果只证明工程流水线可运行，不冻结新数据阈值或Keep/Notch模型选择结论。
- 2026-08-03：针对暗黏膜可能被亮度轮廓误裁的问题，增加原图FOV反查的
  `dark_crop_safety_review.py`。89张样本按“框外FOV比例/单边越界比例”生成四档联图：
  敏感档42张、中等档14张、保守档5张、强风险档4张。肉眼初查确认候选中既有应放宽的
  暗部误裁，也有放宽后会重新带回设备UI或视频进度条的情况，因此该步骤只负责筛查和
  生成保守候选，禁止整批自动替换。随后将候选改为按边定向放宽：已检出进度条时锁住
  当前底边，只恢复越界明显的顶部、左侧或右侧；保守档5张中3张恢复顶部、2张恢复右侧，
  均未重新放宽底边。用户肉眼比较四档后确认C保守档“不多也不少”，据此冻结正式复核
  阈值为框外FOV比例`>=0.05`或单边越界比例`>=0.06`；逐图仍需记录最终选择。
- 2026-08-04：收到`胃早癌概念提取训练集0804`并完成只读初审。3577张均可解码，图片
  标签近似平衡，但患者数为487癌对1369非癌，且分辨率、画幅和尺寸仍明显关联标签；
  暂停直接全量裁剪训练，先构建来源内患者与风格匹配清单。
- 2026-08-05：完成0804标注包、双路径Keep manifest和3577张冻结几何的一致性验收；
  zip SHA256为`62bb00f3d76107e83cedb0437a2eb869f5985eeb50f83be650ca8f211363e4ed`。
  四角PIP全量候选为177张，人工联系表复核确认30张假阳性，其余147张暂按真PIP记录；
  120张来源与标签平衡的阴性抽查中新发现1张假阴性，与既往底边融合漏检图不是同一张。
  两张假阴性均已进入待手动画框侧表，并已从冻结正式处理输入复制到
  `03_画中画筛查_全量_20260804/人工框选包_画中画假阴性_20260805/`。人工红框复核后，
  两张图均回填为`xyxy=(0,0,312,250)`（对应`xywh=(0,0,312,250)`）。已修正
  Notch生成脚本的人工框优先逻辑，并改为按冻结Keep manifest的
  `final_processed_path`逐图取图，避免第一届WL紧裁图退回旧FOV目录。正确版Notch
  已生成149张/120人，与Keep几何一致；错误图像根读取版已移入归档。
  框完整性初筛剩26张/25人待最终人眼确认，其中大多为尺寸簇提示；
  第一届WL的2张画中画已在紧裁坐标下完整去除，不需要再做改变bbox几何的空间裁剪。
  随后用户确认26张框QC全部通过；正式Keep/Notch配对manifest为每分3577张、
  0尺寸错误。Notch分支分组为3428张`normal`、147张`pip_notch`和2张
  `normal_after_external_pip_removal`；后两张是画中画完全位于主视野外的第一届WL图。
- 2026-08-05：冻结M0全量诊断开发集为3348张/1732人，排除补充外院216张、
  跨标签患者10张和重复SHA副本3张；生成共用患者split的Keep/Notch正式清单。
  训练工具已切换为val患者AUC选checkpoint，并加入val患者筛查阈值扫描和
  `--defer-test`。ResNet50 Keep/Notch与EfficientNet-B0 Keep的1+1 epoch小样本冒烟全部通过；
  调试指标仅用于验证工程链路，不参与正式结论。
- 2026-08-05：完成M0正式val矩阵第1/12组`Keep + ResNet50 + seed42`。最佳为
  S2 epoch 5，val患者AUC 0.8713；验证患者阈值0.03251下Sensitivity 0.9254、
  Specificity 0.6684。源码快照与配置SHA一致，完整训练日志已落盘，且未生成
  internal test预测。早先一次运行因运行期间报告字段代码快照变化，仅作pilot完整归档，
  不计入正式矩阵。
- 2026-08-05：完成M0全量诊断正式val矩阵12/12组及2000次患者整簇bootstrap。
  全量阶段暂选Keep与EfficientNet-B0：其跨种子患者AUC为`0.9091±0.0041`，患者
  Accuracy/Sensitivity/Specificity均值为`0.7885/0.9154/0.7444`；图片AUC为
  `0.8485±0.0090`，图片Accuracy/Sensitivity/Specificity均值为
  `0.7606/0.8181/0.7069`。Notch在平均AUC上与Keep接近，但最差种子的患者
  Specificity下降超过预注册0.02门槛，因此不进入下一轮主训练。该选择仍需来源内平衡
  M0确认；internal test和外部test均未参与选择、尚未在本轮评估。
- 2026-08-05：完成当前Keep流程下的独立外部多中心预处理与6组锁定模型评估。
  外部1329人/1941张与M0开发集无SHA或患者标识直接重叠；35张暗部风险图经人工确认
  使用保守回退。EfficientNet-B0跨种子患者AUC/图片AUC为`0.7399±0.0075`/
  `0.7238±0.0079`，患者/图片Accuracy为`0.6353/0.6452`；ResNet50对应为
  `0.7158±0.0205`/`0.6885±0.0228`和`0.5661/0.5932`。结果确认明显域偏移，同时
  支持EfficientNet-B0继续作为来源内1:1.3平衡M0的主候选；外部结果不用于反向调参。
- 2026-08-05：冻结来源内1:1.3主平衡清单1373张/1020人，癌:非癌患者为446:574、
  图片为597:776；患者/图片风格TV为0.0192/0.0461。同步生成严格1:1敏感性清单
  1246张/892人。两清单均无患者或SHA跨split、无缺图、每患者最多3张；双骨干1+1
  epoch冒烟通过，正式6组GPU训练入口与逐组日志已准备。
- 2026-08-05：完成来源内1:1.3平衡M0正式6组val矩阵并锁定EfficientNet-B0为M1-M5
  主骨干。其患者/图片AUC为`0.8523±0.0070/0.8187±0.0098`，ResNet50为
  `0.8273±0.0092/0.7946±0.0115`；internal test未评估。随后完成独立外部多中心
  1329人/1941张评估，EfficientNet-B0外部患者/图片AUC为
  `0.6807±0.0107/0.6563±0.0131`，ResNet50为`0.6828±0.0193/0.6569±0.0132`。
  平衡训练未改善外部AUC，域偏移仍明显；外部结果不用于反向调参或改变骨干选择。
- 2026-08-05：完成严格1:1敏感性（ENet+Keep，3种子，严格清单1246张/892人，
  癌:非癌623:623）。val患者AUC为`0.8389±0.0067`（1:1.3为0.8523，差0.013）、
  锁定阈值下Spec均值0.458（1:1.3为0.663）；独立外部患者AUC为`0.6846±0.0055`
  （1:1.3为0.6807，基本持平），外部Sens/Spec均值0.865/0.409。结论：类别比例只
  影响val（1:1.3多出的非癌患者提高val Spec）、不影响外部；全量→平衡的外部下降
  （0.74→0.68）非比例驱动，更倾向平衡移除可转移的shortcut、外部0.68接近真实泛化，
  数据量因素不能完全排除（需来源/风格审计佐证）。1:1.3主清单维持不变。
- 2026-08-05：完成M0尺度增强A0/A1/A2单变量消融（EfficientNet-B0+Keep，1:1.3平衡，
  3种子，仅改RandomResizedCrop scale，ratio固定）。A0=(0.85,1.0)患者AUC
  `0.8523±0.0070`、A1=(0.7,1.0)`0.8560±0.0054`、A2=(0.5,1.0)`0.8437±0.0062`；
  A2明确更差（Spec降至0.585）排除，A1与A0在种子噪声内持平。锁定A0(0.85,1.0)为
  正式scale：A1增益不构成稳定收益，且保守增强降低后续bbox定位阶段裁掉病灶的风险。
  训练工具已加入`--crop-scale-min/max`参数并快照记录。
- 2026-08-07：收到医学生病灶bbox标注（7个CVAT XML共1773张癌图每图1框，外院/第一/二届
  各1 + 武大省人民按年5；另重标annotations.xml 34张=29张重标+5张漏标补标）。完成
  只读审查与整合：
  - 命名映射验证：武大省人民/第一届用basename、第二届用`{患者}_{basename}`、外院用
    `{中心}_{患者}_{basename}`，重建后100%映射回0804癌图；标注尺寸100%一致、框100%界内；
  - 整合为`数据/标注与汇总/bbox_manifest_20260807.csv`：**1779张癌图全部有bbox**
    （含processed_relative_path、patient_id、label、bbox_xyxy、pip_present、notch_intersect）；
  - 发现1：跨患者重复图`01.0000000025730.0047.1499388698.jpg`出现在
    `01.0000000025730`与`01.0000000265169`两患者目录，处理图SHA完全相同（同一图复制到
    两患者），bbox已同时赋给两份，但划分审计需处理（防同图跨split泄漏）；
  - 发现2：29张bbox-PIP相交经重标后相交面积大幅缩小（最大从~5.5万降到~1.6万，占框
    ≤~9%），但仍有**21张残余相交**；A/B方案待定（Notch臂跳过定位损失 vs 逐张确认
    病灶可见），已在manifest标记`notch_intersect`；
  - 5张漏标（武大省人民2024患者`01.0000000393194`）已补。M1待定：A/B处理、跨患者
    重复图，随后启动CenterNet式辅助定位。
- 2026-08-10：冻结M1来源内1:1.3 Keep主清单
  `08_M1辅助定位清单_20260810/m1_balanced_keep_primary_1to1p3_split_seed42.csv`。
  清单复用M0患者split，共1373张/1020人；597张癌图bbox覆盖率100%，776张非癌图明确
  不计算M1定位损失；图片路径、实际尺寸、框界内、患者跨split/标签和跨患者SHA检查
  全部通过。此前0025730跨患者重复图未进入主清单；Notch相交标记不阻塞Keep主线。
  新增`efficientnet_m1_localization.py`，实现`features[5]`引出的14x14
  `heatmap+size+offset`定位头、bbox感知A0增强、warm-up与联合微调、分类/定位/联合三类
  checkpoint及验证定位指标。物理GPU 1上的6人小样本1+1 epoch冒烟完整通过，定位损失可下降，
  增强后GT框联系表肉眼未见坐标错位；调试指标不代表模型效果，internal test未推理。
- 2026-08-10：补齐M1全量诊断对照。M0全量Keep清单已在上游排除跨患者副本，实际仅
  保留0025730原患者路径一份；3348张中无跨患者SHA，1670张癌图全部匹配bbox。生成
  `09_M1全量诊断清单_20260810/m1_full_keep_split_seed42.csv`并通过路径、尺寸、框界内、
  患者split和checksums验收。全量清单加载同种子全量M0权重的CUDA 1+1 epoch冒烟通过，
  GT框增强联系表未见错位，未推理internal test。新增`run_m1_matrix.sh`，统一平衡/全量
  ×种子42/202/503的checkpoint、参数、输出和日志；脚本要求显式范围与种子，且不传
  `--evaluate-test`。
- 2026-08-10：更新正式代码审查规范。训练、评估、Grad-CAM、MOCE、SAE和M-CBM等
  主要模块除整体职责外，每个主要函数/类还需在docstring中简要说明输入参数名称、类型、
  作用以及返回值类型与含义；张量和bbox注明shape、坐标范围或统计层级。已先对M1训练
  脚本和M1清单构建脚本完成该格式补充，不改变模型结构、损失、冻结参数或既有结果。
- 2026-08-10：完成M1来源内1:1.3平衡主实验seed42。warmup阶段分类器冻结，患者AUC
  按预期保持M0的`0.84467`；定位损失由`1.6653`降至`1.1791`。联合微调定位最佳为
  joint epoch6：中心命中率`0.9310`、mean IoU `0.4543`，但患者AUC `0.83894`，比
  预注册非劣下限`0.83967`低约`0.00073`，因此不作为联合最佳。最终联合门槛checkpoint
  回选warmup epoch3：患者AUC `0.84467`，中心命中率`0.9080`（固定画面中心基线
  `0.6437`），mean/median IoU `0.4437/0.4609`，IoU≥0.3/0.5为
  `0.7586/0.4368`，病灶覆盖率`0.7460`。结论限于单一seed：M0冻结特征已含明显可定位
  信息，但当前联合微调尚未同时通过分类非劣与更高定位；继续跑平衡seed202/503，暂不
  改损失权重、学习率或门槛，internal test保持锁定。
- 2026-08-10：完成M1平衡第一版seed202/503。seed202的joint最高患者AUC为
  `0.85109`（epoch8；非劣下限`0.85338`），seed503为`0.84675`（epoch8；下限
  `0.84887`）；两者都在第8轮达到当前最高AUC，却因旧逻辑把“未过门槛”累计为8轮
  “无联合改善”而停止。三种子第一版均未产生通过非劣门槛的joint epoch，最终按规则
  回选warm-up定位头，说明第一版尚不能判断后续epoch能否恢复分类。修正早停为独立监测
  joint val患者AUC，patience 8→12；非劣门槛、损失、学习率、数据和20轮上限均不变。
  修正版统一以`_pat12`后缀从头复跑3种子，不覆盖第一版，不读取internal test。
- 2026-08-10：完成M1平衡早停修正版`_pat12`3种子，三组均跑满20轮joint。
  seed42/202的joint最高患者AUC分别为`0.8487/0.8605`，均通过各自非劣门槛并高于
  M0；seed503最高为`0.8473`，比非劣下限低`0.0016`。三种子joint最高AUC均值
  `0.8521`，与M0均值`0.8523`基本持平；对应中心命中率/mean IoU均值为
  `0.9042/0.4622`，高于固定画面中心基线`0.6437`。结论：延长早停耐心纠正了旧版
  过早停止，M1定位明确可学，但分类非劣只有2/3种子通过。不再单纯增加epoch，下一步
  在平衡train/val上仅将joint LR调为`3e-5`做M1b 3种子受控对照；不读取internal test。
- 2026-08-10：冻结M1b预注册与运行入口。`run_m1_matrix.sh`新增向后兼容的
  `JOINT_LR`（默认`1e-4`），M1b固定使用`JOINT_LR=3e-5`和`RUN_SUFFIX=_lr3e5`。
  训练脚本新增真joint阶段的AUC最佳/非劣合格定位checkpoint及结构化摘要；CPU 6人
  `1+1 epoch`冒烟已确认两个checkpoint、`joint_stage_selection`和不读取internal test的
  输出链路正常。冒烟指标不代表正式性能。M1b主成功判据为3种子的真joint最高患者
  AUC全部通过各自M0减`0.005`的非劣线，不允许用warm-up回退充当成功。
- 2026-08-10：M1b seed42完成。真joint阶段最高患者AUC为`0.8374`（epoch17），
  低于预注册非劣线`0.8397`约`0.0023`，相对M0低`0.0073`；合格joint epoch为0，
  全局交付checkpoint回选warm-up。与`1e-4` seed42的joint最高AUC `0.8487`相比，
  低LR在joint初期的AUC跌幅略小，但后期恢复不足，当前更符合联合阶段欠适应。
  M1b的3/3主成功判据已不可能达成，但为完整检验低LR对seed503的原始假设并避免
  选择性停止，seed202/503仍按冻结参数串行完成。未读取internal test。
- 2026-08-10：M1b seed202完成。真joint阶段最高患者AUC为`0.8520`（epoch13），
  低于非劣线`0.8534`约`0.0014`，相对M0低`0.0064`；合格joint epoch为0，全局交付
  checkpoint回选warm-up。原`1e-4` seed202的joint最高AUC为`0.8605`，低LR同样
  降低了后期恢复上限。seed42/202两组方向一致，当前证据不支持`3e-5`优于
  `1e-4`；seed503仍按预注册计划完成，不中途调参，未读取internal test。
- 2026-08-10：M1b seed503及整个3种子矩阵完成。seed503真joint最高患者AUC为
  `0.8436`（epoch15），低于非劣线`0.8489`约`0.0052`，相对M0低`0.0102`；合格
  joint epoch为0并回选warm-up。M1b三种子真joint AUC为`0.8374/0.8520/0.8436`，
  非劣通过0/3，均值`0.8443`；mean IoU均值`0.4492`。原`1e-4 + patience12`对应为
  AUC均值`0.8521`、mean IoU `0.4622`、非劣2/3。按预注册否决`3e-5`，后续全量
  诊断锁定`1e-4 + patience12 + 20 joint epochs`，其余参数不变；全程未读取internal test。
- 2026-08-11：验收服务器中断后的M1全量诊断矩阵。seed42跑满20轮joint，seed202按
  patience12正常早停于第13轮，seed503跑满20轮；三组`config.json`、历史、val预测与
  checkpoint均完整，且`test_evaluated=false`。服务器问题发生在结果落盘之后，无需重跑。
  真joint最高患者AUC为`0.9091/0.9052/0.8944`，对应非劣通过2/3；均值`0.9029`
  比M0均值`0.9091`低`0.0062`。center-hit/mean IoU均值为`0.9222/0.5002`，显示
  定位可学，但共享Encoder的joint微调未稳定保持分类；三组全局checkpoint均回选
  warm-up。暂不进入M2，先评估将冻结Encoder定位头作为M1阶段产物的方案。
- 2026-08-11：冻结M1正式产品为warmup-only定位头。M0 Encoder/分类头全程冻结，
  只训练定位头5轮；候选先通过`center-hit >= fixed-center-hit + 0.10`和`mean IoU >= 0.40`，
  再按mean IoU、中心距离、center-hit顺序选择。历史六组有3组的center-hit/IoU最佳
  epoch不同，因此不混用旧`m1_best_joint.pth`，统一轻量重提取六组。代码新增
  `--warmup-only`、`WARMUP_ONLY=1`和`m1_best_warmup_localization.pth`；CPU 6人`1 epoch`冒烟
  确认不运行joint、不读取internal test且产物/选择记录完整。M2重构为冻结Encoder
  的FPN定位精度候选；M3先从warmup-only产品加入非癌热图负监督，不更新分类路径。
- 2026-08-11：完成平衡主实验与全量诊断各3种子的warmup-only统一提取。平衡入选epoch
  为`3/2/4`，患者AUC/center-hit/mean IoU均值为`0.8523/0.9004/0.4417`；全量入选
  epoch为`5/4/5`，对应`0.9092/0.9042/0.4623`。六组均生成独立warm-up checkpoint、
  选择记录、val预测和QC图，`test_evaluated=false`。平衡组未出现第5轮边界且跨种子
  IoU稳定，不增加正式warm-up轮数；全量两组触及第5轮，仅预留统一10轮训练长度
  敏感性实验，不选择性补跑、不替换当前冻结结果。M1正式产品已可向M3交接。
- 2026-08-11：冻结M3预注册协议并完成正式实现。新增`efficientnet_m3_suppression.py`
  与`run_m3_matrix.sh`：冻结Encoder/分类头，只训练14x14定位头；癌/非癌热图损失按
  每图空间总和独立归一化，非癌权重在epoch1-5由0升至1；以固定M1阈值下癌召回、
  mean IoU和center-hit为门槛，再最小化非癌FP。代码记录`passed_seed_success`，只有
  balanced 3/3种子均达到非癌FP绝对下降`>=0.10`才允许启动full。已修复阈值浮点
  取整问题并增加`--self-test`，10/240例目标召回0.90均精确通过；Python编译、Shell
  语法、loss反传、成功/失败冒烟、冻结checksum与分类概率不变性均通过。M3不提供
  internal test评估入口；下一步正式运行balanced seed42/202/503。
- 2026-08-11：完成M3 balanced三种子正式实验。seed42入选epoch11，IoU/cenhit/
  癌召回/非癌FP为`0.4334/0.8851/0.8966/0.5398`，相对基线FP下降`0.2743`并成功；
  seed202/503均无epoch同时通过三门槛，按协议失败且未保存checkpoint。最终仅`1/3`
  成功，否决M3当前Heatmap负监督方案并锁住full腿；M1保持正式定位产品。既有val后验
  诊断显示，冻结M0分类概率在癌召回`0.9080`时的非癌FP为
  `0.5752/0.4956/0.4867`，比M1定位置信度约`0.81`稳定，但该结果仅用于提出独立
  存在性门控候选，须另行预注册，不能替代M3正式结论。现有M4草稿需先修订再运行。
- 2026-08-11：M3c-B独立区域门控头正式完成并3/3通过（冻结M1框+ROI门控头，非癌FP
  压到0.47-0.58、癌召回0.908/患者0.925、框几何零改动），成为M3阶段正式框展示门控；
  完成M3c-A（分类器门控）2/3正式归档。
- 2026-08-13：按冻结协议完成M3c-B内部test三seed一次性评价。`q_region`图像AUC均值
  `0.8299`，非癌FP由M1均值`0.8185`降至`0.5060`，但癌召回由`0.9457`降至
  `0.9070`；seed42癌召回门槛失败，三seed large组均超过“相对M1最多多漏1张”的限制，
  正式结果为`0/3`未确认。M1框mean IoU约`0.4464`、center-hit `0.9070`，定位几何本身
  稳定。M3c-B降级为辅助置信评分，不再作为自动隐藏框的产品硬门控；不以正式确认名义
  继续跑external。
- 2026-08-11：M4修订预注册定稿（冻结M1+M3c-B、癌图真值ROI、非癌M1预测ROI、局部
  Encoder副本+局部/融合头、病灶大小/来源/分辨率分层、元数据审计train→val、患者
  top2_mean、成功判据3/3Δ患者AUC>0且平均≥0.02）。实现包（build_m4_roi_manifest.py、
  efficientnet_m4_fusion.py、两个启动器）经三轮审阅修复闭环，全部验证通过，可正式放行；
  M5按"冻结M1+M3c-B预测ROI"待M4完成后修订。
