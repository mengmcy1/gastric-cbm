# 第二批数据弱标注匹配参数 v1

> 冻结日期：2026-07-22  
> 输入：`数据整理记录/第二批/筛图_v1_1/full/patient_limited_manifest.csv`  
> 正式输出：`数据整理记录/第二批/匹配_v1`  
> 随机种子：42

## 1. 匹配单位与边界

- 匹配单位为患者，同一患者的标签和中心必须唯一。
- 癌与非癌只允许在同一中心内匹配，禁止跨中心回退。
- 患者风格签名由入选图的 `size_group`、`aspect_group`、`frame_profile` 稳定众数组成；年份取患者可用年份中位数。
- 使用全局最小成本指派，而非逐患者贪心匹配，避免先处理的患者占用更合适的对照。
- 匹配不读取模型预测、概率、Grad-CAM、解剖部位或病灶可见性；全部样本保持 `review_status=pending`。

## 2. 全病例级联松弛匹配

在每个中心内按以下优先级执行 1:1 和 1:2 匹配：

1. Level 1：同 `style_group` 且双方年份已知、年份差不超过 2 年。
2. Level 2：同 `style_group`，忽略年份限制；年份缺失也进入本级。
3. Level 3：同中心内选择最接近风格；依次优先缩小尺寸档位距离、比例差异、画框差异、年份距离。

Level 3 仅解决病例覆盖问题，不代表风格已经平衡。正式文件因此明确命名为：

- `matched_weak_relaxed_all_cases_1to1_seed42.csv`
- `matched_weak_relaxed_all_cases_1to2_seed42.csv`

1:1 覆盖 415 名癌患者和 415 名非癌患者，共 1623 张图；Level 1/2/3 分别为 42/42/331 对。1:2 覆盖 415 名癌患者和 830 名非癌患者，共 2197 张图；Level 1/2/3 分别为 70/76/684 个病例-对照配对。

## 3. 共同支持问题

自动平衡审计结论为 `FAIL_COMMON_SUPPORT`。全病例 1:1 虽将中心总变异距离降为 0，但仍有：

| 指标 | 匹配前 | 全病例 1:1 |
| --- | ---: | ---: |
| style_group TV | 0.8206 | 0.7855 |
| size_group SMD | 1.4207 | 0.9963 |
| year SMD | 1.0806 | 1.1694 |
| 每患者入选图数 SMD | 1.0763 | 1.3554 |

主要原因是缺少标签间共同支持：`xlarge_gt1600|landscape_16x9|contour_frame` 中 1073 张图全部为癌图，同中心内不存在同风格非癌对照。级联松弛无法从现有数据中创造缺失的对照，因此全病例清单只能用于覆盖性/探索性分析，不得表述为已完成设备风格去偏，也不得作为论文唯一核心结果。

## 4. 严格共同支持集

另行构建 `matched_weak_strict_common_support_v1_seed42.csv`：

- 同中心、同精确 `style_group`、患者 1:1 匹配。
- 每对癌/非癌使用相同张数的该风格图像。
- 在满足共同风格的可行配对中优先缩小年份距离。
- 共 90 对、180 位患者、238 张图，癌/非癌各 119 张；覆盖 21.69% 的癌患者。
- 90 对按中心分布为：武大省人民 57、宜昌一医 10、宜昌中心 9、武汉市中心 7、孝感 5、荆门石化 2。
- 风格 TV、尺寸 SMD 和每患者图数 SMD 均为 0；年份 SMD 仍为 0.6139。
- 44 对有一方或双方年份缺失；46 对年份均已知，其中 41 对年份差不超过 2 年。

该清单适合作为严格风格控制的敏感性分析，但因样本小、年份缺失较多，不能单独承担总体性能结论。

## 5. 清单用途

- `full_manifest.csv`：全部 5229 张通过预处理的图像，作为完整数据对照和归档。
- `patient_limited_manifest.csv`：P2 后 4099 张图，作为匹配输入和患者限图对照。
- `matched_weak_relaxed_all_cases_1to1_seed42.csv`：全癌病例覆盖的探索性 1:1 集。
- `matched_weak_relaxed_all_cases_1to2_seed42.csv`：全癌病例覆盖的比例敏感性集。
- `matched_weak_strict_common_support_v1_seed42.csv`：严格风格共同支持敏感性集。
- `usable_unmatched_manifest.csv`：未进入松弛 1:1 的可用非癌图，仅归档并仍可出现在 full 实验中。
- `excluded_manifest.csv`：P2 未入选图，保留排除原因，不参与 matched 实验。

## 6. 下一步门槛

1. 在上述三个候选数据集上重跑仅元数据基线，量化尺寸、风格、中心和年份对标签的剩余预测力。
2. 冻结患者级划分；严格集需按标签、中心和风格分层，并报告小中心样本限制。
3. 训练报告必须并列给出完整数据、全病例松弛匹配和严格共同支持结果，不将任一单一结果解释为无偏总体性能。
4. 逐图病灶可见性未经临床确认，所有本轮结果仍属于完全弱标注验证。
